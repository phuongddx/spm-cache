---
phase: 11-homebrew-release-automation
plan: 03
type: execute
wave: 2
depends_on:
  - "11-01"
  - "11-02"
files_modified:
  - .planning/phases/11-homebrew-release-automation/11-LIVE-RUN.md
autonomous: false
requirements:
  - REL-04
  - REL-05
  - REL-06
  - REL-07
  - REL-08
  - REL-09
user_setup:
  - service: github-app
    why: "Tap-push authentication without a human-owned expiring credential (REL-04) — the App and its secrets cannot be created by the executor"
    dashboard_config:
      - task: "Create a GitHub App owned by phuongddx: Contents read & write + Metadata read; NEVER the workflow scope; generate and download the private key (.pem)"
        location: "GitHub Settings -> Developer settings -> GitHub Apps -> New GitHub App"
      - task: "Install the App on phuongddx/homebrew-spm-cache ONLY (select no other repositories)"
        location: "The App's settings -> Install App"
      - task: "Provide the numeric App ID and the .pem private key to the executor (or set repo secrets TAP_APP_ID and TAP_APP_PRIVATE_KEY directly)"
        location: "phuongddx/spm-cache -> Settings -> Secrets and variables -> Actions"
      - task: "Confirm deletion of the dead classic-PAT secret (name per 11-RESEARCH Runtime State Inventory)"
        location: "phuongddx/spm-cache -> Settings -> Secrets and variables -> Actions"
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The repo secrets list on phuongddx/spm-cache contains TAP_APP_ID and TAP_APP_PRIVATE_KEY and no longer contains the dead classic-PAT secret (REL-04 credential cutover)"
    - statement: "A live workflow_dispatch run with tag input v0.3.0 completes the update-tap job GREEN on the explicit already-up-to-date notice branch — live proof of real GitHub App auth (REL-04), the integrity-gated tarball download (REL-05), the anchored idempotent edit + push path (REL-06/07), and input-sourced tag resolution on dispatch (REL-09)"
      verification: backstop
    - statement: "On that same dry-run the verify-publish job goes RED specifically at the version assertion, failing with the pre-fix unknown-option signature of the v0.3.0 tarball — the live fail-first proof that the REL-08 assertion has teeth; the first fully-green verify job is expected at the first release whose tarball contains the 11-01 intercept (the v0.4.0 release itself)"
      verification: backstop
    - statement: "Any failure OTHER than the documented verify-job signature (auth error, integrity gate, substitution count, push failure) is treated as a real defect and fixed before the phase is declared complete"
      verification: backstop
  artifacts:
    - path: ".planning/phases/11-homebrew-release-automation/11-LIVE-RUN.md"
      provides: "Record of the operator gate and the live dispatch dry-run (run URL, per-job conclusions, log evidence)"
  key_links:
    - from: "repo secrets TAP_APP_ID / TAP_APP_PRIVATE_KEY"
      to: "update-tap job credential guard + token mint step"
      via: "the rewritten workflow consumes exactly these two secret names; the gate exists to make them real"
      pattern: "TAP_APP_ID"
  prohibitions:
    - statement: "MUST NOT print, echo, or log the private key, the app id value, or any minted token at any point during the gate or the dry-run — names only; secret values flow exclusively through gh secret set (which masks them) or the operator's own UI action"
    - statement: "MUST NOT create, edit, re-publish, or pre-release any GitHub release in order to obtain a green verify job — the v0.3.0 dry-run's red verify job is the expected fail-first proof, and full green waits for the real next release"
---

<objective>
Execute the operator gate (GitHub App + repo secrets, the one-time human step outside the repo) and prove the rewritten publish path live via an idempotent dispatch dry-run.

Purpose: REL-04's durable token requires credentials that do not exist yet (verified 2026-08-29:
only the dead classic PAT is present) and cannot be created by automation — GitHub App creation and
installation are UI-only. This plan pauses for that step (the roadmap's Operator gate), then runs
the workflow once against the existing v0.3.0 tag: the update-tap job exercises real auth, real
integrity gating, real anchored edits, and lands on the already-up-to-date branch with zero side
effects on the tap; the verify-publish job is EXPECTED to go red at the version assertion because
the v0.3.0 tarball predates the 11-01 intercept — the live fail-first proof of REL-08. Ordering
per 11-RESEARCH Open Question 3: code + specs first (11-01/11-02), operator checkpoint, then the
live dry-run.

Output: configured secrets, a deleted dead secret, and `11-LIVE-RUN.md` recording the evidence.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-homebrew-release-automation/11-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1 (OPERATOR GATE): create the GitHub App, install it on the tap, provide credentials</name>
  <read_first>
  - .planning/ROADMAP.md — Phase 11 "Operator gate" paragraph (the locked scope decision)
  - .planning/STATE.md — "Locked scope decisions" (2026-08-27) and "Operator Next Steps"
  - .planning/phases/11-homebrew-release-automation/11-CONTEXT.md — Authentication & the Operator Gate decision
  - .planning/phases/11-homebrew-release-automation/11-RESEARCH.md — Runtime State Inventory (current secret list), Pattern 1 (exact token-step contract the App must satisfy)
  </read_first>
  <action>
  Operator creates the durable credential (UI-only; no API exists for GitHub App creation):
  1. Create a GitHub App owned by the phuongddx account — permissions: Contents read & write,
     Metadata read; NEVER the workflow scope (a workflow-scoped token could alter the tap's own
     workflows). Any homepage/webhook URL is fine; the webhook does not need to be active.
  2. Install the App on phuongddx/homebrew-spm-cache ONLY — select no other repository.
  3. Generate and download the App private key (.pem file).
  4. Either hand the executor the numeric App ID plus the path to the .pem (the executor sets the
     repo secrets via gh secret set, which masks values from logs), or set the two repo secrets
     directly: TAP_APP_ID = the numeric App ID, TAP_APP_PRIVATE_KEY = the full .pem contents.
  5. Authorize deleting the dead classic-PAT secret (the executor performs the deletion via gh in
     Task 2).
  Accepted lower-ceremony substitute per the locked decision: a write-access deploy key on the tap
  repo — if the operator pivots to this, say so at the checkpoint; it requires a small visible
  workflow change (checkout ssh-key input instead of the token input) before the dry-run, and the
  REL-04 truth weakens to a long-lived machine credential (still non-human, but non-expiring).
  </action>
  <instructions>
  Everything automatable is already done: 11-01 shipped the version flag fix and 11-02 shipped the
  rewritten workflow consuming exactly the two secret names above. The only remaining human-only
  steps are the App creation, installation, and private-key generation above — GitHub provides no
  API for them. Do not set any other permission or install anywhere else.
  </instructions>
  <verification>
  Executor verifies after resume (Task 2): the secrets list on phuongddx/spm-cache shows TAP_APP_ID
  and TAP_APP_PRIVATE_KEY and no longer shows the dead classic-PAT secret. The dry-run (Task 3)
  then proves the installation's Contents write access end-to-end.
  </verification>
  <resume-signal>Type "done" once the App exists, is installed on homebrew-spm-cache only, and the App ID + private key are provided (or the secrets are set directly); or state "pivot: deploy key" to take the substitute path.</resume-signal>
  <acceptance_criteria>
  - Operator confirms: App owned by phuongddx with Contents R/W + Metadata R only, installed on phuongddx/homebrew-spm-cache only
  - Executor holds either (a) the numeric App ID and a readable .pem path for CLI secret upload, or (b) operator confirmation that both secrets were set in the UI
  - The gate is a blocking-human checkpoint — never auto-approved in any mode
  </acceptance_criteria>
  <done>The durable credential exists and is installed with the exact scoped permissions; values never appear in any log.</done>
</task>

<task type="auto">
  <name>Task 2: apply the gate via CLI — set secrets, delete the dead secret, verify the credential surface</name>
  <files>(no source files; repo settings via gh)</files>
  <read_first>
  - .planning/phases/11-homebrew-release-automation/11-RESEARCH.md — Runtime State Inventory (the exact current secret list: only the dead classic PAT), Environment Availability
  - CLAUDE.md — gh account rule: switch to the phuongddx user before any gh command against this repo's remote
  </read_first>
  <precondition>Task 1's checkpoint passed with either credentials handed to the executor (option a) or operator-set secrets (option b), and `gh auth status` shows phuongddx as the active account (it was at planning time).</precondition>
  <action>
  Run gh auth switch for the github.com hostname to the phuongddx user first (CLAUDE.md rule; both
  phuongddx and phuongdoanduy are logged in locally). If option (a): set the two repo secrets with
  gh secret set against phuongddx/spm-cache — TAP_APP_ID from the provided numeric id, and
  TAP_APP_PRIVATE_KEY from the provided .pem file (read the file into the command; never echo it).
  Delete the dead classic-PAT secret by name with gh secret delete (name per the RESEARCH Runtime
  State Inventory), unless the operator already deleted it in the UI. Then verify: list the repo's
  actions secrets via gh api and confirm exactly the expected names are present and the dead one is
  gone. Record the resulting name list (names only — never values) for the SUMMARY.
  </action>
  <verify>
    <automated>gh auth status | grep -q 'Active account: true' && [ "$(gh api repos/phuongddx/spm-cache/actions/secrets --jq '.secrets[].name' | grep -c 'TAP_APP_ID')" -ge 1 ] && [ "$(gh api repos/phuongddx/spm-cache/actions/secrets --jq '.secrets[].name' | grep -c 'TAP_APP_PRIVATE_KEY')" -ge 1 ] && ! gh api repos/phuongddx/spm-cache/actions/secrets --jq '.secrets[].name' | grep -q 'TAP_REPO_TOKEN'</automated>
  </verify>
  <acceptance_criteria>
  - gh api repos/phuongddx/spm-cache/actions/secrets lists both TAP_APP_ID and TAP_APP_PRIVATE_KEY
  - The dead classic-PAT secret (TAP_REPO_TOKEN) is absent from the list
  - No command output anywhere in this task contains a secret VALUE (names only)
  </acceptance_criteria>
  <done>The credential surface is real: exactly the two App secrets exist, the dead PAT secret is deleted, and the active gh account is phuongddx.</done>
</task>

<task type="auto">
  <name>Task 3: live idempotent dispatch dry-run (tag v0.3.0) + evidence record</name>
  <files>.planning/phases/11-homebrew-release-automation/11-LIVE-RUN.md</files>
  <read_first>
  - .github/workflows/update-tap.yml (the rewritten workflow being exercised — know every step before interpreting failures)
  - .planning/phases/11-homebrew-release-automation/11-RESEARCH.md — Open Question 3 (the dry-run design and why v0.3.0 is side-effect-free), Assumptions A1/A2 (expected job durations, macOS 26 runner), Pitfall 8 (the macos-15 fallback if an OS-specific failure appears)
  - .planning/phases/11-homebrew-release-automation/11-01-SUMMARY.md (the intercept — why the verify job is EXPECTED red on a pre-intercept tarball)
  - CLAUDE.md (gh account rule)
  </read_first>
  <precondition>Task 2's verification passed, and the rewritten workflow file exists on the ref to be dispatched (the current phase branch carries it; the default branch works too once merged) — check with git branch --show-current and gh api for the file's presence on that ref if unsure.</precondition>
  <action>
  Dispatch the dry-run: with phuongddx active in gh, run gh workflow run update-tap.yml against the
  ref that carries the rewritten workflow, passing the tag input as v0.3.0 (the currently published
  release — the formula already points at it, so the run is idempotent and side-effect-free).
  Capture the new run id from gh run list filtered to the update-tap workflow. Wait for completion
  with gh run watch and generous patience — the verify job builds the formula from source
  (brew ruby + a release swift build), expect roughly 10-25 minutes (RESEARCH A1); do not cancel.
  Then collect evidence: per-job conclusions via gh run view with the json jobs query, the update-tap
  job log (confirm the already-up-to-date notice annotation and that no commit/push happened), and
  the verify-publish job log (confirm the failure is AT the version assertion, showing the
  pre-intercept unknown-option signature from the v0.3.0 artifact — this is the expected,
  documented fail-first proof of the REL-08 assertion, NOT a defect). Interpretation rule: any
  OTHER failure signature — auth/token error, integrity gate, substitution count, postcondition,
  push failure, runner/OS issue — is a real defect: diagnose and fix before the phase completes
  (an OS-specific formula failure on the macOS runner falls back to pinning the runner label,
  Pitfall 8). Write the evidence to 11-LIVE-RUN.md in this phase directory (do NOT name the file
  anything ending in VERIFICATION.md — a Phase 9 tooling quirk makes extra files with that suffix
  shadow status reads): run URL, dispatch command and ref, per-job conclusions with log excerpts
  (secrets never appear in logs — Actions masks them), and the explicit statement that the
  verify-job red is the expected pre-intercept signature with the first full green expected at the
  v0.4.0 release.
  </action>
  <verify>
    <automated>RUN_ID=$(gh run list --workflow=update-tap.yml --limit 1 --json databaseId,status --jq '.[0].databaseId') && gh run view "$RUN_ID" --json jobs --jq '.jobs[] | {name: .name, conclusion: .conclusion}' | tee /tmp/11-03-jobs.txt && grep -q 'update-tap' /tmp/11-03-jobs.txt && test -f .planning/phases/11-homebrew-release-automation/11-LIVE-RUN.md</automated>
  </verify>
  <acceptance_criteria>
  - The dispatched run used input tag v0.3.0 (visible in the run's inputs/event) and was NOT triggered by any release event — no release was created, edited, or re-published
  - The update-tap job conclusion is success and its log contains the already-up-to-date notice annotation (idempotent branch — the tap repo has no new commit from this run)
  - The verify-publish job conclusion is failure with the version-assertion signature in its log (the unknown-option error of the pre-intercept v0.3.0 artifact) — or, if the operator pivoted to a post-intercept tag, a green conclusion with matching versions
  - 11-LIVE-RUN.md exists in the phase directory (name not ending in VERIFICATION.md) containing the run URL, dispatch command, per-job conclusions, and log excerpts
  - Any non-documented failure signature triggered a fix, not a waiver
  </acceptance_criteria>
  <done>The rewritten publish path is proven live: real App-token auth, integrity-gated download, anchored idempotent edit, dispatch-input tag resolution, and a verify job whose version assertion demonstrably has teeth.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan produces:
- Repo-settings state: secrets `TAP_APP_ID` + `TAP_APP_PRIVATE_KEY` present on phuongddx/spm-cache; dead classic-PAT secret deleted (operator gate applied via gh CLI)
- `.planning/phases/11-homebrew-release-automation/11-LIVE-RUN.md` — live dispatch dry-run evidence (run URL, per-job conclusions, log excerpts, expected-red rationale for the verify job on v0.3.0)

Sibling plans produced: the `SPMCache::Main.run` `--version` intercept + `spec/main_version_spec.rb` (11-01); the rewritten `.github/workflows/update-tap.yml` (jobs `update-tap`/`verify-publish`, steps version/release-existence/credential-guard/app-token/sha/tap-checkout/edit/commit-push + brew install/version assert, trigger `workflow_dispatch` input `tag`, concurrency group `update-tap`) + `spec/update_tap_workflow_spec.rb` (11-02).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → repo secrets | the App private key crosses from the operator's hands into repo secret storage |
| live CI → external tap repo | the first real use of the installation token's write access |
| public release artifact → runner | the v0.3.0 tarball is downloaded and integrity-gated for real |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-GATE-01 | Information Disclosure | private key / app id handling during the gate | high | mitigate | Values flow only via gh secret set (masked in output) or the operator's own UI action; the executor's verification queries NAMES only; no task ever echoes a value; gate is blocking-human so no unattended run touches credentials |
| T-11-GATE-02 | Elevation of Privilege | App installed beyond the tap repo, or granted the workflow scope | high | mitigate | Checkpoint instructions pin the exact permission set (Contents R/W + Metadata R) and the single-repo installation; the operator confirms in the gate; the mint step's owner/repositories inputs narrow the token regardless |
| T-11-DRY-03 | Tampering | dispatching the dry-run with a wrong or injected tag | low | mitigate | Dry-run uses the fixed existing tag v0.3.0; the workflow itself validates the v-then-digit shape and checks release existence before acting |
| T-11-DRY-04 | Repudiation | dry-run outcome misread as pass/fail | medium | mitigate | 11-LIVE-RUN.md records per-job conclusions plus log excerpts and the documented expected-red rationale, so the phase verification reads evidence, not memory |
| T-11-SC | Supply chain | no packages installed by this plan | low | accept | gh/brew are first-party and preinstalled; no registry packages added |
</threat_model>

<verification>
- Secrets surface: gh api secrets list shows exactly the two App secret names; dead secret gone (Task 2 automated gate)
- Live run: update-tap job green on the idempotent notice branch; verify-publish red with the documented pre-intercept signature (or green on a post-intercept tag) — recorded in 11-LIVE-RUN.md
- No release was created, edited, or re-published by the dry-run
- Phase-level: `bundle exec rspec` still green after all three plans (no source changes in this plan)
</verification>

<success_criteria>
- The operator gate completed with the exact scoped App (Contents R/W + Metadata R, single-repo install, no workflow scope)
- The live dry-run proves REL-04/05/06/07/09 end-to-end green on the idempotent path, and REL-08's assertion demonstrably fails red against a version-reporting defect (fail-first proof)
- Every outcome is recorded in 11-LIVE-RUN.md; any unexpected failure was fixed, not waived
</success_criteria>

<output>
Create `.planning/phases/11-homebrew-release-automation/11-03-SUMMARY.md` when done — the gate transcript (names only), the dry-run interpretation against the documented expectations, and a pointer to 11-LIVE-RUN.md.
</output>
