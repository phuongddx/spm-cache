---
phase: 11-homebrew-release-automation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/spm_cache/main.rb
  - spec/main_version_spec.rb
autonomous: true
requirements:
  - REL-08
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Running `spm-cache --version` prints exactly the gem version string to stdout followed by a newline and exits 0 — the REL-08 CLI half; today it exits 1 with an unknown-option banner (reproduced at planning time)"
    - "The printed value equals the stripped contents of the VERSION file (spec cross-checks SPMCache::VERSION against File.read of VERSION), so a formula-installed gem reports the released version — the gemspec ships the VERSION file"
    - "argv that does not begin with the literal --version token is dispatched unchanged to Command.run — default-subcommand routing and all existing invocations untouched (full suite stays green; baseline 416 examples, 0 failures, measured 2026-08-30)"
  artifacts:
    - path: "lib/spm_cache/main.rb"
      provides: "--version intercept before CLAide default-subcommand routing"
      contains: "argv.first"
    - path: "spec/main_version_spec.rb"
      provides: "stdout-capture spec for the --version intercept"
      contains: "to output"
  key_links:
    - from: "bin/spm-cache"
      to: "lib/spm_cache/main.rb"
      via: "executable entry calls Main.run(ARGV); the intercept sits between load_all and Command.run so CLAide never routes a bare --version into the default use subcommand"
      pattern: "return puts\\(SPMCache::VERSION\\) if argv\\.first =="
    - from: "lib/spm_cache/main.rb"
      to: "lib/spm_cache/version.rb"
      via: "intercept prints the SPMCache::VERSION constant (String read from the shipped VERSION file)"
      pattern: "SPMCache::VERSION"
  prohibitions:
    - statement: "MUST NOT hardcode a version literal in the intercept or its spec — the VERSION file is the single source of truth (read by lib/spm_cache/version.rb, shipped by the gemspec spec.files), so the intercept prints SPMCache::VERSION and the spec interpolates the same constant plus one File.read cross-check"
  flagged_assumptions:
    - requirement_id: REL-08
      probe: "unclassified — review manually"
      note: "Spec-less edge probe could not classify REL-08; the truths above are planner-authored from CONTEXT/RESEARCH (Pitfall 1 + Pattern 6), not probe-derived. No silent dismissal — carried as a flagged assumption per the fallback protocol."
---

<objective>
Make `spm-cache --version` work: print the gem version and exit 0.

Purpose: REL-08 requires the post-publish verify job to assert `spm-cache --version` matches the
released tag — but the flag currently exits 1 because CLAide routes a bare `--version` through the
`use` default subcommand, whose options lack the root-only flag (11-RESEARCH Pitfall 1, verified by
local execution at planning time: unknown-option banner, exit 1). Without this fix, success
criterion 4's assertion literally cannot pass. This is the CLI half of REL-08; the workflow
verify-job half is 11-02 Task 3.

Output: a 1-line intercept in `lib/spm_cache/main.rb` plus `spec/main_version_spec.rb`, delivered
through a full RED → GREEN cycle.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-homebrew-release-automation/11-CONTEXT.md
</context>

<feature>
  <name>`spm-cache --version` intercept before CLAide dispatch</name>
  <files>lib/spm_cache/main.rb, spec/main_version_spec.rb</files>
  <behavior>
    Expected behavior in testable terms (both are the RED suite — they fail against current code):
    - Case 1: `SPMCache::Main.run(["--version"])` writes exactly `"#{SPMCache::VERSION}\n"` to stdout (whole-string compare via the `output(...).to_stdout` matcher) and does not raise.
    - Case 2: the expected string equals `File.read("VERSION").strip + "\n"` — proving the printed value is the VERSION file's contents (single source of truth), not a hardcoded literal.
    Cases: input `["--version"]` → stdout `SPMCache::VERSION` + newline, exit 0; any other argv → unchanged dispatch to `Command.run` (covered by the existing 416-example suite, not re-specified here).
  </behavior>
  <implementation>
    Insert one guarded line in `SPMCache::Main.run` between the `load_all` call and `Command.run`:
    return-puts of `SPMCache::VERSION` when `argv.first` is exactly the version flag string. The
    guard must precede `Command.run` because CLAide's `Command.parse` sends a bare flag through
    `default_subcommand` (`use`), whose option set rejects it. Do not modify `load_all`, the
    trailing auto-require, or anything else. Proven working this session (11-RESEARCH Pattern 6,
    which quotes the exact target shape).
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 (RED): failing spec for the --version intercept</name>
  <files>spec/main_version_spec.rb</files>
  <read_first>
  - lib/spm_cache/main.rb (the 25-line file being extended — intercept slots between load_all and Command.run)
  - lib/spm_cache/version.rb (VERSION constant is a String read from the shipped VERSION file)
  - .planning/phases/11-homebrew-release-automation/11-RESEARCH.md — Pattern 6 and Pitfall 1 (root cause, proven fix, CLAide cite)
  - .planning/phases/11-homebrew-release-automation/11-PATTERNS.md — "spec/main_version_spec.rb (test, unit stdout capture)" section (exact idiom to port)
  - spec/core_spec.rb lines 42-51 (the repo's `.method` + `output(...).to_stdout` describe idiom)
  </read_first>
  <behavior>
  - Test 1: `expect { described_class.run(["--version"]) }.to output("#{SPMCache::VERSION}\n").to_stdout` — fails today (SystemExit / banner output instead of the version).
  - Test 2: same matcher with the expected string built from `File.read("VERSION").strip` — fails today for the same reason; after GREEN it pins the VERSION file as the source of truth.
  </behavior>
  <action>
  Create spec/main_version_spec.rb per the PATTERNS analog: frozen_string_literal magic comment,
  require spec_helper (it already loads spm_cache/main), an explanatory comment stating the field
  bug being guarded (CLAide default-subcommand routing rejects the root-only version flag — without
  the intercept the flag exits 1), then `RSpec.describe SPMCache::Main` with a nested string
  describe for the run class method containing exactly the two examples from the behavior block.
  Whole-string compare with trailing newline is load-bearing: the intercept puts the constant
  directly and CLAide's own version print format is the bare version string. Do not stub or mock
  anything; call the real class method. Run the spec and confirm it FAILS for the right reason —
  the run under test exits with the unknown-option failure instead of printing the version (repro
  command from planning time: bundle exec bin/spm-cache --version exits 1). Commit with message
  starting `test(11-01):`.
  </action>
  <verify>
    <automated>! bundle exec rspec spec/main_version_spec.rb</automated>
  </verify>
  <acceptance_criteria>
  - spec/main_version_spec.rb exists, starts with the frozen_string_literal magic comment and requires spec_helper
  - It contains exactly 2 examples, both using the output-to-stdout matcher against described_class.run with the version flag as sole argv element
  - One example interpolates the SPMCache::VERSION constant; the other builds the expectation from File.read of the VERSION file
  - `bundle exec rspec spec/main_version_spec.rb` exits non-zero reporting 2 failures (not syntax/load errors — failure for the right reason: no version output)
  - A commit whose message matches ^test\(11-01\) exists containing only the spec file
  </acceptance_criteria>
  <done>RED confirmed and committed: both examples fail because --version does not print the version today.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): intercept --version in Main.run before CLAide dispatch</name>
  <files>lib/spm_cache/main.rb</files>
  <read_first>
  - lib/spm_cache/main.rb (modify self.run only)
  - .planning/phases/11-homebrew-release-automation/11-RESEARCH.md — Pattern 6 (verbatim proven target shape, placement rationale)
  - </read_first>
  <reversibility rating="reversible">Single guarded line added to one method; trivially revertible, no data or interface change.</reversibility>
  <action>
  In the self.run class method of SPMCache::Main, insert the guard between the load_all call and
  the Command.run call: when the first argv element is exactly the version flag string, puts the
  SPMCache::VERSION constant and return — before CLAide can route a bare flag through the default
  subcommand. Change nothing else: keep the load_all method, the trailing auto-require, and the
  existing comment. Keep the frozen_string_literal magic comment intact. Run the new spec (must
  pass), then the full suite (must stay green — proves other argv dispatch is untouched). Commit
  with message starting `feat(11-01):`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/main_version_spec.rb && bundle exec bin/spm-cache --version && bundle exec rspec</automated>
  </verify>
  <acceptance_criteria>
  - lib/spm_cache/main.rb self.run contains the guard between load_all and Command.run, printing the SPMCache::VERSION constant (no version literal)
  - `bundle exec bin/spm-cache --version` prints the VERSION file contents and exits 0
  - `bundle exec rspec spec/main_version_spec.rb` passes 2 examples
  - Full suite green (baseline 416 examples / 0 failures measured 2026-08-30, plus the 2 new examples)
  - A commit whose message matches ^feat\(11-01\) exists containing lib/spm_cache/main.rb
  </acceptance_criteria>
  <done>GREEN: --version prints the gem version and exits 0; suite fully green; RED→GREEN commits present.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan produces:
- `SPMCache::Main.run` `--version` intercept — `lib/spm_cache/main.rb` (guard on `argv.first`, prints `SPMCache::VERSION` before `Command.run`)
- `spec/main_version_spec.rb` — 2-example stdout-capture spec (REL-08 CLI half)

Sibling plans produce: `.github/workflows/update-tap.yml` rewrite — jobs `update-tap` (outputs: `version`) and `verify-publish`; step ids `version` (outputs `tag`, `version`), `app-token`, `sha` (output `sha256`); trigger `workflow_dispatch` input `tag` (required string); secrets `TAP_APP_ID`, `TAP_APP_PRIVATE_KEY`; concurrency group `update-tap` (cancel-in-progress false); step/job env `DISPATCH_TAG`, `TAP_APP_ID`, `TAP_APP_PRIVATE_KEY`, `GH_TOKEN`, `HOMEBREW_NO_AUTO_UPDATE`, `EXPECTED_VERSION` (11-02); the operator gate + live dispatch dry-run record (11-03).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell → CLI argv | untrusted argument vector crosses into Main.run before any validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-CLI-01 | Tampering | Main.run intercept | low | accept | The guard performs one string equality check on argv[0] and prints a constant; argv content is never evaluated, interpolated into a command, or written anywhere |
| T-11-CLI-02 | Information Disclosure | --version output | low | accept | Prints the public gem version only (already public in the VERSION file and gemspec); no secrets, paths, or env content reachable on this path |
</threat_model>

<verification>
- `bundle exec rspec spec/main_version_spec.rb` — 2 examples, 0 failures
- `bundle exec bin/spm-cache --version` — prints the VERSION file contents, exit 0
- `bundle exec rspec` — full suite green (416 baseline + 2 new)
- TDD gate: git log shows a ^test\(11-01\) commit preceding a ^feat\(11-01\) commit
</verification>

<success_criteria>
- RED then GREEN commits exist for the --version feature (gate sequence enforced — type: tdd plan)
- The flag prints the VERSION-file value and exits 0; behavior pinned by the VERSION-file cross-check example
- No other CLI behavior changed (full suite green)
</success_criteria>

<output>
Create `.planning/phases/11-homebrew-release-automation/11-01-SUMMARY.md` when done — include RED (what failed, why), GREEN (the intercept), REFACTOR (if any), and the commit list.
</output>
