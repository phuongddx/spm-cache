---
phase: 10-fidelity-regression-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/fidelity_edge_matrix_spec.rb
autonomous: true
requirements:
  - TEST-03
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "TEST-03 matrix: all eight v0.2.x edge classes each have exactly one passing matrix example — binary target (Class E), macro with narrow swift-syntax pin, vendored .xcodeproj, plugin-only, transitive-only, resource bundle, private Clang shim, product-not-equal-target rename"
    - "TEST-03 additive: no existing spec file is modified — the matrix is NEW coverage running alongside the scattered edge-class specs (SC3's passes-unchanged means no churn, no migration)"
    - "TEST-03 macro class: a swift-syntax identity at a narrow pinned revision that drifts is warned and classified resolution-incompatible, and the agreeing narrow pin stays host-pinned — the pin-fidelity contract holds for the macro class as pure pin data"
    - "TEST-03 resource-bundle class: a describe-JSON target with a resources array yields parsed resource paths, and its *.bundle is delivered into the assembled framework output by the real copy behavior"
    - "SC4: matrix examples perform zero real swift/xcodebuild invocations — strict not_to receive guards where no shell is expected, default-deny allowlist otherwise; the plugin/transitive legs invoke only the local compiled proxy binary (no network)"
    - "Ruby 3.1 compatibility: no syntax newer than the oldest CI matrix leg appears in the new spec file; the suite stays green on the existing ci.yml Ruby 3.1/3.2/3.3 matrix"
    - "FLAGGED ASSUMPTION (probe unclassified, unresolved): TEST-03's requirement text carried one edge-class probe category the deterministic classifier could not classify. Assumed meaning: each class's example pins the class's CURRENT production behavior as the non-regression baseline; in particular the resource-bundle class is assumed to mean a describe-JSON resources array whose bundle must reach the framework (RESEARCH A2). If the operator intends a different v0.2.x incident shape, the fixture asserts the wrong thing — surfaced for confirmation at verify time, never auto-resolved."
  artifacts:
    - path: spec/fidelity_edge_matrix_spec.rb
      provides: "TEST-03 eight-class edge matrix (one example per class, table-organized)"
      contains: "TEST-03"
  key_links:
    - from: spec/fidelity_edge_matrix_spec.rb
      to: lib/spm_cache/spm/build_pipeline.rb
      via: "classes 1/2/3/7/8 drive BuildPipeline.run on the tier-1 seam and assert sidecars/shims/rename behavior"
      pattern: "BuildPipeline"
    - from: spec/fidelity_edge_matrix_spec.rb
      to: lib/spm_cache/spm/xcframework/slice.rb
      via: "the resource-bundle class drives the real copy_resource_bundles behavior over a fabricated bundle"
      pattern: "bundle"
  prohibitions:
    - statement: "MUST NOT modify, migrate, or consolidate the existing scattered edge-class spec files — the matrix runs alongside them (CONTEXT lock: SC3 passes unchanged means no churn)"
      status: resolved
      verification: test
    - statement: "MUST NOT add a networked or real-xcodebuild leg to the matrix — SC4 hermeticity is an executable assertion (strict guards / default-deny stubs), not a convention"
      status: resolved
      verification: test
---

<objective>
Deliver TEST-03: the edge-class fixture matrix `spec/fidelity_edge_matrix_spec.rb` enumerating all 8 v0.2.x edge classes — binary target (Class E), macro with a narrow swift-syntax pin, vendored .xcodeproj, plugin-only, transitive-only, resource bundle, private Clang shim, and product-not-equal-target rename — so the matrix "passes unchanged" as new coverage alongside the untouched existing specs.

Research findings honored: 6 of 8 classes have proven hermetic shapes to lift verbatim from existing green specs; 2 (macro swift-syntax pin, resource bundle) have NO existing fidelity-scenario coverage and need new pure-data fixture shapes per RESEARCH "New fixture shape" guidance. Ruby-side only (the Swift companion keeps its own 36-test suite); macro swift-syntax pinning is lockfile/pin-driven and hermetically coverable from Ruby.

Purpose: SC3 of Phase 10 — pin the v0.2.x compatibility surface so the fidelity work of Phases 6-9 cannot regress it. Zero production code changes.

Output: One new spec file (approx. 350-450 lines, 8 matrix examples + SC4 guards), green under `bundle exec rspec spec/fidelity_edge_matrix_spec.rb`.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fidelity-regression-coverage/10-PATTERNS.md
@.planning/phases/10-fidelity-regression-coverage/10-RESEARCH.md
</context>

## Artifacts this phase produces

This plan creates:
- `spec/fidelity_edge_matrix_spec.rb` — new spec file (no production files touched)
- Local helper methods inside the spec: `stub_desc_products`, `write_resolved`, per-class fixture builders (class_e_layout, vendored_xcodeproj_pkg, clang_shim_desc_raw, rename_product_raw, macro_pins, resource_bundle_target_raw), and the tier-3 `binary`/`statuses_from` pair for the plugin leg
- Describe/it strings carrying the `TEST-03` ID and the class number (e.g. "class 1/8") for verifier traceability

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — matrix scaffold + class 1/8 binary target (Class E) end-to-end</name>
  <files>spec/fidelity_edge_matrix_spec.rb</files>
  <read_first>
  - spec/build_pipeline_spec.rb (lines 450-513 — Class E desc raw with the two-hop dummy.m forwarder chain terminating at a BinaryTarget, plus the artifacts layout and the strict no-shell guards)
  - spec/build_pipeline_provenance_spec.rb (lines 566-713 — the fidelity variant of the same layout; lines 600-640 the host-pinned sidecar assertion with pins FirebaseAnalytics => "aaa"; lines 622-623 strict guards; tier-1 scaffold lines 13-45)
  - lib/spm_cache/spm/build_pipeline.rb (lines 539 TRIVIAL_FORWARDER_SOURCES; lines 1069-1136 locate_prebuilt_xcframework and the checkouts/artifacts sibling requirement; lines 102-154 report_fidelity)
  - .planning/phases/10-fidelity-regression-coverage/10-PATTERNS.md (section "spec/fidelity_edge_matrix_spec.rb", Class 1 excerpt)
  </read_first>
  <action>
  Create `spec/fidelity_edge_matrix_spec.rb`: frozen_string_literal header, requires (spec_helper, tmpdir, json), header comment stating the matrix contract — one example per edge class, all hermetic on the tier-1 seam (plugin/transitive legs use the local compiled binary), existing specs untouched. Top-level describe: `RSpec.describe SPMCache::SPM::BuildPipeline, "TEST-03: v0.2.x edge-class fixture matrix"`. Copy the tier-1 scaffold helpers (stub_desc_products, write_resolved, tmpdir/pkg_dir/out_dir lets, XCFramework stub, after-cleanup) from the provenance spec analog.

  Class 1/8 example, `it "TEST-03 class 1/8: binary target (Class E) copies the real prebuilt xcframework and records a fidelity sidecar"` — lift the fixture verbatim from build_pipeline_provenance_spec.rb lines 600-613: build_root under tmpdir with checkouts/firebase-ios-sdk as pkg_dir, the prebuilt artifacts sibling layout artifacts/firebase-ios-sdk/FirebaseAnalytics/FirebaseAnalytics.xcframework containing ios-arm64/FirebaseAnalytics.framework/Headers/FIRAnalytics.h and an Info.plist, plus the desc raw carrying the forwarder chain whose sources are the trivial dummy list terminating at the binary target. Install the strict SC4 guards from lines 622-623 for this example (expect Buildable not_to receive(:new) and Core::Sh not_to receive(:run) — the direct-copy path must shell out to nothing). Assert the returned path is the copied xcframework AND the sidecar records host-pinned with the FirebaseAnalytics pin preserved (the fidelity angle from lines 600-640).
  </action>
  <verify>
    <automated>bundle exec rspec spec/fidelity_edge_matrix_spec.rb --format progress 2>&1 | tee /tmp/10-03-t1.out && grep -E "[0-9]+ examples?, 0 failures" /tmp/10-03-t1.out && ! grep -qi "pending" /tmp/10-03-t1.out</automated>
  </verify>
  <acceptance_criteria>
  - spec/fidelity_edge_matrix_spec.rb exists, first line `# frozen_string_literal: true`, describe string contains TEST-03
  - The Class E example creates the checkouts/artifacts sibling layout (source assertion: paths containing both "checkouts/firebase-ios-sdk" and "artifacts/firebase-ios-sdk" appear in the builder)
  - The strict no-shell guards appear for the Class E example (source assertion: grep -c "not_to receive(:run)" >= 1)
  - The example asserts BOTH the copy outcome and the sidecar fidelity_status read from disk
  - Fail-first mutation proof documented in the SUMMARY: temporarily remove the prebuilt Info.plist (or the Headers file) from the layout and confirm the class 1/8 example FAILS, then restore and re-run green
  </acceptance_criteria>
  <done>The matrix scaffold is proven on the most layout-sensitive class: Class E runs green through the real copy path with zero shell-outs and a fidelity sidecar.</done>
</task>

<task type="auto">
  <name>Task 2: Classes 3/8 vendored .xcodeproj, 4/8 plugin-only, 5/8 transitive-only, 7/8 Clang shim, 8/8 rename</name>
  <files>spec/fidelity_edge_matrix_spec.rb</files>
  <precondition>tools/spm-cache-proxy/.build/release/spm-cache-proxy is executable for the plugin/transitive legs — if missing, run `make proxy.build`; if it cannot be built, those examples skip by design and this task cannot be accepted as done.</precondition>
  <read_first>
  - spec/fidelity_edge_matrix_spec.rb (the file from Task 1)
  - spec/build_pipeline_seeding_spec.rb (lines 165-251 — vendored .xcodeproj shape) and spec/build_pipeline_provenance_spec.rb (lines 447-470 — the not-graph-pinned sidecar assertion with empty pins)
  - spec/gen_proxy_plugin_spec.rb (whole file — fixture loading lines 20-27, plugin status expectation lines 51-57, MixedLib/MixedPlugin granularity lines 72-82, be_nil idiom line 77)
  - spec/installer_consumed_dependencies_spec.rb (consumed-products set semantics for the transitive-only classification)
  - spec/build_pipeline_spec.rb (lines 113-178 — private Clang shim: desc raw with target_dependencies on a ClangTarget absent from product names, shim header on disk, find_object_file stub, XCFramework.new dispatch, shims.json containing the shim name; lines 327-360 — product-not-equal-target rename: Buildable receiving hash_including(scheme:/module_name:) and create_framework naming the framework after the target)
  - lib/spm_cache/spm/resolved_graph.rb (lines 61-63 — vendored_xcodeproj? is a glob)
  </read_first>
  <action>
  Add four tier-1 classes and one tier-3 class, one `it` each with the TEST-03 class number in the string (per PATTERNS: one it per class matches the suite's dominant style; per-class local builder methods keep it DRY):

  Class 3/8 vendored .xcodeproj: builder creates FileUtils.mkdir_p(File.join(pkg_dir, "CryptoSwift.xcodeproj")) — a single directory, since the classifier is a glob; run BuildPipeline.run and assert the sidecar fidelity_status is not-graph-pinned with an empty pins hash.

  Class 4/8 plugin-only (tier-3): separate describe block or context with the skip-guard let(:binary); load spec/fixtures/plugin-lockfile.json the way spec/gen_proxy_plugin_spec.rb loads it, run the binary, assert the SwiftGenPlugin module reports the plugin status via statuses_from.

  Class 5/8 transitive-only (tier-3, same block): a lockfile package absent from the consumed set asserts NO graph entry (be_nil idiom) — its classification is input-side membership: present in the declared universe, pinned via its consumer; assert both facts (absence from graph.json AND presence in the fixture's package list) in one example.

  Class 7/8 private Clang shim: lift the shape from build_pipeline_spec.rb lines 113-164 — desc raw whose SwiftTarget has target_dependencies naming a ClangTarget that is not a product name (the _NumericsShims shape), shim header file on disk, find_object_file stub returning the shim object file, XCFramework.new stub dispatching on name; assert the assembled framework's shims.json lists the shim target name.

  Class 8/8 product-not-equal-target rename: product raw whose targets array names a target differing from the product name; expect Buildable to receive(:new).with(hash_including(scheme: matching the product, module_name: matching the target)) and assert create_framework names the framework after the target.
  </action>
  <verify>
    <automated>test -x tools/spm-cache-proxy/.build/release/spm-cache-proxy && bundle exec rspec spec/fidelity_edge_matrix_spec.rb --format progress 2>&1 | tee /tmp/10-03-t2.out && grep -E "[0-9]+ examples?, 0 failures" /tmp/10-03-t2.out && ! grep -qi "pending" /tmp/10-03-t2.out</automated>
  </verify>
  <acceptance_criteria>
  - Five new class examples exist, each it string containing "TEST-03" and its class number 3/8, 4/8, 5/8, 7/8, 8/8 (source assertion: grep counts)
  - The vendored-xcodeproj example asserts the not-graph-pinned status value read from the sidecar file
  - The plugin example asserts on statuses_from output for the SwiftGenPlugin module (tier-3, binary executed — 0 pending in the run)
  - The transitive-only example asserts BOTH graph absence (be_nil) and input-side presence in one example
  - The shim example asserts the shims.json content on disk names the shim target
  - The rename example asserts the Buildable received hash_including with BOTH scheme and module_name keys
  </acceptance_criteria>
  <done>Six of eight classes are green: the vendored, plugin, transitive, shim, and rename legs join the Class E tracer on the same scaffold.</done>
</task>

<task type="auto">
  <name>Task 3: Classes 2/8 macro swift-syntax pin and 6/8 resource bundle — the two no-analog shapes + SC4 sweep</name>
  <files>spec/fidelity_edge_matrix_spec.rb</files>
  <read_first>
  - spec/fidelity_edge_matrix_spec.rb (the file from Tasks 1-2)
  - lib/spm_cache/spm/desc/target.rb (lines 123-125 — resource_paths parses the resources array into joined paths)
  - lib/spm_cache/spm/xcframework/slice.rb (lines 122-128 — copy_resource_bundles globs *.bundle from build products into the framework path, skipping existing destinations)
  - spec/build_pipeline_provenance_spec.rb (lines 787-806 run_with_realized_pins shape for the macro drift variant)
  - .planning/phases/10-fidelity-regression-coverage/10-RESEARCH.md (Edge-Class Matrix rows 2 and 6 — the new fixture shapes; Assumption A2)
  </read_first>
  <action>
  Class 2/8 macro with narrow swift-syntax pin — new pure-data shape (the class IS the pin-fidelity contract; no macro-specific production code exists to drive): seed resolved_pins_file with a pin whose identity is the literal swift-syntax package identity at a narrow revision (e.g. aaa111); run once with the realized read-back agreeing — assert host-pinned and no warn; run once with build_for_destination rewriting the realized swift-syntax pin to a different revision (e.g. bbb222) — assert the drift warn names the swift-syntax identity with both values and the sidecar is resolution-incompatible. One example covering both directions, or two examples — either is acceptable; keep the TEST-03 class 2/8 string.

  Class 6/8 resource bundle — new shape: fabricate a desc-JSON target carrying a resources array (path entries pointing under pkg_dir); first assert the REAL Desc::Target parser resolves them (construct the real target object from the raw hash and assert its resource_paths method returns the joined path). Then drive the real bundle-copy behavior: create a fake SomeResources.bundle directory under the build products location the real Slice copy behavior globs, construct/drive the real Slice (or the real XCFramework assembly with shell stubbed) against a fresh framework destination directory, and assert the bundle directory lands inside the assembled framework path (the copy-with-unless-exists semantics — Pitfall 14's stale-bundle behavior is what this pins).

  SC4 sweep: end the file with a hermeticity example or before-hook audit — every tier-1 example runs under the default-deny Core::Sh guard (raise on unexpected run/capture_output), and the tier-3 legs' only subprocess is the local binary. Re-run the whole file as the task gate (targeted run only); record the wall-clock of the three fidelity specs in the SUMMARY against the 2-3s target. The full-suite regression sample is the serial wave-completion check — deferred to the orchestrator's post-merge gate after all Wave 1 plans merge, never run as an in-task parallel gate (another Wave 1 plan inside its documented fail-first mutation window leaves a spec file temporarily red, which would spuriously fail a parallel full-suite run).
  </action>
  <verify>
    <automated>bundle exec rspec spec/fidelity_edge_matrix_spec.rb --format progress 2>&1 | tee /tmp/10-03-t3.out && grep -E "[0-9]+ examples?, 0 failures" /tmp/10-03-t3.out && ! grep -qi "pending" /tmp/10-03-t3.out</automated>
    <note>Full-suite regression is NOT an in-task gate: the targeted file run above is the task gate. The `bundle exec rspec` full-suite sample runs serially at wave completion (the orchestrator's post-merge gate executes it after all Wave 1 plans — 10-01/10-02/10-03 — merge). If a full-suite run is made earlier and fails only inside spec/fidelity_drift_regression_spec.rb or spec/fidelity_bucket_partition_spec.rb (another Wave 1 plan's documented fail-first mutation window leaves that file temporarily red), treat it as interference: accept and re-run after wave completion, never edit another plan's spec to force green.</note>
  </verify>
  <acceptance_criteria>
  - All eight class numbers 1/8 through 8/8 appear exactly once each as example strings in the file (source assertion: grep -c "class 2/8" == 1, "class 6/8" == 1, etc.)
  - The macro example asserts BOTH directions: agreeing narrow pin stays host-pinned with no warn, drifted revision warns naming the swift-syntax identity and classifies resolution-incompatible (source assertion: the identity literal appears in a stderr-matching regex)
  - The resource-bundle example asserts the real parser output (resource_paths on a real Desc::Target) AND the copied bundle's existence inside the framework path on disk
  - Fail-first mutation proofs documented in the SUMMARY for BOTH no-analog classes: remove the resources array (class 6 fails) and equalize the macro realized revision (class 2 fails); restored green after
  - Default-deny guard text present (grep -c "unexpected real invocation" >= 2)
  - Full suite `bundle exec rspec` exits 0 — verified as the serial wave-completion check, not inside this task's gate (the orchestrator's post-merge gate runs it after all Wave 1 plans merge; a failure traced to another Wave 1 plan's temporarily-red mutation window is interference — accept and re-run after wave completion); the three fidelity spec files' combined runtime recorded in the SUMMARY
  </acceptance_criteria>
  <done>All eight v0.2.x edge classes are pinned in one matrix file, hermetic under SC4, additive to the untouched existing specs, with both no-analog classes mutation-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec fixture data → production parsers | repo-authored desc-JSON / Package.resolved / fixture data crosses into Desc::Target, BuildPipeline, and Slice as data only |
| Spec → local compiled binary | the plugin/transitive legs shell out to the local spm-cache-proxy via system() with tmpdir paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | desc-JSON / fixture content | low | accept | fabricated hashes consumed by real parsers; no eval/exec of fixture content exists or is added |
| T-10-02 | Information Disclosure | committed fixture data | low | accept | fake identities/revisions/paths only; the only loaded committed fixture is the pre-existing plugin-lockfile.json |
| T-10-03 | Tampering | tier-3 system() invocation | low | mitigate | fixed argv against Dir.mktmpdir paths; no fixture-derived free text reaches the command string |
| T-10-SC | Supply chain (package installs) | none | low | accept | zero installs this phase; RESEARCH Package Legitimacy Audit records none |
</threat_model>

<verification>
- `bundle exec rspec spec/fidelity_edge_matrix_spec.rb` — 0 failures, 0 pending
- `bundle exec rspec` full suite — green; `git status` shows no existing spec file modified; this sample is serial and wave-completion scoped (the orchestrator's post-merge gate runs it after all Wave 1 plans merge) — an in-task parallel run failing inside another Wave 1 plan's fail-first mutation window is interference, not a regression: accept and re-run after wave completion
- `bundle exec rubocop spec/fidelity_edge_matrix_spec.rb` — clean
- Combined runtime of the three new fidelity spec files recorded against the 2-3s target
</verification>

<success_criteria>
- The v0.2.x edge-class fixture matrix passes unchanged: all 8 classes green in one file
- Existing scattered edge-class specs are byte-identical to before the phase
- The two previously-uncovered classes (macro swift-syntax pin, resource bundle) have real, mutation-proven coverage
- Hermetic per SC4; Ruby 3.1-compatible syntax throughout
</success_criteria>

<output>
Create `.planning/phases/10-fidelity-regression-coverage/10-03-SUMMARY.md` when done
</output>
