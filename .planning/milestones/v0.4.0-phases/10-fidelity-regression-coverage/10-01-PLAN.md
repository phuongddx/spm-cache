---
phase: 10-fidelity-regression-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/fidelity_drift_regression_spec.rb
autonomous: true
requirements:
  - TEST-01
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "TEST-01 drift direction: when the realized pin differs from the intended pin, BuildPipeline.run warns on stderr naming the identity with both pin values, prints resolution-incompatible on stdout, and writes a sidecar whose fidelity_status is resolution-incompatible and whose pins record the realized value"
    - "TEST-01 false-positive guard: exactly-equal intended and realized pins produce no Core::UI.warn call and a host-pinned sidecar with the pins preserved"
    - "TEST-01 pin-value precedence: revision wins over version in the drift comparison; equal version-only pins (empty revision on both sides) are not drift"
    - "TEST-01 sidecar-disagreement leg: a provenance sidecar whose recorded pin disagrees with the current pin yields a missed cache decision, never a silent hit; an agreeing sidecar pin stays a hit and an absent sidecar is a miss (Phase 9 semantics)"
    - "SC4: the drift spec executes zero real shell-outs on the tier-1 legs — default-deny Core::Sh.run / Core::Sh.capture_output stubs raise on any unexpected invocation"
    - "Edge adjacency (probe): pins that are exactly equal merge into the host-pinned classification and never collide into a false drift warning"
    - "Edge empty (probe): empty or unreadable realized pin input yields an empty drifted set — host-pinned status with an empty pins sidecar — and never raises"
    - "Edge ordering (probe): drift assertions bind per identity (single-identity examples or identity-keyed expectations); no example depends on the unspecified ordering of multiple drift warnings"
  artifacts:
    - path: spec/fidelity_drift_regression_spec.rb
      provides: "Named TEST-01 drift regression contract (resolution read-back + provenance-sidecar disagreement, both assertion directions)"
      contains: "TEST-01"
  key_links:
    - from: spec/fidelity_drift_regression_spec.rb
      to: lib/spm_cache/spm/build_pipeline.rb
      via: "BuildPipeline.run drives the real report_fidelity drift read-back; assertions bind to stdout/stderr and the sidecar JSON on disk"
      pattern: "resolution-incompatible"
    - from: spec/fidelity_drift_regression_spec.rb
      to: tools/spm-cache-proxy
      via: "sidecar-disagreement leg runs the compiled proxy binary (gen-proxy) and reads graph.json statuses"
      pattern: "gen-proxy"
  prohibitions:
    - statement: "MUST NOT modify spec/gen_proxy_provenance_spec.rb or any other existing spec file — Phase 10 coverage is strictly additive (CONTEXT lock: Phase 9's real-binary SC1-SC3 coverage stays as-is, untouched)"
      status: resolved
      verification: test
    - statement: "MUST NOT assert drift on the test double's internals — every assertion binds to production output (stdout/stderr strings or the provenance sidecar JSON on disk), never to what the stub received"
      status: resolved
      verification: test
---

<objective>
Deliver TEST-01: the named hermetic regression spec `spec/fidelity_drift_regression_spec.rb` that pins the transitive-version drift contract from Phases 8-9 — an out-of-range pin is detected and reported, never silently re-resolved — in both assertion directions (drift MUST warn; agreeing pins MUST NOT warn) and from both drift-injection sources (resolution read-back via `BuildPipeline#report_fidelity`, and provenance-sidecar disagreement via Phase 9 hit/miss semantics).

Purpose: SC1 of Phase 10 — "a regression spec fails if an out-of-range pin is silently re-resolved instead of being detected and reported". This is the v0.3.0 lesson ("an implemented feature is not a done phase") applied as executable coverage. Zero production code changes.

Output: One new spec file (approx. 250-350 lines, 6-8 examples), green under `bundle exec rspec spec/fidelity_drift_regression_spec.rb`, with documented fail-first mutation proofs.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fidelity-regression-coverage/10-PATTERNS.md
@.planning/phases/10-fidelity-regression-coverage/10-RESEARCH.md
</context>

## Artifacts this phase produces

This plan creates:
- `spec/fidelity_drift_regression_spec.rb` — new spec file (no production files touched)
- Local helper methods inside that spec (flat spec/ convention, no `spec/support/`): `stub_desc_products`, `write_resolved`, `run_with_realized_pins`, `write_lockfile`, `write_sidecar`, `run_gen_proxy`, `statuses_from`
- Describe strings carrying the `TEST-01` ID for verifier traceability

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — scaffold + one drift example end-to-end through BuildPipeline.run</name>
  <files>spec/fidelity_drift_regression_spec.rb</files>
  <read_first>
  - spec/build_pipeline_provenance_spec.rb (the analog — header comment style lines 1-12, let-scaffold lines 13-45, stub_desc_products lines 18-27, write_resolved lines 29-31, drift injection lines 47-61, assertion idiom lines 69-93)
  - lib/spm_cache/spm/build_pipeline.rb (lines 50-60 run signature; lines 102-154 report_fidelity; lines 187-191 host_pin_value; lines 220-237 sidecar write)
  - lib/spm_cache/core/log.rb (lines 14-24 — UI.info goes to stdout, UI.warn prefixes "[warn] " on stderr)
  - .planning/phases/10-fidelity-regression-coverage/10-PATTERNS.md (section "spec/fidelity_drift_regression_spec.rb")
  </read_first>
  <action>
  Create `spec/fidelity_drift_regression_spec.rb` with `# frozen_string_literal: true`, requires for spec_helper/tmpdir/json, and a header comment block in the style of spec/build_pipeline_provenance_spec.rb lines 4-11 stating what is stubbed (Desc::Description, Buildable, XCFramework via instance_double) and what stays real (BuildPipeline.run including report_fidelity, the real filesystem for Package.resolved and sidecars).

  Top-level describe: `RSpec.describe SPMCache::SPM::BuildPipeline, "TEST-01: transitive-version drift regression (read-back + provenance)"`.

  Local helper methods copied verbatim from the analog: `stub_desc_products(products)` (build_pipeline_provenance_spec.rb lines 18-27) and `write_resolved(path, identity, revision)` (lines 29-31). let-scaffold: `tmpdir`/`pkg_dir`/`out_dir`/`resolved_pins_file`; `before` block does FileUtils.mkdir_p on pkg_dir and out_dir, write_resolved(resolved_pins_file, "SomePkg", "aaa111"), stub_desc_products with one library product SomePkg, and stubs XCFramework.new to return a double whose build returns out_dir/SomePkg.xcframework; `after { FileUtils.rm_rf(tmpdir) }`.

  SC4 executable guard in the before block (per CONTEXT "Hermeticity & CI", Pattern 3 of 10-PATTERNS.md): install default-deny stubs so that any Core::Sh.run or Core::Sh.capture_output call raises with a message naming the unexpected command, then re-allow nothing on this path — the tier-1 seam must need zero shell-outs.

  Drift injection exactly per the analog lines 47-61: Buildable instance_double whose build_for_destination block rewrites pkg_dir/Package.resolved to revision bbb222 before returning the artifacts hash (derived_data, object_file, nil swiftmodule/swiftdoc/swiftsourceinfo/swiftinterface), plus a create_framework stub creating the framework directory.

  One example `it "TEST-01/SC1: a silently re-resolved pin is detected and reported, not swallowed"` asserting, in a single expect block, that BuildPipeline.run(name "SomePkg", pkg_dir, destinations ["iphonesimulator"], out_dir, resolved_pins_file, config "debug") outputs /resolution-incompatible/ to stdout AND /SomePkg.*aaa111.*bbb222/ to stderr (both channels chained — Pitfall 5), then parsing the sidecar at "#{result}.provenance.json" to assert fidelity_status equals resolution-incompatible and pins equals the realized value keyed by SomePkg.
  </action>
  <verify>
    <automated>bundle exec rspec spec/fidelity_drift_regression_spec.rb --format progress 2>&1 | tee /tmp/10-01-t1.out && grep -E "[0-9]+ examples?, 0 failures" /tmp/10-01-t1.out && ! grep -qi "pending" /tmp/10-01-t1.out</automated>
  </verify>
  <acceptance_criteria>
  - spec/fidelity_drift_regression_spec.rb exists and its first line is `# frozen_string_literal: true`
  - `grep -c 'TEST-01' spec/fidelity_drift_regression_spec.rb` is >= 2 (describe string + example string)
  - `grep -c 'unexpected real invocation' spec/fidelity_drift_regression_spec.rb` is >= 2 (default-deny guard on both Core::Sh entry points)
  - The drift example chains both output channels in one expect block (source assertion: `.to_stdout` and `.to_stderr` appear in the same expect block)
  - The sidecar assertion reads the JSON file from disk (source assertion: File.read on a path ending in provenance.json) — no assertion on the double's internals
  - Fail-first mutation proof documented in the plan SUMMARY: temporarily change the drift-injection rewrite value from bbb222 to aaa111 (making realized equal intended), re-run the file, confirm the TEST-01/SC1 example FAILS, then restore bbb222 and re-run green. The mutation is never committed.
  </acceptance_criteria>
  <done>One drift example passes end-to-end through the real report_fidelity path with zero real shell-outs, and its fail-first sensitivity is proven by the documented mutation check.</done>
</task>

<task type="auto">
  <name>Task 2: False-positive guard + pin-precedence variants + empty-input edges</name>
  <files>spec/fidelity_drift_regression_spec.rb</files>
  <read_first>
  - spec/fidelity_drift_regression_spec.rb (the file created in Task 1)
  - spec/build_pipeline_provenance_spec.rb (lines 96-124 false-positive guard, key line 110 not_to receive(:warn); lines 787-806 run_with_realized_pins helper; lines 852-874 revision-vs-version precedents)
  - lib/spm_cache/spm/build_pipeline.rb (lines 156-191 drifted_identities intersection-only semantics and host_pin_value revision-wins-over-version; the comment on nil either side carrying no drift evidence)
  - lib/spm_cache/core/package_resolved.rb (lines 60-72 pins_or_nil tolerant reader)
  </read_first>
  <action>
  Add a parametrized runner helper `run_with_realized_pins(realized)` modeled on build_pipeline_provenance_spec.rb lines 787-806: installs the Buildable stub writing an arbitrary realized Package.resolved JSON (multi-pin capable: the helper accepts a hash of identity => pin-state), calls described_class.run with the standard scaffold, returns the result path.

  False-positive guard example: `it "TEST-01: agreeing pins never warn (false-positive guard)"` — build_for_destination leaves pkg_dir/Package.resolved at the seeded aaa111 value (or rewrites the same value); expect Core::UI not_to receive(:warn) around the run; then assert the sidecar fidelity_status is host-pinned and pins preserve the agreeing value keyed by SomePkg.

  Pin-precedence examples (two, from the analog lines 852-874): (a) intended revision aaa111 vs realized pin carrying a version with an empty revision — the comparison uses the version string on the realized side, values differ, so drift IS detected (warn + resolution-incompatible sidecar); (b) both sides version-only with equal versions and empty revisions — no drift, host-pinned, no warn.

  Empty/unreadable-input examples: (c) a resolved file whose top-level pins array is empty — the drifted set is empty, sidecar fidelity_status is host-pinned with an empty pins hash, no warn, no raise; (d) a run whose pkg_dir/Package.resolved is never written (missing file) — same host-pinned + empty-pins outcome, no raise (nil realized carries no drift evidence per the drifted_identities comment).
  </action>
  <verify>
    <automated>bundle exec rspec spec/fidelity_drift_regression_spec.rb --format progress 2>&1 | tee /tmp/10-01-t2.out && grep -E "[0-9]+ examples?, 0 failures" /tmp/10-01-t2.out && ! grep -qi "pending" /tmp/10-01-t2.out</automated>
  </verify>
  <acceptance_criteria>
  - The false-positive example uses `expect(SPMCache::Core::UI).not_to receive(:warn)` (source assertion: grep "not_to receive(:warn)" >= 1)
  - At least 4 new examples exist in this task (false-positive, revision-vs-version drift, equal version-only, empty pins) plus the missing-file example — file runs with at least 6 examples total, 0 failures, 0 pending
  - The empty-pins example asserts the sidecar pins equal an empty Ruby hash on disk (source assertion: an expectation against {} read from the parsed sidecar)
  - No example in the file contains a sleep call (source assertion: grep -c "sleep" equals 0)
  </acceptance_criteria>
  <done>Both assertion directions and the pin-value precedence rules are pinned; empty and missing realized inputs are proven benign (host-pinned, never a raise).</done>
</task>

<task type="auto">
  <name>Task 3: Provenance-sidecar disagreement leg (Phase 9 semantics, tier-3 real-binary)</name>
  <files>spec/fidelity_drift_regression_spec.rb</files>
  <precondition>tools/spm-cache-proxy/.build/release/spm-cache-proxy is executable — if missing, run `make proxy.build` before this task; if it cannot be built, these examples skip by design (CI builds it before RSpec on every matrix leg) and the task cannot be accepted as done.</precondition>
  <read_first>
  - spec/gen_proxy_provenance_spec.rb (the whole file — skip-guard lines 15-27, write_lockfile lines 29-37, write_sidecar lines 39-44, run_gen_proxy lines 46-50, statuses_from lines 52-55, SC1-SC3 scenarios lines 57-156)
  - spec/fidelity_drift_regression_spec.rb (the file from Tasks 1-2)
  - lib/spm_cache/cache/cachemap.rb (lines 15-33 status readers)
  </read_first>
  <action>
  Add a second top-level describe block to the same file: `RSpec.describe "TEST-01: provenance-sidecar disagreement is a cache miss, never a silent hit (gen-proxy real-binary)"` carrying the tier-3 pattern copied from spec/gen_proxy_provenance_spec.rb: skip-guard `let(:binary)` resolving tools/spm-cache-proxy/.build/release/spm-cache-proxy via SPMCache::ROOT with a `skip "spm-cache-proxy binary not built (run make proxy.build)"` before-hook, plus local write_lockfile / write_sidecar / run_gen_proxy / statuses_from helper methods (verbatim from lines 29-55 of the analog). Do NOT require the gen-proxy legs to run under the tier-1 default-deny Sh guard — the real-binary leg invokes the local proxy via system() with output redirected to File::NULL, which involves no Core::Sh call and no network.

  Examples (one lockfile package pinned at revision aaa111, cache dir holding fabricated sidecars):
  - agreeing sidecar pin (aaa111) for the module — statuses_from reports the module as hit
  - disagreeing sidecar pin (bbb222) — statuses_from reports the module as missed (this IS the drift signal at the cache layer: disagreement can never serve the stale artifact)
  - no sidecar at all — statuses_from reports missed (the v0.3.0-upgrade signal)

  Keep the existing spec/gen_proxy_provenance_spec.rb completely untouched — this leg re-covers the contract under the TEST-01 name inside the new file.
  </action>
  <verify>
    <automated>test -x tools/spm-cache-proxy/.build/release/spm-cache-proxy && bundle exec rspec spec/fidelity_drift_regression_spec.rb --format progress 2>&1 | tee /tmp/10-01-t3.out && grep -E "[0-9]+ examples?, 0 failures" /tmp/10-01-t3.out && ! grep -qi "pending" /tmp/10-01-t3.out</automated>
  </verify>
  <acceptance_criteria>
  - The file contains a second describe block whose string contains TEST-01 (source assertion: grep -c "TEST-01" >= 4 across describe/example strings)
  - The skip-guard literal `skip "spm-cache-proxy binary not built` is present exactly once for this describe block
  - Exactly three sidecar-leg examples exist (hit on agreement, missed on disagreement, missed on absence), each asserting via statuses_from on graph.json — no assertion on the fabricated sidecar file alone
  - The binary was actually exercised in the accepting run: /tmp/10-01-t3.out reports 0 failures AND 0 pending (a skipped leg would report pending and fails this criterion)
  - Fail-first mutation proof documented in the SUMMARY: temporarily change the disagreeing-sidecar example's fabricated pin to the agreeing value and confirm that example FAILS, then restore and re-run green
  - git status confirms spec/gen_proxy_provenance_spec.rb is unmodified
  </acceptance_criteria>
  <done>The Phase 9 provenance-disagreement contract is pinned under the TEST-01 name: pin disagreement and sidecar absence are misses, never silent hits; the file is fully green with the real binary exercised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec fixture data → production parsers | repo-authored JSON (Package.resolved / sidecars / lockfile) crosses into Core::PackageResolved and report_fidelity as data only |
| Spec → local compiled binary | the tier-3 leg shells out to the local spm-cache-proxy binary via system() with tmpdir-derived paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | spec fixture content | low | accept | fixtures are inert JSON consumed by parsers; no eval/exec of fixture content exists or is added — convention retained |
| T-10-02 | Information Disclosure | committed fixtures/sidecars | medium | mitigate | fake identities/revisions/URLs only, no credentials (existing convention); reviewer inspects the committed spec in code review |
| T-10-03 | Tampering | run_gen_proxy system() command construction | low | mitigate | paths originate from Dir.mktmpdir and SPMCache::ROOT; no fixture-derived free text reaches the command string (Shellwords escaping per the gen_proxy_ignore precedent if patterns are ever passed) |
| T-10-SC | Supply chain (package installs) | none | low | accept | no package-manager installs this phase (test-only; stdlib + existing dev deps); RESEARCH Package Legitimacy Audit records zero installs |
</threat_model>

<verification>
- `bundle exec rspec spec/fidelity_drift_regression_spec.rb` — 0 failures, 0 pending (proxy binary present)
- `bundle exec rspec` full suite — no regression to the existing 387 examples
- `bundle exec rubocop spec/fidelity_drift_regression_spec.rb` (or `make format`) — clean
- Fail-first sensitivity of the drift example and the sidecar-disagreement example documented in the SUMMARY (mutation runs, never committed)
</verification>

<success_criteria>
- A named regression spec exists that FAILS when an out-of-range pin is silently re-resolved (mutation-proven), and FAILS when a disagreeing provenance sidecar would be served as a hit (mutation-proven)
- Agreeing pins provably do not warn (false-positive guard)
- Zero real shell-outs on tier-1 legs (executable default-deny guard), zero network, zero xcodebuild
- Ruby 3.1-compatible syntax only; file passes RuboCop
</success_criteria>

<output>
Create `.planning/phases/10-fidelity-regression-coverage/10-01-SUMMARY.md` when done
</output>
