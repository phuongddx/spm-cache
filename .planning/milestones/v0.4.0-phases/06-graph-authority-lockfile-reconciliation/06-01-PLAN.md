---
phase: 06-graph-authority-lockfile-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md
  - .planning/STATE.md
autonomous: true
requirements: [FID-01, FID-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The stale-transitive release build on the reference 59-70 package project is reproduced, and the cause is attributed per-package to exactly one of H-lock / H-wrongfile / H-float (ROADMAP success criterion 4)"
    - "The attribution is falsifiable: every verdict is backed by a printed H (host pin) / L (lock entry) / U (umbrella resolved pin) triple plus the umbrella's emitted requirement kind (revision: vs from:)"
    - "The `main`-branch zero-overlap finding and the two-candidate `Package.resolved` finding are recorded as dated field evidence before any locator change touches the tree (D-12)"
    - "The attribution is readable by the Phase 6 verifier from the artifact trail: measurement file + a dated STATE.md decision (D-13)"
  artifacts:
    - path: ".planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md"
      provides: "M1 reproduction record and per-package attribution table"
      contains: "H-wrongfile"
      min_lines: 60
    - path: ".planning/STATE.md"
      provides: "Dated M1 attribution decision in the Decisions list"
      contains: "[Phase 06 — M1]"
  key_links:
    - from: ".planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md"
      to: ".planning/STATE.md"
      via: "the attribution verdict recorded in the measurement file is restated verbatim as a dated STATE.md decision"
      pattern: "Phase 06 — M1"
---

<objective>
Run M1 — the blocking measurement that is the **first work of Phase 6**. Reproduce the
motivating stale-transitive release build on the real reference project and attribute the
failure, per package, between three competing mechanisms: the stale lockfile chain
(H-lock), the wrong-file locator (H-wrongfile), and isolated per-package re-resolution
(H-float).

Purpose: Phase 7's design is locked against this answer, and research finding A moved
H-wrongfile to leading-hypothesis status on `main`. Running the read-only half of M1
*before* any locator change is the only chance to observe pre-fix behavior. If this runs
after Plan 02, the evidence is destroyed.

Output: `06-M1-MEASUREMENT.md` carrying the candidate-file inventory, the live
`DiffDetector` verdict, a per-package H/L/U comparison table, and a stated verdict per the
research Q7 decision table — plus a dated STATE.md decision restating it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-graph-authority-lockfile-reconciliation/06-RESEARCH.md
@.planning/phases/06-graph-authority-lockfile-reconciliation/06-CONTEXT.md
</context>

## Decision ID Map (CONTEXT.md `<decisions>` → stable IDs)

`06-CONTEXT.md` records decisions as prose bullets with no IDs. These IDs are assigned here
and used verbatim across all four Phase 6 plans.

| ID | Locked decision (CONTEXT.md `<decisions>`) |
|----|--------------------------------------------|
| D-01 | Package in lock but absent from host `Package.resolved` is **dropped** |
| D-02 | Package in host resolved but absent from lock is **added with no enriched products**, leaving existing enrichment to populate products later |
| D-03 | Reconciliation runs **whenever `DiffDetector` reports a non-empty diff**, before umbrella generation |
| D-04 | Missing or unreadable host `Package.resolved` → **warn once and leave the lock untouched** |
| D-05 | Enriched `products[]` survives reconciliation intact; reconciliation updates `version`/`revision` only |
| D-06 | `doctor` check compares **set membership AND version**, not versions on the intersection only |
| D-07 | Drift → **`:warn`** verdict with fix hint "run `spm-cache use` to reconcile" |
| D-08 | Comparison uses **`revision` primarily, falling back to `version`** |
| D-09 | No lockfile yet → **`:ok`** |
| D-10 | The check is **static**: reads two files, compares them; no build, no resolve, no shell-out |
| D-11 | Reproduce against the **`feature/spm-cache-integration`** branch of the reference project |
| D-12 | Additionally record **`main`'s zero-overlap finding** as already-captured field evidence |
| D-13 | Attribution recorded in **Phase 6 SUMMARY.md plus a dated STATE.md decision** |
| D-14 | If reconciliation alone fully explains the field case, **Phase 7 still proceeds, re-scoped** |
| D-15 | Claude's discretion: internal structure of `Core::PackageResolved`, method naming, how reconciliation is factored out |
| D-16 | Claude's discretion: spec organization and fixture shape, subject to the hermetic seam convention |
| D-17 | Claude's discretion: exact wording of warnings and the `doctor` report line |

<tasks>

<task type="auto">
  <name>Task 1: M1 steps 0-3 — read-only candidate inventory, live DiffDetector verdict, and per-package H/L/U comparison</name>
  <precondition>The reference project directory `/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/StressMonitor` exists and `git -C` on it reports a working tree (external artifact; not produced by any plan)</precondition>
  <reversibility rating="reversible">Pure read-only measurement plus one new documentation file; undo is deleting the file.</reversibility>
  <files>.planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md</files>
  <read_first>
    - `.planning/phases/06-graph-authority-lockfile-reconciliation/06-RESEARCH.md` sections "Q1 — Finding A" and "Q7 — M1 Method (Falsifiable)" — the commands and the decision table are specified there; do not re-derive them
    - `lib/spm_cache/core/diff_detector.rb` lines 121-165 (`live_packages`, `find_package_resolved`, `merge_project_refs`) — the code being measured
    - `lib/spm_cache/installer.rb` lines 164-192 (`generate_lockfile_from_resolved`) — the early-return that freezes the lock
    - `tools/spm-cache-proxy/Sources/Core/Lockfile.swift` lines 100-135 (`versionRequirement`) — decides whether a package is emitted `revision:` or `from:`, which is the H-float falsifier
  </read_first>
  <action>
Do NOT modify any file under `lib/`, `spec/`, or `tools/` in this task. This is a
measurement of pre-fix behavior; changing the locator first destroys the evidence.

Create `.planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md`
with these five sections, in order:

`## Step 0 — Resolved-file candidate inventory`. For BOTH the reference project's `main`
branch and its `feature/spm-cache-integration` branch (per D-11 and D-12), enumerate every
`Package.resolved` under the project root excluding `*/.build/*`, and for each record: mtime,
pin count, and the sorted list of pin identities. Then print the raw ordering that
`Dir.glob(File.join(<the .xcodeproj>, "**/Package.resolved"))` returns and state which entry
`.find { |f| File.exist?(f) }` selects. Preserve `spm-cache.lock` to a scratch copy before
switching branches, and record `git status --porcelain` and `git stash list` for the project
before touching it so the pre-existing dirty state is on record.

`## Step 1 — Live DiffDetector verdict`. Instantiate `SPMCache::Core::DiffDetector` against
the reference project from within this repo (`bundle exec ruby -e` with `$LOAD_PATH.unshift "lib"`),
print the path `find_package_resolved` selects, and print `detect.summary` plus the raw
`added` / `removed` / `updated` array sizes. Record this for both branches.

`## Step 2 — Set arithmetic per candidate`. For EACH candidate resolved file found in step 0
(not only the one the locator picks), print `|lock|`, `|resolved|`, and `|intersection|`,
keyed with `DiffDetector`'s own identity semantics — reuse `identity_key` / `normalize_url`
from `lib/spm_cache/core/diff_detector.rb` rather than comparing raw URL strings, so
ssh/https and `.git`-suffix variants do not register as false non-overlap.

`## Step 3 — Umbrella emission and realization`. Print every `.package(` line from
`<ref>/spm-cache/packages/umbrella/Package.swift`, noting for each package whether the
emitted requirement is an exact commit pin or a floating lower bound. Then print every pin
from `<ref>/spm-cache/packages/umbrella/Package.resolved` as `identity / version / revision`.

`## Per-package attribution table`. One row per package, columns:
`package | H (host pin) | L (lock entry) | U (umbrella resolved pin) | emitted requirement kind | verdict`.
Assign the verdict strictly by the research Q7 decision table: `U == L` and `L != H` is
H-lock; `U == L == H` on the picked file while the picked file differs from the file Xcode
writes is H-wrongfile; `U != L` and `L == H` is H-float; `U != L` and `L != H` is both.
Add a closing `### Falsifiers applied` paragraph naming, for at least one concrete package,
which observation ruled out which hypothesis — an exact-commit emission cannot float, so any
package emitted as an exact pin whose umbrella pin differs from its lock entry refutes
H-float for that package.

Leave a `## Step 4 — Release build reproduction` heading with the single line `Pending — Task 2`.
Task 2 replaces that placeholder line; the two tasks are strictly sequential within this plan,
so Task 2's file-wide absence gate on it is satisfiable by construction.

<!-- planner-region-allow: Pending — Task 2 -->

If the `feature/spm-cache-integration` branch does not contain the ExyteChat/MediaPicker
state described in D-11 (research assumption A3), do not improvise a substitute: record that
under a `### A3 — assumption failed` heading with the actual branch contents, complete every
other section against `main`, and state plainly that success criterion 4's reproduction half
is blocked pending a re-scope. Return the plan-level blocker rather than inventing evidence.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md && for h in "## Step 0" "## Step 1" "## Step 2" "## Step 3" "## Per-package attribution table" "### Falsifiers applied"; do grep -q "$h" .planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md || { echo "MISSING SECTION: $h"; exit 1; }; done; grep -Eq 'H-lock|H-wrongfile|H-float' .planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md</automated>
    <automated>git -C . diff --name-only HEAD -- lib spec tools | grep -q . && { echo "FAIL: measurement task modified source"; exit 1; } || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `06-M1-MEASUREMENT.md` exists and contains all six required headings: `## Step 0 — Resolved-file candidate inventory`, `## Step 1 — Live DiffDetector verdict`, `## Step 2 — Set arithmetic per candidate`, `## Step 3 — Umbrella emission and realization`, `## Per-package attribution table`, `### Falsifiers applied`
    - Step 0 lists **at least two** `Package.resolved` candidates for the reference project with differing pin counts, and names which one `.find { |f| File.exist?(f) }` selects
    - Step 0 records the pre-existing `git status --porcelain` and `git stash list` output for the reference project
    - Step 2 reports `|intersection|` for **every** candidate file, not only the selected one, and states that keying used `DiffDetector#identity_key` / `#normalize_url`
    - The per-package attribution table has one row per package and every row's `verdict` cell is exactly one of `H-lock`, `H-wrongfile`, `H-float`, or `both`
    - `### Falsifiers applied` names at least one package and the specific observation that ruled out a hypothesis for it
    - `git diff --name-only HEAD -- lib spec tools` is empty (no source file was touched by this task)
    - The `## Step 4 — Release build reproduction` heading exists with the placeholder line
  </acceptance_criteria>
  <done>The pre-fix behavior of the locator, the lock, and the umbrella is recorded as printed evidence, and every package in the reference graph carries a hypothesis verdict drawn from the Q7 decision table rather than from narrative.</done>
</task>

<task type="auto">
  <name>Task 2: Reproduce the release build (M1 step 4) and record the attribution as a dated STATE.md decision</name>
  <precondition>Branch `feature/spm-cache-integration` exists in the reference project (`git -C <ref> rev-parse --verify feature/spm-cache-integration` succeeds) — research assumption A3</precondition>
  <reversibility rating="reversible">Appends to one documentation file and one STATE.md list; undo is a revert of the two edits.</reversibility>
  <files>.planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md` — Task 1's output; the attribution table is the input to this task's decision text
    - `.planning/STATE.md` lines 125-136 (`## Decisions`) — match the existing `- [Phase NN — Name]: …` bullet shape exactly
    - `.planning/phases/06-graph-authority-lockfile-reconciliation/06-RESEARCH.md` section "Q7 — M1 Method (Falsifiable)" step 4 — the release-build command and the scheme-name caveat (A4)
    - `.planning/ROADMAP.md` Phase 6 `**Measurements (blocking)**` — M1's stated obligation and what it blocks
  </read_first>
  <action>
Run the Release-configuration `xcodebuild` on the reference project's
`feature/spm-cache-integration` branch, capturing the full log to a scratch path. Confirm the
scheme name with `xcodebuild -list` first rather than assuming `StressMonitor` (research
assumption A4). If the build fails for a reason unrelated to dependency versions (signing,
missing SDK, unresolvable local path), record the failure verbatim and continue — the
measurement's purpose is version attribution, not a green build.

Fill in `## Step 4 — Release build reproduction` in
`06-M1-MEASUREMENT.md` with: the exact command run, the exit status, and for each package
named in the attribution table the version actually linked according to the log, compared
against that package's host pin. State explicitly whether the motivating symptom (a linked
transitive version OLDER than the host pin) reproduced.

Then add `## M1 verdict` to the same file: one paragraph stating the dominant mechanism, the
per-package counts for each verdict class, and what this implies for Phase 7's scope. Per
D-14, state that Phase 7 proceeds regardless — the upward-drift mechanism was independently
reproduced in research — and name the specific re-scope implied by the counts.

Finally append one bullet to `.planning/STATE.md`'s `## Decisions` list (D-13), prefixed
`- [Phase 06 — M1, 2026-08-27]:` and matching the surrounding bullet style. It must state the
dominant mechanism, the per-package verdict counts, and the Phase 7 implication in one
sentence, and reference `06-M1-MEASUREMENT.md` as the evidence. Do not restructure or reflow
any other part of STATE.md; append only.
  </action>
  <verify>
    <automated>grep -q '## Step 4 — Release build reproduction' .planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md && ! grep -q 'Pending — Task 2' .planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md && grep -q '## M1 verdict' .planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md && grep -q '\[Phase 06 — M1' .planning/STATE.md</automated>
    <automated>git diff --stat HEAD -- .planning/STATE.md | grep -Eq '1 file changed, [1-9][0-9]? insertions?\(\+\)' || { echo "FAIL: STATE.md edit is not append-only"; exit 1; }</automated>
    <human-check>Open the captured build log and confirm the recorded per-package linked versions match what the log actually shows — this is the one claim in the phase that no automated gate can validate, and a mis-transcribed version invalidates Phase 7's design lock.</human-check>
  </verify>
  <acceptance_criteria>
    - `## Step 4 — Release build reproduction` contains the exact `xcodebuild` command, its exit status, and a per-package linked-version-vs-host-pin comparison
    - The placeholder line `Pending — Task 2` is gone from the measurement file
    - Step 4 states explicitly, in one sentence, whether a linked transitive version older than the host pin reproduced
    - `## M1 verdict` names the dominant mechanism and gives an integer count for each of `H-lock`, `H-wrongfile`, `H-float`, `both`
    - `## M1 verdict` states that Phase 7 proceeds (D-14) and names the concrete re-scope the counts imply
    - `.planning/STATE.md` contains exactly one new bullet starting `- [Phase 06 — M1, 2026-08-27]:` that names the dominant mechanism, the verdict counts, and cites `06-M1-MEASUREMENT.md`
    - `git diff --stat HEAD -- .planning/STATE.md` shows insertions only (no deletions) — the edit is append-only
    - If A4 forced a different scheme name, the actual scheme is recorded verbatim in Step 4
  </acceptance_criteria>
  <done>M1 is closed: the field failure is reproduced (or its non-reproduction is recorded with cause), the attribution is stated with per-verdict counts, and the answer Phase 7's design lock depends on lives in both the measurement file and STATE.md.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 01 scope)

| Kind | Symbol / path | Notes |
|------|---------------|-------|
| New file | `.planning/phases/06-graph-authority-lockfile-reconciliation/06-M1-MEASUREMENT.md` | M1 record: candidate inventory, DiffDetector verdict, set arithmetic, umbrella emission, per-package attribution table, release-build reproduction, verdict |
| Modified file | `.planning/STATE.md` | One appended `## Decisions` bullet: `[Phase 06 — M1, 2026-08-27]` |
| No code symbols | — | This plan creates no classes, methods, CLI flags, spec files, or diagnostics check names |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference project files → this repo's Ruby code | `Package.resolved`, `spm-cache.lock`, and `project.pbxproj` from a separate on-disk project are parsed by code in this repo. Untrusted only to the degree any repo you already build is. |
| reference project git state → measurement record | The measurement switches branches in a project that is not this repo and may have uncommitted work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Tampering | reference project working tree (branch switch in Task 1/2) | medium | mitigate | Record `git status --porcelain` and `git stash list` BEFORE any branch switch, and copy `spm-cache.lock` to a scratch path first, so uncommitted reference-project work is recoverable. Never commit in the reference project. |
| T-06-12 | Information Disclosure | `06-M1-MEASUREMENT.md` committed to this repo | low | mitigate | Record package identities, versions, revisions, and file paths only. Do not paste build logs wholesale — no signing identities, team IDs, provisioning profile UUIDs, or absolute paths containing credentials. |
| T-06-13 | Repudiation | attribution verdict | medium | mitigate | Every verdict cell must be derivable from the printed H/L/U triple in the same table, so a later reader can recompute it. Narrative-only attribution is rejected by the acceptance criteria. |

**Package legitimacy:** no `npm`/`pip`/`cargo` install task exists in this plan or this phase — research recorded the Package Legitimacy Audit as N/A (no external package is introduced). No `T-06-SC` supply-chain row and no legitimacy checkpoint applies.
</threat_model>

<verification>
- `bundle exec rspec` — must remain green and unchanged in example count (baseline 258); this plan touches no source
- `git diff --name-only HEAD -- lib spec tools` — must be empty
- The two artifacts exist and pass the structural greps in each task's `<automated>` block
</verification>

<success_criteria>
- ROADMAP success criterion 4 is either satisfied (reproduced + attributed) or its blockage is recorded with the concrete failed assumption (A3/A4)
- Every package in the reference graph carries exactly one verdict from `{H-lock, H-wrongfile, H-float, both}`
- The `main`-branch zero-overlap and two-candidate findings are on record as dated evidence (D-12)
- A dated `[Phase 06 — M1, 2026-08-27]` decision exists in `.planning/STATE.md` (D-13)
- No file under `lib/`, `spec/`, or `tools/` was modified
</success_criteria>

<output>
Create `.planning/phases/06-graph-authority-lockfile-reconciliation/06-01-SUMMARY.md` when done.
It MUST restate the M1 verdict and per-verdict counts inline (D-13 requires the attribution in
SUMMARY.md, not only in the measurement file).
</output>
