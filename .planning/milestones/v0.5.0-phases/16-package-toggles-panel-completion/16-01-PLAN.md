---
phase: 16-package-toggles-panel-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/spm_cache/core/config.rb
  - lib/spm_cache/command/off.rb
  - lib/spm_cache/web/router.rb
  - lib/spm_cache/web/read_models/state.rb
  - .gitignore
  - spec/web_integration_spec.rb
  - spec/config_mutator_spec.rb
  - spec/command_off_shared_mutator_spec.rb
autonomous: true
requirements: [TOGL-01]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low   # estimate-calibration: 0 samples → factor 1, confidence low

must_haves:
  truths:
    - "D-04 (the tracer truth): an authenticated POST /api/toggle turns a package's caching off THROUGH the shared Config mutator — the on-disk spm-cache.yml's ignore array gains the exact entry, the write is atomic (tmpfile + rename over the config path), and the very next GET /api/state serves that row as saved-not-cached while its applied graph status still says cached — bundle exec rspec spec/web_integration_spec.rb"
    - "D-04 (sidecar lock target — PROBED P5/P6): the mutual exclusion is an flock on the inode-stable SIDECAR `<config_path>.lock`, NEVER on the yml inode the rename replaces; a second process holding the sidecar blocks a concurrent mutation until release, and the lock is released in ensure even when the write raises — bundle exec rspec spec/config_mutator_spec.rb"
    - "D-04 (clobber-proof, PROBED P3 as a spec): the mutator RE-READS the config from disk INSIDE the lock, so a toggle applied against a boot-time snapshot can never discard an ignore entry another writer added since — the stale-writer scenario that silently loses an entry today ends with BOTH entries present — bundle exec rspec spec/config_mutator_spec.rb"
    - "D-03 (one source of truth): `spm-cache off` writes the ignore list through the SAME Config mutator the web POST uses, and its published contract is unchanged — both output lines byte-exact, exit status unchanged, and the resulting file byte-identical to today's full-9-key YAML.dump shape on the uncontended path — bundle exec rspec spec/command_off_shared_mutator_spec.rb"
    - "Pitfall 3 (purity): a mutation ASSIGNS a new array to raw['ignore'] and never mutates in place, so DEFAULT_CONFIG['ignore'] stays empty and a freshly constructed Config never inherits another project's entries — bundle exec rspec spec/config_mutator_spec.rb"
    - "Hermeticity + blast radius: Config.configure(project_dir:) re-derives config_path from that project_dir unless an explicit config_path: is supplied, so no mutator write — from a spec or from a server booted elsewhere — can ever reach a spm-cache.yml outside the configured project — bundle exec rspec spec/config_mutator_spec.rb"
  artifacts:
    - "lib/spm_cache/core/config.rb (MOD: #set_ignored_all / #set_ignored shared mutators with sidecar-flock + in-lock reload; #save upgraded in place to same-dir Tempfile + File.rename with mode preservation; #config_lock_path; Config.configure re-derives config_path)"
    - "lib/spm_cache/command/off.rb (MOD: run routed through the shared mutator — the ONLY behavior-preserving change to a published CLI verb)"
    - "lib/spm_cache/web/router.rb (MOD: the /api/toggle dispatch arm — tracer shape; the full validation matrix is 16-04)"
    - "lib/spm_cache/web/read_models/state.rb (MOD: per-row saved_cached / applied_cached / pending from a FRESH per-call disk read of the ignore list; the reason + toggleable derivation is 16-03)"
    - ".gitignore (MOD: the sidecar lock artifact `spm-cache.yml.lock`)"
    - "spec/config_mutator_spec.rb (NEW: lock target, in-lock reload, clobber-proofing, atomicity, purity, release-on-raise, path derivation)"
    - "spec/command_off_shared_mutator_spec.rb (NEW: the D-03 byte-identical pins — no Command::Off spec exists today)"
    - "spec/web_integration_spec.rb (MOD: the toggle→disk→state tracer rows)"
  key_links:
    - "The mutator lives on Core::Config, so BOTH callers — `spm-cache off` in its own process and the web POST in the server process — inherit the lock, the fresh in-lock read, and the atomic replace by construction; there is no second write path to keep in sync (D-03)"
    - "The lock is a SIDECAR because the write is a rename: PROBED P5 showed a rename over a lock-held yml orphans the lock chain and lets a second writer in; PROBED P6 showed the sidecar survives the rewrite (build.rb:88-104 is the idiom, only the path differs)"
    - "The read model reads the ignore list from DISK per call and never touches the Config singleton's @raw — the web singleton is a boot-time snapshot (web.rb:41-43) and trusting it is exactly CP1"
    - "Config#save is upgraded IN PLACE rather than shadowed by a second atomic writer, so `spm-cache init`'s save inherits atomicity too and no caller has to choose between two save methods"
  prohibitions:
    - statement: "MUST NOT flock the config file itself — the tmp+rename write orphans that lock (PROBED P5); the sidecar `<config_path>.lock` is the only sound target"
      status: resolved
      verification: test
    - statement: "MUST NOT merge the toggle into an in-memory snapshot — the mutator RE-READS the config from disk inside the lock before assigning (CP1/Pitfall 2)"
      status: resolved
      verification: test
    - statement: "MUST NOT mutate raw['ignore'] in place (`<<`, push, delete) — DEFAULT_CONFIG's inner arrays are shared by dup and the frozen hash does not protect them (Pitfall 3); assign a new array"
      status: resolved
      verification: test
    - statement: "MUST NOT change `spm-cache off`'s observable contract — the two output lines, the exit status and the written file shape are the published surface and stay byte-identical on the uncontended path (D-03, costly)"
      status: resolved
      verification: test
    - statement: "MUST NOT read the flock(LOCK_NB) verdict as an exception — Ruby returns false under contention and never raises (PROBED P4); any probe must be a truthiness check"
      status: resolved
      verification: test
    - statement: "MUST NOT slot-gate the toggle route or touch Web::Jobs from it — the slot governs Apply-now only (D-08)"
      status: resolved
      verification: test
    - statement: "MUST NOT print, announce or log anything from the mutator — a web request has no stream and the terminal running spm-cache web stays quiet (T-13-03)"
      status: resolved
      verification: judgment
---

# Plan 16-01 — TRACER: one browser toggle, end to end (POST → shared mutator → locked atomic yml write → saved-vs-applied on /api/state)

<planning_context>
phase: 16 — Package Toggles + Panel Completion
mode: standard (tracer-first)
sources: 16-CONTEXT.md D-03 (costly), D-04, D-05, D-08; 16-RESEARCH.md §Q1 (ignore-list truth, the off write path, PROBED P1-P3), §Q2 (the sidecar-flock merge-write, PROBED P4-P6, failure posture), §Q3 (saved/applied truths, the fresh-read requirement), Pitfalls 1-5; 16-UI-SPEC.md Interaction contract (the `POST /api/toggle {"package","cached"}` shape) and A1; 16-VALIDATION.md per-task rows 1-2 and the Wave-1 write-path contract; ROADMAP Phase 16 SC1; REQUIREMENTS TOGL-01
tracer rationale: the phase's unproven architecture is the WRITE path — a config file that until now only a single-process CLI ever rewrote, now written by a long-lived server under contention, with a lock whose only sound target was settled by machine probe. The thin slice crosses every server-side layer the phase touches (HTTP mutation arm → shared mutator → sidecar flock → atomic replace → fresh-read derivation on the read model) and proves them together on one package. The frontend layer is deliberately outside the tracer (13/14/15 proved that stack browser-true; 16-05 builds on it and 16-06 nets it). Plans 16-02..16-05 expand from this slice.
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase. Plan 16-03 replaces the tracer's minimal
row derivation with the full toggleable/reason matrix on the same seam; 16-04 widens the /api/toggle
arm into the full validation matrix and adds /api/revert + /api/apply on the mutator landed here;
16-05's checkbox POSTs to this exact endpoint and renders these exact row fields. The mutator
signature, the `<config_path>.lock` sidecar path and the row field names are contracts from the
moment this plan commits.
</downstream_consumer>

<objective>
Make the dashboard a config writer, safely, on one package: an authenticated POST /api/toggle calls
one shared Config mutator that takes an flock on the inode-stable sidecar, RE-READS spm-cache.yml
from disk, assigns the single toggled key, replaces the file atomically by rename, releases in
ensure — and the next GET /api/state serves that row as saved-not-cached while its applied graph
status still says cached. `spm-cache off` is refactored onto the same mutator with its published
contract pinned byte-for-byte.

Purpose: TOGL-01 and SC1 — "one source of truth (`off` refactored onto the shared mutators), with
an atomic save that cannot clobber concurrent CLI edits" — landed as a production-quality slice on
the agent's best early-context tokens, so a wrong lock target or a stale-snapshot merge costs one
commit instead of a shipped data-loss bug.

Output: the Config mutator + atomic save, the refactored `off`, the /api/toggle tracer arm, the
saved/applied row fields, and the three spec files that pin them.
</objective>

## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| `Config#set_ignored_all` / `#set_ignored` | lib (mod) | sidecar flock → in-lock `load` → key-level assign → atomic `save` → unlock in ensure |
| `Config#save` (upgraded) | lib (mod) | same-dir `Tempfile` + `File.rename`, existing mode preserved — one save path for every caller |
| `Config#config_lock_path` + `Config.configure` fix | lib (mod) | `<config_path>.lock`; configure re-derives `config_path` from `project_dir` |
| `Command::Off#run` | lib (mod) | routed through the mutator; output/exit/file shape unchanged |
| `/api/toggle` (tracer arm) | lib (mod) | minimal gate + body validation → mutator → `ok_envelope('package','cached')`; never touches `@jobs` |
| `State` row fields | lib (mod) | `saved_cached`, `applied_cached`, `pending` from a fresh per-call disk read |
| `spec/config_mutator_spec.rb` | spec (new) | lock target, reload, clobber, atomicity, purity, release-on-raise |
| `spec/command_off_shared_mutator_spec.rb` | spec (new) | the D-03 byte-identical contract |
| Integration rows | spec (mod) | POST → yml on disk → `/api/state` divergence |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-package-toggles-panel-completion/16-CONTEXT.md
@.planning/phases/16-package-toggles-panel-completion/16-RESEARCH.md
@.planning/phases/16-package-toggles-panel-completion/16-UI-SPEC.md
@.planning/phases/16-package-toggles-panel-completion/16-VALIDATION.md
@lib/spm_cache/core/config.rb
@lib/spm_cache/command/off.rb
@lib/spm_cache/web/router.rb
@lib/spm_cache/web/read_models/state.rb
@lib/spm_cache/installer/build.rb
@lib/spm_cache/web/marker.rb
@spec/support/web_server_boot.rb
@spec/config_spec.rb
@spec/web_integration_spec.rb
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — POST /api/toggle writes the ignore list through a locked, atomic shared mutator and /api/state shows saved ≠ applied (RED → GREEN)</name>
  <precondition>`bundle exec rspec spec/web_integration_spec.rb spec/config_spec.rb` is green on the current tree before starting.</precondition>
  <reversibility rating="costly">D-03's own rating: `spm-cache off` is a published CLI contract and this task moves the code under it. The write shape is settled by machine probe (16-RESEARCH P1-P6) — implement the probed design, not a variant; a later change of lock target or write mechanism means re-proving the whole write path.</reversibility>
  <read_first>
  - .planning/phases/16-package-toggles-panel-completion/16-RESEARCH.md — §Q1 in full (DEFAULT_CONFIG shape, load/save verbatim, what off writes, PROBED P1-P3, the shallow-dup hazard), §Q2 in full (the sidecar verdict P5/P6, the flock idiom, the recommended algorithm, the never-raise-into-the-request posture), §Q3 (saved truth = exact-entry `include?`, applied truth = graph status `ignored`, the state model's fresh-read requirement), Pitfalls 1-5
  - .planning/phases/16-package-toggles-panel-completion/16-CONTEXT.md — D-03, D-04, D-05, D-08
  - .planning/phases/16-package-toggles-panel-completion/16-UI-SPEC.md — the Interaction contract paragraph pinning the POST body shape and A1 (field names are planner-discretionary)
  - lib/spm_cache/core/config.rb lines 15-71 (DEFAULT_CONFIG, initialize, configure, load, save) and 150-152, 201-203 (ignore_list, should_ignore?)
  - lib/spm_cache/installer/build.rb lines 88-114 (the acquire/release flock idiom this mutator mirrors — note the LOCK_NB truthiness comment)
  - lib/spm_cache/web/marker.rb lines 35-59 (the same-dir Tempfile + rename atomic write, including the chmod-before-rename ordering)
  - lib/spm_cache/web/router.rb lines 95-127 (dispatch), 154-178 (api_read's gate order), 242-299 (api_mutate — the shape the toggle arm mirrors MINUS the slot), 301-338 (envelope helpers)
  - lib/spm_cache/web/read_models/state.rb in full (the row shape the new fields land beside, and the "re-read on EVERY call" stance)
  - spec/web_integration_spec.rb lines 1-100 (the ONE boot, order: :defined, the handle/get/post helpers)
  - spec/support/web_server_boot.rb (with_server kwargs, http_post's positional body)
  </read_first>
  <files>lib/spm_cache/core/config.rb, lib/spm_cache/command/off.rb, lib/spm_cache/web/router.rb, lib/spm_cache/web/read_models/state.rb, spec/web_integration_spec.rb</files>
  <behavior>
  - RED first: new examples appended to spec/web_integration_spec.rb (reusing its ONE boot; order: :defined is already set):
    1. An authenticated POST /api/toggle with body {"package":"Alamofire","cached":false} answers 200 with the standard envelope, and data carries package 'Alamofire' and cached false.
    2. The write actually reached disk THROUGH the shared path: spm-cache.yml in the booted project now parses with an ignore array containing exactly 'Alamofire', and the file carries the full default key set (the shape `spm-cache off` has always written — PROBED P2).
    3. The read model sees it immediately: GET /api/state's Alamofire row reports saved-not-cached while its applied signal still says cached (a graph.json fixture giving Alamofire status 'hit'), and the row is marked pending. A package that is neither ignored nor graph-ignored is not pending.
    4. Toggling the same package back to cached:true removes exactly that entry — the ignore array returns to empty, the row is saved-cached again and no longer pending.
    5. The toggle route never consults the spawn slot: with the injected Jobs slot deliberately held (a sleeping fake-bin run claimed through POST /api/build), a POST /api/toggle still answers 200 and writes (D-08 — no 409 from this route, ever).
  - Regression: the pre-existing route×auth matrix, the SSE rows and the mutation rows in the file stay green; `/api/state`'s existing keys (name, config, size_bytes, state, fidelity, has_macro) are unchanged in name and meaning — the new fields land BESIDE them.
  - Scope note — do not overclaim: this task lands the tracer arm's validation only (token, POST-only, parseable body, package must be a non-empty String, cached must be exactly true or false). The full matrix (unknown_package, not_toggleable, config_write_failed, revert, apply) is 16-04's, and the reason/toggleable derivation is 16-03's. `pending` here is "has an applied signal AND saved ≠ applied"; 16-03 narrows it to toggleable rows only.
  </behavior>
  <action>
  Implement the mutator on Core::Config first, exactly as 16-RESEARCH §Q2 pins it.

  Add a lock-path reader that answers the config path with a `.lock` suffix — the SIDECAR, chosen because a rename over a lock-held yml orphans the lock chain (machine-probed) while the sidecar is inode-stable across rewrites. Add a batch mutator taking a mapping of package name to the desired ignored flag, and a single-package convenience that delegates to it with one pair, so a revert of N rows costs one lock acquisition rather than N. Inside the batch mutator: ensure the config's directory exists, open the sidecar with CREAT|RDWR, take a BLOCKING exclusive flock (no announce — a web request has no stream and the terminal must stay quiet), then in a begin/ensure: `load` (the fresh in-lock re-read that destroys any boot-time snapshot), fold the requested changes into a NEW array assigned to `raw['ignore']` (adding appends and uniq's, removing subtracts the exact entry — assignment only, never in-place mutation), `save`, and answer the resulting array. The ensure unlocks and closes unconditionally. Nothing in the mutator prints, warns, or logs.

  Upgrade `save` IN PLACE to the atomic write instead of adding a second saver: same-dir Tempfile, write the same `YAML.dump(@raw)` bytes, close, chmod the tempfile to the existing file's mode when the config already exists (otherwise the house default for a non-secret project file) so an atomic replace never silently tightens or loosens permissions, rename over the config path, and unlink the tempfile in ensure if it survives. Require 'tempfile' at the top of the file. The rendered bytes must stay byte-identical to today's `File.write(@config_path, YAML.dump(@raw))` output — only the delivery mechanism changes.

  Fix the path-derivation hazard while you are in this file: `Config.configure` currently sets project_dir without re-deriving config_path, so a caller that points the singleton at a different project keeps writing to the config of the process's boot cwd. Make configure re-derive config_path from the new project_dir whenever an explicit config_path is not supplied; an explicit config_path still wins. This is load-bearing for hermeticity — without it, a spec that configures a tmpdir project would have the mutator write to the developer's own spm-cache.yml.

  Then the read model: in State.call, read the SAVED ignore list from DISK for this call — a local YAML parse of the config path, never `config.load` on the singleton and never the singleton's @raw (a read model that mutates shared state from a GET is exactly the CP1 posture this phase exists to fix). A missing config file reads as an empty list; a malformed one reads as an empty list too, matching the house "spm-cache.yml is user-authored, not adversarial" tolerance rather than raising a Psych error past api_read's rescue set into WEBrick's error log. Add three fields to each row beside the existing ones: whether the SAVED config keeps this package cached (the exact-entry test — the checkbox's own truth, served pre-inverted so the client does no math), whether the LAST SYNC kept it cached (derived from the graph status: ignored means not cached, and a row with no graph entry has no applied signal at all), and whether the row is pending (an applied signal exists and the two disagree).

  Then the route: add the '/api/toggle' dispatch arm and a private handler mirroring api_mutate's gate ORDER — token first (401), then the house 404 for anything but POST — then parse the body (malformed → 400 with the machine reason for a bad body), require the package to be a non-empty String (400, its own reason) and `cached` to be EXACTLY true or false with no truthy coercion (400, its own reason — the V5 posture the scope check already models). Call the mutator, and answer the standard ok envelope carrying the package name and its new cached state. The handler must not reference @jobs at all.

  Finally, route `Command::Off#run` through the mutator: build the name-to-true mapping from the parsed targets and hand it to the batch mutator in place of the read-modify-save trio, keeping both `puts` lines and their order exactly as they are. Drop the now-vestigial `config.load` at the top of the method — the mutator's in-lock load is the only load whose result feeds the write, and leaving a second one invites a future edit to merge from the stale snapshot again. Task 3 pins that this changed nothing observable.

  Commit RED first: `test(16-01): failing toggle→config→state tracer rows`, then GREEN: `feat(16-01): shared config mutator (sidecar flock + atomic save) behind POST /api/toggle`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_integration_spec.rb spec/config_spec.rb spec/web_state_spec.rb spec/web_build_routes_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the toggle row observes a 409 from /api/toggle while the slot is held; or the on-disk assertion passes against a config outside the booted tmpdir project; or any pre-existing /api/state key changed name or meaning</fails_when>
  </verify>
  <acceptance_criteria>
  - The five new integration examples are green, including the slot-held row
  - The yml assertion reads the file from the booted project directory and parses it — never asserts against the source text of config.rb
  - The pre-existing route×auth matrix, SSE rows and build/rollback rows in the file are still green
  - No flock is taken on the config path itself anywhere in the tree; no `<<`/push/delete against raw['ignore'] exists
  </acceptance_criteria>
  <done>A real user can flip one package's caching off from an authenticated request and see the dashboard's own read model report it as saved-but-not-applied, with the config file replaced atomically under a lock that a rename cannot orphan. Committed RED then GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expansion — the mutator unit matrix: lock target, in-lock reload, clobber-proofing, atomicity, purity, release-on-raise (RED → GREEN)</name>
  <precondition>Task 1 GREEN and committed.</precondition>
  <reversibility rating="reversible">Unit coverage plus small hardening of a collaborator that already landed.</reversibility>
  <read_first>
  - .planning/phases/16-package-toggles-panel-completion/16-VALIDATION.md — the Wave-1 mutator row verbatim (it enumerates the contracts this file must assert)
  - .planning/phases/16-package-toggles-panel-completion/16-RESEARCH.md — §Q2 (algorithm + failure posture), the Machine Probes table rows P3-P7, Pitfalls 1-4
  - lib/spm_cache/core/config.rb (Task 1 output)
  - spec/build_lock_spec.rb — the fork-based cross-process OS-lock proof idiom this file reuses for contention
  - spec/config_spec.rb lines 142-173 — the existing ignore/fnmatch rows that must stay untouched and green
  </read_first>
  <files>spec/config_mutator_spec.rb, lib/spm_cache/core/config.rb, .gitignore</files>
  <behavior>
  - RED first: spec/config_mutator_spec.rb, hermetic (tmpdir project, no server, no CLI), covering:
    1. Add and remove: mutating a package to not-cached appends the exact entry; mutating it back removes exactly that entry and leaves every other entry, including glob patterns, untouched.
    2. Batch: one call with several changes applies all of them and acquires the sidecar lock ONCE (assert the single acquisition by counting opens through a seam or by asserting the observable result of a batch whose middle element would deadlock under re-entrant locking — pick the assertion that does not depend on implementation internals).
    3. The lock target is the sidecar: after a mutation the sidecar file exists beside the config; while another PROCESS holds an exclusive flock on the sidecar, a non-blocking probe from this process is refused — and that verdict is read as a false return value, never a rescued exception.
    4. Rename-vs-lock (the P5/P6 pin): a lock held on the SIDECAR is still held after the config has been replaced by a mutation — the sidecar is inode-stable — whereas the config file's own inode changes across the write.
    5. Clobber-proofing (P3 as a spec): a mutation performed against a Config instance holding a STALE in-memory snapshot does not discard an ignore entry written to disk after that snapshot was taken — both entries are present afterwards.
    6. Atomicity: the config path never observes a truncated file — assert the tempfile-and-rename mechanism through its observable consequences (the config's inode changes across a save; no leftover temp files remain in the directory afterwards) and that an existing config's file mode survives the replace.
    7. Purity (Pitfall 3): after mutations, DEFAULT_CONFIG['ignore'] is still empty and a freshly reset Config carries no entries.
    8. Release-on-raise: when the write raises (make the config path unwritable, or stub the save to raise), the exception propagates AND the sidecar lock is free afterwards — a second mutation from another process is not blocked.
    9. Path derivation: configuring the singleton with a project_dir re-derives the config path into that project; an explicitly supplied config path still wins; a mutation therefore writes inside the configured project and nowhere else.
  - Regression: spec/config_spec.rb stays green untouched.
  </behavior>
  <action>
  Author spec/config_mutator_spec.rb per the behavior bullets, using Dir.mktmpdir projects and restoring the Config singleton in an ensure (the config_spec / web_state_spec discipline). Use a forked child (the build_lock_spec idiom) for every cross-process contention row — a same-process second flock on the same file is granted by the OS and would prove nothing.

  Harden lib/spm_cache/core/config.rb only where a row proves it necessary; the expected deltas are small (mode preservation, temp-file cleanup on the raise path, the batch's single acquisition). Do not add a retry loop, a Mutex, a second lock, or an announce line: the OS lock IS the mechanism and the critical section is one read plus one small write.

  Add `spm-cache.yml.lock` to .gitignore beside the existing `spm-cache.yml` entry — the sidecar is a zero-content runtime artifact created on demand in the user's project (16-RESEARCH A2), and this repo dogfoods itself.

  Commit RED first: `test(16-01): failing Config mutator matrix (lock, clobber, atomicity, purity)`, then GREEN: `feat(16-01): harden the config mutator — mode-preserving atomic save, sidecar ignored`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/config_mutator_spec.rb spec/config_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or a contention row uses a same-process second flock instead of a forked child; or the release-on-raise row passes without the raise actually happening; or a temp file survives in the project directory after any example</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/config_mutator_spec.rb green with the nine described contracts, hermetic (no sockets, no CLI, no real project)
  - The clobber row fails against the pre-refactor write path (it is the P3 scenario, written as a regression pin)
  - spec/config_spec.rb untouched and green
  - `.gitignore` carries the sidecar entry
  </acceptance_criteria>
  <done>The write path is provably clobber-proof under real cross-process contention, atomic, permission-preserving, and free of the shared-array hazard — and it releases its lock even when the write fails. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The D-03 contract — `spm-cache off` byte-identical through the shared mutator (RED → GREEN)</name>
  <precondition>Tasks 1-2 GREEN and committed; `off.rb` already routed through the mutator by Task 1.</precondition>
  <reversibility rating="costly">`off` is a published CLI verb; these pins ARE its contract, because no Command::Off spec exists today. If a pin cannot be met, the refactor — not the pin — is what changes.</reversibility>
  <read_first>
  - .planning/phases/16-package-toggles-panel-completion/16-VALIDATION.md — the Wave-1 `off` row verbatim
  - .planning/phases/16-package-toggles-panel-completion/16-RESEARCH.md — §Q1's "What `spm-cache off` writes today" including the PROBED P2 output block (the exact 9-key file shape), and the behavior-preservation paragraph
  - lib/spm_cache/command/off.rb (Task 1 output) and the pre-refactor body quoted in §Q1
  - spec/config_spec.rb lines 142-173 (ignore semantics) and any existing command spec for the argv/`arguments!` construction idiom
  </read_first>
  <files>spec/command_off_shared_mutator_spec.rb, lib/spm_cache/command/off.rb</files>
  <behavior>
  - RED first: spec/command_off_shared_mutator_spec.rb, hermetic (tmpdir project configured on the singleton, restored in ensure):
    1. Output: running the verb against one target prints exactly the two established lines, in order, byte-for-byte — the added-to-ignore-list line naming the targets comma-joined, then the source-mode advice line — and prints nothing else.
    2. Multiple targets: the joined names appear in argv order in the first line, and all of them land in the ignore list, de-duplicated, in that same order.
    3. File shape: run against a project seeded with the shipped config template, the resulting file parses to the full default key set with the ignore array holding the target — the PROBED P2 shape — and the hand-written comments are gone (D-05's honesty sentence describes existing behavior, and this row is the evidence).
    4. Idempotence: running the verb twice for the same target leaves exactly one entry.
    5. Shared path: the entry `off` wrote is visible to the same exact-entry test the web read model uses, and a subsequent web-side mutation removing it leaves the rest of the file intact — one source of truth, two callers.
    6. Existing entries and glob patterns already in the file survive the write untouched.
  - Regression: spec/config_spec.rb and spec/config_mutator_spec.rb stay green.
  </behavior>
  <action>
  Author spec/command_off_shared_mutator_spec.rb per the behavior bullets. Drive the command through its real constructor with a real argv object so `arguments!` parses the targets exactly as the CLI does, capture stdout, and assert the two lines byte-for-byte rather than by regex fragment — the point of the file is that the published strings cannot drift.

  Seed row 3's project from the shipped `spm-cache.yml.template` asset so the comment-loss assertion is about the real template, not a hand-written stub.

  Touch lib/spm_cache/command/off.rb only if a pin fails; the expected outcome is zero production delta in this task. If a pin cannot be met, fix `off` to meet it — never relax the pin.

  Commit RED first: `test(16-01): failing spm-cache off byte-identical contract pins`, then GREEN: `refactor(16-01): pin spm-cache off's contract on the shared mutator`. Then run the wave gate: full suite `bundle exec rspec` green.
  </action>
  <verify>
    <automated>bundle exec rspec spec/command_off_shared_mutator_spec.rb spec/config_mutator_spec.rb spec/config_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or an output assertion matches a fragment instead of the full line; or row 3 asserts against a hand-written config instead of the shipped template; or the file-shape row tolerates a key set different from the default nine</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/command_off_shared_mutator_spec.rb green with the six described contracts
  - Zero production changes were needed in off.rb (or, if any were, they exist to satisfy a pin and are described in the summary)
  - Full suite `bundle exec rspec` green — the Wave 1 merge gate for this plan
  </acceptance_criteria>
  <done>`spm-cache off` demonstrably behaves exactly as it always has while writing through the same clobber-proof, atomic mutator the browser now uses — and that equivalence is pinned by the first spec the verb has ever had. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/toggle | the first request surface in the project that writes USER STATE: crosses the Host/Origin + token gate and then reaches a file the CLI depends on |
| request JSON body → the ignore list | untrusted strings arriving at a persisted config value other tools read back |
| server process ↔ concurrent CLI process | two independent writers of one file; the sidecar flock is the only thing serializing them |
| in-memory config snapshot → disk | a stale snapshot written back is silent data loss for whatever changed since |

## STRIDE Threat Register (ASVS Level 1; block on high)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-16-01 | Tampering (lost update: a web write silently discarding a concurrent CLI edit) | Config mutator | high | mitigate | blocking flock on the inode-stable sidecar + a FRESH in-lock re-read before assigning; the P3 stale-writer scenario is a regression row in spec/config_mutator_spec.rb |
| T-16-02 | Tampering (a rename orphaning the lock so two writers both believe they hold it) | lock target | high | mitigate | the lock is the sidecar, never the yml inode (PROBED P5/P6); a spec pins that a held sidecar lock survives a config rewrite |
| T-16-03 | Denial of service / integrity (a torn or truncated spm-cache.yml after a crash mid-write) | Config#save | high | mitigate | same-dir Tempfile + File.rename — the reader either sees the whole old file or the whole new one; temp files are cleaned in ensure |
| T-16-04 | Elevation of privilege (drive-by cross-site POST rewriting a developer's config) | /api/toggle | high | mitigate | the arm rides the SAME structural gate as every other route (Host exact-match, Origin-if-present allowlist, X-SPM-Token) via the single catch-all servlet; no second token, no query-param token |
| T-16-05 | Tampering (config injection through a crafted package name) | request body → yml | medium | mitigate | the value round-trips as a DATA structure through YAML.safe_load/YAML.dump and is never interpolated into yml text; the body validation requires a non-empty String and rejects non-boolean `cached` with no coercion |
| T-16-06 | Tampering (a mutation writing outside the configured project) | config_path derivation | medium | mitigate | Config.configure re-derives config_path from project_dir; a spec pins that a configured tmpdir project is the only file touched |
| T-16-07 | Tampering (a pre-planted symlink at the sidecar path) | sidecar lock file | low | accept | identical exposure and identical posture to the shipped build lock (`.spm-cache-build.lock`, config.rb:110-112): the project directory is the user's own, not an adversarial input; the lock file carries no content |
| T-16-08 | Information disclosure (raw exception text from a failed write) | 500 envelope | low | accept | loopback-only single-user tool; the frontend renders the message solely through the pinned failure template via textContent (16-UI-SPEC prohibition 2), and the envelope reason is for programs |

No package installs in this plan.
</threat_model>

<verification>
- `bundle exec rspec spec/config_mutator_spec.rb` — green (16-VALIDATION Wave-1 mutator row).
- `bundle exec rspec spec/command_off_shared_mutator_spec.rb` — green (16-VALIDATION Wave-1 `off` row).
- `bundle exec rspec spec/web_integration_spec.rb` — green, including the toggle→disk→state tracer rows.
- `bundle exec rspec spec/config_spec.rb spec/web_state_spec.rb spec/web_build_routes_spec.rb` — green (the new arm and the new fields perturb neither the existing config contract, the state rows, nor the mutation matrix).
- `bundle exec rspec` — full suite green (Wave 1 merge gate, run at the end of Task 3).
- The browser half (a real checkbox producing this POST) is 16-06's recorded probe row 1; nothing here claims it.
</verification>

<success_criteria>
- TOGL-01's persistence half lands exactly as D-03/D-04 pin it: one mutator, two callers, an flock on the only sound target, a fresh in-lock read, and an atomic replace.
- SC1's "cannot clobber concurrent CLI edits" is a passing regression row, not an assertion of intent.
- `spm-cache off` is provably unchanged in output, exit status and written file shape — pinned by the first spec it has ever had.
- The remaining plans expand a proven write path instead of guessing at one.
</success_criteria>

<output>
Create `.planning/phases/16-package-toggles-panel-completion/16-01-SUMMARY.md` when done
</output>
