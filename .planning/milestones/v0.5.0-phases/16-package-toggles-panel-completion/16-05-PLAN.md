---
phase: 16-package-toggles-panel-completion
plan: 05
type: execute
wave: 4
depends_on: ['16-03', '16-04']
files_modified:
  - lib/spm_cache/web/assets/app.js
  - lib/spm_cache/web/assets/styles.css
  - spec/web_frontend_spec.rb
autonomous: true
requirements: [TOGL-01, TOGL-02, TOGL-03]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low   # estimate-calibration: 0 samples → factor 1, confidence low

must_haves:
  truths:
    - "D-01/D-02 (the control): the Cache State table grows a SIXTH column headed Cached whose cell holds a native checkbox — checked from the row's saved state, natively disabled where the row is not toggleable, carrying the accessible name Toggle caching for the RAW package name (no macro prefix) — with no role=switch, no styled-div toggle and no animation — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-09 (verbatim chips): a disabled row renders exactly one reason chip whose text and title are the server's reason string rendered verbatim through textContent, mapped to the existing badge classes only; an unrecognised reason renders verbatim in the neutral class rather than being filtered, remapped or dropped — bundle exec rspec spec/web_frontend_spec.rb"
    - "A8 (poll integrity): while any toggle POST is in flight the five-second poll skips BOTH the table redraw and the freshness stamp, so the stamp never claims data newer than what is shown and a checkbox never bounces back for a cycle; a manual Refresh click bypasses the skip, and the counter tolerates overlapping toggles — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-05/D-06 (the bar): whenever at least one row is pending, an unsaved-changes group renders above the table carrying the comment-loss honesty sentence byte-for-byte, exactly ONE Apply now action, a Revert all affordance, and a polite message slot — and it is absent when nothing is pending; no master checkbox and no per-row apply exist anywhere — bundle exec rspec spec/web_frontend_spec.rb"
    - "A4/A5 (the slot amendment): the app-wide busy string is ONE constant now naming three verbs, superseding the two-verb Phase 15 string, and the freeze set covers four controls — Build, Rebuild all, Rollback and Apply now — including a bar rendered while a run is already in flight — bundle exec rspec spec/web_frontend_spec.rb"
    - "Prohibition 13 (no optimistic claims): pending markers and the bar clear only when a poll shows convergence — never on a POST answer; and a failed toggle renders the pinned failure line above the table that SURVIVES poll re-renders until the next successful mutation or a Refresh — bundle exec rspec spec/web_frontend_spec.rb"
    - "Prohibition 1/2 (the asset contract): every change lands in the two existing asset files, index.html and log.js are untouched, no new file or reference is added, and every dynamic string — package names, reason words, envelope messages — reaches the DOM through element construction and textContent only — bundle exec rspec spec/web_frontend_spec.rb"
  artifacts:
    - "lib/spm_cache/web/assets/app.js (MOD: the sixth column and its toggle cell, the toggle POST with its in-flight counter and failure line, the unsaved-changes bar with Apply now and Revert all, the amended copy constants and the four-control freeze)"
    - "lib/spm_cache/web/assets/styles.css (MOD: the re-partitioned column widths and the cached column, checkbox accent/disabled/focus rules, the sync-bar geometry on the confirm-bar pattern, and the toggle-failure line joined to the existing error treatment)"
    - "spec/web_frontend_spec.rb (MOD: the byte pins for every string, class, width and structural rule above)"
  key_links:
    - "The client's only computation is rendering what the read model already decided: checked from the saved field, disabled from the toggleable field, chip from the reason field, marker from the pending field, bar from whether any row is pending — no pattern matching, no status vocabulary, no divergence math (CP10, prohibition 9)"
    - "The bar's Apply now shares Phase 15's spawn slot, so it joins the same freeze set — but unlike the three static controls it is created and destroyed by every table render, which is why the freeze state is remembered and re-applied rather than held as a stale node reference"
    - "The poll-skip guard lives in the poll loop rather than inside the refresh function, which is exactly what makes a user-invoked Refresh bypass it (A6) while the automatic cycle stays honest (A8)"
    - "The toggle failure line is a separate node from the panel's fetch-error line so a poll failure and a write failure cannot silently replace one another, while sharing the same fail treatment through one extended selector rather than duplicated CSS"
  prohibitions:
    - statement: "MUST NOT add a file, an asset reference, a framework, a build step, a CDN or a scheme-absolute URL — the changes land in the two existing asset files and index.html and log.js stay untouched (prohibition 1)"
      status: resolved
      verification: test
    - statement: "MUST NOT assign markup strings to the DOM — no innerHTML, insertAdjacentHTML, outerHTML or document.write; every dynamic value renders through element construction and textContent (prohibition 2)"
      status: resolved
      verification: test
    - statement: "MUST NOT use a native dialog for anything — Revert all gets no confirm, and no alert, confirm or prompt exists (prohibition 3)"
      status: resolved
      verification: test
    - statement: "MUST NOT add a timer, a polling channel, a second event stream, or any cross-tab channel — the existing state poll and the run-progress event are the only sources, and each tab converges through its own poll (prohibition 4, A6)"
      status: resolved
      verification: test
    - statement: "MUST NOT read a client clock — nothing in the toggle surface may call the current-time API (prohibition 5, T-13-16)"
      status: resolved
      verification: test
    - statement: "MUST NOT reinvent the switch — a native checkbox only, no role=switch, no styled div, no animation of the control (prohibition 6, D-02)"
      status: resolved
      verification: test
    - statement: "MUST NOT derive a reason, a pattern match, a plugin verdict or a fidelity gate client-side, and MUST NOT filter or remap an unrecognised reason value (prohibition 9)"
      status: resolved
      verification: test
    - statement: "MUST NOT add bulk controls — no master checkbox, no multi-select, no per-row apply; exactly one Apply now, at bar level (prohibition 8)"
      status: resolved
      verification: test
    - statement: "MUST NOT claim a change is applied before a poll shows convergence — the bar and the pending markers clear on server truth only (prohibition 13)"
      status: resolved
      verification: test
    - statement: "MUST NOT render a raw status code or envelope internal — only the pinned templates interpolate the server's message (prohibition 11)"
      status: resolved
      verification: test
    - statement: "MUST NOT introduce a second mutation token or put the token in a query parameter — the three POSTs ride the same per-launch token through the header-only helper (prohibition 7, D-04 posture)"
      status: resolved
      verification: test
    - statement: "MUST NOT offer any way to edit ignore-list patterns from the UI — pattern-managed rows are read-only and a yml editor is out of scope by REQUIREMENTS (prohibition 10)"
      status: resolved
      verification: test
    - statement: "MUST NOT leave a state silent — a disabled checkbox always carries its reason chip, a rejected toggle always renders its failure line, and a held slot always explains itself in the bar's message slot (prohibition 12)"
      status: resolved
      verification: test
---

# Plan 16-05 — The panel completed: the checkbox column, the reasons, and the unsaved-changes bar

<planning_context>
phase: 16 — Package Toggles + Panel Completion
mode: standard (expansion — the client half, rendering the derivation 16-03 serves and calling the endpoints 16-04 landed)
sources: 16-UI-SPEC.md in full and BINDING (the Copywriting Contract's pinned strings, the layout contract's column widths and toggle-cell anatomy, the interaction contract's per-control behavior, the accessibility contract, prohibitions 1-13, assumptions A1-A10); 16-CONTEXT.md D-01, D-02, D-05, D-06; 16-RESEARCH.md §Q3's poll-skip seams and §Q5's frontend amendment paragraph; 16-VALIDATION.md the Wave-3 UI-contract row; ROADMAP Phase 16 SC2-SC3; REQUIREMENTS TOGL-01, TOGL-02, TOGL-03
planner decisions recorded here: the in-flight guard is a COUNTER rather than a single boolean, because the spec explicitly blesses a rapid second flip as a legitimate second POST and a boolean cleared by the first answer would unskip while the second is still in flight; the freeze set is extended by remembering the frozen state and re-applying it to the bar's buttons on every render, rather than by adding a bar button to the boot-time control map, because the bar's nodes are created and destroyed by every table render and a map entry would go stale after the first poll. Both are faithful implementations of A8 and A5 respectively, and both are pinned by rows.
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase. Plan 16-06 drives every one of these
surfaces in a real browser and records what it observes; nothing else consumes this plan. Because
the repo runs no JavaScript in CI, every claim here is a source-and-served byte pin until that probe
runs — the specs prove the strings and the structure exist, the probe proves they behave.
</downstream_consumer>

<objective>
Complete the Cache State panel: a sixth column of native checkboxes reflecting saved config, one
verbatim reason chip on every row that cannot be toggled, a pending marker on every row whose saved
state has not been applied, and an unsaved-changes bar carrying the comment-loss honesty sentence
with exactly one Apply now and one Revert all — all of it rendering server-derived values, none of
it claiming anything a poll has not confirmed.

Purpose: SC2's saved-vs-applied surface with its explicit Apply-now action, and SC3's visible WHY —
the two halves of the phase a user actually sees.

Output: the toggle column, the bar, the amended slot copy and freeze set in the two existing asset
files, plus the byte pins that keep every locked string honest.
</objective>

## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| Sixth column + toggle cell | asset (mod) | checkbox, reason chip, pending marker, in one nowrap cell |
| Toggle POST + failure line | asset (mod) | instant write, in-flight counter, poll-skip, poll-surviving failure line |
| Unsaved-changes bar | asset (mod) | honesty sentence, Revert all, Apply now, polite message slot |
| Copy + freeze amendments | asset (mod) | one three-verb busy constant, the apply and revert in-flight strings, four-control freeze |
| Sheet extensions | asset (mod) | column widths, checkbox rules, bar geometry, shared fail treatment |
| Frontend pins | spec (mod) | every string, class, width, structural and prohibition rule above |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-package-toggles-panel-completion/16-CONTEXT.md
@.planning/phases/16-package-toggles-panel-completion/16-UI-SPEC.md
@.planning/phases/16-package-toggles-panel-completion/16-RESEARCH.md
@.planning/phases/16-package-toggles-panel-completion/16-VALIDATION.md
@.planning/phases/16-package-toggles-panel-completion/16-03-SUMMARY.md
@.planning/phases/16-package-toggles-panel-completion/16-04-SUMMARY.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-SPEC.md
@lib/spm_cache/web/assets/app.js
@lib/spm_cache/web/assets/styles.css
@lib/spm_cache/web/assets/index.html
@spec/web_frontend_spec.rb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The sixth column — checkbox, reason chip, pending marker, instant POST, poll integrity, failure line (RED → GREEN)</name>
  <precondition>Plans 16-03 and 16-04 committed and the full suite green; /api/state serves the toggle fields and /api/toggle answers the envelope this task branches on.</precondition>
  <reversibility rating="reversible">Additive rendering on an existing table; every string is pinned and the control is native.</reversibility>
  <read_first>
  - .planning/phases/16-package-toggles-panel-completion/16-UI-SPEC.md — the Layout contract's column table and toggle-cell anatomy, the Copywriting Contract rows for the column header, the reason chips, the pending chip, the checkbox accessible name and the toggle save failure, the Interaction contract's toggle-click and poll-integrity bullets, the Accessibility contract, the Color table's reason-chip mapping, and prohibitions 1-13
  - .planning/phases/16-package-toggles-panel-completion/16-03-SUMMARY.md — the exact row field names and the reason vocabulary as they shipped
  - lib/spm_cache/web/assets/app.js lines 21-59 (the element helper, the status vocabularies, the column arrays), 109-151 (the row renderer and the table renderer including the stamp write and the empty-state early return), 153-168 (the refresh function), 303-317 (the boot and the poll loop), 320-352 (the copy constants and the POST helper)
  - lib/spm_cache/web/assets/styles.css lines 229-291 (the table, the column widths, the badge vocabulary) and 326-340 (the panel error treatment this shares)
  - spec/web_frontend_spec.rb lines 1-90 (the served-bytes and file-bytes pin idiom) and the Phase 15 control-row pins later in the file (the precedent for pinning a copy constant byte-for-byte)
  </read_first>
  <files>lib/spm_cache/web/assets/app.js, lib/spm_cache/web/assets/styles.css, spec/web_frontend_spec.rb</files>
  <behavior>
  - RED first: a toggle-column describe block in spec/web_frontend_spec.rb, pinning source and served bytes:
    1. The column arrays carry a sixth entry — the pinned header word and its class — and the sheet re-partitions the widths to the pinned six-column split summing to the full width, with the previous five-column split gone.
    2. The row renderer builds a native checkbox input: its checked state comes from the row's saved field, its disabled state from the negated toggleable field, and its accessible name is the pinned template carrying the RAW package name — pin that the macro prefix is not applied to it.
    3. The reason chip renders only on non-toggleable rows, with the row's reason as both its text and its title, and its class comes from a mapping whose five entries are exactly the pinned badge classes; an unrecognised value falls back to the neutral class rather than being dropped — pin the fallback explicitly.
    4. The pending marker renders on pending rows with its pinned word as text and title in the neutral class, and co-renders after the reason chip when both apply.
    5. The change handler POSTs to the toggle endpoint with the raw package name and the checkbox's NEW value, through the existing POST helper — pin that no new fetch wrapper is introduced and that no per-row busy state or disable is applied on click.
    6. Poll integrity: the in-flight counter is incremented before the request and decremented in the completion path; the poll loop skips the whole refresh — redraw and stamp together — while the counter is non-zero, and the Refresh button's handler is not routed through the skip.
    7. The failure line: a failed toggle renders the pinned failure template with the package and the server's message, above the table, in the fail treatment; it is stored so the table renderer re-inserts it on every subsequent render, and it is cleared by the next successful mutation or by Refresh — pin all three behaviors.
    8. The sheet carries the checkbox rules: the accent variable for the checked state, the sheet's disabled opacity, and the focus ring pattern extended to the checkbox — and no new color value is introduced anywhere in the diff.
    9. Prohibition sweep over the changed files: no markup-assignment API, no native dialog, no new timer or interval, no clock call, no role=switch, no master checkbox, no per-row apply button, and no new asset reference in index.html (pin index.html and log.js byte-unchanged).
  - Regression: every pre-existing frontend pin stays green, including the Phase 13 five-column pins that this task supersedes — update exactly those, and pin the supersession explicitly so the change is deliberate rather than incidental.
  </behavior>
  <action>
  Extend the two asset files, nothing else.

  In app.js: grow the column arrays by the pinned header and class. Add a frozen reason-to-badge-class map beside the existing status vocabularies — five entries, existing classes only, with the neutral class as the fallback for anything else. In the row renderer, append the toggle cell: create the checkbox through the DOM API (the element helper does not cover input attributes), set its checked and disabled state from the row's fields, set the accessible name from the pinned template using the raw name, and attach a change listener that fires the toggle POST with the checkbox's new value. Append the reason chip when the row is not toggleable and carries a reason, then the pending marker when the row is pending.

  Add a small mutation section beside the Phase 15 control code: a module-scoped in-flight counter, a module-scoped failure-line string, and a function that sends the toggle POST — increment, post, on success clear the failure string and remove any rendered failure line, on failure set the failure string from the pinned template and render it immediately above the table, and decrement in the completion path either way. Do not disable the checkbox, do not re-render the table from the answer, and do not mark anything applied: the next poll is the only thing allowed to change the table's truth.

  In the table renderer, re-insert the failure line before the table whenever the failure string is set, so a rejected click survives every poll until it is cleared. In the poll loop, skip the refresh call entirely while the counter is non-zero — that single placement is what keeps the stamp honest and still lets the Refresh button read through. Give the Refresh handler the clearing behavior the spec pins.

  In styles.css: re-partition the five existing column widths to the pinned six-column split and add the new column's width; add the checkbox rules (accent colour from the existing variable, the sheet's disabled opacity, the established focus-visible ring) scoped to the state table; and join the toggle failure line to the existing panel-error rule by extending that selector rather than duplicating its declarations. Introduce no new colour value.

  Commit RED first: `test(16-05): failing toggle column, chip and poll-integrity pins`, then GREEN: `feat(16-05): the Cached column — native toggles, reason chips, honest polling`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the six column widths do not sum to the full width; or a pinned string is asserted by fragment instead of byte-for-byte; or the prohibition sweep is absent for any of the nine listed rules; or index.html or log.js changed; or a new colour value appears in the sheet</fails_when>
  </verify>
  <acceptance_criteria>
  - The nine described contracts are green and every pre-existing frontend pin passes, with the superseded five-column pins deliberately updated
  - The toggle cell is built entirely through element construction and textContent
  - The poll-skip guard sits in the poll loop, and the Refresh path demonstrably bypasses it
  </acceptance_criteria>
  <done>Every row in the Cache State table now shows whether the config keeps that package cached, lets the user change it with one keystroke or click, says why when it cannot, and never lies about how fresh what you are looking at is. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The unsaved-changes bar — the honesty sentence, Apply now, Revert all, and the slot amendments (RED → GREEN)</name>
  <precondition>Task 1 GREEN and committed.</precondition>
  <reversibility rating="reversible">Additive rendering plus a copy amendment; the superseded Phase 15 string is replaced in one constant and pinned by a row.</reversibility>
  <read_first>
  - .planning/phases/16-package-toggles-panel-completion/16-UI-SPEC.md — the Copywriting Contract rows for the primary action, the revert affordance, the sync-bar sentence, both in-flight messages, the busy amendment, the apply and revert failure templates and the bar group label; the Layout contract's bar anatomy and DOM order; the Interaction contract's bar-appearance, apply-click, slot-sharing and revert-click bullets; the Accessibility contract's bar paragraph; assumptions A3, A4, A5, A6, A7
  - .planning/phases/16-package-toggles-panel-completion/16-04-SUMMARY.md — the apply and revert endpoints' exact status codes and envelope shapes
  - lib/spm_cache/web/assets/app.js lines 320-418 (the copy constants, the POST helper, the message and freeze helpers, the settle branch including the 409 arm, the confirm-bar precedent, and the run-progress listener that returns the row to idle)
  - lib/spm_cache/web/assets/styles.css lines 165-227 (the message and error treatments, the quiet button, the confirm-bar geometry this bar mirrors) and 530-536 (the warn-alpha banner fill)
  - lib/spm_cache/web/assets/index.html (the confirm-bar markup as the structural precedent — note that THIS bar is JS-rendered because the panel body is replaced on every render)
  - spec/web_frontend_spec.rb — the Phase 15 control-row and confirm-bar pins, including the pin on the two-verb busy string that this task supersedes
  </read_first>
  <files>lib/spm_cache/web/assets/app.js, lib/spm_cache/web/assets/styles.css, spec/web_frontend_spec.rb</files>
  <behavior>
  - RED first: an unsaved-changes-bar describe block in spec/web_frontend_spec.rb:
    1. Existence rule: the bar is rendered as the panel body's first child, above the table, when at least one served row is pending, and is absent otherwise — pin both directions, and pin that the empty-state render is unchanged (zero rows means zero pending, by construction).
    2. Structure and DOM order: a group wrapper carrying the pinned group label and a polite live region, then the bar text, then the quiet Revert all button, then the accent Apply now button, then a hidden message slot reusing the established message classes.
    3. Copy: the honesty sentence is pinned byte-for-byte including its comment-loss clause; the two button labels are pinned byte-for-byte with their exact capitalisation; the apply and revert in-flight messages and the two failure templates are pinned byte-for-byte.
    4. Exactly one Apply now exists in the rendered output and no per-row apply or master checkbox exists anywhere — assert by count, so a future per-row variant cannot slip in.
    5. The busy amendment: the app-wide busy constant is the pinned THREE-verb sentence, the two-verb Phase 15 string appears nowhere in the tree, and there is exactly ONE such constant.
    6. The freeze set: clicking any of the three static controls disables the bar's Apply now too, and a bar rendered while a run is already in flight comes up already disabled — pin the remembered-state mechanism, not just the immediate call, because the bar's nodes are recreated by every render.
    7. Apply behavior: the click disables both bar buttons immediately, POSTs to the apply endpoint with an empty body through the existing POST helper, shows the pinned applying message on success, renders the amended busy sentence in the bar's message slot on the busy answer with the buttons re-enabled, and renders the apply failure template on any other failure with the buttons re-enabled.
    8. Revert behavior: the click disables both bar buttons, POSTs to the revert endpoint with an empty body, clears the message and re-enables on success WITHOUT hiding the bar, and renders the revert failure template on failure — pin that nothing in the success path removes the bar or clears a pending marker, because only a poll may do that.
    9. The exit: the run-progress ended milestone re-enables the controls and clears the bar's message, and the bar's own disappearance is driven solely by a subsequent render seeing no pending rows.
    10. The sheet: the bar geometry mirrors the confirm bar (flex, wrapping, the pinned spacing tokens, the four-pixel radius, the warn ten-percent fill) and introduces no new colour value; the message slot reuses the existing classes rather than new ones.
  - Regression: every pre-existing frontend pin stays green, with exactly one deliberate supersession — the Phase 15 two-verb busy pin, updated to the three-verb string and annotated as the recorded cross-phase amendment.
  </behavior>
  <action>
  In app.js: amend the copy constants first — replace the busy sentence with the pinned three-verb string (one constant, superseding Phase 15's; leave a comment naming it as the recorded amendment so a reader does not "fix" it back), add the applying and reverting in-flight strings to the existing in-flight map, and add the two new failure templates beside the existing one. Reuse the existing generic failure template for the apply case if and only if it produces the pinned sentence exactly; the revert and toggle templates have their own shapes and need their own entries.

  Extend the freeze helper to remember the frozen state in a module-scoped flag and to apply the state to the bar's buttons when they exist, and have the bar renderer initialise its buttons from that flag. This is what makes the four-control freeze survive the bar being recreated on every poll; do not add a bar button to the boot-time control map, which would go stale after the first render.

  Add the bar renderer: build the group wrapper with its label and live region, the text node with the honesty sentence, the two buttons in the pinned quiet-then-primary order, and the hidden message slot, wiring each button to its POST. Call it from the table renderer before appending the table, only when at least one row is pending. Keep the bar's message writes in the bar's own slot — never in the Run Log controls row's message, which belongs to the run.

  Wire the apply click: disable both buttons, POST the empty body, then branch — success shows the applying message and freezes all four controls; the busy answer shows the amended busy sentence in the error treatment and re-enables; any other failure shows the apply failure template and re-enables. Wire the revert click the same way, but on success only clear the message and re-enable — nothing in that path may hide the bar or clear a marker. Extend the existing run-progress listener's ended branch to clear the bar's message alongside the row's.

  In styles.css: add the sync-bar rules mirroring the confirm bar's geometry with the warn ten-percent fill, and the bar text rule at the Body role. Reuse the existing message and error classes for the slot. Introduce no new colour value and no new spacing value.

  Commit RED first: `test(16-05): failing unsaved-changes bar and slot-amendment pins`, then GREEN: `feat(16-05): the unsaved-changes bar — Apply now, Revert all, three-verb slot copy`. Then run the wave gate: full suite `bundle exec rspec` green.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb spec/web_integration_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the superseded two-verb busy string still appears in lib/spm_cache/web/assets/ (scope the check to the asset directory — the planning artifacts of Phases 15 and 16 quote both strings by design and a tree-wide grep would self-invalidate); or more than one Apply now can be rendered; or the revert success path hides the bar or clears a pending marker; or the freeze rows pass without covering a bar rendered during an in-flight run; or a new colour or spacing value appears in the sheet</fails_when>
  </verify>
  <acceptance_criteria>
  - The ten described contracts are green, every pinned string asserted byte-for-byte
  - Exactly one busy constant exists and it names three verbs; the superseded Phase 15 pin is updated with an annotation
  - Full suite `bundle exec rspec` green — the Wave 4 merge gate for this plan
  </acceptance_criteria>
  <done>When saved and applied disagree, the panel says so above the table, admits in the same breath that saving rewrites the config file and drops its comments, and offers exactly two honest ways out — apply for real, or put it back — neither of which pretends to have worked before the server says so. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server envelope values → the DOM | package names, reason words and error messages are server-authored strings rendered into the page |
| the rendered disabled state → the server's permission check | the checkbox's disabled attribute is UX; the authority lives in the route |
| the user's click → a config write | a single click now rewrites a project file, so the copy must state the cost before the action |

## STRIDE Threat Register (ASVS Level 1; block on high)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-16-24 | Tampering / cross-site scripting (a hostile package name, reason value or envelope message injected as markup) | every new rendered string | high | mitigate | all new nodes are built through element construction and textContent; the prohibition sweep pins the absence of every markup-assignment API in the changed files (prohibition 2) |
| T-16-25 | Tampering (the UI presenting a locked row as changeable, or a stale render inviting a forbidden write) | disabled state | medium | mitigate | disabled derives from the server's toggleable field on every render, and the route re-derives permission independently — the client state is never the guard (16-04 T-16-18) |
| T-16-26 | Repudiation (a rejected write vanishing on the next poll, so the user believes it succeeded) | failure line | medium | mitigate | the failure string is remembered and re-inserted by every render until a successful mutation or an explicit Refresh clears it, pinned by its own row (prohibition 12) |
| T-16-27 | Repudiation (a data-destroying side effect the user was never told about — the config rewrite dropping their comments) | bar copy | medium | mitigate | the honesty sentence is pinned byte-for-byte and is visible whenever the bar is, so it precedes both the apply and the revert action (D-05) |
| T-16-28 | Denial of service (a click storm spawning repeated syncs, or overlapping toggles tearing the render) | freeze + in-flight counter | low | mitigate | both bar buttons disable on click, the four-control freeze covers the shared slot, and the counter keeps the poll honest across overlapping toggles; the server's slot and lock are the real guarantees behind them |
| T-16-29 | Information disclosure (raw status codes or envelope internals rendered to the user) | message slot | low | mitigate | only the pinned templates interpolate the server's message; the machine reasons are never rendered (prohibition 11, A9) |

No package installs in this plan.
</threat_model>

<verification>
- `bundle exec rspec spec/web_frontend_spec.rb` — green (16-VALIDATION's Wave-3 UI-contract row: the sixth column, the chips, the bar markup and copy, the busy amendment, the four-control freeze, the poll-skip guard).
- `bundle exec rspec spec/web_integration_spec.rb` — green (the served asset bytes and the server-side story still agree).
- `bundle exec rspec` — full suite green (Wave 4 merge gate, run at the end of Task 2).
- Every interactive claim in this plan is a source pin until 16-06's browser probe runs — that plan, not this one, is where these behaviors are proven.
</verification>

<success_criteria>
- SC2 is visible: saved and applied are distinguishable per row, and the single Apply-now action is one click away with an honest account of what saving costs.
- SC3 is visible: no disabled checkbox exists without a word beside it saying why.
- The dashboard's asset contract survives the phase intact — two files touched, no new asset, no framework, no timer, no dialog, no client clock.
</success_criteria>

<output>
Create `.planning/phases/16-package-toggles-panel-completion/16-05-SUMMARY.md` when done
</output>
