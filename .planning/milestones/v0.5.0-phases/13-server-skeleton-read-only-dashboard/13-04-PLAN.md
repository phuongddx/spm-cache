---
phase: 13-server-skeleton-read-only-dashboard
plan: 04
type: execute
wave: 3
depends_on: ['13-02', '13-03']
files_modified:
  - lib/spm_cache/web/middleware.rb
  - spec/web_integration_spec.rb
  - spec/web_packaging_spec.rb
autonomous: true
requirements: [WEB-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low   # estimate-calibration: no samples for this task profile → factor 1, confidence low

must_haves:
  truths:
    - "WEB-04 (matrix): for EVERY route (/, /assets/styles.css, /api/state, /api/doctor, /api/graph) the full reject matrix holds — Origin 'http://evil.com' → 403, Host 'evil.com:{port}' → 403, absent Origin + no token on /api/* → 401 — and the happy path (correct Host, absent Origin, valid X-SPM-Token) → 200; one real ephemeral-port boot proves all of it — bundle exec rspec spec/web_integration_spec.rb"
    - "WEB-04 (drive-by proof): a simulated browser drive-by (POST + GET with a foreign Origin AND a same-Host no-Origin tokenless form) is rejected on every /api/* route; a passing-gate unknown method/path 404s — bundle exec rspec spec/web_integration_spec.rb"
    - "WEB-04 (packaging): the built gem ships lib/spm_cache/web/assets/** (index.html, app.js, styles.css, cytoscape.min.js all present in Gem::Specification files) AND declares webrick >= 1.8, < 2 as a runtime dependency — bundle exec rspec spec/web_packaging_spec.rb"
    - "The full dashboard answers end-to-end from ONE boot: token bootstrap redirect → index → three assets → three API payloads with valid token — the phase's single exhaustive cross-route integration spec — bundle exec rspec spec/web_integration_spec.rb"
    - "X-Frame-Options: DENY and Cache-Control: no-store present on every HTML/API response in the integration pass — bundle exec rspec spec/web_integration_spec.rb"
  artifacts:
    - "spec/web_integration_spec.rb (NEW: one-boot exhaustive route × auth-state matrix + full-page-load sequence)"
    - "spec/web_packaging_spec.rb (NEW: gemspec dependency pin + files-glob ships the dashboard + offline gate re-assertion on the packaged set)"
    - "lib/spm_cache/web/middleware.rb (MOD only if the matrix exposes a gap — fix the source, never the test)"
  key_links:
    - "spec/web_integration_spec.rb consumes every earlier plan's output: 13-01 server/middleware/assets-route, 13-02 three read models, 13-03 four assets — it is the phase's cross-plan weld and the first thing Phase 14's streaming work will copy as a fixture"
    - "spec/web_packaging_spec.rb pins the two load-bearing gemspec facts from research: the webrick declaration (CP8) and the lib/**/* glob actually shipping the vendored assets (a gem built without a dashboard is a silent shipping bug)"
  prohibitions:
    - statement: "MUST NOT weaken a middleware predicate to make the matrix pass — a failing matrix row means a code bug in 13-01/13-02 surfaces, fix it there"
      status: resolved
      verification: judgment
    - statement: "MUST NOT add new routes, endpoints, or features in this plan — it proves and pins only"
      status: resolved
      verification: judgment
---

# Plan 13-04 — Cross-route hardening matrix + packaging/offline proof (phase weld)

<planning_context>
phase: 13 — Server Skeleton + Read-Only Dashboard
mode: standard
sources: ROADMAP SC3 (reject + offline criterion), REQUIREMENTS WEB-04, research/SUMMARY.md (CP7 hermetic-suite rule — "at most one port-0 integration spec"; CP8 webrick gemspec load-bearing; CP13 localhost-not-trusted), 13-CONTEXT.md "Token & Security Middleware (WEB-04)"
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase and the phase's final gate before /gsd-verify-work: its two spec files are the evidence base for the WEB-04 sign-off rows in 13-VALIDATION.md. Phase 14 reuses the integration boot as its streaming-test fixture.
</downstream_consumer>

<objective>
Prove the phase's security and shipping claims end-to-end instead of per-piece: one real
server boot runs the exhaustive route × auth-state matrix (every route, every reject path,
the drive-by simulation), the full browser page-load sequence, and the packaging truths
(gem ships the dashboard; webrick declared). Any gap found is fixed in the owning module,
never in the test.

Purpose: SC3's reject half and the CP7/CP8/CP13 pitfall dispositions, with evidence dense
enough for the phase audit and for Phase 15 to inherit the middleware without re-litigating it.

Output: two spec files (and a middleware fix if — and only if — the matrix exposes a real gap),
all green in the full suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md
@.planning/research/SUMMARY.md
@lib/spm_cache/web/middleware.rb
@lib/spm_cache/web/router.rb
@lib/spm_cache/web/server.rb
@spm_cache.gemspec
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: spec/web_integration_spec.rb — one-boot route × auth matrix + full page-load sequence</name>
  <precondition>Plans 13-01/02/03 committed (all routes and assets exist).</precondition>
  <reversibility rating="reversible">Pure additive spec; the only permitted production edit is a genuine middleware gap fix.</reversibility>
  <read_first>
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md — "Token & Security Middleware (WEB-04)" (the locked matrix semantics this spec encodes)
  - .planning/research/SUMMARY.md — Critical Pitfalls CP7 (one port-0 integration spec), CP13 (localhost drive-by/DNS-rebinding threat model), "Recommended Stack" (webrick + offline assets)
  - lib/spm_cache/web/middleware.rb + router.rb + server.rb (13-01 output — the surface under test; read fully before writing the matrix)
  - lib/spm_cache/web/read_models/*.rb (13-02 output — read-model wiring the boot needs)
  - spec/web_server_spec.rb (13-01 output — the boot helper this spec generalizes; keep ONE shared boot here, do not duplicate helpers across files)
  - spec/web_signals_spec.rb (13-01 output — the other real-process spec; keep this file's runtime in the same envelope)
  </read_first>
  <files>spec/web_integration_spec.rb, lib/spm_cache/web/middleware.rb</files>
  <behavior>
  - RED first: the matrix file fails or is vacuous until the boot helper + fixtures exist; write the spec against the pinned contract (it should largely pass immediately — that is EXPECTED here; this task is a verification net, and any failing row is a real bug to fix in the owning module before GREEN).
  - Matrix (single before(:all)-style boot on port 0 with a tmpdir project: fixture graph.json + cache dirs; Net::HTTP with explicit Host/Origin/X-SPM-Token control): for each of {/, /assets/styles.css, /api/state, /api/doctor, /api/graph}: (a) valid Host, no Origin, valid token (header for /api/*; ?token= for /) → 2xx/3xx per route contract; (b) Origin "http://evil.com" → 403; (c) Host "evil.com:{port}" → 403; (d) /api/* with no token → 401; (e) /api/* with wrong token → 401. 25 matrix cells.
  - Drive-by simulation: POST /api/state with Origin evil.com → 403; POST /api/state no-Origin no-token → 401; GET /api/state with Origin "null" → 403 (sandboxed-iframe null-origin is not same-origin).
  - Page-load sequence: GET / (no token) → 302 Location exactly "/?token={server.token}" → GET that Location → 200 text/html containing "Cache State" → GET the three relative assets as parsed FROM the served HTML → 200 each with correct content types → GET the three API paths with the token → 200 envelopes with the 13-02 payload keys.
  - Header sweep: X-Frame-Options: DENY on every HTML+API response in the sequence; Cache-Control: no-store on / and /api/*.
  - localhost-Host variant: valid "localhost:{port}" Host accepted (one cell — the allowlist's second entry).
  - Runtime bound: whole file < 15 s (one boot, ~30 requests).
  </behavior>
  <action>
  Create spec/web_integration_spec.rb with ONE Web::Server boot (port 0, tmpdir project via Core::Config configure/reset!, injected assets root defaulting to the real lib/spm_cache/web/assets so the real 13-03 files are what loads). Express the matrix as a table-driven example (routes × cases) so a failing row names itself. Assert the bootstrap-redirect token EXACTLY equals server.token. If any row fails: fix the owning module (middleware predicate, router dispatch, read model) — middleware.rb is in this task's files_modified for exactly that case — and record the gap + fix in the plan SUMMARY. Do not adjust a row's expectation: the expectations are the 13-01/13-02 contracts.
  Commit: `test(13-04): exhaustive route × auth matrix + full page-load integration spec` (single commit — RED here means "matrix not yet written", unlike feature tasks).
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_integration_spec.rb</automated>
    <fails_when>exit code non-zero (a matrix row or sequence step failed — trace the failing cell to its owning module and fix there); or "0 examples, 0 failures"; or runtime > 15 s</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_integration_spec.rb green: ≥ 30 examples (25 matrix cells + drive-by trio + page-load sequence + header sweep + localhost variant)
  - Single boot for the whole file; no other spec file boots a second server in this plan
  - Any gap found was fixed in the owning module with its own spec already covering the fixed behavior (no test-weakening)
  </acceptance_criteria>
  <done>The WEB-04 reject contract is proven across every route and every attack shape from one real boot — evidence for the phase audit and the base Phase 14 copies. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: spec/web_packaging_spec.rb — gem ships the dashboard, webrick declared, offline set complete</name>
  <precondition>Task 1 GREEN and committed.</precondition>
  <reversibility rating="reversible">Pure additive spec (a gemspec tweak only if the pin exposes a real gap).</reversibility>
  <read_first>
  - .planning/research/SUMMARY.md — "Recommended Stack" (webrick >= 1.8, < 2 load-bearing — machine-probed require failures; CP8) and Key Findings offline-assets stance
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md — "Frontend Architecture (offline constraint)" ("gemspec glob ships the dir" — verify the existing {lib,…}/**/* glob already covers lib/spm_cache/web/assets/**; pin it so a future glob narrowing cannot silently drop the dashboard)
  - spm_cache.gemspec lines 15-34 (files glob + dependency block — the two facts under pin)
  - spec/update_tap_workflow_spec.rb (a precedent for specs asserting on repo release metadata rather than runtime behavior)
  </read_first>
  <files>spm_cache.gemspec, spec/web_packaging_spec.rb</files>
  <behavior>
  - RED first: packaging spec fails only if a pin is genuinely unmet (expected state: mostly green immediately — this is a pin, not a feature).
  - Gem::Specification.load('spm_cache.gemspec').files includes all four assets under lib/spm_cache/web/assets/ (this is the "glob ships the dir" locked decision, made checkable).
  - Runtime dependencies include webrick with a requirement satisfying ">= 1.8" and excluding ">= 2" (assert the requirement object's satisfied_by?(Gem::Version.new('1.8')) && !satisfied_by?(Gem::Version.new('2.0')) shape).
  - `require 'webrick'` succeeds in-process (CP8 smoke under the bundler context; the gemspec pin is the load-bearing half).
  - Optional real-build smoke (guarded, e.g. tagged or skipped if `gem build` unavailable in CI): `gem build spm_cache.gemspec` in a tmpdir copy produces a .gem whose file list contains the four assets — delete the .gem after. If the build is too slow/flaky for the suite, keep the Gem::Specification.files assertion as the automated gate and move the real build to 13-VALIDATION's manual table.
  - Offline set twin (scoped per checker finding): the three FIRST-PARTY packaged assets (index.html, app.js, styles.css), read from the SPEC'S file list, contain zero scheme-absolute URLs and zero cdn. references; cytoscape.min.js is asserted present in the file list with its structural pins (first-line version comment, no scheme URL on line 1, > 300 KB) — never byte-gated, because the official dist legitimately carries scheme URLs inside bundled MIT license comments (packaging-level twin of 13-03's scoped gate).
  </behavior>
  <action>
  Create spec/web_packaging_spec.rb per the behavior bullets (Gem::Specification.load; no network; tmpdir for the optional build smoke). Add a comment on the glob pin citing 13-CONTEXT "Frontend Architecture" and CP8 for the dependency pin. If the glob genuinely misses an asset (it should not — {lib,…}/**/* covers it), extend the glob and note it in the SUMMARY.
  Commit: `test(13-04): packaging pins — dashboard ships in the gem, webrick declared`.
  Then run the phase-level close-out: full suite `bundle exec rspec` green.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_packaging_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the files-list pin cannot find one of the four assets (silent shipping bug); or the webrick requirement pin fails</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_packaging_spec.rb green: four assets in spec.files, webrick requirement window pinned, require smoke, packaged-set offline twin
  - Full suite `bundle exec rspec` green — the phase's complete regression net (pre-existing examples + ~100 new across 13-01..04)
  </acceptance_criteria>
  <done>The gem provably ships a fully-offline dashboard and declares its only new runtime dependency exactly as researched. Phase 13's server + read + UI surfaces are welded and pinned. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| released gem tarball → user machine | what ships in spec.files is what users run — a dropped asset or missing dependency is a supply-chain/shipping integrity issue, not just a bug |
| matrix spec → middleware truth | the spec is the phase's security assertion of record; weakening it to pass would silently reopen CP13 |

## STRIDE Threat Register (ASVS Level 1)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-13-17 | Tampering (regression of the auth gate before Phase 15 POSTs) | spec/web_integration_spec.rb | high | mitigate | exhaustive route × case matrix is the standing regression net; any middleware/router change that opens a cell fails the suite by name — the gate Phase 15 inherits |
| T-13-18 | Tampering (drive-by POST/GET variants not covered) | middleware matrix cases | medium | mitigate | explicit rows: foreign-Origin POST, same-Host no-Origin tokenless form POST, Origin "null" — the three shapes a browser page can actually produce against localhost (research CP13) |
| T-13-19 | Information disclosure (gem ships without dashboard assets) | spm_cache.gemspec files glob | medium | mitigate | packaging spec pins all four asset paths inside spec.files — a future glob narrowing fails CI instead of shipping a dead dashboard |
| T-13-20 | DoS (unbounded spec runtime / extra server boots) | integration + packaging specs | low | mitigate | single-boot matrix design, 15 s runtime bound pinned in the spec; socket discipline: ONE exhaustive cross-route integration spec (single before-all boot), while single-feature thread-booted serve-through examples inside unit specs remain hermetic (loopback, ephemeral port, tmpdir) |

No package installs in this plan.
</threat_model>

<verification>
- `bundle exec rspec spec/web_integration_spec.rb spec/web_packaging_spec.rb` — both green.
- `bundle exec rspec` — the FULL suite green: the phase's exit evidence (every 13-01..04 contract plus all pre-existing behavior).
- Manual (13-VALIDATION): the real-browser walkthrough and true-offline load remain the human half of SC3/SC4/SC5 sign-off.
</verification>

<success_criteria>
- SC3 (WEB-04, complete): reject matrix proven on every route + fully-offline load (13-03 gate + packaging pin + manual offline item) — the criterion that spans 13-01/03/04 closes here with evidence.
- CP7 (hermetic discipline): one exhaustive cross-route integration spec (single before-all boot, < 15 s); single-feature thread-booted serve-through examples inside unit specs remain hermetic (loopback, ephemeral, tmpdir).
- CP8 (webrick declaration): pinned by the packaging spec, not just by review.
- CP13 (localhost not trusted): every threat shape from the research (drive-by GET/POST, DNS-rebinding Host, null-origin, tokenless absent-Origin) has a named, failing-if-regressed matrix row.
- Phase 15 can add POST endpoints behind the gate with zero new middleware work — the inheritance claim is now test-backed.
</success_criteria>

<output>
Create `.planning/phases/13-server-skeleton-read-only-dashboard/13-04-SUMMARY.md` when done
</output>
