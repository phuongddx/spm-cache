---
phase: 15-ui-build-controls
plan: 06
type: execute
wave: 4
depends_on: ['15-05']
files_modified:
  - .planning/phases/15-ui-build-controls/15-VALIDATION.md
autonomous: false
requirements: [BLD-01, BLD-02, BLD-03, BLD-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low   # estimate-calibration: 0 samples → factor 1, confidence low

must_haves:
  truths:
    - "D-14-pattern probe, build flows (BLD-01/D-03/D-09/D-01/A8): in a REAL headless browser against a REAL server and a scratch project, clicking Build disables the controls, shows the pinned in-flight message, and the spawned run arrives through the existing auto-switch path with the verbatim 'ui' badge in the identity card and the dropdown while its lines stream live; clicking Rebuild all does the same with its own pinned message and an argv row showing the forced-rebuild flag — recorded verbatim in 15-06-SUMMARY.md, human-check verification, never auto-passed"
    - "D-14-pattern probe, rejection and queue (BLD-01/D-05/BLD-02/D-06/A3): a second mutation attempt during an in-flight run renders the pinned busy sentence INLINE with no native dialog and no queue, while a terminal-held build lock leaves the buttons ENABLED and produces a visible queue — the row's waiting message and the identical in-stream line — that clears when the lock is released — recorded and ticked, human-check"
    - "D-14-pattern probe, destructive and failure paths (BLD-04/D-08/A6/BLD-03): the Rollback click swaps in the confirm bar with focus on Cancel and Tab reaching Confirm, Cancel restores focus to Rollback, Confirm spawns a rollback whose stream shows it taking the build lock; a failing build surfaces the sticky exit-status banner with fail-coloured error lines and a working jump, and the row returns to idle for a one-click retry — recorded and ticked, human-check"
    - "D-05/A1 (cross-tab honesty): with two tabs open, a run spawned in one is visible in both through the switch event, and the other tab's attempt to start a second run gets the inline busy message — the single slot is server-side, not a per-tab illusion — recorded and ticked, human-check"
    - "Recording integrity: every one of the 15-VALIDATION manual rows is recorded with its exact commands, observations and date — a row that cannot be executed is recorded as NOT RUN with its blocker, never as a pass; a FAILING row is a gap fixed in the owning module spec-first and re-run before this plan completes"
    - "Phase gate: the full suite is green at the end of the probe, with the hermetic posture intact — no probe artifact, scratch project, spawned build, or server process survives the run — bundle exec rspec"
  artifacts:
    - ".planning/phases/15-ui-build-controls/15-VALIDATION.md (MOD: the 7 manual rows ticked with dates and evidence pointers; the per-task map's Task ID/Plan/Wave columns already filled at planning time)"
    - ".planning/phases/15-ui-build-controls/15-06-SUMMARY.md (the probe recording itself — per-row commands, observations, verdicts)"
  key_links:
    - "This is the same net that caught G-13-1 in Phase 13 and two copy/status defects in Phase 14's D-14 probe — 119 and 898 green examples respectively did not catch what a real browser did, which is precisely why the repo's no-JS-runtime posture requires it"
    - "Rows 1 and 4 close the loop the tracer opened: 15-01 proved the spawn reaches a raw SSE subscriber, this proves it reaches a rendered dashboard with the right badge and argv"
    - "Row 3 is the only place BLD-02's two surfaces are seen together — the row message and the in-stream line, from the same frozen string emitted by the blocked run itself (15-02)"
    - "Row 5 is the only end-to-end proof that the web rollback path inherits the CLI's new lock (D-07): the browser watches a spawned rollback wait on a lock the web layer never touches"
  prohibitions:
    - statement: "MUST NOT mark any manual row as passing without an execution record — NOT RUN plus a blocker is the only sanctioned non-pass state (recording integrity is the audit's source of truth)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT run the probe against a project with uncommitted user work — the rollback row removes proxy packages from a real Xcode project; use a scratch project only"
      status: resolved
      verification: judgment
    - statement: "MUST NOT edit production code to make a probe row pass without a spec first — a failing row is a gap in the owning module, fixed there with its own failing-test-first commit and re-run"
      status: resolved
      verification: judgment
    - statement: "MUST NOT leave a server, a spawned build, a scratch project, or a browser process running after the probe — background processes are started through the async-job pattern and reaped in the same task"
      status: resolved
      verification: judgment
    - statement: "MUST NOT set 15-VALIDATION.md's status or nyquist_compliant frontmatter fields — that lifecycle belongs to validate-phase, not to this plan"
      status: resolved
      verification: judgment
---

# Plan 15-06 — Phase weld: the recorded agent-browser probe over the seven manual rows

<planning_context>
phase: 15 — UI Build Controls
mode: standard (weld — verification and recording, no feature work planned)
sources: 15-VALIDATION.md § Manual-Only Verifications (the 7 rows this plan executes and records, and the D-14 pattern note explaining why they are manual); 15-CONTEXT.md D-01, D-03, D-05, D-06, D-08, D-09; 15-UI-SPEC.md (the pinned copy and focus semantics each row asserts); 15-RESEARCH.md §Q6 and the P6 probe (a fail-fast CLI run in a scratch directory exercises the failure path without xcodebuild); 14-05-PLAN.md Task 3 (the precedent shape for an orchestrator-executed, human-check probe task); ROADMAP Phase 15 SC1-SC4
execution note: the probe is driven by the ORCHESTRATOR, not by an autonomous executor — the browser-visible assertions are confirmed through a real headless browser session (the 14-05 pattern), and every row's outcome is written down before this plan closes. Background processes (the server, the lock holder, the fake failing build) use the repo's async-job pattern so nothing is left running.
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase and the phase's final gate before
/gsd-verify-work: its recording is the evidence base for the phase audit and for 15-VALIDATION's
sign-off. Phase 16 inherits both the controls surface and this net.
</downstream_consumer>

<objective>
Point the real-browser net at everything Phase 15 claims: trigger builds and a rollback from a
rendered dashboard against a real server and a scratch project, watch the badge, the stream, the
busy message, the queue, the confirm bar, the failure banner and the second tab — and write down
exactly what happened, row by row, with the full suite green.

Purpose: the repo runs no JavaScript in CI, so every interactive claim in 15-05 is a source pin
until a browser proves it. This is the net that caught Phase 13's ship-blocker and two Phase 14
defects that hundreds of green examples missed.

Output: a per-row recording in 15-06-SUMMARY.md, the seven manual rows ticked in 15-VALIDATION.md
with dates, and a green full suite.
</objective>

## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| Probe recording | record | `15-06-SUMMARY.md` — per row: exact commands, observed outcome, verdict, date |
| Ticked manual table | planning (mod) | `15-VALIDATION.md` § Manual-Only Verifications — rows ticked with dates; frontmatter lifecycle fields left to validate-phase |
| Phase gate evidence | record | the full-suite result recorded alongside the rows |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ui-build-controls/15-CONTEXT.md
@.planning/phases/15-ui-build-controls/15-UI-SPEC.md
@.planning/phases/15-ui-build-controls/15-VALIDATION.md
@.planning/phases/15-ui-build-controls/15-05-SUMMARY.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-05-SUMMARY.md
@lib/spm_cache/web/marker.rb
@lib/spm_cache/web/assets/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe rows 1-4 — build, busy rejection, terminal-lock queue, forced rebuild</name>
  <precondition>Plans 15-01..15-05 committed and the full suite green; a scratch project directory with no uncommitted user work is available; a headless browser is available (the 14-05 session used one successfully).</precondition>
  <reversibility rating="reversible">Verification only — no production edits are expected; any gap found is fixed in the owning module spec-first and the row re-run.</reversibility>
  <read_first>
  - .planning/phases/15-ui-build-controls/15-VALIDATION.md — § Manual-Only Verifications rows 1-4 verbatim (they carry their own test instructions; execute them as written)
  - .planning/phases/15-ui-build-controls/15-UI-SPEC.md — the Copywriting Contract table (every string a row asserts) and the Interaction contract bullets for the build, rebuild and terminal-held-lock cases
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-05-SUMMARY.md § D-14 — the precedent recording format and the probe's operating discipline
  - .planning/phases/15-ui-build-controls/15-05-SUMMARY.md — the ids, copy and state transitions as they actually shipped
  - lib/spm_cache/web/marker.rb — the marker file carrying the bound port and launch token the probe's dashboard URL needs
  </read_first>
  <files>.planning/phases/15-ui-build-controls/15-VALIDATION.md</files>
  <behavior>
  - Executed protocol (orchestrator-driven; background processes started through the async-job pattern and reaped in this task):
    1. Boot: start the web server in a scratch project with the browser auto-open suppressed and an ephemeral port; read the port and token from the marker file; open the dashboard in the headless browser.
    2. Row 1 — Build: click Build. Observe and record: all three controls disable instantly; the pinned in-flight message renders; the spawned run arrives via auto-switch; the identity card and the recent-runs dropdown show the verbatim lowercase trigger badge; lines stream live; the row returns to idle when the run ends.
    3. Row 2 — Busy rejection: during an in-flight run, force a second mutation attempt (a second tab, or re-enabling the control through the browser's own tooling). Observe and record: HTTP 409, the pinned busy sentence rendered inline in the message slot, no native dialog, no queued second run, and the controls' enabled state per the spec.
    4. Row 3 — Terminal-held lock: hold the project's build lock from a separate short-lived process, then click Build. Observe and record: the buttons are NOT disabled by the lock, the POST is accepted, the row shows the frozen wait message, the identical line appears in the stream, and releasing the lock lets the run proceed with the message clearing on the run's next line.
    5. Row 4 — Rebuild all: click Rebuild all. Observe and record: the pinned forced in-flight message, and the identity card's argv row showing the forced-rebuild flag exactly as the CLI spells it; the run completes.
    6. Record every row in 15-06-SUMMARY.md — exact commands, timestamps, observed outcomes with evidence, verdict — and tick the corresponding rows in 15-VALIDATION.md with the date.
  - Honesty rule: a row that cannot be executed is recorded as NOT RUN with its blocker — never a pass, never a silent skip. A FAILING row is a gap: fix the owning module with a failing test first, then re-run the row before this task completes.
  - Cleanup: the server, any spawned build, the lock holder and the browser session are all stopped in this task; the scratch project is removed or left untouched by design, and the fact is recorded.
  </behavior>
  <action>
  Execute the protocol above against a REAL server and a REAL scratch project — a fail-fast run in a scratch directory exercises the spawn, badge, stream and completion paths without any toolchain build, which is exactly how the research probes exercised them. Use the repo's async-job pattern for the server and the lock holder so no process is orphaned. Do not modify production code in this task; if a row fails, stop, record the failure, fix it in the owning module with its own failing-test-first commit, and re-run the row.

  Write the recording into 15-06-SUMMARY.md as you go — one section per row with the command, the observation and the verdict — and tick rows 1-4 in 15-VALIDATION.md's manual table with the date. Commit: `test(15-06): record agent-browser probe rows 1-4 (build, busy, lock queue, rebuild)`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb spec/web_build_routes_spec.rb spec/web_integration_spec.rb</automated>
    <fails_when>exit code non-zero; or a probe row is ticked in 15-VALIDATION.md without a corresponding recording in the summary; or any row is marked passing without an observation; or a server, build, lock holder or browser process survives the task</fails_when>
    <human-check>Rows 1-4 as observed in the real browser: (1) Build disables the controls, shows the pinned in-flight message, and the run arrives with the verbatim lowercase trigger badge in the card and the dropdown while lines stream live; (2) a second attempt during that run renders the pinned busy sentence inline — no native dialog, no second run; (3) with the build lock held from another process the buttons stay enabled, the click is accepted, and the frozen wait message shows in the row and identically in the stream, clearing when the lock is released; (4) Rebuild all shows its own pinned message and the identity card's argv row carries the forced-rebuild flag.</human-check>
  </verify>
  <acceptance_criteria>
  - Rows 1-4 recorded in 15-06-SUMMARY.md with commands, observations and verdicts (pass with evidence, or NOT RUN with a blocker)
  - Rows 1-4 ticked in 15-VALIDATION.md with the date
  - No process or artifact left behind by the probe
  </acceptance_criteria>
  <done>The phase's core claim — click a button, get a real, correctly attributed, live-streaming CLI run, with an honest answer when something else is already running — is proven in a real browser and written down. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Probe rows 5-7 — rollback confirm bar, failure banner chain, cross-tab slot — plus the phase gate</name>
  <precondition>Task 1 complete with rows 1-4 recorded; the scratch project is still available (or freshly recreated).</precondition>
  <reversibility rating="reversible">Verification only; the destructive row runs against a scratch project by construction.</reversibility>
  <read_first>
  - .planning/phases/15-ui-build-controls/15-VALIDATION.md — § Manual-Only Verifications rows 5-7 verbatim
  - .planning/phases/15-ui-build-controls/15-UI-SPEC.md — the rollback bullet and A6 (focus semantics), A7 (the row never duplicates failure copy), the failure row of the copy table (BLD-03 adds no new strings)
  - .planning/phases/15-ui-build-controls/15-02-SUMMARY.md — the rollback lock as it landed (the stream evidence row 5 looks for)
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-05-SUMMARY.md § D-14 — the failure-row precedent (a failing run in an empty directory exercises the failure paths without a toolchain build)
  </read_first>
  <files>.planning/phases/15-ui-build-controls/15-VALIDATION.md</files>
  <behavior>
  - Executed protocol (same operating discipline as Task 1):
    1. Row 5 — Rollback confirm bar: click Rollback. Observe and record: the confirm bar swaps in with the pinned sentence, focus lands on Cancel, Tab reaches Confirm, Cancel returns to the row with focus back on Rollback; then Confirm — the controls disable, the POST is accepted, the row shows the pinned restoring message, and the spawned rollback's own stream shows it acquiring the build lock (hold the lock from another process to make the wait line observable).
    2. Row 6 — Failing build: force a failing run (a build in a scratch directory without a project fails fast — the research probe measured header and exit line landing in under a second). Observe and record: the sticky banner with the exit status, error lines prefixed and fail-coloured, a working jump to the first error, and the row returning to idle with the controls re-enabled for a one-click retry.
    3. Row 7 — Cross-tab slot: with two tabs open before the spawn, start a run in tab A. Observe and record: tab B sees the run through the switch event, and tab B's attempt to start another gets the inline busy message.
    4. Record rows 5-7 in 15-06-SUMMARY.md and tick them in 15-VALIDATION.md with the date.
    5. Phase gate: run the full suite and record the result (example count and failures) in the summary alongside the rows.
  - Honesty rule and cleanup: identical to Task 1 — NOT RUN with a blocker is the only sanctioned non-pass, failing rows are fixed spec-first in the owning module and re-run, and no process or scratch artifact survives the task.
  </behavior>
  <action>
  Execute rows 5-7 against the real server and scratch project, using the async-job pattern for the server and the lock holder. The rollback row is destructive by design — confirm the target is a scratch project with no user work before clicking Confirm.

  Record each row in 15-06-SUMMARY.md with its command, observation and verdict; tick rows 5-7 in 15-VALIDATION.md with the date. Then run the full suite as the phase gate and record its result. Commit: `test(15-06): record agent-browser probe rows 5-7 + phase gate (rollback, failure, cross-tab)`.
  </action>
  <verify>
    <automated>bundle exec rspec</automated>
    <fails_when>exit code non-zero; or the recorded example count regressed below the pre-phase baseline; or any of the seven manual rows remains unticked without a NOT RUN plus blocker note; or the summary lacks a per-row recording; or a probe process or scratch artifact survives</fails_when>
    <human-check>Rows 5-7 as observed in the real browser: (5) the Rollback click swaps in the confirm bar with the pinned sentence, focus on Cancel, Tab reaching Confirm, Cancel restoring focus to Rollback, and Confirm producing a spawned rollback whose stream shows it waiting for and taking the build lock; (6) a failing build shows the sticky exit-status banner, fail-coloured prefixed error lines, a working jump to the first error, and a row back at idle with the controls re-enabled; (7) with two tabs open, both see the spawned run and the second tab's attempt renders the inline busy message.</human-check>
  </verify>
  <acceptance_criteria>
  - All seven manual rows recorded in 15-06-SUMMARY.md and ticked in 15-VALIDATION.md with dates (or recorded as NOT RUN with blockers)
  - The full suite is green and its result is recorded — the phase gate
  - 15-VALIDATION.md's status and nyquist_compliant frontmatter fields are left untouched for validate-phase
  </acceptance_criteria>
  <done>Every interactive claim Phase 15 makes has been exercised by a real browser against a real server, including the destructive one, and the evidence is written where the phase audit will look. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the probe's recording → the phase sign-off | the recorded evidence is what the audit reads; a fabricated or silently skipped row would launder an unverified claim |
| the probe's scratch project → the developer's real projects | the rollback row is destructive and must never point at real work |
| probe background processes → the developer's machine | servers, lock holders and spawned builds started by the probe outlive it unless reaped |

## STRIDE Threat Register (ASVS Level 1; block on high)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-15-28 | Repudiation (claiming verification that did not happen) | the probe recording | high | mitigate | per-row evidence with exact commands and observations; NOT RUN plus a blocker is the only sanctioned non-pass; the human-check gate keeps the browser assertions honest |
| T-15-29 | Tampering (a destructive probe row run against a real project) | row 5 | high | mitigate | the precondition and the prohibition both pin a scratch project with no uncommitted work; the target is confirmed before the confirm click |
| T-15-30 | Denial of service (an orphaned server, lock holder or spawned build left running on the developer's machine) | probe processes | medium | mitigate | the async-job pattern for every background process, with reaping in the same task and a cleanup assertion in the fails-when |
| T-15-31 | Tampering (patching production code to make a red row go green without a test) | gap handling | medium | mitigate | the honesty rule and the prohibition: a failing row is fixed in the owning module with a failing test first, then the row is re-run |

No package installs in this plan.
</threat_model>

<verification>
- `bundle exec rspec spec/web_frontend_spec.rb spec/web_build_routes_spec.rb spec/web_integration_spec.rb` — green (Task 1's automated floor).
- `bundle exec rspec` — full suite green, example count recorded (Task 2, the phase gate).
- The seven recorded manual rows in 15-06-SUMMARY.md and the ticked table in 15-VALIDATION.md — the manual half of the phase's sign-off (per the human-check gates above).
</verification>

<success_criteria>
- SC1 proven browser-true: real CLI runs triggered from the dashboard, streaming live, with the single-slot rejection visible and clear.
- SC2 proven browser-true: the lock-derived waiting state is visible on both surfaces and never silent, and a terminal-held lock never disables a control.
- SC3 proven browser-true: a failed build shows its exit status with highlighted errors, and retrying is one click.
- SC4 proven browser-true: the dashboard's Rollback restores source mode behind a confirm step, and the spawned rollback takes the build lock.
- The phase is ready for /gsd-verify-work with zero unticked validation rows and a green suite.
</success_criteria>

<output>
Create `.planning/phases/15-ui-build-controls/15-06-SUMMARY.md` when done (including the per-row probe recording)
</output>
