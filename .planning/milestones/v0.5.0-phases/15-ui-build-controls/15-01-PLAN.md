---
phase: 15-ui-build-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/spm_cache/web/jobs.rb
  - lib/spm_cache/web/router.rb
  - lib/spm_cache/web/read_models/runs.rb
  - spec/support/web_server_boot.rb
  - spec/fixtures/fake_spm_cache_bin.rb
  - spec/web_integration_spec.rb
  - spec/web_jobs_spec.rb
autonomous: true
requirements: [BLD-01]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low   # estimate-calibration: 0 samples → factor 1, confidence low

must_haves:
  truths:
    - "D-02 (spawn shape): a UI build is spawned as ARRAY argv [RbConfig.ruby, BIN_PATH, 'build'] — never a shell string — with chdir = Core::Config.instance.project_dir, env = ENV.to_h.merge('SPM_CACHE_TRIGGER' => 'ui'), out:/err: → File::NULL, pgroup: true, then Process.detach; asserted through an injected fake-bin's recorded side effects (argv, cwd, trigger env, pgid == own pid, null stdio) — bundle exec rspec spec/web_jobs_spec.rb"
    - "D-02/WEB-03 (the tracer truth): POST /api/build spawns the real subprocess seam end-to-end — the child writes its OWN run log, that run reaches a live /api/events subscriber with header trigger 'ui', and server.shutdown WITH the build still in flight returns within seconds leaving the child ALIVE (nothing waits on, reaps, or kills it) — bundle exec rspec spec/web_integration_spec.rb"
    - "D-05 (single slot): the check-then-claim is atomic inside one Mutex — N concurrent threads racing the same claim produce EXACTLY ONE spawn and N-1 busy answers; the slot is verb-agnostic, so a rollback claim collides with a build claim (UI-SPEC A1) — bundle exec rspec spec/web_jobs_spec.rb"
    - "D-05/CP10 (honest release): the slot never learns 'ended' from waitpid (detach discards status) — it derives: the newest run whose header pid is the slot pid decides via ReadModels::Runs (run_end authoritative, pid liveness fallback, CP14 interrupted honored); before the child has written its header the raw pid probe decides; a finished run frees the slot on the NEXT claim, with no monitoring thread anywhere — bundle exec rspec spec/web_jobs_spec.rb"
    - "D-04 (gate inheritance): POST /api/build rides the SAME per-launch token via X-SPM-Token and the SAME Host/Origin gate as every GET — no second token, no query-param token on the POST path; a tokenless POST is 401 and a foreign-Origin POST is 403 before dispatch — bundle exec rspec spec/web_integration_spec.rb"
    - "D-05 (rejection shape): a second claim while the slot is held answers HTTP 409 carrying the standard error envelope plus a machine-readable data.reason 'slot_busy'; a 2xx answer carries data.scope and a freshly derived data.lock snapshot (Runs.lock_state) — bundle exec rspec spec/web_integration_spec.rb"
    - "V5 (fixed argv fragments): scope reaches Process.spawn ONLY through a frozen scope→argv table — the request string is never interpolated, concatenated, or splatted into the command line; an unknown scope is 400 before any spawn attempt — bundle exec rspec spec/web_jobs_spec.rb spec/web_integration_spec.rb"
  artifacts:
    - "lib/spm_cache/web/jobs.rb (NEW: Web::Jobs — BIN_PATH, frozen scope→argv table, Mutex slot, spawn_run, derive-based liveness)"
    - "lib/spm_cache/web/router.rb (MOD: /api/build dispatch arm, api_mutate helper, error_envelope reason: kwarg, @jobs collaborator defaulted like @events)"
    - "lib/spm_cache/web/read_models/runs.rb (MOD: derive_for_pid + pid_alive? made public — the ONE liveness derivation the slot consumes)"
    - "spec/fixtures/fake_spm_cache_bin.rb (NEW: injectable fake CLI — records spawn side effects, writes a real run log, optional in-flight sleep mode)"
    - "spec/support/web_server_boot.rb (MOD: jobs: kwarg forwarded to the Router; http_post gains a body)"
    - "spec/web_integration_spec.rb (MOD: the POST→spawn→stream row and the shutdown-with-in-flight-build row — the file's LAST describe)"
    - "spec/web_jobs_spec.rb (NEW: spawn shape, mutex atomicity, derive release, busy answer)"
  key_links:
    - "Jobs is defaulted inside Router#initialize exactly like @events (router.rb:57-62) — Command::Web#build_server needs NO edit, and specs inject a fake-bin-backed Jobs through the same seam"
    - "The child is the writer: it opens its own run log, so the run reaches the browser through 14's tailer/broadcaster with zero server-side piping (D-09) — the server never holds build output in memory"
    - "Slot liveness rides ReadModels::Runs (derive/status_for/pid_alive?) — the SAME derivation the card, dropdown and hello use, so the slot can never disagree with the dashboard about whether a run is running (CP10)"
    - "Shutdown does NOTHING about the child (no kill, no wait): WEB-03's exit-0 and D-02's 'stopping the server must not kill the build' are the same code path — the absence IS the contract, pinned by the integration row"
  prohibitions:
    - statement: "MUST NOT pass a shell string to Process.spawn or interpolate any request value into argv — array argv with fixed fragments only (D-02, Pitfall 5)"
      status: resolved
      verification: test
    - statement: "MUST NOT wait on, reap, kill, or signal the spawned child from the server — no waitpid, no monitoring thread, no shutdown hook touching the process group (D-02, Pitfall 3; a stop control is WEB2-02 and ships nowhere in this phase)"
      status: resolved
      verification: test
    - statement: "MUST NOT let the child inherit the server's stdout/stderr — File::NULL on both, so the terminal running spm-cache web stays quiet (T-13-03, Pitfall 4)"
      status: resolved
      verification: test
    - statement: "MUST NOT put the launch token, or any secret, into the spawned child's environment — the only added variable is the attribution marker (D-04 posture, web.rb:75)"
      status: resolved
      verification: test
    - statement: "MUST NOT keep server-side memory of run state beyond the slot's pid/scope — status is always re-derived from disk (CP10); the slot is not a second source of truth"
      status: resolved
      verification: judgment
    - statement: "MUST NOT treat the build flock as the slot — a terminal-held lock must never reject a UI POST (UI-SPEC A3); the slot guards UI-originated spawns only (D-05)"
      status: resolved
      verification: test
---

# Plan 15-01 — TRACER: one UI-triggered build, end to end (POST → slot → detached CLI → run log → stream → clean shutdown)

<planning_context>
phase: 15 — UI Build Controls
mode: standard (tracer-first)
sources: 15-CONTEXT.md D-02 (costly: pgroup lifecycle touches shutdown), D-04, D-05, D-09; 15-RESEARCH.md §Q1 (spawn mechanics + PROBED P1-P8), §Q4 (slot + routes), §Architectural Responsibility Map, Pitfalls 1-5; 15-UI-SPEC.md Interaction contract (endpoint paths, 409 shape, A1 shared slot, A9 undisplayed reason); 15-VALIDATION.md rows 1-3 + the Wave-1 integration row; ROADMAP Phase 15 SC1; REQUIREMENTS BLD-01
tracer rationale: the thin path crosses every layer whose architecture is UNPROVEN this phase — HTTP mutation gate → in-process slot → detached process group → CLI child → run-log file → SSE relay → server shutdown. The frontend layer is deliberately outside the tracer: 14 already proved that stack browser-true (14-05 D-14) and the controls are additive DOM on it, so a thin button would add no architectural information while costing a browser probe per commit. Expansion plans 15-02..15-06 build out from this slice.
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase. Plan 15-04 widens the scope table and adds /api/rollback on the seams landed here; 15-05's controls row POSTs to this exact endpoint and branches on this exact 409 shape; 15-06's browser probe drives it for real. The envelope shapes and the Jobs constructor seam are contracts from the moment this plan commits.
</downstream_consumer>

<objective>
Prove the whole Phase-15 mutation architecture with ONE working path: an authenticated POST to
/api/build claims a Mutex-guarded single slot, spawns the REAL CLI as an array-argv, own-process-group,
detached child carrying the UI trigger marker, and the child's own run log reaches a live
/api/events subscriber — while stopping the server leaves that build running and still exits
immediately.

Purpose: BLD-01's spawn half and SC1's "spawns the real CLI subprocess (array argv, own process
group) and its output streams live into the log view", landed as a production-quality slice on the
agent's best early-context tokens so an architectural dead end costs one commit, not ten.

Output: lib/spm_cache/web/jobs.rb, the /api/build dispatch arm and api_mutate helper, one public
liveness derivation on ReadModels::Runs, a fake-bin fixture, and the two integration rows that pin
the end-to-end path.
</objective>

## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| `Web::Jobs` | lib (new) | `BIN_PATH` (P8 relative resolution), frozen `SCOPES` argv table, `Mutex` slot, `spawn_run(scope:)`, derive-based `busy?` |
| `/api/build` + `api_mutate` | lib (mod) | `router.rb` dispatch arm mirroring `api_read`'s gate order; `error_envelope(message, reason:)`; 2xx carries `scope` + `lock` |
| `Runs.derive_for_pid` / public `Runs.pid_alive?` | lib (mod) | the one liveness derivation the slot consumes — no pid math in `Jobs` |
| `spec/fixtures/fake_spm_cache_bin.rb` | fixture (new) | records argv/cwd/env/pgid/stdio to a probe file, writes a real run log, `FAKE_BIN_SLEEP` in-flight mode |
| `WebServerBoot` jobs injection + POST bodies | spec support (mod) | `jobs:` forwarded to the Router; `http_post(..., body:)` |
| Integration rows | spec (mod) | POST→spawn→stream row; shutdown-with-in-flight-build row (file's last describe) |
| `spec/web_jobs_spec.rb` | spec (new) | spawn shape, mutex atomicity, derive release, busy answer |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ui-build-controls/15-CONTEXT.md
@.planning/phases/15-ui-build-controls/15-RESEARCH.md
@.planning/phases/15-ui-build-controls/15-UI-SPEC.md
@.planning/phases/15-ui-build-controls/15-VALIDATION.md
@lib/spm_cache/web/router.rb
@lib/spm_cache/web/server.rb
@lib/spm_cache/web/read_models/runs.rb
@lib/spm_cache/command/web.rb
@lib/spm_cache/core/run_log.rb
@spec/support/web_server_boot.rb
@spec/web_integration_spec.rb
@spec/web_events_route_spec.rb
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — POST /api/build spawns a detached CLI whose run streams, and shutdown leaves it running (RED → GREEN)</name>
  <precondition>`bundle exec rspec spec/web_integration_spec.rb spec/web_events_route_spec.rb` is green on the current tree before starting.</precondition>
  <reversibility rating="costly">D-02's own rating: the pgroup/detach lifecycle touches the server's shutdown contract; changing it later means re-proving WEB-03's exit-0 with an in-flight build. Implement the probed shape (15-RESEARCH P1-P5), not a variant.</reversibility>
  <read_first>
  - .planning/phases/15-ui-build-controls/15-RESEARCH.md — §Q1 in full (the spawn sketch, the env-merge rationale, the pgroup/detach probe results P1-P5, the how-the-slot-learns-ENDED paragraph), §Q4 (slot structure, route placement, body validation, lock snapshot, spawn-failure 500), Pitfalls 1-5
  - .planning/phases/15-ui-build-controls/15-CONTEXT.md — D-02 (costly), D-04, D-05, D-09
  - .planning/phases/15-ui-build-controls/15-UI-SPEC.md — Interaction contract paragraph 1 (endpoint paths, body shapes, envelope, 409 machine-readable reason) and A1/A9
  - lib/spm_cache/web/router.rb lines 41-78 (constructor collaborators + the gate) and 139-163 (api_read — the helper shape api_mutate mirrors) and 227-262 (envelope helpers)
  - lib/spm_cache/web/read_models/runs.rb lines 45-81 (derive), 113-122 (lock_state), 130-200 (private helpers incl. run_files/status_for/pid_alive?)
  - lib/spm_cache/core/run_log.rb lines 106-140 (run-log filename shape + the run_start header keys the fake bin must mimic) and 265-291 (run_end shape)
  - spec/support/web_server_boot.rb (with_server kwargs, raw_stream_open/raw_read_until, http_post, shutdown, wait_accepting)
  - spec/web_integration_spec.rb lines 26-97 (the ONE boot, order: :defined, weld fixtures, handle/get/post helpers) and the SSE rows around line 290-320
  </read_first>
  <files>lib/spm_cache/web/jobs.rb, lib/spm_cache/web/router.rb, lib/spm_cache/web/read_models/runs.rb, spec/fixtures/fake_spm_cache_bin.rb, spec/support/web_server_boot.rb, spec/web_integration_spec.rb</files>
  <behavior>
  - RED first: the fake-bin fixture plus 4 new failing examples in spec/web_integration_spec.rb (the ONE boot, CP7 — no second listener):
    1. Authenticated POST /api/build with body {"scope":"build"} answers 200 with the standard envelope; data carries scope 'build' and a lock snapshot whose 'state' is 'free' or 'held' (the Runs.lock_state shape, freshly derived).
    2. The spawn actually happened, with the probed shape: the fake bin's probe file records argv ending in ['build'], cwd == the boot's project_dir, ENV['SPM_CACHE_TRIGGER'] == 'ui', its own pgid == its own pid (group leader — P1), and both its stdout and stderr resolve to the null device (P7). The probe file also proves the parent's own $stdout was NOT inherited.
    3. The run reaches the browser: a raw /api/events subscriber opened BEFORE the POST receives the new run within a bounded read, and the run's header trigger is 'ui'. (Scope note — do not overclaim: this asserts the marker is DELIVERED to the child's env and echoed into a real run log; that Main.run NORMALIZES it into the header is 15-02's unit row.)
    4. Second POST while the first is still in flight answers 409, envelope status 'error', data.reason == 'slot_busy', and NO second spawn occurs (the probe file still records exactly one invocation).
  - RED first: the file's LAST describe (order: :defined is already set) — shutdown with an in-flight build:
    5. With a sleeping fake-bin build in flight, WebServerBoot.shutdown(@server) returns in under 5 s AND the child is still alive immediately afterwards (Process.kill(0, pid) succeeds) — the server neither waits on, reaps, nor kills the detached group (P5; WEB-03 + D-02 in one row). The example reaps the child itself in an ensure (TERM the process GROUP, bounded liveness poll) so no example leaks a process.
  - Fake bin contract (spec/fixtures/fake_spm_cache_bin.rb, run as [RbConfig.ruby, fixture, *argv]): appends one JSON object to ENV['FAKE_BIN_PROBE'] recording argv, Dir.pwd, the trigger env value, Process.pid, Process.getpgid(Process.pid), and whether $stdout/$stderr share the null device's rdev; writes a REAL run log into the runs dir it derives from cwd (run_start header with the run_log.rb key set — command, argv, pid, started_at, trigger from the env marker, cycle false — then, after an optional ENV['FAKE_BIN_SLEEP'] pause, a run_end line with the exit status). Filename follows the run_log.rb convention so the tailer and Runs.derive treat it as any other run.
  - Regression: the pre-existing 25-cell route×auth matrix and every SSE row in the file stay green (the new arm must not perturb dispatch or the gate).
  </behavior>
  <action>
  Extend spec/support/web_server_boot.rb first: forward a `jobs:` kwarg into the Router the same way `events:` is forwarded (present-only, so existing callers are untouched), and give `http_post` an optional body plus content type so POST rows can send JSON.

  Author spec/fixtures/fake_spm_cache_bin.rb per the fake-bin contract bullet. It must be pure stdlib (json, fileutils, time) and must NOT require the gem — it stands in for the CLI, it does not exercise it. Keep it a fixture file, never a _spec file, so RSpec does not load it as a suite member.

  Write the five RED examples described above into spec/web_integration_spec.rb, reusing its existing boot, handle/get/post helpers and the raw SSE helpers. The shutdown row is appended as the file's final describe because the boot is file-scoped and order is :defined; after(:all)'s shutdown stays in place and is idempotent.

  Then implement, smallest production-quality shape that satisfies them:

  lib/spm_cache/web/jobs.rb — Web::Jobs. Constants: BIN_PATH resolved with File.expand_path('../../../bin/spm-cache', __dir__) (P8), TRIGGER_ENV naming the marker variable, and a FROZEN scope→argv table whose only entry this plan ships is 'build' → ['build'] (15-04 widens it; the table is the whole point — no request value is ever interpolated). Constructor takes config: (defaulting to Core::Config.instance) and bin_path: (defaulting to BIN_PATH — the fake-bin seam), and allocates one Mutex plus an empty slot. spawn_run(scope:) synchronizes the ENTIRE check-then-claim-then-record sequence on that Mutex (Pitfall 1: two WEBrick threads racing a double-click must not both pass the check), returning nil when the slot is still held and the new pid otherwise. The spawn itself is Process.spawn with an explicit env hash of ENV.to_h merged with the trigger marker set to 'ui', the interpreter path from RbConfig.ruby, the bin path, the table's argv fragments, chdir: the config's project_dir, out: and err: File::NULL, and pgroup: true — followed immediately by Process.detach so the reaper thread owns the corpse and the server never waits (P3/P5). Spawn failures propagate to the caller; nothing is claimed when the spawn raises. Liveness (busy?) is DERIVED, never remembered: with a slot pid, ask the read model for the newest run whose header pid matches; if such a run exists the slot is held only while its derived status is 'running'; if no run file exists yet (the child has not published its header) fall back to the raw pid probe. A freed slot is cleared in place. There is no monitoring thread and no waitpid anywhere in this file; document the CP10 rationale in a comment.

  lib/spm_cache/web/read_models/runs.rb — expose the two derivations Jobs needs instead of duplicating them (the Don't-Hand-Roll rule): add a public class method that returns the newest derive() result whose header pid equals a given pid (nil when none), and move the existing pid liveness probe above the private marker so it is callable. No behavior change to any existing method.

  lib/spm_cache/web/router.rb — accept and default a jobs: collaborator exactly as events: is defaulted (constructed lazily-by-default, injectable for specs, so command/web.rb needs no edit). Add the '/api/build' dispatch arm. Add a private api_mutate helper mirroring api_read's gate ORDER — token check first (401 envelope), then a request-method check that answers the house 404 for anything but POST — then read and JSON-parse the request body, answering 400 with a machine-readable reason for a malformed body and 400 with its own reason for a scope outside the frozen table. Ask Jobs to spawn; a nil answer is the 409 envelope carrying reason 'slot_busy'; a raised spawn failure is the 500 envelope carrying reason 'spawn_failed' and the exception message; success is the standard ok envelope whose data carries the scope and a freshly derived Runs.lock_state snapshot (§Q4's recommendation, and the datum 15-05's waiting flavor reads on entry). Extend error_envelope with an optional reason keyword that adds the key only when given, so every existing rejection's bytes are unchanged.

  Commit RED first: `test(15-01): failing end-to-end spawn/stream/shutdown rows + fake-bin fixture`, then GREEN: `feat(15-01): POST /api/build spawns a detached CLI run; shutdown leaves it running`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_integration_spec.rb spec/web_events_route_spec.rb spec/web_server_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the shutdown row observes the child dead after shutdown (the server killed the build — D-02 violated); or the shutdown row exceeds its bound (the server waited on the build — WEB-03 violated); or the double-POST row records two invocations in the probe file; or any example leaves a fake-bin process alive after the suite</fails_when>
  </verify>
  <acceptance_criteria>
  - The five new integration examples are green, including the last-describe shutdown row
  - The spawn assertions read the fake bin's RECORDED side effects (argv, cwd, trigger env, pgid, null stdio) — not the source text of jobs.rb
  - The pre-existing route×auth matrix and SSE rows in the file are still green
  - No waitpid, no monitoring thread, and no signal call to the child exists outside the spec's own cleanup
  </acceptance_criteria>
  <done>A real user can POST /api/build with their launch token and watch a real detached CLI run stream into the dashboard, and stopping the server does not stop their build. Committed RED then GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expansion — the Jobs unit matrix: spawn shape, Mutex atomicity, derive-based release, verb-agnostic slot (RED → GREEN)</name>
  <precondition>Task 1 GREEN and committed.</precondition>
  <reversibility rating="reversible">Unit coverage plus small hardening of an already-landed collaborator.</reversibility>
  <read_first>
  - .planning/phases/15-ui-build-controls/15-VALIDATION.md — per-task rows 1 and 2 (the fixed spawn-shape and slot contracts this file must assert)
  - .planning/phases/15-ui-build-controls/15-RESEARCH.md — §Q1 'How the slot learns the run ENDED' (options a and b, and why either is CP10-honest), Pitfall 1 and Pitfall 2 (the ~50 ms false-alive window and why POST granularity makes it irrelevant)
  - lib/spm_cache/web/jobs.rb (Task 1 output)
  - lib/spm_cache/web/read_models/runs.rb lines 45-81 and the newly public helpers
  - spec/events_tailer_spec.rb (the DEAD_PID idiom for a pid that is ESRCH by construction) and spec/run_log_spec.rb lines 12-46 (runs-dir tmpdir fixtures + after-hook cleanup)
  </read_first>
  <files>spec/web_jobs_spec.rb, lib/spm_cache/web/jobs.rb</files>
  <behavior>
  - RED first: spec/web_jobs_spec.rb with 9 failing examples, all hermetic (tmpdir project, fake bin, no sockets, no real xcodebuild):
    1. Argv identity: a build spawn records exactly [ruby-interpreter, bin_path, 'build'] — the frozen table's fragments, in order, with nothing appended from the request.
    2. Environment: the child's SPM_CACHE_TRIGGER is 'ui' and the rest of the parent environment is inherited (a sentinel variable set in the parent reaches the child), while no launch-token-shaped variable is added by Jobs.
    3. Working directory: the child's cwd is the configured project_dir, not the spec process's cwd.
    4. Process group: the child's pgid equals its own pid (group leader), which is what makes a future group-kill and today's Ctrl-C immunity possible.
    5. Null stdio: the child's stdout and stderr both point at the null device; a sentinel the child writes to stdout never reaches the parent's captured output.
    6. Mutex atomicity: 8 threads calling spawn_run concurrently against a held-open fake bin produce exactly ONE non-nil pid and 7 nils, and the probe file records exactly one invocation (Pitfall 1 — the client-side disable is UX, this is the guard).
    7. Verb-agnostic slot (UI-SPEC A1): while a build claim is live, a claim for any other scope value in the table is refused the same way — one slot, not one per verb.
    8. Derive-based release, run_end path: once the claimed run's log carries a run_end line, the next claim succeeds — the slot consulted the run log, not waitpid.
    9. Derive-based release, dead-pid path: a claimed pid that is ESRCH with no run file at all frees the slot on the next claim (the pre-header window's fallback), and a claimed pid whose run file exists but whose header pid never matches does not wedge the slot forever.
  - Regression: no example spawns anything that outlives it — every claimed child is reaped in an ensure.
  </behavior>
  <action>
  Author spec/web_jobs_spec.rb per the behavior bullets, reusing the Task-1 fake bin through the bin_path: seam and a tmpdir project_dir configured via Core::Config.configure with restore-in-ensure (the WebServerBoot Config discipline, applied without a server). Drive liveness rows by writing run-log files directly into the runs dir (the run_log.rb filename convention) rather than sleeping on real timing, so the file is deterministic; use the ESRCH-by-construction dead-pid idiom for the fallback row.

  Harden lib/spm_cache/web/jobs.rb only where a row proves it necessary — the expected deltas are the pre-header pid fallback and clearing a freed slot in place. Do not add a monitoring thread, a status cache, or a second liveness path: if a row is hard to satisfy, the fix is in the derivation the read model already owns.

  Commit RED first: `test(15-01): failing Web::Jobs spawn/slot/release matrix (9 examples)`, then GREEN: `feat(15-01): harden the spawn slot — atomic claim, derived release`. Then run the wave gate: full suite `bundle exec rspec` green.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_jobs_spec.rb spec/web_integration_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the concurrency row observes two spawns; or any row asserts by reading jobs.rb's source text instead of the fake bin's recorded side effects; or a spawned fake bin survives the example</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_jobs_spec.rb green with the 9 described examples, hermetic (no sockets, no real CLI, no xcodebuild)
  - Full suite `bundle exec rspec` green — the Wave 1 merge gate for this plan
  - jobs.rb contains no waitpid, no monitoring thread, and no independent pid-status vocabulary
  </acceptance_criteria>
  <done>The slot is provably a single slot under real thread contention, its release is derived from the run log rather than remembered, and the spawn shape is pinned by a child that reports what it actually received. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/build | the first MUTATING request surface in the project: crosses the Host/Origin + token gate and then reaches Process.spawn |
| request JSON body → argv | untrusted strings arriving at a command-line construction site |
| server process → detached child process group | a lifecycle boundary: the child outlives the server by design, so shutdown must not reach across it |
| child env → CLI behavior | the trigger marker is attribution-only; anything reading it for control flow would be a forgeable privilege seam |

## STRIDE Threat Register (ASVS Level 1; block on high)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-15-01 | Elevation of privilege (drive-by cross-site POST triggering builds — CSRF) | /api/build | high | mitigate | POST rides the SAME structural gate as every GET (single catch-all servlet, server.rb:87-97): Host exact-match, Origin-if-present allowlist, X-SPM-Token; integration rows assert 401 tokenless and 403 foreign-Origin on the POST path (D-04) |
| T-15-02 | Tampering (shell injection / argv smuggling through the scope value) | Jobs scope→argv table | high | mitigate | frozen scope→argv table + array-argv Process.spawn; unknown scope is rejected 400 BEFORE any spawn; no interpolation or concatenation reaches the command line (V5, Pitfall 5) |
| T-15-03 | Denial of service (slot bypass spawning unbounded builds — double-click or two tabs) | Mutex slot | high | mitigate | check-then-claim entirely inside one Mutex; the 8-thread concurrency row proves exactly one spawn; the flock still protects artifacts behind it (Pitfall 1) |
| T-15-04 | Denial of service / Tampering (server shutdown killing or blocking on an in-flight build) | Server#shutdown ↔ detached child | high | mitigate | the server does NOTHING about the child; the last-describe integration row asserts bounded shutdown AND a still-alive child afterwards (WEB-03 + D-02, P5) |
| T-15-05 | Information disclosure (secrets or build chatter leaking through the child) | spawn env + stdio | medium | mitigate | env adds only the attribution marker (the token lives in the marker file and constructors, never ENV); out:/err: File::NULL keeps build output off the web terminal (T-13-03, Pitfall 4) |
| T-15-06 | Information disclosure (raw exception text from a failed spawn) | 500 envelope | low | accept | the envelope carries the exception message on a loopback-only, single-user tool; the frontend wraps it in the pinned POST-failure template and never renders internals (UI-SPEC prohibition 8) |
| T-15-07 | Repudiation (a UI-spawned run indistinguishable from a terminal run) | trigger marker | low | mitigate | the marker rides the child env into the run_start header; attribution-only, never a control-flow input (Pitfall 7); the normalization row is 15-02's |

No package installs in this plan.
</threat_model>

<verification>
- `bundle exec rspec spec/web_jobs_spec.rb spec/web_integration_spec.rb` — green (15-VALIDATION rows 1, 2 and the Wave-1 integration row).
- `bundle exec rspec spec/web_events_route_spec.rb spec/web_server_spec.rb` — green (the new dispatch arm perturbs neither the gate nor the SSE route).
- `bundle exec rspec` — full suite green (Wave 1 merge gate, run at the end of Task 2).
- The browser half of this slice (click → badge → stream) is 15-06's recorded probe row 1; nothing here claims it.
</verification>

<success_criteria>
- BLD-01's spawn half lands exactly as D-02 pins it: array argv, own process group, detached, marker-carrying, null-stdio, with the server never becoming the build's signal parent.
- D-05's single slot is atomic under real thread contention and honest about release (derived, never remembered).
- WEB-03 survives the phase's biggest risk: shutdown with an in-flight build stays fast and non-destructive, pinned by a regression row.
- The remaining plans expand a proven slice instead of guessing at one.
</success_criteria>

<output>
Create `.planning/phases/15-ui-build-controls/15-01-SUMMARY.md` when done
</output>
