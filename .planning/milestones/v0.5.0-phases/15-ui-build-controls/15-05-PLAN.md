---
phase: 15-ui-build-controls
plan: 05
type: execute
wave: 3
depends_on: ['15-04']
files_modified:
  - lib/spm_cache/web/assets/index.html
  - lib/spm_cache/web/assets/styles.css
  - lib/spm_cache/web/assets/app.js
  - lib/spm_cache/web/assets/log.js
  - spec/web_frontend_spec.rb
autonomous: true
requirements: [BLD-01, BLD-02, BLD-03]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low   # estimate-calibration: 0 samples → factor 1, confidence low

must_haves:
  truths:
    - "D-01 (two verb-level controls, pinned copy): the Run Log panel body's first child is a controls row carrying native buttons labelled 'Build', 'Rebuild all' and 'Rollback' plus an aria-live message slot, rendered unconditionally — including over the cold 'No runs yet' state (UI-SPEC A5) — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-04 (the POST rides the same token, header-only): clicks POST to /api/build with the scope body or /api/rollback with an empty body, carrying the launch token in the X-SPM-Token header — never a query parameter, never a body field — and 401/403 renders the shared token-invalid page (app.js's existing handling, inherited) — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-05 (the busy answer is branchable and inline): the POST helper returns the HTTP status alongside the envelope so a 409 is distinguishable from a 500, and a 409 renders the pinned busy sentence in the message slot with the buttons re-enabled — never a native dialog, never a silent queue, never the server's reason string (UI-SPEC A9, Pitfall 8) — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-05 (double-submit guard): a click disables all three buttons immediately; a 2xx enters the in-flight state with the verb's pinned message ('Building…' / 'Rebuilding all…' / 'Restoring source mode…'), while a 409 or an error re-enables them with its message — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-08 (two-step inline confirm): the Rollback click swaps the row for a confirm bar carrying the pinned sentence with Confirm and Cancel; focus moves to Cancel (A6), Cancel restores the row and returns focus to Rollback, Confirm disables both bar buttons and POSTs — no alert, confirm or prompt anywhere in the asset set — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-06/BLD-02 (waiting is lock-derived, never guessed): the row shows 'Waiting for build lock…' — byte-identical to the Installer's frozen line — from the spawn answer's lock snapshot or the displayed run's own announce line, and clears to the verb's baseline message on the run's next stream line (UI-SPEC A2) — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-09/A10 (one stream, one sanctioned coupling): log.js publishes displayed-run milestones as a single DOM CustomEvent with a waiting/active/ended phase from exactly the existing body-line and run-end code points, and app.js listens — no second EventSource, no new polling channel, no new timer, no shared mutable state — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-05/D-09 (in-flight exit): when the displayed run ends, the row returns to idle — buttons re-enabled, message cleared — so a failed build is one click from a retry, and BLD-03's failure surface stays the 14 banner chain with the row adding nothing (UI-SPEC A7) — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC A3 (a terminal-held lock never disables the controls): the buttons' disabled state derives ONLY from this tab's own pending POST or in-flight run — never from lock data — so a click during a terminal build forms a visible queue instead of finding a dead control — bundle exec rspec spec/web_frontend_spec.rb"
    - "14-UI-REVIEW polish (one pass over the surface this phase touches): accent- and destructive-fill labels render in the dark foreground that measures AA (W1, retroactive to 14's pills); the switch notice and identity-card row gaps match the pinned spacing tokens (W5, M1); the pause/filter pills and anchor chips are persistent nodes that are patched rather than recreated, so keyboard focus survives a live run (W3); a single narrow-viewport breakpoint stacks the rail below the viewport and lets the overlay and panel actions wrap (W4) — bundle exec rspec spec/web_frontend_spec.rb"
  artifacts:
    - "lib/spm_cache/web/assets/index.html (MOD: the controls row and the confirm bar as the first children of the Run Log panel body)"
    - "lib/spm_cache/web/assets/styles.css (MOD: controls/confirm/danger/quiet rules on existing tokens + the W1/W5/M1 folds + the W4 breakpoint)"
    - "lib/spm_cache/web/assets/app.js (MOD: the POST helper returning status, the controls state machine, the run-progress listener)"
    - "lib/spm_cache/web/assets/log.js (MOD: the three-point run-progress CustomEvent emission + the W3 persistent-node repair)"
    - "spec/web_frontend_spec.rb (MOD: controls/confirm/progress/polish describes — this plan's automated gates)"
  key_links:
    - "The controls row is the only new DOM in the phase and it lives in the EXISTING index.html — no new asset file is referenced anywhere, so the G-13-1 broken-asset class cannot recur (UI-SPEC prohibition 1)"
    - "app.js and log.js still do not import each other: the coupling is one DOM CustomEvent plus the sessionStorage token, exactly as A10 sanctions"
    - "The row's waiting copy and the in-stream line are the SAME frozen string the Installer emits (15-02) — one string, now three surfaces"
    - "The trigger badge needs zero code here: 14's D-11 machinery renders the header value verbatim, so a UI run's 'ui' badge lights up by construction (D-03)"
    - "Failure stays the banner's job (14 D-03/D-10) — the row owns action state, the banner owns run outcome (UI-SPEC A7), so BLD-03 needs no new strings"
  prohibitions:
    - statement: "MUST NOT add a new asset file, a framework, a build step, a CDN or a scheme-absolute URL — every change lands in the four existing assets (UI-SPEC prohibition 1)"
      status: resolved
      verification: test
    - statement: "MUST NOT assign markup strings to the DOM — every new dynamic string, including envelope messages, renders through the element helper and text content only (UI-SPEC prohibition 2, T-13-13)"
      status: resolved
      verification: test
    - statement: "MUST NOT use a native modal dialog for the busy message or the destructive confirm — both are inline (UI-SPEC prohibition 3, D-05, D-08)"
      status: resolved
      verification: test
    - statement: "MUST NOT add a second event source, a new polling channel, or any new timer — the state table's existing poll stays the page's only timer and carries no controls state (UI-SPEC prohibition 4)"
      status: resolved
      verification: test
    - statement: "MUST NOT read a client clock — the controls render no timestamps (UI-SPEC prohibition 5, T-13-16)"
      status: resolved
      verification: test
    - statement: "MUST NOT display the server's 409 reason or any envelope internals — only the POST-failure templates interpolate the envelope message (UI-SPEC prohibition 8, A9)"
      status: resolved
      verification: test
    - statement: "MUST NOT ship a stop/cancel control or a per-package scope picker — deferred to WEB2-02 and Phase 16 (UI-SPEC prohibition 6)"
      status: resolved
      verification: test
    - statement: "MUST NOT disable a control without a message explaining the hold — no silent states (UI-SPEC prohibition 9)"
      status: resolved
      verification: test
    - statement: "MUST NOT change 14's pinned copy or the empty-state strings while folding the polish items — the folds are CSS tokens and node-lifecycle repairs only (UI-SPEC A5)"
      status: resolved
      verification: test
---

# Plan 15-05 — The controls surface: build/rebuild/rollback row, inline confirm, lock-derived waiting, and the 14-UI-REVIEW polish fold

<planning_context>
phase: 15 — UI Build Controls
mode: standard (expansion — the browser half of the proven slice)
sources: 15-UI-SPEC.md (approved; the whole document is binding — layout, copy table, interaction contract, accessibility contract, state coverage, prohibitions 1-9, A1-A10 incl. the AA-amended color rows); 15-CONTEXT.md D-01, D-04, D-05, D-06, D-08, D-09; 15-RESEARCH.md §Q5 (the request-layer gap, the three CustomEvent code points, the polish-fold analysis) and Pitfall 8; 14-UI-REVIEW.md (W1, W3, W4, W5, M1); ROADMAP Phase 15 SC1-SC3; REQUIREMENTS BLD-01, BLD-02, BLD-03
planner decision on Open Question 2 (W1 vs the spec's letter): resolved before planning — 15-UI-SPEC's Color table was amended 2026-09-02 to the dark foreground on accent and destructive fills, with the amendment applying retroactively to 14's accent-fill pill labels in this phase's polish task. The spec is approved in that amended state, so there is no tension left to resolve at implementation time: implement the amended table.
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase. Plan 15-06's recorded browser probe drives every interaction pinned here; because this repo runs no JavaScript in CI, the specs below are byte- and source-level pins and the probe is the behavioral net — the same division that caught G-13-1.
</downstream_consumer>

<objective>
Put the three controls in the dashboard and wire their whole state machine: a click disables and
POSTs, a 2xx enters the verb's in-flight message, a 409 renders the pinned busy sentence inline, the
rollback path goes through a two-step inline confirm bar with safe-default focus, the waiting flavor
comes from lock truth rather than a guess, and the row returns to idle when the run ends. Then fold
the 14-UI-REVIEW findings that live on exactly this surface.

Purpose: SC1's browser half (trigger a build, see it stream, get a clear busy message), SC2's
visible queue (BLD-02), SC3's failure visibility riding the existing banner chain (BLD-03), and the
one polish pass this surface will get before the milestone closes.

Output: the controls row and confirm bar in the existing index.html, their rules on the existing
token sheet, the POST helper and state machine in app.js, three CustomEvent emissions plus the
persistent-node repair in log.js, and the frontend spec's new describes.
</objective>

## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| Controls row + confirm bar | asset (mod) | `index.html` — first children of the Run Log panel body; the confirm bar is a hidden swap (the card/banner pattern), grouped and labelled |
| POST request helper | asset (mod) | `app.js` — returns ok/status/envelope so 409 is branchable (Pitfall 8), keeps the shared 401/403 token-invalid behavior |
| Controls state machine | asset (mod) | `app.js` — idle / pending / in-flight / waiting / rejected, all message-bearing (prohibition 9) |
| `spm-run-progress` emission | asset (mod) | `log.js` — waiting and active from the body-line path, ended from the run-end path, displayed-run-scoped |
| Controls CSS + polish folds | asset (mod) | `styles.css` — controls/confirm/danger/quiet on existing tokens; W1 foreground, W5 notice gap, M1 card gap, W4 breakpoint |
| Persistent-node repair | asset (mod) | `log.js` — pills and chips patched instead of recreated (W3) |
| Frontend spec describes | spec (mod) | `spec/web_frontend_spec.rb` — the automated gates for all of the above |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ui-build-controls/15-CONTEXT.md
@.planning/phases/15-ui-build-controls/15-UI-SPEC.md
@.planning/phases/15-ui-build-controls/15-RESEARCH.md
@.planning/phases/15-ui-build-controls/15-VALIDATION.md
@.planning/phases/15-ui-build-controls/15-04-SUMMARY.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-REVIEW.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-SPEC.md
@lib/spm_cache/web/assets/index.html
@lib/spm_cache/web/assets/app.js
@lib/spm_cache/web/assets/log.js
@lib/spm_cache/web/assets/styles.css
@spec/web_frontend_spec.rb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The controls row — POST helper, Build and Rebuild all, and the whole message-bearing state machine (RED → GREEN)</name>
  <precondition>Plan 15-04 committed: both mutation routes answer with the pinned statuses, reasons and 2xx lock snapshot.</precondition>
  <reversibility rating="reversible">Additive DOM and behavior on an approved, pinned contract.</reversibility>
  <read_first>
  - .planning/phases/15-ui-build-controls/15-UI-SPEC.md — the Layout contract (the row's position, flex/wrap/margin rules, the right-aligned message slot), the whole Copywriting Contract table, the Interaction contract bullets for the build and rebuild clicks, the Accessibility contract, and the State coverage rows for the controls row
  - .planning/phases/15-ui-build-controls/15-04-SUMMARY.md — the shipped endpoint paths, body shapes, status codes, reason values and 2xx data shape as they actually landed
  - .planning/phases/15-ui-build-controls/15-RESEARCH.md — §Q5 request-layer paragraph (why the existing helper cannot express a 409) and Pitfall 8
  - lib/spm_cache/web/assets/app.js lines 20-30 (the element helper — the only sanctioned DOM construction path), lines 60-90 (the token-invalid render and the existing GET helper this one parallels), lines 300-320 (boot and the single existing timer)
  - lib/spm_cache/web/assets/index.html lines 19-45 (the Run Log panel header and body, the card/banner/switch hidden-swap precedent)
  - lib/spm_cache/web/assets/styles.css lines 120-150 (the shared button rule, its focus ring) and the banner/notice rules for the row's spacing rhythm
  - spec/web_frontend_spec.rb lines 1-60 (the byte-pin idiom: served HTML, asset file reads, why source-level assertions stand in for a JS runtime)
  </read_first>
  <files>lib/spm_cache/web/assets/index.html, lib/spm_cache/web/assets/styles.css, lib/spm_cache/web/assets/app.js, spec/web_frontend_spec.rb</files>
  <behavior>
  - RED first: 12 new failing examples in spec/web_frontend_spec.rb:
    1. Structure: the served index carries the controls row as the first child of the Run Log panel body, ahead of the identity card, with the three native buttons in the pinned DOM order and the message slot.
    2. Copy: the three button labels are byte-exact against the spec's copy table (including the capitalization of the forced-rebuild label).
    3. Accessibility: every control is a native button element with an explicit non-submit type; the message slot is polite-live and starts hidden so the silent initial state never announces.
    4. POST helper: a dedicated request path sends the launch token in the custom header, sets a JSON content type, and returns the HTTP status alongside the parsed envelope rather than throwing — pinned as a source contract, with the token absent from the request URL.
    5. Auth inheritance: the POST path reuses the existing token-invalid page replacement for 401 and 403 rather than rendering a row message.
    6. Bodies: the incremental click sends the build scope and the forced click sends the rebuild scope, to the path 15-04 shipped.
    7. Pending state: a click disables all three controls before the request resolves (the double-submit guard).
    8. In-flight state: a 2xx sets the verb's pinned message and leaves the controls disabled.
    9. Busy state: a 409 renders the pinned busy sentence and re-enables the controls; the server's reason value appears nowhere in the asset sources.
    10. Failure state: a non-409 failure renders the POST-failure template with the envelope message interpolated, and re-enables the controls.
    11. Prohibitions: the new code assigns no markup strings and opens no native dialog; the index references no asset that does not exist in the asset directory (the G-13-1 class gate, extended to the new markup).
    12. No new timer and no client clock in the controls code; the state table's existing poll is still the only scheduled work in app.js.
  - Regression: every existing example in spec/web_frontend_spec.rb stays green — the row is additive and 14's pinned copy is untouched.
  </behavior>
  <action>
  Extend spec/web_frontend_spec.rb with a controls describe following the file's existing pin idiom (served HTML for structure and order, asset-file source for behavior contracts and prohibitions, existing-asset resolution for every reference). Write the 12 RED examples above.

  Then implement. In index.html, add the controls row as the panel body's first child: a container holding three native buttons — the incremental build, the forced rebuild in the accent style, and the destructive rollback in the danger style — plus a right-aligned, polite-live, initially hidden message slot; and, directly after it, the hidden confirm-bar container that Task 2 fills (declare it here so the swap pattern and spacing land in one CSS pass). Use stable ids in the existing naming style and mark the buttons as non-submit.

  In styles.css, add a build-controls section on existing tokens only: the row as a wrapping flex line with the pinned gap and bottom margin, the message slot pushed to the end at the Label size in the muted color with an error variant in the fail color, a danger button rule mirroring the shared button rule with the fail fill, a quiet button rule on the panel fill, and focus-visible rings for both. Introduce no new color values, no fixed widths and no animation.

  In app.js, add the POST helper beside the existing GET helper: same token header, a JSON content type and body, the same network-error wrapping, the same token-invalid handling for 401 and 403, but returning the status and the parsed envelope so callers can branch on the busy answer instead of catching an opaque error. Then add the controls section: a small state machine over idle, pending, in-flight, and rejected, where every transition sets both the disabled state of the three buttons and the message slot's text through the element helper, and a single place owns the pinned copy (the phase's copy table, declared as constants beside the existing panel copy). Wire the two build clicks. Leave the rollback click to Task 2 but keep the state machine verb-agnostic so it needs no reshaping.

  Commit RED first: `test(15-05): failing controls-row and POST-helper specs (12 examples)`, then GREEN: `feat(15-05): build controls row, POST helper and row state machine (D-01/D-04/D-05)`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb spec/web_assets_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or a button label differs from the pinned copy by a single byte; or the busy path is indistinguishable from a failure path at the call site; or the served index references an asset that does not exist; or a new scheduled timer appears in app.js</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_frontend_spec.rb green with the 12 new examples and every pre-existing example untouched
  - The three buttons, the message slot and the (still empty) confirm container are served in the pinned order
  - Every new string in the assets matches the spec copy table byte-for-byte
  </acceptance_criteria>
  <done>A user can click Build or Rebuild all, watch the controls lock, and read exactly what happened — including a clear inline sentence when something else is already running. Committed RED then GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rollback confirm bar, the run-progress coupling, and the lock-derived waiting flavor (RED → GREEN)</name>
  <precondition>Task 1 GREEN and committed.</precondition>
  <reversibility rating="reversible">Interaction layer behind the approved contract; the destructive action's real safety comes from the CLI-side lock (15-02) and the confirm step pinned here.</reversibility>
  <read_first>
  - .planning/phases/15-ui-build-controls/15-UI-SPEC.md — the Interaction contract's rollback bullet, the in-flight-exit bullet, the waiting-flavor bullet, the terminal-held-lock bullet, the cross-tab bullet; the Accessibility contract's confirm-swap paragraph; A2, A6, A7, A10; the confirm-bar copy rows and the confirm-bar geometry in the Layout contract
  - .planning/phases/15-ui-build-controls/15-CONTEXT.md — D-06, D-08, D-09
  - .planning/phases/15-ui-build-controls/15-RESEARCH.md — §Q5's three named emission points and the displayed-run-scoped constraint
  - lib/spm_cache/web/assets/log.js lines 28-54 (the pinned copy table — the frozen lock-wait line lives in the run's own bytes, not here), 525-560 (the body-line append path and the run-end path: the exact three code points), 560-575 (the entry router that keys on the event field)
  - lib/spm_cache/web/assets/app.js (Task 1 output — the state machine the listener drives)
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-02-SUMMARY.md — the frozen wait line as it landed at the Installer sites
  </read_first>
  <files>lib/spm_cache/web/assets/index.html, lib/spm_cache/web/assets/styles.css, lib/spm_cache/web/assets/app.js, lib/spm_cache/web/assets/log.js, spec/web_frontend_spec.rb</files>
  <behavior>
  - RED first: 12 new failing examples in spec/web_frontend_spec.rb:
    1. Confirm bar structure: the bar is a grouped, labelled region carrying the pinned sentence, a danger Confirm and a quiet Cancel; it and the controls row are a hidden swap — exactly one is visible at a time.
    2. Confirm copy: the bar sentence, the Confirm label and the Cancel label are byte-exact, em dash included.
    3. Arming: the rollback click hides the row, shows the bar, and moves focus to Cancel (the safe default, A6).
    4. Cancelling: Cancel restores the row and returns focus to the rollback button.
    5. Confirming: Confirm disables both bar buttons, POSTs to the rollback path with an empty body, and on 2xx collapses the bar into the in-flight row with the pinned restoring message; on 409 or error the bar collapses and the message slot explains.
    6. No dialogs: no native dialog function appears anywhere in the asset set (the whole prohibition, not just this file).
    7. Emission points: the run-progress event is dispatched from exactly the existing body-line and run-end code points — no new traversal, no polling, no timer, and no import between the two modules.
    8. Waiting phase: a body line byte-equal to the frozen lock-wait string carries the waiting phase; any following body line carries the active phase (the Installer prints nothing while blocked, so a next line is proof the wait ended — A2).
    9. Ended phase: the run-end path carries the ended phase, from the same facts that flip the identity card.
    10. Listener mapping: app.js maps waiting to the frozen wait string in the row, active to the verb's baseline message, and ended to idle — buttons re-enabled and the message cleared.
    11. Entry assist: an accepted spawn whose answer reports the lock held shows the waiting message immediately, without waiting for the next poll (the 15-04 snapshot), while an accepted spawn with a free lock shows the verb's baseline message.
    12. A3: nothing in the controls code disables a button because lock data says held — the disabled state derives only from this tab's pending POST or in-flight run.
  - Regression: 14's stream behavior is unchanged — the emission adds no rendering, no class, and no special-casing of the wait line (its verbatim in-stream rendering rule stands).
  </behavior>
  <action>
  Extend spec/web_frontend_spec.rb with the confirm-bar and run-progress describes and write the 12 RED examples.

  Then fill the confirm bar in index.html (the container Task 1 declared): the pinned sentence, the danger Confirm, the quiet Cancel, grouped and labelled for assistive technology, hidden by default. Add its rules in styles.css using the warn ten-percent-alpha fill of the existing banner pattern, the pinned padding, radius, gap and bottom margin, with wrapping allowed — no new tokens.

  In log.js, dispatch one DOM CustomEvent from the three existing code points, carrying only a phase in its detail: the body-line path emits the waiting phase when the line is byte-equal to the frozen wait string and the active phase for any other body line, and the run-end path emits the ended phase. Keep the emission displayed-run-scoped and idempotent for the listener, add no new state, and change nothing about how any line renders. Comment the coupling as the A10-sanctioned one.

  In app.js, add the rollback click path (arm the bar, manage focus both ways, POST on confirm) and the run-progress listener that maps the three phases onto the Task-1 state machine, plus the entry assist that reads the spawn answer's lock snapshot to choose between the waiting message and the verb's baseline message. The frozen wait string is declared once, byte-identical to the Installer's line. Do not disable anything on the basis of lock data.

  Commit RED first: `test(15-05): failing confirm-bar and run-progress specs (12 examples)`, then GREEN: `feat(15-05): rollback confirm bar + lock-derived waiting via the run-progress event (D-06/D-08/D-09)`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the confirm swap can render both containers at once; or focus lands on Confirm instead of Cancel; or the emission introduces a traversal, timer or module import; or the wait string in app.js differs from the Installer's frozen line by a byte; or any button's disabled state is derived from lock data</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_frontend_spec.rb green with all 24 new examples across Tasks 1 and 2
  - Exactly three emission sites exist, all at pre-existing code points
  - The destructive action is reachable only through the two-step inline confirm
  </acceptance_criteria>
  <done>Rollback is safe to click, the row tells the truth about waiting on the lock, and the run's own end returns everything to idle. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The 14-UI-REVIEW polish fold — W1 contrast, W5 and M1 spacing, W3 focus preservation, W4 breakpoint (RED → GREEN)</name>
  <precondition>Tasks 1-2 GREEN and committed.</precondition>
  <reversibility rating="reversible">Token-level CSS corrections and a node-lifecycle repair; no contract or copy changes.</reversibility>
  <read_first>
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-REVIEW.md — the W1 measurement table and its concrete fix, W3's two named render functions and their failure mode, W4's width arithmetic and the three elements it names, W5 and M1's exact token divergences
  - .planning/phases/15-ui-build-controls/15-UI-SPEC.md — the amended Color table rows (the dark foreground on accent and destructive fills, and the note that the amendment applies retroactively to 14's accent-fill pill labels) and the Spacing scale
  - .planning/phases/15-ui-build-controls/15-RESEARCH.md — §Q5's polish-fold paragraph (which folds are same-surface and which are scope calls)
  - lib/spm_cache/web/assets/styles.css lines 120-150 (the shared button rule), the pill-button rule, the switch-notice rule, the identity-card rule, the rail rule, the overlay row rule, and the panel-actions rule
  - lib/spm_cache/web/assets/log.js lines 195-215 (the pill render that replaces its children) and lines 480-500 (the chip render that removes and recreates every chip)
  </read_first>
  <files>lib/spm_cache/web/assets/styles.css, lib/spm_cache/web/assets/log.js, spec/web_frontend_spec.rb</files>
  <behavior>
  - RED first: 8 new failing examples in spec/web_frontend_spec.rb:
    1. W1 — the shared button rule and the pill-button rule both render their label in the dark foreground on the accent fill (the amended spec value), so the follow pill, filter pill, jump button and run-id control all clear AA; the new danger buttons use the same dark foreground on the fail fill.
    2. W1 regression — no rule anywhere in the sheet still pairs a pure-white label with the accent or fail fill.
    3. W5 — the switch notice carries the pinned bottom-margin token, so it no longer sits flush against the stream row.
    4. M1 — the identity card's internal row gap is the pinned token, matching 14's own spacing contract.
    5. W3 pills — the pause and filter controls are persistent nodes whose visibility and label are patched; the pill container is not wholesale replaced on every queued line.
    6. W3 chips — chips are reconciled against the anchor set rather than removed and recreated: an unchanged chip's node survives a re-render, and the active state is patched in place.
    7. W3 focus proof — the reconciliation is expressed so that the focused node is not detached when unrelated anchors arrive (the source contract the browser probe then confirms).
    8. W4 — a single narrow-viewport media query stacks the anchor rail below the viewport at full width, allows the overlay pill row to wrap, and allows the panel actions to wrap; the sheet has exactly one breakpoint and no fixed rail width applies below it.
  - Regression: every pinned Phase-13 and Phase-14 spacing, typography and copy assertion in the frontend spec stays green — this task changes tokens and node lifecycles, never contracts.
  </behavior>
  <action>
  Extend spec/web_frontend_spec.rb with a polish describe and write the 8 RED examples as sheet- and source-level pins (the file's established idiom for CSS contracts).

  Then apply the folds. In styles.css: change the label color on the shared button rule and the pill-button rule to the amended dark foreground and use the same foreground on the new danger rule; add the pinned bottom margin to the switch notice; correct the identity card's internal gap to the pinned token; and add ONE narrow-viewport media query that stacks the rail below the stream column at full width, lets the overlay pill row wrap, and lets the panel actions wrap. Add no new tokens and no animation.

  In log.js: repair the two render functions to reconcile rather than rebuild. The pill container keeps persistent button nodes whose hidden state and label text are patched; the chip rail reconciles against the anchor list, appending only genuinely new chips, patching labels and pressed state in place, and leaving untouched nodes attached — so a keyboard user's focus survives a live run's anchor arrivals. Behavior, copy and ordering stay exactly as 14 pinned them.

  Commit RED first: `test(15-05): failing UI-review polish specs (8 examples)`, then GREEN: `fix(15-05): fold 14-UI-REVIEW W1/W3/W4/W5/M1 into the controls surface`. Then run the phase gate for this plan: full suite `bundle exec rspec` green.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb spec/web_assets_spec.rb spec/web_integration_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or any accent- or fail-filled control still carries a pure-white label; or a chip or pill re-render still detaches unchanged nodes; or more than one breakpoint exists in the sheet; or a pre-existing spacing/typography/copy pin had to be edited to pass</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_frontend_spec.rb green with all 32 new examples across the plan's three tasks
  - The five folded findings are each pinned by at least one example
  - Full suite `bundle exec rspec` green — the Wave 3 merge gate
  </acceptance_criteria>
  <done>The one surface this phase touches leaves it measurably better than 14 left it: AA-clean labels, contract-true spacing, focus that survives a live build, and a usable narrow viewport. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| envelope messages → the row's message slot | server-authored strings (including a spawn failure's exception text) reaching the DOM |
| run-log body lines → the run-progress event → controls state | project-derived, attacker-adjacent text deciding an interface state transition |
| a click → a destructive project mutation | the dashboard's first destructive control; the confirm bar is the human gate in front of it |
| the launch token → POST requests | the token must travel in the custom header only, never in a URL, body, or log |

## STRIDE Threat Register (ASVS Level 1; block on high)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-15-22 | Tampering (stored cross-site scripting through envelope messages or run text reaching the new DOM surfaces) | message slot, confirm bar | high | mitigate | every new node is built through the element helper with text content only; no markup string is ever assigned; the prohibition example covers the whole asset set, not just the new code (T-13-13) |
| T-15-23 | Tampering (an accidental destructive click — the dashboard's first irreversible action) | rollback control | high | mitigate | two-step inline confirm with focus on the safe option, both buttons disabled on submit, and the pinned sentence naming exactly what will be removed (D-08, A6) |
| T-15-24 | Information disclosure (the launch token leaking into a URL, a body, a Referer or a log through the new POST path) | POST helper | medium | mitigate | custom header only, asserted as a source contract; the existing bootstrap already scrubs the token from the URL before first render (D-04) |
| T-15-25 | Information disclosure (server internals rendered to the user through the 409 reason or envelope innards) | busy/error copy | low | mitigate | client-pinned copy for the busy answer; only the failure template interpolates the envelope message (A9, prohibition 8) |
| T-15-26 | Denial of service (a wedged interface: buttons disabled forever after a missed end-of-run milestone) | state machine | medium | mitigate | the ended milestone, the 409 backstop and a reload are three independent recoveries, and a disabled control always carries a message explaining the hold (prohibition 9; the pinned-older-run caveat is documented in the spec's interaction contract) |
| T-15-27 | Tampering (a forged run line spoofing the waiting state) | run-progress emission | low | accept | the wait phase is derived from a byte-equal match on the run's own frozen line; a run that can write its own log can already say anything in the stream, and the state it can reach is a muted message — no capability is gated on it |

No package installs in this plan.
</threat_model>

<verification>
- `bundle exec rspec spec/web_frontend_spec.rb` — green (the plan's automated gates: structure, copy, prohibitions, state machine, emission points, polish folds).
- `bundle exec rspec spec/web_assets_spec.rb spec/web_integration_spec.rb` — green (asset resolution and the served dashboard stay intact; the G-13-1 class cannot recur).
- `bundle exec rspec` — full suite green (Wave 3 merge gate, run at the end of Task 3).
- Behavioral truth in a real browser is 15-06's recorded probe — this repo runs no JavaScript in CI, and these specs are pins, not proof of interaction.
</verification>

<success_criteria>
- SC1's browser half: Build and Rebuild all trigger real runs, the controls lock, and a second attempt gets a clear inline busy message.
- SC2: the waiting state is visible and lock-derived, and a terminal-held lock never produces a dead control.
- SC3: failures keep surfacing through the 14 banner chain, with the row returning to idle so a retry is one click.
- BLD-04's control exists and is safe: an inline two-step confirm in front of the only destructive action in the dashboard.
- The one polish pass this surface gets lands with it: AA labels, contract spacing, focus preservation, and a narrow-viewport breakpoint.
</success_criteria>

<output>
Create `.planning/phases/15-ui-build-controls/15-05-SUMMARY.md` when done
</output>
