---
phase: 15-ui-build-controls
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/spm_cache/installer/rollback.rb
  - lib/spm_cache/main.rb
  - spec/installer_rollback_lock_spec.rb
  - spec/run_log_trigger_spec.rb
autonomous: true
requirements: [BLD-04, BLD-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low   # estimate-calibration: 0 samples → factor 1, confidence low

must_haves:
  truths:
    - "BLD-04/CP4 (the race closes in the CLI, for every caller): Installer::Rollback#perform_install acquires the build flock BEFORE it touches the project and holds it across both steps — while another process holds the lock, the sandbox directory is still present; it is removed only after the holder releases — bundle exec rspec spec/installer_rollback_lock_spec.rb"
    - "BLD-04 (release is unconditional): the lock is released in an ensure — a rollback that raises mid-removal leaves the flock free, so the next build/use is never wedged behind a corpse (Pitfall 6) — bundle exec rspec spec/installer_rollback_lock_spec.rb"
    - "BLD-02/D-05 (one string, three surfaces): under contention rollback announces 'Waiting for build lock…' byte-identically to the two existing flock sites, exactly once, via Core::UI.info so the Phase-12 tee lands it in the blocked run's own JSONL — a UI-spawned rollback therefore shows its wait in the browser with zero new machinery — bundle exec rspec spec/installer_rollback_lock_spec.rb"
    - "Byte-identity: the uncontended rollback path is byte-identical to today — same output, same order, no notice, no new lines (terminal users see no change) — bundle exec rspec spec/installer_rollback_lock_spec.rb spec/installer_spec.rb"
    - "D-07 (the fix lives in the installer, not the web layer): the lock wraps Installer::Rollback#perform_install, so terminal rollback, a UI-spawned rollback, and any other caller of the installer all inherit it — the server never re-implements rollback — bundle exec rspec spec/installer_rollback_lock_spec.rb"
    - "D-03/LOGS-05 (UI-run identity): with SPM_CACHE_TRIGGER='ui' in the environment, a run's run_start header records trigger 'ui'; unset, empty, or ANY other value records 'terminal' — a whitelist normalization, never passthrough — bundle exec rspec spec/run_log_trigger_spec.rb"
    - "Pitfall 7 (attribution only): the marker feeds the header value and nothing else — no verb, option, or code path branches on it, so the forgeable variable can never become a privilege seam — bundle exec rspec spec/run_log_trigger_spec.rb"
    - "D-03 (zero frontend work): 'ui' is the LOGS-05 vocabulary value 14's D-11 badge machinery already renders verbatim in the identity card and the recent-runs dropdown — the header contract is the whole change — bundle exec rspec spec/run_log_trigger_spec.rb"
  artifacts:
    - "lib/spm_cache/installer/rollback.rb (MOD: probe → announce → block flock around perform_install, ensure-release)"
    - "lib/spm_cache/main.rb (MOD: the trigger kwarg at the single whole-run RunLog.open site becomes an env whitelist normalization)"
    - "spec/installer_rollback_lock_spec.rb (NEW: order-before-removal, release-on-raise, byte-exact announce under a thread-held flock, free-path byte-identity)"
    - "spec/run_log_trigger_spec.rb (NEW: env → header trigger normalization matrix + the no-behavior-change pin)"
  key_links:
    - "The announce rides Core::UI.info → $stdout → the Phase-12 TeeIO → the blocked run's JSONL (installer_lock_notice_spec proved the mechanism for build/use) — rollback joins the same two-surface contract for free"
    - "main.rb:22-27 is the ONLY whole-run RunLog.open site; RunLog::CycleWrapper's 'watch' trigger and RunLog.pre_scan are untouched, so the three-value LOGS-05 vocabulary stays closed"
    - "Web::Jobs (15-01) plants SPM_CACHE_TRIGGER in the spawned child's env; THIS plan is the half that turns it into a header value — the two meet in 15-06's browser probe where the badge reads 'ui'"
    - "A UI-spawned rollback that contends now streams its own wait line, which is exactly what 15-05's row message mirrors (D-06 waiting flavor)"
  prohibitions:
    - statement: "MUST NOT put the rollback lock in Command::Rollback or in the web layer — non-CLI callers of the installer would miss the fix and D-07 forbids a server-side rollback path"
      status: resolved
      verification: test
    - statement: "MUST NOT change the uncontended rollback's output or ordering — byte-identical to today"
      status: resolved
      verification: test
    - statement: "MUST NOT introduce polling, backoff, retry, or a timeout around the flock — the OS blocking semantics ARE the mechanism (the build/use design comments stay true)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT reword, re-case, or re-punctuate the lock-wait line — it is frozen across terminal and browser and feeds the row copy in 15-05"
      status: resolved
      verification: test
    - statement: "MUST NOT branch any CLI behavior on the trigger environment variable, and MUST NOT widen the LOGS-05 vocabulary — anything not the UI marker normalizes to the terminal value (Pitfall 7)"
      status: resolved
      verification: test
    - statement: "MUST NOT touch RunLog.pre_scan or the watch CycleWrapper trigger — the seam is the single kwarg at the whole-run open site"
      status: resolved
      verification: test
---

# Plan 15-02 — CLI-side truth: rollback acquires the build lock (BLD-04/CP4) + UI-run attribution (D-03)

<planning_context>
phase: 15 — UI Build Controls
mode: standard (expansion — CLI-side half of the tracer's slice)
sources: 15-CONTEXT.md D-07 (costly: changes rollback's concurrency contract for ALL callers) and D-03; 15-RESEARCH.md §Q2 (the critical section, the lock site, the spec conventions) and §Q3 (the one-line trigger seam, the flag-alternative rejection, Pitfall 7), Pitfall 6; .planning/research/SUMMARY.md CP4; ROADMAP Phase 15 SC4; REQUIREMENTS BLD-04
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase. Plan 15-04's /api/rollback route spawns the verb this plan makes lock-safe, and 15-06's probe row 5 watches a UI-spawned rollback's wait line in the browser. The trigger normalization here is what makes 15-01's env marker visible as the 'ui' badge.
</downstream_consumer>

<objective>
Close the milestone's last known concurrency race in the place every caller inherits it: rollback
acquires the build flock before it removes anything and releases it in an ensure, announcing
contention with the same frozen line the other two flock sites use. Then give UI-spawned runs their
identity: the run_start header records trigger 'ui' when the spawner's marker is present, and
'terminal' for everything else.

Purpose: SC4's rollback half (BLD-04/CP4 — 'rollback now acquires the build lock, the current
lock-free rollback race is closed') and D-03's UI-run identity, both landed in the CLI so the web
layer inherits them by spawning rather than re-implementing (D-07).

Output: two small library edits (one flock wrapper, one kwarg normalization) and two new hermetic
spec files.
</objective>

## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| `Installer::Rollback#perform_install` under flock | lib (mod) | probe → announce → block around `restore_packages` + `remove_proxy`; `ensure`-release mirroring `installer/build.rb:97-102` |
| `spec/installer_rollback_lock_spec.rb` | spec (new) | fixed Wave-0 file: ordering, release-on-raise, byte-exact announce, free-path identity |
| Trigger normalization at the whole-run open site | lib (mod) | `main.rb:22-27` — the single hard-coded `trigger:` kwarg becomes a closed whitelist over the env marker |
| `spec/run_log_trigger_spec.rb` | spec (new) | fixed Wave-0 file: env matrix → header value, plus the no-behavior-change pin |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ui-build-controls/15-CONTEXT.md
@.planning/phases/15-ui-build-controls/15-RESEARCH.md
@.planning/phases/15-ui-build-controls/15-VALIDATION.md
@lib/spm_cache/installer/rollback.rb
@lib/spm_cache/installer/build.rb
@lib/spm_cache/installer/use.rb
@lib/spm_cache/command/rollback.rb
@lib/spm_cache/main.rb
@lib/spm_cache/core/run_log.rb
@spec/installer_lock_notice_spec.rb
@spec/main_run_log_spec.rb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rollback acquires and releases the build lock, announcing contention (BLD-04/CP4) (RED → GREEN)</name>
  <precondition>`bundle exec rspec spec/installer_spec.rb spec/installer_lock_notice_spec.rb spec/build_lock_spec.rb` is green on the current tree before starting.</precondition>
  <reversibility rating="costly">D-07's own rating: this changes rollback's concurrency contract for ALL callers, terminal included. A terminal rollback that used to proceed while a build ran now waits — that is the intended fix (CP4), and reverting it re-opens the race.</reversibility>
  <read_first>
  - .planning/phases/15-ui-build-controls/15-RESEARCH.md — §Q2 in full (what rollback touches today, where the lock goes and why not in the Command or the web layer, the spec-first shape, the dashboard consequence) and Pitfall 6
  - .planning/phases/15-ui-build-controls/15-CONTEXT.md — D-07 (costly rationale) and D-06 (the wait is lock-derived, surfaced by the run itself)
  - lib/spm_cache/installer/build.rb lines 70-102 (the probe → announce → block shape and the ensure-release pair this mirrors, including the design comments that must stay true)
  - lib/spm_cache/installer/use.rb lines 60-90 (the second existing copy of the same idiom — duplication is the established convention here, not an accident)
  - lib/spm_cache/installer/rollback.rb (all 25 lines — the whole critical section is the info print plus the sandbox removal)
  - lib/spm_cache/installer.rb lines 20-30 (the base constructor: project_dir is derived from the project path, which is what makes the lock path resolvable in specs)
  - spec/installer_lock_notice_spec.rb — the conventions to follow verbatim: manual $stdout swap with begin/ensure restore, the thread-held real-OS flock helper, the byte-exact pinned copy, bounded joins
  - spec/build_lock_spec.rb — the existing cross-process lock semantics this plan must not disturb
  </read_first>
  <files>lib/spm_cache/installer/rollback.rb, spec/installer_rollback_lock_spec.rb</files>
  <behavior>
  - RED first: spec/installer_rollback_lock_spec.rb with 6 failing examples (hermetic: tmpdir project, a sandbox directory fixture, a real OS flock held by a bounded thread — no shell-outs):
    1. Ordering — the lock is held BEFORE anything is touched: with a holder thread owning the flock, a rollback invoked on another thread has NOT removed the sandbox directory while it waits; after the holder releases, the call completes and the directory is gone. (The assertion is on observable project state, not on call ordering inside the method.)
    2. Mutual exclusion — while the rollback runs, a non-blocking acquire from the spec fails; after it returns, the same probe succeeds (the lock is genuinely held across the critical section and genuinely released).
    3. Release on raise — with the removal step stubbed to raise, the raise propagates unchanged AND a subsequent non-blocking acquire succeeds (Pitfall 6: no wedged lock after a failed rollback).
    4. Announce under contention — exactly one 'Waiting for build lock…' line on stdout, byte-identical to the frozen string (capital W, single ellipsis character, no trailing period), asserted against the same literal the build site uses.
    5. Free path byte-identity — with no holder, captured stdout contains the existing two info lines and NO notice, in the existing order.
    6. Stream discipline — the notice rides stdout (Core::UI.info), never stderr; the contended case leaves a stderr capture empty.
  - Regression: spec/installer_spec.rb and the two existing lock spec files stay green — build and use are not touched by this task.
  </behavior>
  <action>
  Create spec/installer_rollback_lock_spec.rb following installer_lock_notice_spec.rb's conventions exactly (thread-held flock on the real Core::Config#build_lock_path under a tmpdir project, bounded join, release in ensure; manual $stdout/$stderr swap with begin/ensure restore; the pinned literal declared once at the top of the file). Build the installer with a project path inside the tmpdir so the base constructor points Config at it, and create the sandbox directory fixture so the removal is observable.

  Then edit lib/spm_cache/installer/rollback.rb: wrap perform_install's two steps in the same three-step acquisition the build site uses — mkdir_p the lock file's directory, open the lock path with create+read-write, attempt a non-blocking exclusive acquire, and only when that returns falsey emit the frozen wait line through Core::UI.info and then take the blocking exclusive lock — with an ensure that unlocks and closes exactly as the build site's release helper does. Keep the acquisition and release as private methods on Installer::Rollback, deliberately parallel to the copies in build.rb and use.rb: extracting a shared helper across those three files is a refactor this phase did not ask for and would collide with Plan 15-03's edits to build.rb. Comment the method with the CP4/D-07 rationale (rollback previously ran lock-free; every caller of the installer now inherits the fix) and cite that the wording is frozen across terminal and browser.

  Commit RED first: `test(15-02): failing rollback build-lock specs (6 examples)`, then GREEN: `feat(15-02): rollback acquires the build lock before removing the sandbox (BLD-04/CP4)`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/installer_rollback_lock_spec.rb spec/installer_spec.rb spec/installer_lock_notice_spec.rb spec/build_lock_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the free-path example observes the notice (byte-identity broken); or the release-on-raise example's follow-up acquire blocks or fails (wedged lock); or the ordering example finds the sandbox already removed while the holder still owns the lock</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/installer_rollback_lock_spec.rb green with the 6 described examples
  - The wait line in rollback.rb is the same literal as in build.rb and use.rb (one string, now three sites)
  - Existing installer and lock specs green — build and use behavior unchanged
  </acceptance_criteria>
  <done>Rollback can no longer race a build: it waits, announces its wait where both the terminal and the stream can see it, and always releases. Committed RED then GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UI-run attribution — the trigger marker normalizes into the run_start header (D-03) (RED → GREEN)</name>
  <precondition>Task 1 GREEN and committed; `bundle exec rspec spec/main_run_log_spec.rb spec/run_log_spec.rb` green.</precondition>
  <reversibility rating="reversible">One kwarg expression at a single call site; the header field and its vocabulary already exist and are already rendered.</reversibility>
  <read_first>
  - .planning/phases/15-ui-build-controls/15-RESEARCH.md — §Q3 in full (the seam at the whole-run open site, why the env marker beats a CLAide flag, the scope-rides-argv rule, Pitfall 7) and the P6/P7 probe rows
  - .planning/phases/15-ui-build-controls/15-CONTEXT.md — D-03 (the CONTRACT is the header value; the badge is already built)
  - lib/spm_cache/main.rb lines 15-35 (the pre-scan, the single RunLog.open call with the hard-coded trigger kwarg, the tee installation order)
  - lib/spm_cache/core/run_log.rb lines 106-140 (the open signature and the header keys) and 525-545 (CycleWrapper's own 'watch' trigger — the other vocabulary value, untouched)
  - spec/main_run_log_spec.rb lines 47-135 (the default-deny Sh guard, the $stdout swap helper, the stubbed Command.run invocation shape, and the existing header assertion that pins 'terminal')
  </read_first>
  <files>lib/spm_cache/main.rb, spec/run_log_trigger_spec.rb</files>
  <behavior>
  - RED first: spec/run_log_trigger_spec.rb with 5 failing examples (hermetic: stubbed Command.run, tmpdir log dir, environment set and restored in an ensure, default-deny Sh guard):
    1. Marker present — with the trigger variable set to the UI value, the run's run_start header records trigger 'ui'; everything else about the header (command, argv, pid, version, cycle) is unchanged.
    2. Marker absent — with the variable unset, the header records 'terminal' (today's behavior, pinned as a regression).
    3. Marker with any other value — an arbitrary string, an empty string, and a case variant of the UI value all normalize to 'terminal': a closed whitelist, never passthrough (the variable is forgeable by any terminal user).
    4. No behavior change — with the marker set, the stubbed command still runs, terminal bytes are identical to the unset case, and the exit status is unchanged: the marker is attribution only (Pitfall 7).
    5. Vocabulary closure — the watch cycle path still records 'watch' with the marker set (the CycleWrapper trigger is not routed through this seam), so the LOGS-05 vocabulary stays exactly three values.
  - Regression: spec/main_run_log_spec.rb's existing header example (which pins 'terminal') stays green with no edit.
  </behavior>
  <action>
  Create spec/run_log_trigger_spec.rb using main_run_log_spec.rb's idioms (default-deny Core::Sh stubs, the manual stream swap, invoking Main.run with a log-dir flag pointing at a tmpdir and a stubbed Command.run). Add a small with-env helper that sets and restores the variable in an ensure so no example leaks environment state into the suite.

  Then edit lib/spm_cache/main.rb: replace the hard-coded trigger kwarg at the single whole-run open site with a closed normalization — the UI value when the environment variable equals it exactly, otherwise the terminal value. Keep it an expression at the call site (or a tiny private helper on Main if that reads better), and comment it with D-03, the LOGS-05 vocabulary, and Pitfall 7's rule that nothing may branch behavior on this variable. Do not touch RunLog.pre_scan, the CycleWrapper trigger, or the CLAide option surfaces — the flag alternative was rejected in research precisely to keep argv clean for the scope decision that rides it (A8, Plan 15-03).

  Commit RED first: `test(15-02): failing trigger-marker normalization specs (5 examples)`, then GREEN: `feat(15-02): UI-spawned runs record trigger 'ui' in the run header (D-03)`. Then run the wave gate: full suite `bundle exec rspec` green.
  </action>
  <verify>
    <automated>bundle exec rspec spec/run_log_trigger_spec.rb spec/main_run_log_spec.rb spec/run_log_spec.rb spec/watch_loop_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or any example observes a header trigger outside the three-value vocabulary; or an unset/foreign marker yields anything but the terminal value; or the environment leaks past an example</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/run_log_trigger_spec.rb green with the 5 described examples
  - main.rb's diff is confined to the trigger kwarg (plus its comment) — pre_scan and the watch path untouched
  - Full suite `bundle exec rspec` green — the Wave 1 merge gate for this plan
  </acceptance_criteria>
  <done>A run spawned by the dashboard says so in its own header, the vocabulary stays closed, and the badge that renders it needed no code at all. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two concurrent CLI processes → the build flock | rollback joins the mutex that already guards build and use; the OS is the arbiter, no second mechanism |
| rollback → the Xcode project's on-disk state | the destructive step (removing the proxy sandbox) is what the lock now fences |
| ambient environment → run-log header | a forgeable, user-settable variable reaching a recorded provenance field |

## STRIDE Threat Register (ASVS Level 1; block on high)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-15-08 | Tampering (a rollback deleting the sandbox out from under a running build — the CP4 race this phase closes) | Installer::Rollback#perform_install | high | mitigate | acquire the build flock before the first mutation and hold it across both steps; the ordering example asserts project state while a holder waits |
| T-15-09 | Denial of service (a raised rollback leaving the flock held, wedging every future build/use) | the ensure-release | high | mitigate | the build site's ensure shape verbatim; the release-on-raise example proves a subsequent acquire succeeds (Pitfall 6) |
| T-15-10 | Spoofing (forging UI attribution by exporting the marker in a terminal) | trigger normalization | low | accept | attribution-only by design: nothing reads the variable for control flow, the header value grants no capability, and the dashboard's badge is descriptive (Pitfall 7). Documented, not defended — defending a self-signed label on a single-user local tool would buy nothing |
| T-15-11 | Tampering (widening the LOGS-05 vocabulary through passthrough of an arbitrary env string into a rendered header field) | trigger normalization | medium | mitigate | closed whitelist — any value that is not exactly the UI marker becomes the terminal value; the foreign-value example pins it, and the frontend renders the field verbatim so an unbounded value would reach the DOM |
| T-15-12 | Information disclosure (leaking the lock holder's identity or path in the wait line) | the announce | low | accept | the frozen line carries no slots and no holder identity; attribution is derived elsewhere (CP10) |

No package installs in this plan.
</threat_model>

<verification>
- `bundle exec rspec spec/installer_rollback_lock_spec.rb spec/run_log_trigger_spec.rb` — green (15-VALIDATION rows 4 and 5).
- `bundle exec rspec spec/installer_spec.rb spec/installer_lock_notice_spec.rb spec/build_lock_spec.rb spec/main_run_log_spec.rb spec/watch_loop_spec.rb` — green (the untouched neighbors).
- `bundle exec rspec` — full suite green (Wave 1 merge gate, run at the end of Task 2).
- The browser-visible halves — a UI rollback's wait line in the stream, and the 'ui' badge on the identity card — are 15-06 probe rows 3 and 5.
</verification>

<success_criteria>
- BLD-04 complete at the CLI layer: rollback restores source mode under the build lock, for every caller, with the race closed and the lock never leaked.
- D-03 complete: UI-spawned runs are identifiable in their own headers, using the vocabulary the dashboard already renders, with no CLI behavior change and no new option surface.
- Terminal users see exactly one behavioral difference: a contended rollback waits and says so.
</success_criteria>

<output>
Create `.planning/phases/15-ui-build-controls/15-02-SUMMARY.md` when done
</output>
