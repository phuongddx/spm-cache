---
phase: 02-diagnostics-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/spm-cache-proxy/Sources/CLI.swift
  - spec/doctor_companion_version_spec.rb
  - spec/doctor_spec.rb
  - lib/spm_cache/command/doctor.rb
  - .planning/ROADMAP.md
  - .planning/phases/02-diagnostics-command/SUMMARY.md
  - docs/project-roadmap.md
autonomous: true
requirements: [REL-02, REL-03]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low   # estimate-calibration: sample_count 0, factor 1 — derived, not self-rated

must_haves:
  truths:
    # ROADMAP Phase-2 criterion 1 (REL-02)
    - "SC1: `bundle exec bin/spm-cache doctor` renders exactly 7 check lines in registration order (xcode_version, swift_version, toolchain_path, cache_dir_health, library_evolution_compatibility, remote_backend_connectivity, companion_binary) with ✓/!/✗ markers, a `↳ fix_hint` line under every non-ok check, and a trailing `Summary: N ok, N warnings, N failures` line; a run with zero :fail verdicts exits 0. Plain markers instead of ANSI color are user-accepted deviation #1 (02-CONTEXT, 2026-08-24) — a documented deviation, NOT a gap."
    # ROADMAP Phase-2 criterion 2 (REL-03)
    - "SC2: `bundle exec bin/spm-cache doctor --json` emits JSON.pretty_generate output that JSON.parse accepts, with checks[] of 7 entries {name, status(String), message, fix_hint} and summary{ok, warnings, failures}; process exit code is 1 iff summary.failures > 0 (warn-only runs exit 0)."
    # ROADMAP Phase-2 criterion 3 (REL-02)
    - "SC3: A check added via SPMCache::Core::Diagnostics.register(name, fix_hint:, &block) appears in BOTH the text and --json reports, in registration order, with zero edits to lib/spm_cache/command/doctor.rb; deleting its register block removes it. Accepted interpretation (2026-08-24): 'via config' = the data-driven Ruby registry; a yml doctor.checks: enable/disable list was explicitly rejected."
    # ROADMAP Phase-2 criterion 4 (REL-02)
    - "SC4: doctor is unit-testable without a real Xcode install — spec/doctor_spec.rb contains hermetic examples that inject stubbed shell-output collectors via allow(SPMCache::Core::Sh).to receive(:capture_output) (the existing Core::Sh seam) and assert exact ok/fail/warn verdict paths and messages of the shell-probing checks; `bundle exec rspec spec/doctor_spec.rb` passes on any host without asserting environment-specific verdicts."
    # Specless-probe UNCLASSIFIED rows, resolved by authored truth (backed by new specs)
    - "PROBE-REL-02 (authored truth): on a host where every shell probe fails (no Xcode/CLT), `doctor` still renders ALL 7 checks — each failing probe is isolated to its own :fail Result via the per-check rescue path — prints the full Summary line, and exits 1; the report never aborts mid-run."
    - "PROBE-REL-03 (authored truth): when a registered check raises mid-run, `doctor --json` still emits valid JSON containing that check with status \"fail\" and a message beginning \"Check raised an error:\", counts it in summary.failures, keeps every other check present, and exits 1."
    # Honored accepted decision (02-CONTEXT version-drift scope)
    - "COMPANION-VERSION: the Swift companion binary supports `--version` (exit 0, non-empty semver string from a single constant in tools/spm-cache-proxy/Sources/CLI.swift); after `make proxy.build`, live `bundle exec bin/spm-cache doctor` shows the companion_binary ok message ending with the \" (version)\" suffix. Gem-vs-companion drift stays VISIBLE-only (never compared, never gated) per accepted deviation #2."
    # Locked decision: strictly read-only diagnostics
    - "READ-ONLY: no file writes, deletions, or auto-remediation anywhere in the doctor path; doctor.rb exposes no --fix option (rejected 2026-08-24)."
    # Doc drift closure
    - "DOC-CLOSURE: all six RESEARCH.md drift items are closed — ROADMAP Phase-2 criteria 1/3/4 annotated with accepted deviations; SUMMARY.md line counts corrected (diagnostics.rb 156, doctor.rb 82) and spec provenance clarified (4 doctor + 3 spec_helper examples = 7); docs/project-roadmap.md v0.3.0 doctor item checked; doctor --help/self.description no longer promise color; the stale spec-header comment in spec/doctor_spec.rb is rewritten to describe the delivered approach; REL-02 'orphans'/'connectivity' wording deltas recorded as accepted-as-shipped (count-only health; config-presence check)."
  artifacts:
    - .planning/phases/02-diagnostics-command/02-01-PLAN.md
    - tools/spm-cache-proxy/Sources/CLI.swift
    - spec/doctor_companion_version_spec.rb
    - spec/doctor_spec.rb
    - lib/spm_cache/command/doctor.rb
    - .planning/ROADMAP.md
    - .planning/phases/02-diagnostics-command/SUMMARY.md
    - docs/project-roadmap.md
    - .planning/phases/02-diagnostics-command/02-01-SUMMARY.md
  key_links:
    - "CLI.swift CommandConfiguration version: parameter → Core::Sh probe \"#{bin} --version 2>/dev/null\" (diagnostics.rb:145) → companion_binary message suffix (diagnostics.rb:149) → doctor text + --json output"
    - "spec-level collector injection (allow(SPMCache::Core::Sh).to receive(:capture_output)) → per-check verdict paths → ROADMAP criterion 4 substance"
    - "ROADMAP Phase-2 criterion annotations ↔ SUMMARY.md 'Documented deviations' section ↔ 02-CONTEXT.md accepted limitations (verifier cross-reference chain)"
  prohibitions:
    - requirement_id: REL-02
      category: safety
      status: unresolved
      verification: unverified
      statement: "doctor MUST NOT mutate user state — no auto-remediation, no --fix flag, no write/delete filesystem calls anywhere in the doctor path (strictly read-only diagnostics; --fix explicitly rejected 2026-08-24)."
      reason: "Kept from adversarial prohibition recall; descriptor-less and flagged unverified — verify-phase must confirm (no File/Dir write APIs in diagnostics.rb/doctor.rb doctor path; no --fix in Doctor.options)."
    - requirement_id: REL-02
      category: values
      status: unresolved
      verification: unverified
      statement: "doctor MUST NOT exit non-zero on :warn verdicts or on gem↔companion version drift — exit 1 is reserved for :fail only; version drift is displayed, never compared or gated (accepted exit semantics + accepted limitation #2, 2026-08-24)."
      reason: "Kept from adversarial prohibition recall; flagged unverified — verify-phase confirms exit 1 iff any :fail (doctor.rb:42) and that no version comparison exists."
    - requirement_id: REL-02
      category: privacy
      status: unresolved
      verification: unverified
      statement: "doctor output (text and --json) MUST NOT echo secrets or credential-bearing values from spm-cache.yml — the remote-backend check reports config presence only; remote URLs/keys are never interpolated into messages or fix hints."
      reason: "Kept from adversarial prohibition recall; flagged unverified — verify-phase confirms messages are static strings and raw['remote'] is only tested for nil/empty (diagnostics.rb:133-138)."
---

<objective>
Verification-scoped closure of the ALREADY-SHIPPED `spm-cache doctor` (commit 5ea68a5): prove the four ROADMAP Phase-2 success criteria against the live code with automated evidence, close the one functional gap found (the dead `companion_binary --version` probe), upgrade the spec suite to hermetic collector-injection coverage (criterion 4 substance), and close all six documented doc-drift items. NO re-implementation; diagnostics registry architecture is locked (accepted as shipped, 02-CONTEXT 2026-08-24).

Purpose: REL-02/REL-03 are marked Complete in REQUIREMENTS.md but were never GSD-verified; the phase must leave behind machine-checkable proofs plus recorded deviations (2 user-accepted limitations, 4 accepted-as-shipped wording interpretations) so the verifier counts zero unexplained gaps.

Output: rebuilt companion binary with `--version`; hermetic + binary-gated specs; annotated ROADMAP/SUMMARY/project-roadmap docs; corrected doctor help text.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-diagnostics-command/02-CONTEXT.md
@.planning/phases/02-diagnostics-command/RESEARCH.md
@.planning/phases/02-diagnostics-command/02-PATTERNS.md
@.planning/phases/02-diagnostics-command/02-VALIDATION.md
@lib/spm_cache/core/diagnostics.rb
@lib/spm_cache/command/doctor.rb
@spec/doctor_spec.rb
</context>

<open_question_resolutions>
Decisions routed by RESEARCH.md Open Questions 1–4, resolved at plan time under the acceptance hierarchy (02-CONTEXT user decisions 2026-08-24 > STATE.md locked decisions > doc-amendment for wording drift):

1. **companion_binary `--version` dead branch (RESEARCH Pitfall 1) → FIX (Swift side).** 02-CONTEXT (accepted as shipped) says the check "reports the companion `--version` string when present — drift made VISIBLE". A limitation record would contradict an accepted decision; the fix honors it and is small: swift-argument-parser 1.8.2 (Package.resolved) supports the root-level `CommandConfiguration(version:)` flag — a one-constant change in CLI.swift, no new file, no new subcommand, no new dependency. Ruby side needs zero changes (diagnostics.rb:141-153 already probes and interpolates the suffix). Task 1.
2. **Criterion 4 "injected shell-output collectors" → BOTH: substance + record.** Build the hermetic coverage at the existing `Core::Sh` seam via RSpec stubbing (no production-code refactor — a constructor/parameter seam would change the public `register` block contract, out of verification scope), AND record the delivered mechanism as an accepted interpretation in ROADMAP + SUMMARY. Task 2 + Task 3.
3. **REL-02 "orphans"/"connectivity" wording → accept-as-shipped records.** Orphan detection and a network probe are new feature work, out of verification scope; both are recorded as documented deviations (MI-01 pattern). Task 3.
4. **`doctor --help` color wording → fix under granted discretion** ("minor doc-phrasing fixes", 02-CONTEXT Claude's Discretion). Task 3 (ships inside Task 3's doctor.rb edit).

Specless-probe UNCLASSIFIED rows (REL-02, REL-03 "review manually") — both resolved by authored truths, now spec-backed (Task 2): toolchain-absent full-report behavior (truth PROBE-REL-02) and raising-check-mid-run `--json` behavior (truth PROBE-REL-03). Interpretation is defensible directly from diagnostics.rb:47-57 (`run_check` rescue-to-:fail) and doctor.rb:42 (exit 1 iff any :fail).

Deferred (0 items). Nothing from the CONTEXT.md Deferred Ideas list (yml check enable/disable; --fix) appears in any task.
</open_question_resolutions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Companion `--version` flag — RED binary-gated spec, then GREEN Swift fix + rebuild</name>
  <files>spec/doctor_companion_version_spec.rb, tools/spm-cache-proxy/Sources/CLI.swift</files>
  <read_first>
    - tools/spm-cache-proxy/Sources/CLI.swift (target: the single `@main struct CLI` CommandConfiguration)
    - tools/spm-cache-proxy/Sources/CLI/GenProxy.swift (subcommand style reference — but do NOT add a subcommand; root `--version` flag is the accepted design)
    - spec/gen_proxy_ignore_spec.rb:1-41 (binary-gated spec convention: `SPMCache::ROOT.join("tools","spm-cache-proxy",".build","release","spm-cache-proxy")`, `File.executable?`, `skip "spm-cache-proxy binary not built (run make proxy.build)" unless binary`)
    - lib/spm_cache/core/diagnostics.rb:141-153 (the consumer: `Sh.capture_output("#{bin} --version 2>/dev/null")`, suffix interpolation, rescue-to-'' branch)
    - tools/spm-cache-proxy/Package.resolved (swift-argument-parser 1.8.2 — `version:` parameter is non-deprecated since 1.3.0)
    - VERSION (repo root; currently `0.3.0` — the constant's initial value must match it)
    - Makefile:12-13 (`proxy.build` target = `cd tools/spm-cache-proxy && swift build -c release`)
  </read_first>
  <behavior>
    - Test 1 (binary-gated): running the built binary with `--version` exits 0 and prints a non-empty string matching `\d+\.\d+\.\d+` (RED today: exit 64 "Error: Unknown option '--version'"; GREEN after the fix).
    - Test 2 (binary-gated): `--version` output, stripped, equals the version constant wired into CommandConfiguration (single-source check: `spm-cache-proxy --help` also lists a `--version` option line — ArgumentParser adds it automatically).
    - End-to-end (evidence, not a spec): after `make proxy.build`, `bundle exec bin/spm-cache doctor` companion_binary line ends with ` (0.3.0)`-style suffix; before the rebuild it does not (Pitfall 1 warning sign).
  </behavior>
  <action>
    TDD RED first: create spec/doctor_companion_version_spec.rb following the exact binary-gated convention from spec/gen_proxy_ignore_spec.rb (frozen_string_literal header, `require "spec_helper"`, binary helper with `File.executable?` guard, `before` skip, no tmpdir needed). One describe with two examples: (a) `it "prints its version with --version (exit 0)"` — use `Open3.capture3(binary, "--version")` (NOT string-shell form, so no quoting hazards), assert `$?.success?`/`status.exitstatus == 0` and stripped stdout matches `/\A\d+\.\d+\.\d+\z/`; (b) `it "lists --version in --help"` — `Open3.capture3(binary, "--help")` succeeds and combined output includes the literal `--version`. Run `bundle exec rspec spec/doctor_companion_version_spec.rb` — example (a) MUST fail against the currently-built binary (exit 64); commit RED with `test(02-01): add failing companion --version spec`.
    GREEN: edit tools/spm-cache-proxy/Sources/CLI.swift only — add `static let proxyVersion = "0.3.0"` on `struct CLI` and pass `version: proxyVersion` into its existing `CommandConfiguration(commandName:abstract:...)` call, keeping `subcommands: [GenUmbrella.self, GenProxy.self, Resolve.self]` unchanged. Do NOT create a Version.swift subcommand (a subcommand would not answer the root-level `--version` probe) and do NOT touch any other Swift file. Then rebuild with `make proxy.build`, confirm both spec examples pass, and confirm `cd tools/spm-cache-proxy && swift test` still passes (the test target compiles the modified CLI.swift). Record in the task summary that `proxyVersion` must be bumped in lockstep with the repo VERSION file at release — doctor exists precisely to make any drift visible (accepted limitation #2: displayed, never compared).
  </action>
  <verify>
    <automated>cd tools/spm-cache-proxy && swift build -c release && swift test && cd ../../.. && bundle exec rspec spec/doctor_companion_version_spec.rb && bundle exec bin/spm-cache doctor | grep "companion_binary"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/spm-cache-proxy/.build/release/spm-cache-proxy --version; echo $?` prints `0.3.0` (exit code 0) — previously exit 64.
    - `bundle exec rspec spec/doctor_companion_version_spec.rb` → 2 examples, 0 failures (after rebuild; skips gracefully with the gen_proxy-spec skip message when the binary is absent).
    - `cd tools/spm-cache-proxy && swift test` exits 0 (full existing Swift suite unaffected).
    - `bundle exec bin/spm-cache doctor` output contains a companion_binary line matching `Companion binary present at .* \(0\.3\.0\)` — the version suffix now manifests (RESEARCH Pitfall 1 warning sign eliminated).
    - `git diff --stat` for this task touches exactly: spec/doctor_companion_version_spec.rb (new) and tools/spm-cache-proxy/Sources/CLI.swift; grep for `proxyVersion` in tools/spm-cache-proxy/Sources returns exactly one declaration and one use, both in CLI.swift.
    - TDD provenance: git log shows the RED test commit authored before the GREEN commit (commit messages contain `test(02-01)` then `feat(02-01)`).
  </acceptance_criteria>
  <done>Companion binary answers `--version` with exit 0 and a semver string; doctor live output shows the version suffix; binary-gated spec green; Swift suite green; two atomic commits (RED, GREEN) on the milestone branch.</done>
  <precondition>Swift 6 toolchain on PATH (`swift build --version` works) — verified available locally this cycle; the release binary at tools/spm-cache-proxy/.build/release/spm-cache-proxy may be stale (pre-fix) — the task rebuilds it.</precondition>
</task>

<task type="auto">
  <name>Task 2: Hermetic collector-injection specs — criterion 4 substance + both probe truths</name>
  <files>spec/doctor_spec.rb</files>
  <read_first>
    - spec/doctor_spec.rb (full file — existing 4 doctor examples, the registry save/restore idiom at lines 31-45, the StringIO stdout swap + `allow_any_instance_of(SPMCache::Command::Doctor).to receive(:exit)` idiom at lines 52-63, and the stale header comment at lines 7-10 to rewrite)
    - lib/spm_cache/core/diagnostics.rb:66-153 (exact per-check verdict strings the new examples assert — quote them verbatim)
    - lib/spm_cache/core/sh.rb (confirm `capture_output` is a module-level class method — receiver is the `Core::Sh` module object, so `allow(SPMCache::Core::Sh).to receive(:capture_output)` intercepts every in-check call)
    - lib/spm_cache/core/error.rb (`Core::GeneralError` — construct with `.new('...')` to simulate Sh failure for the xcode_version rescue branch)
    - lib/spm_cache/core/config.rb (`Config::CACHE_DIR`, `raw`, `load`, `reset!` — for instance_double stubs and the cache_dir_health branches)
    - .planning/phases/02-diagnostics-command/RESEARCH.md "Validation Architecture" (what must be stubbed vs live; Pitfall 5 exit-stub rule; Pitfall 6 singleton leak — pass explicit `config:` doubles, never rely on Config.instance)
  </read_first>
  <action>
    Rewrite the header comment (spec/doctor_spec.rb:7-10) to describe the delivered two-layer approach accurately: registry/formatter mechanics exercised through the Command; shell-probing checks exercised hermetically by injecting stubbed collectors over `Core::Sh`; live-host examples assert shape with tolerant verdicts. Then add three describe blocks, keeping every existing example untouched and passing:
    (1) `RSpec.describe SPMCache::Core::Diagnostics, "hermetic per-check paths (injected shell collectors)"` — stub `SPMCache::Core::Sh` per example and `run_all(config: nil)` (or a config double for remote), asserting EXACT verdicts/messages from diagnostics.rb: xcode_version ok (first line of stubbed `xcodebuild -version` output) and fail (stub `capture_output` to raise `SPMCache::Core::GeneralError.new('exit 64')` → message starts `xcodebuild not found or returned no output`); swift_version ok / fail (`swift not found on PATH`); toolchain_path ok / fail (`xcrun could not locate swift — Xcode command-line tools not installed?`); companion_binary present+version (stub `File.executable?` with the exact expanded bin path to true via `and_call_original` + specific-with override, `capture_output` → `0.3.0` → message ends ` (0.3.0)`), present+probe-fails (raise → no suffix), and missing (executable? → false → :warn, message starts `Companion binary not built`); cache_dir_health exists (stub `File.directory?` with `SPMCache::Core::Config::CACHE_DIR` → true, `Dir.children` → two names, `Dir.glob` → empty → message matches `Cache dir .* \(2 config\(s\), ~0 files\)`) and missing (directory? → false → `does not exist yet (no caches built)`); remote_backend_connectivity both branches via `instance_double(SPMCache::Core::Config, load: nil, raw: {...})` — empty raw → `No remote backend configured (local-only)`, raw with a `remote` key → `Remote backend configured` (assert presence of the phrase `run \`spm-cache remote pull\``); plus one registration-order example asserting `described_class.registry.map(&:name)` equals the exact 7-name array in order.
    (2) `RSpec.describe "spm-cache doctor with a fully absent toolchain"` — stub `capture_output` to always raise `SPMCache::Core::GeneralError`; run the real command via `SPMCache::Command.parse(['doctor'])` inside the StringIO stdout swap, with `expect_any_instance_of(SPMCache::Command::Doctor).to receive(:exit).with(1).and_return(nil)`; assert 7 report lines rendered (none dropped), the three toolchain checks render `✗`, the trailing Summary line reports `3 failures`, and stdout contains no marker lines beyond the 7 (report completed).
    (3) Extend the existing `RSpec.describe 'spm-cache doctor --json'` with one example `emits valid JSON even when a check raises mid-run` — registry save/restore idiom (lines 31-45), inject a raising check `register('kaboom_json', fix_hint: 'n/a') { raise 'json boom' }`, run `Command.parse(['doctor', '--json'])` with exit stubbed `with(1)`, then `JSON.parse` the captured stdout and assert: checks array length 8, the entry named `kaboom_json` has status `fail` and message starting `Check raised an error: json boom`, all 7 built-in names still present, and summary `failures` >= 1.
    Conventions throughout: frozen_string_literal header, `require 'spec_helper'` first (already present), per-example stub scoping (RSpec auto-verifying doubles; module-method stubs on `SPMCache::Core::Sh`/`File`/`Dir` use `and_call_original` + specific-argument overrides so unrelated examples are untouched), no edits to lib/ production files.
  </action>
  <verify>
    <automated>bundle exec rspec spec/doctor_spec.rb --format documentation</automated>
  </verify>
  <acceptance_criteria>
    - `bundle exec rspec spec/doctor_spec.rb` → 0 failures; example count = existing 4 doctor + 3 spec_helper + new examples (hermetic block, absent-toolchain block, json-raising example); documentation formatter lists the three new describe/example names containing `hermetic per-check paths (injected shell collectors)`, `spm-cache doctor with a fully absent toolchain`, and `emits valid JSON even when a check raises mid-run`.
    - No new example shells out to real `xcodebuild`/`swift`/`xcrun`: every hermetic example passes with `capture_output` stubbed to raise (proof: the absent-toolchain example itself runs with every probe raising and still passes).
    - `git status` shows no modification under lib/ from this task (production code untouched).
    - Header comment no longer contains the phrase `injected fixtures rather than a real Xcode install` (RESEARCH drift item 2); its replacement names `Core::Sh` collector injection and the tolerant live examples.
    - Full-suite safety: `bundle exec rspec spec/doctor_spec.rb spec/doctor_companion_version_spec.rb` → 0 failures (both files coexist; binary-gated file still green after Task 1).
  </acceptance_criteria>
  <done>Criterion 4 substance delivered: per-check ok/fail/warn paths asserted hermetically through an injected shell-output collector (Core::Sh stub seam) with exact message strings; both probe truths (REL-02 absent-toolchain full report + exit 1; REL-03 raising-check JSON validity + failure count) are automated, deterministic, host-independent examples; all pre-existing examples still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Doc-drift closure + accepted-deviation records (ROADMAP, SUMMARY, project-roadmap, doctor help)</name>
  <files>lib/spm_cache/command/doctor.rb, .planning/ROADMAP.md, .planning/phases/02-diagnostics-command/SUMMARY.md, docs/project-roadmap.md</files>
  <read_first>
    - lib/spm_cache/command/doctor.rb:13-18 (self.summary/self.description/--json option text — the only production-code edit, wording only)
    - lib/spm_cache/command/watch.rb:1-18 (sibling CLAide description style to mirror)
    - .planning/ROADMAP.md:27-37 (Phase-1 SC-2 inline-amendment precedent at line 27; Phase-2 criteria 1/3/4 at lines 34-37 — annotate, do not silently reword)
    - .planning/phases/02-diagnostics-command/SUMMARY.md (stale line counts at lines 7-8, spec-provenance claim at line 9)
    - .planning/phases/02-diagnostics-command/02-CONTEXT.md (the two ACCEPTED LIMITATION texts to mirror verbatim in the deviation record)
    - docs/project-roadmap.md:59-68 (v0.3.0 section; line 63 unchecked doctor item)
    - .planning/phases/02-diagnostics-command/RESEARCH.md "Common Pitfalls" item 7 (the authoritative 6-item drift list this task closes)
  </read_first>
  <action>
    Four scoped edits, no functional code changes:
    (1) lib/spm_cache/command/doctor.rb wording (discretion-granted, 02-CONTEXT): in self.description replace the sentence `Prints a green/yellow/red report.` with `Prints a status report with per-check markers and fix hints.`; in self.options change the `--json` description from `Emit diagnostics as JSON instead of a color-coded report` to `Emit diagnostics as JSON instead of the text report`. Nothing else in the file changes (parsing, formatters, exit logic untouched — `git diff` must show only those two strings).
    (2) .planning/ROADMAP.md Phase-2 section, following the Phase-1 SC-2 inline-amendment pattern (annotate with justification, cross-reference 02-CONTEXT/SUMMARY): SC1 — append a parenthetical noting plain ✓/!/✗ markers replace ANSI color (user-accepted 2026-08-24, terminal-agnostic/pipe-safe) and that cache-dir health is count-only (no orphan detection) and remote-backend is config-presence (no network probe), both accepted as shipped; SC3 — append that "config" is delivered as the Ruby `Core::Diagnostics.register` API (yml enable/disable rejected 2026-08-24); SC4 — append that the delivered mechanism is spec-level injection of stubbed shell-output collectors over the `Core::Sh` seam (hermetic per-check path specs; constructor injection not shipped). The Phase-2 Plans checklist is added by the planner at planning time — verify it is present, do not re-add it.
    (3) .planning/phases/02-diagnostics-command/SUMMARY.md: correct line counts to `diagnostics.rb` 156 and `doctor.rb` 82; reword line 9 spec claim to `4 doctor examples + 3 spec_helper examples = 7 (spec provenance per RESEARCH Pitfall 3)`; add a `## Documented deviations (user-accepted / accepted-as-shipped)` section recording, each with date 2026-08-24 and source: (a) plain markers instead of color — user-accepted, terminal-agnostic; (b) companion version drift displayed never compared — user-accepted; (c) REL-02 "orphans" → count-only health — accepted as shipped; (d) REL-02 "connectivity" → config-presence only — accepted as shipped; (e) SC3 "via config" → Ruby register API — accepted interpretation; (f) SC4 → Sh-seam collector injection, not constructor injection — accepted interpretation; (g) companion `--version` now honored via the proxy root flag (this phase's gap fix, Task 1). Update the Deliverables bullet to mention `--version` support and the hermetic spec layer with their post-fix state.
    (4) docs/project-roadmap.md line 63: change `- [ ] \`spm-cache doctor\` command (diagnose environment, toolchain)` to `- [x] \`spm-cache doctor\` command (diagnose environment, toolchain) — shipped in v0.3.0`.
  </action>
  <verify>
    <automated>bundle exec bin/spm-cache doctor --help | grep -c "green/yellow/red\|color-coded" && bundle exec rspec spec/doctor_spec.rb spec/doctor_companion_version_spec.rb && grep -c "\[x\] \`spm-cache doctor\`" docs/project-roadmap.md && grep -c "156 lines" .planning/phases/02-diagnostics-command/SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `bundle exec bin/spm-cache doctor --help` output: `grep -c "green/yellow/red"` returns 0 and `grep -c "color-coded"` returns 0; `--json` line contains the literal `text report`.
    - `git diff lib/spm_cache/command/doctor.rb` (vs 5ea68a5 state) shows only the two description strings changed — no logic, option parsing, or formatter lines touched; `bundle exec rspec spec/doctor_spec.rb spec/doctor_companion_version_spec.rb` → 0 failures (help edit breaks nothing).
    - `.planning/ROADMAP.md` Phase 2 section: criteria 1, 3, 4 each carry an amendment note naming the accepted deviation/interpretation with its 2026-08-24 source; the section lists `**Plans:** 1` and the `02-01-PLAN.md` checklist item.
    - `.planning/phases/02-diagnostics-command/SUMMARY.md` contains `156 lines` and `82 lines`, the `4 doctor examples + 3 spec_helper examples` provenance string, and a `Documented deviations` section enumerating exactly the seven records listed in the action.
    - `docs/project-roadmap.md` v0.3.0 section: the doctor item is `- [x]` with `shipped in v0.3.0` (grep count 1); no other line in that file changed (`git diff docs/project-roadmap.md` is a one-line hunk).
    - Text-mode deviation recorded, not reintroduced: no file modified by this task introduces an ANSI escape sequence or the words the doctor report was corrected away from (the doctor help no longer promises what the report does not deliver).
  </acceptance_criteria>
  <done>All six RESEARCH drift items closed and cross-recorded (ROADMAP annotations ↔ SUMMARY deviations section ↔ 02-CONTEXT); doctor help text matches delivered marker-based output; both user-accepted limitations plus the four accepted-as-shipped interpretations are recorded as documented deviations so the phase verifier counts them as recorded, not as gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → CLI (argv) | Only the boolean `--json` flag is parsed; no free-text user input reaches any command string |
| filesystem → doctor | Read-only probes: `~/.spm-cache` (Dir.children/Dir.glob), repo tree (`File.executable?`), `spm-cache.yml` (parsed via `YAML.safe_load` in Core::Config) |
| doctor → companion binary | Executes the ROOT-derived constant path `tools/spm-cache-proxy/.build/release/spm-cache-proxy --version` with constant arguments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Core::Sh shell-outs (diagnostics.rb) | low | mitigate | Keep the shipped invariant: all commands are string literals; the only interpolated value is the ROOT-derived binary path. Tasks 1–2 add no interpolation of config or user values into Sh commands (asserted by review of the two diffs). |
| T-02-02 | Tampering | spm-cache.yml loading (Core::Config) | low | mitigate | `YAML.safe_load` already in place (config.rb); this phase does not touch config parsing. |
| T-02-03 | Information disclosure | doctor text/--json messages | low | mitigate | Check messages and fix hints are static strings; `raw['remote']` is only tested for nil/empty — no URLs/credentials echoed. New specs assert exact static messages, locking the contract. |
| T-02-04 | Elevation | companion binary rebuild (`make proxy.build`) | low | mitigate | Build runs in-repo against Package.resolved pins (swift-argument-parser 1.8.2, Rainbow 4.2.1); Task 1 adds no dependency and no new code path beyond a root `--version` flag printing a constant. |
| T-02-SC | Supply chain (package installs) | n/a — no installs this phase | low | accept | Verification-scoped phase: zero gem/pod/swift-package installs; no `[ASSUMED]`/`[SUS]` packages exist to gate (RESEARCH Package Legitimacy Audit: not triggered). |
</threat_model>

<verification>
Phase gate (all locally runnable, per 02-VALIDATION.md sampling contract):
1. `bundle exec rspec spec/doctor_spec.rb spec/doctor_companion_version_spec.rb` → 0 failures (per-task sampling throughout).
2. `make proxy.build && bundle exec rspec` → full suite green (wave merge gate; CI parity).
3. `bundle exec bin/spm-cache doctor; echo $?` → 7 marker lines + `Summary: N ok, N warnings, N failures`, exit 0 on this host (Xcode 26.3, binary built); evidence notes the environment per RESEARCH Pitfall 4.
4. `bundle exec bin/spm-cache doctor --json | ruby -rjson -e 'j=JSON.parse(STDIN.read); abort "shape" unless j["checks"].length==7 && %w[ok warnings failures].all?{|k| j["summary"].key?(k)}; puts "JSON-OK"'` → prints `JSON-OK`.
5. Criterion-3 runtime proof (no command edit): in-process `bundle exec ruby -rspm_cache/main -e '...'` registering a `plan_probe` check via `Core::Diagnostics.register`, running `SPMCache::Command.parse(["doctor"])` with stdout captured, asserting a rendered `plan_probe` line exists — plus structural grep `grep -c "xcode_version\|companion_binary" lib/spm_cache/command/doctor.rb` = 0.
6. Exit-1 CI gate: proven hermetically by Task 2's absent-toolchain and json-raising examples (`expect ... to receive(:exit).with(1)`); no fragile forced-fail CLI setup required.
7. `cd tools/spm-cache-proxy && swift test` → green (Task 1).
</verification>

<success_criteria>
- All four ROADMAP Phase-2 success criteria carry machine-checkable evidence (SC1: CLI smoke + registry-order spec; SC2: JSON shape check + spec; SC3: runtime register proof + structural grep; SC4: hermetic collector-injection specs) or a recorded accepted deviation (color; "via config"; delivered injection mechanism).
- The one functional gap (dead `--version` probe) is fixed end-to-end: Swift flag → rebuilt binary → Ruby probe renders the version suffix live.
- Both specless-probe truths are automated and deterministic (no host dependency).
- All six doc-drift items closed with cross-recorded deviations (ROADMAP ↔ SUMMARY ↔ 02-CONTEXT).
- No deferred idea implemented; no ANSI color, no `--fix`, no yml check config, no orphan detection, no network probe, no second shell seam introduced anywhere.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

**Swift (tools/spm-cache-proxy/Sources/CLI.swift — modified)**
- `CLI.proxyVersion` — `static let` string constant, initial value `0.3.0` (single source for the companion version; bump in lockstep with repo VERSION at release).

**Ruby production (lib/spm_cache/command/doctor.rb — modified, wording only)**
- Rewritten `self.description` sentence and `--json` option description (no new symbols; no logic change).

**Specs**
- `spec/doctor_companion_version_spec.rb` (new) — binary-gated: `prints its version with --version (exit 0)`, `lists --version in --help`.
- `spec/doctor_spec.rb` (modified) — new describes/examples: `hermetic per-check paths (injected shell collectors)` (per-check ok/fail/warn + registry order), `spm-cache doctor with a fully absent toolchain` (7-line report, ✗ markers, Summary with 3 failures, exit 1), `emits valid JSON even when a check raises mid-run` (status "fail", `Check raised an error:` prefix, failures count, all built-ins present); rewritten header comment.

**Docs**
- `.planning/ROADMAP.md` — Phase-2 SC1/SC3/SC4 annotations + Plans checklist.
- `.planning/phases/02-diagnostics-command/SUMMARY.md` — corrected counts/provenance + `## Documented deviations (user-accepted / accepted-as-shipped)` section (7 records).
- `docs/project-roadmap.md` — v0.3.0 doctor item checked as shipped.
- `.planning/phases/02-diagnostics-command/02-01-SUMMARY.md` — created by the executor at completion.

No new modules, classes, gems, or Swift files; no changes to the `Core::Diagnostics.register` public contract.
</artifacts_produced>

<output>
Create `.planning/phases/02-diagnostics-command/02-01-SUMMARY.md` when done
</output>
