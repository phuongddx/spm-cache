---
phase: 03-project-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/spm_cache/command/init.rb
  - spec/init_spec.rb
  - .planning/ROADMAP.md
  - .planning/phases/03-project-bootstrap/SUMMARY.md
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-03]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low   # estimate-calibration: sample_count 0, factor 1 — derived, not self-rated

must_haves:
  truths:
    # ROADMAP Phase-3 criterion 1 (ONBD-01)
    - "SC1: `bundle exec ruby -Ilib bin/spm-cache init --project=<abs-tmpdir>/Fake.xcodeproj --platform=ios --default-config=debug` (fixture: plain .xcodeproj dir + project.xcworkspace/xcshareddata/swiftpm/Package.resolved with one Alamofire pin) exits 0 and creates spm-cache.yml, spm-cache.lock, and a .gitignore containing `spm-cache/`; the seeded lock is the CANONICAL shape — top-level key `Fake.xcodeproj`, `packages` array of {repositoryURL, name, version, revision}, `dependencies: {}`, `platforms: {}` — NOT the Package.resolved pins byte-copy that previously crashed `use`. Amended wording (2026-08-24): the fast path materializes on SUBSEQUENT `use` runs (a first run always fully regenerates because the proxy is not yet materialized — installer.rb fast-path contract); the seed's value is lock continuity."
    # ROADMAP Phase-3 criterion 2 (ONBD-02)
    - "SC2: non-interactive flags work for scripting/CI: an Open3 subprocess invocation passing `--platform=ios --default-config=release --remote=git --remote-url=https://github.com/example/cache.git --branch=release` (stdin piped, not a TTY) exits 0 without reading stdin, and the generated spm-cache.yml carries the flagged values (platforms [ios], default_config release, remote including the git URL). Shipped flag surface is the accepted superset `--project/--platform/--default-config/--remote/--remote-url/--branch/--creds`; ROADMAP's `--config` shipped as `--default-config` — DOCUMENTED DEVIATION (user-accepted 2026-08-24, 03-CONTEXT; CLaide base `Command` already defines `--config` at lib/spm_cache/command.rb:19)."
    # ROADMAP Phase-3 criterion 3 (ONBD-03)
    - "SC3: re-running init idempotently diff-merges: with identical flags, a second CLI run produces a BYTE-STABLE spm-cache.yml (identical sha256); a user key added between runs (e.g. custom_key) survives while init-managed keys update; missing DEFAULT_CONFIG keys are added without overwriting user-set values (Core::Config#load's DEFAULT_CONFIG.merge(file) — file wins); `.gitignore` contains exactly one `spm-cache/` line after two full runs."
    # ROADMAP Phase-3 criterion 4 (ONBD-01/02/03 regression)
    - "SC4: `bundle exec rspec spec/init_spec.rb` passes in tmpdir fixtures with a fixture .xcodeproj — post-fix run count is 10 examples, 0 failures (7 init-owned `it` blocks + 3 spec_helper smoke examples); adjacent contracts `spec/diff_detector_spec.rb` and `spec/installer_use_fast_path_spec.rb` remain 0-failures after the seed-format change."
    # Mandated defect fix (RESEARCH Critical Finding)
    - "LOCK-CANONICAL: both seed_lockfile branches write the canonical lock shape — pins branch: JSON.pretty_generate of {File.basename(project_path)=>{packages: pins.map (installer.rb:178-185 transformation: repositoryURL=location, name=identity, version=state.version, revision=state.revision), dependencies: {}, platforms: {}}}; empty branch: same shape with packages: []. The previously shipped shapes (pins byte-copy via FileUtils.cp; `{\"projects\":[]}` skeleton) are gone from init.rb."
    - "USE-E2E: the init-seeded lock is consumable by the `use` path: Core::DiffDetector.new(project_path:, lockfile_path:).detect on an init-seeded lock raises nothing (previously TypeError `no implicit conversion of String into Integer` at diff_detector.rb:103) and reports an empty diff; a spec-level end-to-end (real Xcodeproj::Project fixture + Package.resolved → Command::Init#run → Installer::Use#perform_install with heavy regeneration methods stubbed) completes without raising with installer.diff empty."
    # Specless-probe UNCLASSIFIED rows (ONBD-01, ONBD-02) — authored truths backed by code
    - "PROBE-ONBD-01 (authored truth): prompts fire only when stdin is a TTY AND neither `--platform` nor `--remote` was supplied (interactive? at init.rb:119-121 checks exactly those two); in CI (piped stdin) or with either flag, no prompt is printed and per-prompt defaults apply (ios/debug/none, branch main). TTY-only empty-Enter nuances (empty platforms input omits the key; empty config input writes an empty string; fallback fires on EOF only) are accepted-as-shipped notes recorded in SUMMARY — manual-only verification per 03-VALIDATION."
    - "PROBE-ONBD-02 (authored truth): every scripting invocation that passes `--platform` and/or `--remote` completes without blocking on stdin — proven by Open3 subprocess proofs (non-TTY stdin) exiting 0 across the full flag matrix."
    # Specless-probe ADJACENCY/EMPTY/ORDERING rows (ONBD-03 diff-merge semantics) — authored truths
    - "PROBE-ONBD-03a (authored truth, adjacency/ordering): re-running init with identical flags yields a byte-stable yml — Config#load merges DEFAULT_CONFIG under the file hash so key order and values converge; proven by sha256 equality across two CLI runs."
    - "PROBE-ONBD-03b (authored truth, empty): a project with NO Package.resolved still gets spm-cache.yml + a canonical empty lock + .gitignore entry, exit 0, with the informational message naming `spm-cache use`."
    - "PROBE-ONBD-03c (authored truth, equal-key merge): on key collision the USER value wins (DEFAULT_CONFIG.merge(file) — the file is the merge argument, config.rb:52); init only assigns its managed keys (platforms, default_config, remote when supplied; default_sdk only when absent)."
    # Doc drift closure (RESEARCH Doc Drift Catalogue items 1-7)
    - "DOC-CLOSURE: all seven RESEARCH drift items closed — (1)(2) SUMMARY.md line counts and spec provenance corrected to post-fix measured reality (wc -l at close time; N init examples + 3 spec_helper = run count); (3) ROADMAP criterion 2 amended with the --default-config deviation + flag superset; (4)(5) TTY-heuristic wording nuances recorded as accepted-as-shipped SUMMARY notes (no CONTEXT edit — locked doc); (6) spec run re-evidenced green; (7) criterion 1's fast-path wording amended AND the seeded-lock format defect FIXED (not recorded as a known issue). docs/project-roadmap.md verified to contain no init item (grep) — no edit needed, check recorded."
  artifacts:
    - .planning/phases/03-project-bootstrap/03-01-PLAN.md
    - lib/spm_cache/command/init.rb
    - spec/init_spec.rb
    - .planning/ROADMAP.md
    - .planning/phases/03-project-bootstrap/SUMMARY.md
    - .planning/phases/03-project-bootstrap/03-01-SUMMARY.md
  key_links:
    - "Command::Init#seed_lockfile (init.rb:144-157) canonical write → Core::DiffDetector#locked_packages parse (diff_detector.rb:101-104: data.each_value → proj_data['packages']) → Installer::Use#perform_install detect_diff (installer.rb:54-60) — the crash link this phase repairs; verified by the new USE-E2E spec"
    - "CLI flags (--platform/--default-config/--remote/--remote-url/--branch) → write_config managed-key assignment (init.rb:126-142) → Core::Config#load DEFAULT_CONFIG.merge(file) (config.rb:47-54) → spm-cache.yml on disk — the idempotency/diff-merge link"
    - "installer.rb:164-193 pins→canonical transformation (source of the seed logic) ↔ init.rb seed_lockfile (consumer of the transformation) — duplicated logic kept visually aligned; diff_detector.rb and installer.rb themselves UNCHANGED"
    - "ROADMAP Phase-3 criterion annotations ↔ SUMMARY.md 'Documented deviations' section ↔ 03-CONTEXT.md accepted decisions (verifier cross-reference chain, Phase-2 pattern)"
  prohibitions:
    - requirement_id: ONBD-01
      category: safety
      status: unresolved
      verification: unverified
      statement: "init MUST NOT invoke the Xcode toolchain or open the project via the xcodeproj gem — the command is pure file I/O (no xcodebuild, no swift, no Xcodeproj::Project.open anywhere in the init path); the canonical-seeding fix must achieve platforms: {} rather than calling installer's detect_platforms for exactly this reason."
      reason: "Kept from adversarial prohibition recall; descriptor-less and flagged unverified — verify-phase confirms no Xcodeproj/xcodebuild references enter lib/spm_cache/command/init.rb and that lockfile platforms consumer defaults to {} (core/lockfile.rb:144-147)."
    - requirement_id: ONBD-03
      category: transparency
      status: unresolved
      verification: unverified
      statement: "a re-run of init MUST NOT lose or overwrite user-set spm-cache.yml keys outside init's managed set (platforms, default_config, remote, default_sdk) — the never-overwrites contract; the only accepted edge (an unreadable/corrupt yml is replaced with defaults) is already documented in RESEARCH."
      reason: "Descriptor-less and flagged unverified — verify-phase confirms write_config assigns only the four managed keys and the idempotency spec/CLI proof still pass."
    - requirement_id: ONBD-02
      category: safety
      status: unresolved
      verification: unverified
      statement: "init MUST NOT block on stdin prompts in non-TTY contexts (CI/piped stdin) — any scripting invocation must terminate without human input."
      reason: "Descriptor-less and flagged unverified — verify-phase confirms interactive? gates on $stdin.tty? (init.rb:119-121) and that all CLI proofs complete under Open3 with non-TTY stdin."
---

<objective>
Verification-scoped closure of the ALREADY-SHIPPED `spm-cache init` (commit c51cedc) PLUS one mandated defect fix: `init` seeds `spm-cache.lock` in a format that crashes the next `spm-cache use` (TypeError at diff_detector.rb:103, e2e-verified in RESEARCH). The fix makes init write the CANONICAL lock shape (reusing the pins→packages transformation that already exists at installer.rb:164-193), proven TDD-style RED (crash evidence) → GREEN (init→use works). The remaining tasks prove the four ROADMAP Phase-3 success criteria against the live CLI with recorded evidence, and close all seven RESEARCH doc-drift items including the user-accepted `--config`→`--default-config` deviation and the criterion-1 fast-path wording amendment. NO wizard re-implementation; the interactive heuristic, flag surface, and idempotency mechanics are locked as shipped (03-CONTEXT 2026-08-24).

Purpose: ONBD-01/02/03 are marked Complete in REQUIREMENTS.md but were never GSD-verified; criterion 1's stated purpose (seeded lock usable by `use`) is empirically false as shipped. The phase must leave a working init→use path, machine-checkable criterion proofs, and recorded deviations so the verifier counts zero unexplained gaps.

Output: canonical lock seeding (both branches) with RED→GREEN spec provenance; three new regression specs (canonical shape, empty-skeleton shape, init→use end-to-end); CLI-level criterion proof evidence; annotated ROADMAP/SUMMARY docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-bootstrap/03-CONTEXT.md
@.planning/phases/03-project-bootstrap/RESEARCH.md
@.planning/phases/03-project-bootstrap/03-PATTERNS.md
@.planning/phases/03-project-bootstrap/03-VALIDATION.md
@lib/spm_cache/command/init.rb
@spec/init_spec.rb
</context>

<open_question_resolutions>
Decisions routed from RESEARCH.md Open Questions 1–3, resolved at plan time under the acceptance hierarchy (03-CONTEXT user decisions 2026-08-24 > orchestrator mandate > doc-amendment for wording drift):

1. **Seeded-lock format crash (RESEARCH OQ1) → FIX, not deviation.** The orchestrator resolved the user scope decision as FIX. The transformation already exists at installer.rb:164-193 and is spec-covered; the fix is surgical (one method body in init.rb), does not re-implement the wizard (CONTEXT's "do NOT re-implement" predates this discovery and scoped the wizard's flow, not a format defect that invalidates criterion 1). Type: TDD — Task 1. CONTEXT's locked seeding POLICY (skip-when-no-Package.resolved with message, exit 0) is untouched; only the FORMAT of what gets written changes on both branches.
2. **Criterion 1 "first `use` fast path" wording (RESEARCH OQ2) → amend inline.** `fast_path?` requires a materialized proxy (use.rb/installer contract: a first run always fully regenerates by design; spec/installer_use_fast_path_spec.rb:113 is titled "regenerates when lockfile is missing (first run)"). Amend ROADMAP criterion 1 to "so subsequent `use` runs can take the fast path" (Phase-2 amendment pattern); the seed's real value is lock continuity. Doc-phrasing change within Claude's discretion.
3. **CONTEXT.md heuristic wording imprecisions (RESEARCH OQ3, drift #4/#5) → record, do not edit CONTEXT.** CONTEXT is a locked decisions doc; both nuances (only --platform/--remote suppress prompts; empty-Enter vs EOF semantics) are captured as accepted-as-shipped notes in the phase SUMMARY. Planner's discretion, per RESEARCH recommendation.

Deferred (0 items). Nothing from 03-CONTEXT Deferred Ideas (`--yes`/`--non-interactive` flag; `--config` alias) appears in any task.

Design decision within the fix: lockfile `platforms` sub-key is written as `{}` — init must not open the project via the xcodeproj gem (pure file-I/O contract; installer's `detect_platforms` at installer.rb:195-209 opens Xcodeproj::Project), and the only Ruby consumer `Core::Lockfile#platforms_for_project` (core/lockfile.rb:144-147) already defaults to `{}` when the key is absent/empty, so `{}` is consumer-identical to omission. `dependencies` is `{}` exactly as installer.rb:186 writes on first generation; `refresh_consumed_dependencies` fills real dependencies on the first full `use`.
</open_question_resolutions>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Canonical lock seeding — RED spec (TypeError) → GREEN seed_lockfile rewrite</name>
  <files>spec/init_spec.rb, lib/spm_cache/command/init.rb</files>
  <read_first>
    - lib/spm_cache/command/init.rb:144-161 (seed_lockfile + find_package_resolved — the defect site: FileUtils.cp byte-copy branch and the empty-skeleton branch)
    - lib/spm_cache/installer.rb:164-193 (generate_lockfile_from_resolved — the canonical pins→packages transformation to reuse: repositoryURL=pin['location'], name=pin['identity'], version=pin.dig('state','version'), revision=pin.dig('state','revision'), keyed File.basename(project_path), plus dependencies/platforms sub-keys; note its early `return if File.exist?` — why the lock never self-heals)
    - lib/spm_cache/core/diff_detector.rb:94-115 (locked_packages — the consumer contract the seed must satisfy: JSON.parse then each_value yielding per-project Hashes whose 'packages' is an Array; String-indexing an Array is the TypeError)
    - spec/init_spec.rb (full file — fixture pattern lines 16-25: plain .xcodeproj dir + Package.resolved with the Alamofire pin {identity:'Alamofire', location:'https://github.com/Alamofire/Alamofire.git', state:{revision:'deadbeef', version:'5.0.0'}}; Config singleton hygiene lines 37-39 and 62/72/88/99: reset! before every cmd.run; the 'fails gracefully' example's separate-tmpdir pattern lines 95-104)
    - spec/installer_use_fast_path_spec.rb:13-60,113-133 (real-project fixture: Xcodeproj::Project.new + new_target(:application,'MyApp',:ios) + save; write_package_resolved helper; the stub list for perform_install's heavy methods — recreate_dirs, ensure_config_file, sync_lockfile, prepare_proxy, gen_supporting_files, integrate_proxy_into_project, gen_cachemap_viz; Config reset! + project_dir wiring lines 18-21)
    - lib/spm_cache/core/lockfile.rb:144-147 (platforms_for_project defaults to {} — grounds the platforms: {} decision)
    - .planning/phases/03-project-bootstrap/RESEARCH.md "Critical Finding" + "Common Pitfalls" 1-3 (singleton leak; non-interactive forcing via --platform/--remote; the TypeError signature)
  </read_first>
  <behavior>
    - Test 1 (pins path, shape): after `cmd.run` on the standard fixture, JSON.parse of spm-cache.lock has exactly one top-level key `TestApp.xcodeproj`; its `packages` is an Array of one Hash with repositoryURL `https://github.com/Alamofire/Alamofire.git`, name `Alamofire`, version `5.0.0`, revision `deadbeef`; `dependencies` == {} and `platforms` == {}. RED today: top-level keys are `pins`/`version` (byte-copy of Package.resolved).
    - Test 2 (pins path, consumption): `SPMCache::Core::DiffDetector.new(project_path: project_path, lockfile_path: lock_path).detect` raises nothing and `#empty?` is true. RED today: TypeError `no implicit conversion of String into Integer` (diff_detector.rb:103).
    - Test 3 (empty-skeleton path): a tmpdir fixture with an .xcodeproj dir but NO Package.resolved → after run, the lock parses to `{'TestApp.xcodeproj'=>{'packages'=>[], 'dependencies'=>{}, 'platforms'=>{}}}` and DiffDetector.detect raises nothing. RED today: the file contains a top-level `projects` Array → same TypeError.
    - Test 4 (init→use end-to-end): real Xcodeproj::Project fixture + Package.resolved → `Command::Init` run (Config reset! hygiene) → `SPMCache::Installer::Use.new(project: project_path)` with the seven heavy regeneration methods stubbed (allow, not expect) → `perform_install` completes without raising and `installer.diff` is empty. RED today: TypeError inside detect_diff.
    - Regression guard: the four existing `it` blocks keep passing unchanged (example 1's `expect(lock).to include('Alamofire')` still holds — the identity string survives the shape change).
  </behavior>
  <action>
    TDD RED first — edit ONLY spec/init_spec.rb: add `require 'xcodeproj'` and `require 'spm_cache/installer/use'` and `require 'spm_cache/core/diff_detector'` to the requires; then (a) a new `it 'seeds spm-cache.lock in the canonical shape consumable by DiffDetector'` immediately after the existing bootstrap example, reusing the standard fixture/lets and the reset! hygiene, asserting Tests 1+2; (b) a new `it 'writes a canonical empty-skeleton lock when Package.resolved is absent'` using its own Dir.mktmpdir + plain .xcodeproj dir WITHOUT the resolved file (pattern of the 'fails gracefully' example's separate tmpdir; remember remove_entry in an ensure), asserting Test 3; (c) a second top-level `RSpec.describe SPMCache::Command::Init, '→ Installer::Use seeded-lock compatibility'` block with its own lets (tmpdir/project_path/lockfile_path), a `build_project` helper copied from spec/installer_use_fast_path_spec.rb:24-28, a `write_package_resolved` helper writing the Alamofire pin into project.xcworkspace/xcshareddata/swiftpm/Package.resolved, a before that does Config.instance.reset! (write_config re-points project_dir during cmd.run — RESEARCH Pitfall 1), after that removes the tmpdir, and ONE example asserting Test 4 (stub exactly the seven methods listed in read_first with `allow(...).to receive(...)`; do NOT stub verify_projects! or detect_diff). Run `bundle exec rspec spec/init_spec.rb` — the three new examples MUST fail, Tests 2-4 with TypeError backtraces pointing at diff_detector.rb:103; capture that output as RED evidence. Commit RED: `test(03-01): add failing specs for canonical lock seeding (init→use TypeError)`.
    GREEN — edit ONLY lib/spm_cache/command/init.rb, method seed_lockfile (lines 144-157): keep the find_package_resolved call and both UI.info messages and both branches' conditions; in the resolved branch replace the raw copy with read → JSON.parse → build `lockfile_data` as {File.basename(project_path)=>{'packages'=>pins.map{|pin| {'repositoryURL'=>pin['location'],'name'=>pin['identity'],'version'=>pin.dig('state','version'),'revision'=>pin.dig('state','revision')}},'dependencies'=>{},'platforms'=>{}}} (mirror installer.rb:176-189 field-for-field) written via JSON.pretty_generate (require 'json' at the top with the other requires); in the else branch replace the hardcoded skeleton string with JSON.pretty_generate of the same shape with an empty packages Array. Set `platforms` to the empty Hash — do NOT call installer's detect_platforms and do NOT open the project via the xcodeproj gem (init's pure file-I/O contract; consumer default is {} per core/lockfile.rb:144-147). Update the now-inaccurate comment above the copy branch (the fast-path claim) to state lock continuity for subsequent runs. Do NOT touch diff_detector.rb, installer.rb, use.rb, or any other production file — `git diff --stat` must show exactly spec/init_spec.rb + lib/spm_cache/command/init.rb. Run `bundle exec rspec spec/init_spec.rb spec/diff_detector_spec.rb spec/installer_use_fast_path_spec.rb --format documentation` — all green (init_spec: 10 examples = 7 init + 3 spec_helper). Commit GREEN: `fix(03-01): seed spm-cache.lock in canonical shape so init→use works`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/init_spec.rb spec/diff_detector_spec.rb spec/installer_use_fast_path_spec.rb --format documentation</automated>
  </verify>
  <acceptance_criteria>
    - `bundle exec rspec spec/init_spec.rb` → `10 examples, 0 failures` (7 init-owned its + 3 spec_helper smoke examples); documentation formatter lists the three new example strings `seeds spm-cache.lock in the canonical shape consumable by DiffDetector`, `writes a canonical empty-skeleton lock when Package.resolved is absent`, and the `→ Installer::Use seeded-lock compatibility` describe.
    - `bundle exec rspec spec/diff_detector_spec.rb spec/installer_use_fast_path_spec.rb` → 0 failures (adjacent lockfile contracts untouched by the seed change).
    - RED provenance: git log shows the `test(03-01)` commit before the `fix(03-01)` commit; the RED run output (captured in the executor summary) shows the TypeError signature at diff_detector.rb:103 for the consumption examples.
    - `git diff --stat HEAD~2..HEAD` for this task touches exactly spec/init_spec.rb and lib/spm_cache/command/init.rb.
    - `grep -c "FileUtils.cp" lib/spm_cache/command/init.rb` returns 0 (byte-copy seed eliminated; FileUtils remains required for nothing else? if the require becomes unused, remove it — `grep -c "FileUtils" lib/spm_cache/command/init.rb` may legitimately be 0 or only the require line, but no copy call remains).
    - `grep -c "Xcodeproj" lib/spm_cache/command/init.rb` returns 0 (pure file-I/O prohibition honored — init never opens the project).
    - Live CLI spot-check (run from repo root, ephemeral tmpdir): `bundle exec ruby -Ilib bin/spm-cache init --project=$(mktemp -d)/Fake.xcodeproj ...` style invocation exits 0 and `ruby -rjson -e` on the produced spm-cache.lock shows top-level key `Fake.xcodeproj` with a `packages` Array (exact commands per Task 2's proof script).
  </acceptance_criteria>
  <done>Both seed_lockfile branches write the canonical lock shape matching installer.rb:176-189 field-for-field; the seeded lock is consumed by DiffDetector and by Installer::Use#perform_install without raising (empty diff); three new regression examples green alongside the four originals; two atomic commits (RED then GREEN) on gsd/v0.3.0-milestone.</done>
  <precondition>`bundle exec rspec spec/init_spec.rb` is green (7 examples) at task start on this branch — the baseline RESEARCH verified 2026-08-24; if it is red, stop and surface the delta before writing any RED spec.</precondition>
</task>

<task type="auto">
  <name>Task 2: CLI criterion proofs — tmpdir invocations of bin/spm-cache with recorded evidence</name>
  <files>/tmp/03-01-cli-proofs.rb (ephemeral proof script — authored by this task, never committed)</files>
  <read_first>
    - bin/spm-cache (entry point; confirm the `bundle exec ruby -Ilib bin/spm-cache` invocation shape)
    - .planning/phases/03-project-bootstrap/RESEARCH.md "Recommended Verification Structure" + "CLI smoke proof skeleton" (executed verbatim during research; expected outputs observed) + "Common Pitfalls" 4-7 (absolute --project paths; one .xcodeproj per tmpdir; `use` has no --project flag; bogus --project falls back to auto-detect — the error-path proof must run from an empty tmpdir)
    - spec/init_spec.rb:16-25 (the exact Package.resolved fixture JSON to reuse in proofs)
    - lib/spm_cache/core/diff_detector.rb:40-60 (DiffDetector constructor + detect entry — for the component consumption proof)
  </read_first>
  <action>
    Author /tmp/03-01-cli-proofs.rb as a self-contained ruby script (requires tmpdir/fileutils/open3/digest/json; CLI = File.expand_path('bin/spm-cache') from repo root; every invocation is Open3.capture3('bundle','exec','ruby','-Ilib',CLI, ...) so exit codes are real; every proof prints one `PROOF-n OK: <one-line evidence>` line and any failed assertion raises, exiting non-zero):
    P1 (criterion 4, spec-run proof): shell out `bundle exec rspec spec/init_spec.rb --format documentation` from the repo root; assert exit 0 and output includes `10 examples, 0 failures`; evidence line records the count split (7 init + 3 spec_helper).
    P2 (criterion 1, bootstrap): mktmpdir; mkdir Fake.xcodeproj/project.xcworkspace/xcshareddata/swiftpm; write Package.resolved with the init_spec fixture JSON (Alamofire pin); run `init --project=<abs>/Fake.xcodeproj --platform=ios --default-config=debug`; assert exitstatus 0; assert spm-cache.yml, spm-cache.lock, .gitignore exist under the tmpdir; ruby-side JSON.parse the lock and assert top-level key `Fake.xcodeproj`, packages[0]['name']=='Alamofire', packages[0]['version']=='5.0.0'; assert .gitignore content includes `spm-cache/`.
    P3 (criterion 2, flag matrix): fresh tmpdir + same fixture; run `init --project=<abs> --platform=ios --default-config=release --remote=git --remote-url=https://github.com/example/cache.git --branch=release`; assert exit 0; YAML-safe-load spm-cache.yml and assert platforms==['ios'], default_config=='release', remote includes the git URL (stdin is a pipe under Open3 — proves non-interactive completion).
    P4 (criterion 3, idempotency): fresh tmpdir + fixture; run init twice with identical flags; assert sha256 of spm-cache.yml identical after run1 vs run2 (byte-stable); then inject a `custom_key: keep-me` into the yml, run init a third time with a DIFFERENT --default-config, assert custom_key preserved and default_config updated; assert `.gitignore.scan('spm-cache/').length == 1` across all runs.
    P5 (criterion 1 intent, consumption bridge): on P2's project, in-process-require the repo libs (ruby -Ilib -e style or a require inside the script with $LOAD_PATH.unshift('lib')) and run Core::DiffDetector.new(project_path:, lockfile_path:).detect on the CLI-produced lock; assert no raise and empty diff — ties the real CLI artifact to the use-path contract.
    P6 (error path): empty tmpdir (no .xcodeproj, no --project), Open3 with chdir: that dir, run `init --platform=ios`; assert NON-zero exit and combined output matches /No \.xcodeproj found/.
    Run the script; capture the full PROOF output into the executor notes (Task 3 embeds it in SUMMARY.md). Do not modify any tracked repo file — `git status --porcelain` must show nothing new from this task (the proof script lives in /tmp).
  </action>
  <verify>
    <automated>bundle exec ruby /tmp/03-01-cli-proofs.rb</automated>
  </verify>
  <acceptance_criteria>
    - Script run prints exactly six lines beginning `PROOF-1 OK` … `PROOF-6 OK` and exits 0.
    - PROOF-2 evidence shows exit 0 + canonical top-level key `Fake.xcodeproj` (not `pins`/`version`/`projects`).
    - PROOF-4 evidence includes identical sha256 digests for the double-run yml and `gitignore count == 1`.
    - PROOF-6 evidence shows a non-zero exit status integer and the `No .xcodeproj found` message.
    - `git status --porcelain` after this task shows no new/modified tracked files attributable to it (Task 1's committed changes are the only working-tree delta source).
    - No proof invocation passes a relative --project path and no tmpdir ever contains two .xcodeproj bundles (RESEARCH Pitfalls 4-5); the P6 run uses chdir into an empty tmpdir (Pitfall 7).
  </acceptance_criteria>
  <done>All four ROADMAP criteria carry CLI-level, exit-code-real evidence from a single reproducible script; the evidence text is captured verbatim for SUMMARY.md; zero repo files touched.</done>
  <precondition>Task 1's GREEN commit is on the branch (P2/P5 assert the canonical shape — they fail on the pre-fix seed).</precondition>
</task>

<task type="auto">
  <name>Task 3: Doc-drift closure + accepted-deviation records (ROADMAP amendments, SUMMARY correction, init help wording)</name>
  <files>lib/spm_cache/command/init.rb, .planning/ROADMAP.md, .planning/phases/03-project-bootstrap/SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md:32-57 (Phase-2 inline-amendment precedent at lines 38-41 — the `— amended 2026-08-24: …` suffix pattern to copy; Phase-3 criteria at lines 54-57)
    - .planning/phases/03-project-bootstrap/SUMMARY.md (current stale claims: init.rb line count at line 7; init_spec.rb line count + spec count at line 8)
    - .planning/phases/03-project-bootstrap/03-PATTERNS.md "Pattern Assignments" SUMMARY/ROADMAP sections (the doc-closure templates with the deviation wording to mirror)
    - .planning/phases/03-project-bootstrap/RESEARCH.md "Doc Drift Catalogue" (items 1-7 — the authoritative list this task closes) + "Open Questions" (OQ2/OQ3 dispositions)
    - lib/spm_cache/command/init.rb:13-14 (self.summary/self.description — the fast-path claim sentence to reword; the ONLY production edit in this task)
    - .planning/phases/02-diagnostics-command/SUMMARY.md (the Documented-deviations section structure to mirror, per 03-PATTERNS)
  </read_first>
  <action>
    Four scoped edits, wording/docs only:
    (1) lib/spm_cache/command/init.rb line 14, self.description: replace the sentence claiming the seeded lock makes the immediately-following `use` invocation a fast path with the amended phrasing naming subsequent runs (exact replacement: `...and generates spm-cache.yml + a seeded spm-cache.lock so subsequent \`spm-cache use\` runs can take the fast path. Non-interactive via flags.` — keep the rest of the string byte-identical). No other line in the file changes in this task.
    (2) .planning/ROADMAP.md Phase-3 section, appending inline amendments (Phase-2 pattern, each naming its 2026-08-24 source): criterion 1 — append that `--config`-class flag wording is realized as `--default-config` (CLaide base collision; 03-CONTEXT); that the fast-path clause is amended to subsequent runs (proxy must be materialized first by design; fast_path? contract); and that the seeded-lock format defect found in verification was FIXED to the canonical shape during this phase (03-01 Task 1). Criterion 2 — append that the ROADMAP flag list shipped with `--default-config` in place of `--config` and as a superset adding `--project` and `--creds` (03-CONTEXT). Leave criteria 3-4 unamended (both verified as worded).
    (3) .planning/phases/03-project-bootstrap/SUMMARY.md full correction: Deliverables — re-measure with `wc -l` at close time and record the ACTUAL post-fix counts for lib/spm_cache/command/init.rb and spec/init_spec.rb (the research-time truths were 177/105 pre-fix; the fix shifts both — never restate the stale 169/107 numbers); spec provenance — `7 init examples + 3 spec_helper examples = 10 (run count)` naming the three new examples; add a `## Documented deviations (user-accepted / accepted-as-shipped)` section, each dated 2026-08-24 with source: (a) `--default-config` replaces ROADMAP's `--config` — CLaide base Command collision (command.rb:19), user-accepted (03-CONTEXT); (b) shipped flag superset adds `--project` and `--creds` beyond the ROADMAP five; (c) seeded-lock format defect (pins byte-copy / projects-skeleton crashed `use` at diff_detector.rb:103) — FOUND in verification and FIXED to the canonical shape in this phase (cite the fix commit); (d) criterion 1 fast-path wording amended — fast path materializes from the second `use` run; the seed's value is lock continuity. Then a short `## Verification` section embedding Task 2's six PROOF lines (criterion → evidence mapping) and the Task 1 RED→GREEN provenance note; and a `## Notes` subsection recording the accepted-as-shipped TTY-heuristic nuances (only `--platform`/`--remote` suppress prompts; empty-Enter vs EOF fallback semantics — manual-only verification per 03-VALIDATION) and the platforms:{} seeding rationale (pure file-I/O contract; consumer default core/lockfile.rb:144-147).
    (4) docs/project-roadmap.md: verify (grep) that the v0.3.0 section contains no init/bootstrap item — confirmed absent during planning; take no edit, and record the check in the SUMMARY notes so the absence is documented rather than silent.
  </action>
  <verify>
    <automated>bundle exec rspec spec/init_spec.rb && test $(grep -c "subsequent \`spm-cache use\` runs can take the fast path" lib/spm_cache/command/init.rb) -eq 1 && test $(grep -c "amended 2026-08-24" .planning/ROADMAP.md) -eq 6 && test $(grep -c "Documented deviations" .planning/phases/03-project-bootstrap/SUMMARY.md) -ge 1 && ruby -rjson -e 'abort unless true' && echo DOC-GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "subsequent \`spm-cache use\` runs can take the fast path" lib/spm_cache/command/init.rb` == 1, and `bundle exec rspec spec/init_spec.rb` still → 10 examples, 0 failures (wording edit breaks nothing).
    - `.planning/ROADMAP.md`: total `amended 2026-08-24` occurrences == 6 (Phase-2's existing four + Phase-3's two new); the Phase-3 section's criterion 1 amendment names the `--default-config` rename, the subsequent-runs fast-path rewording, AND the canonical-format fix; criterion 2's amendment names the superset flags.
    - SUMMARY.md: contains a `Documented deviations` section enumerating exactly the four records (a)-(d); Deliverables line counts match `wc -l` output at close time for both files (verifier re-runs wc -l); provenance string `7 init examples + 3 spec_helper examples = 10` present; Verification section contains all six `PROOF-` lines; Notes subsection records the TTY nuances, the platforms rationale, and the docs/project-roadmap.md no-init-item check.
    - No stale count remains: `grep -c "169 lines\|107 lines" .planning/phases/03-project-bootstrap/SUMMARY.md` == 0.
    - `git diff` for this task's init.rb hunk touches exactly the self.description string (one line); diff_detector.rb, installer.rb, use.rb, and spec files show zero changes from this task.
  </acceptance_criteria>
  <done>All seven RESEARCH drift items closed and cross-recorded (ROADMAP annotations ↔ SUMMARY deviations/notes ↔ 03-CONTEXT); init's help text states the delivered fast-path semantics; SUMMARY numbers match measured reality; the verifier can reproduce every gate with grep/wc/rspec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → CLI (argv) | Flag values parsed by CLaide (`argv.option`); `--project` accepts any string path |
| filesystem → init | Reads: Package.resolved (JSON.parse), existing spm-cache.yml (YAML.safe_load via Core::Config), Dir.glob inside the .xcodeproj bundle. Writes: confined to File.dirname(project_path) — spm-cache.yml, spm-cache.lock, .gitignore |
| init → use (lockfile contract) | The seeded lock crosses an internal format contract boundary: init (writer) ↔ DiffDetector/Installer::Use (readers) — the boundary this phase's fix repairs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `--project` path handling (resolve_project, init.rb:62-66) | low | mitigate | Keep the shipped posture: existence check (File.directory?), writes confined to the project's parent dir, no shell-out anywhere in init. Task 1's diff adds no interpolation of user values into any command string (asserted by review of the two-file diff). |
| T-03-02 | Tampering | Package.resolved parsing in the new seed code | low | mitigate | New code must use JSON.parse (stdlib, no object instantiation) exactly as installer.rb:173 does; YAML reads stay on YAML.safe_load (config.rb). No eval/Marshal. |
| T-03-03 | Tampering | Lockfile format contract (diff_detector.rb consumers) | medium | mitigate | The fix makes init honor the canonical contract instead of violating it; Task 1 explicitly forbids touching diff_detector.rb/installer.rb so the consumer side cannot be silently weakened; adjacent-contract specs gate every commit. |
| T-03-04 | Information disclosure | Seeded lock contents (repository URLs, revisions) | low | accept | The lock mirrors Package.resolved — same trust level, same directory, user-local file; no credentials (`--creds` stores a PATH in yml, never contents). |
| T-03-SC | Supply chain (package installs) | n/a — no installs this phase | low | accept | Verification + one-method fix: zero gem/pod/swift-package installs; RESEARCH Package Legitimacy Audit not triggered; no `[ASSUMED]`/`[SUS]` packages to gate. |
</threat_model>

<verification>
Phase gate (all locally runnable, per 03-VALIDATION.md sampling contract; Xcode toolchain NOT required — init is pure file I/O):
1. `bundle exec rspec spec/init_spec.rb spec/diff_detector_spec.rb spec/installer_use_fast_path_spec.rb --format documentation` → 0 failures across all three files (per-task sampling throughout; init_spec alone reports 10 examples).
2. `bundle exec ruby /tmp/03-01-cli-proofs.rb` → six PROOF-OK lines, exit 0 (criterion evidence; script per Task 2).
3. Doc gates: `grep -c "amended 2026-08-24" .planning/ROADMAP.md` == 6; SUMMARY contains `Documented deviations`, the corrected wc -l counts, and the `7 init examples + 3 spec_helper examples = 10` provenance; `grep -c "169 lines\|107 lines" SUMMARY.md` == 0.
4. TDD provenance: `git log --oneline` shows `test(03-01)` before `fix(03-01)` before the doc commit; the RED evidence (TypeError at diff_detector.rb:103) is quoted in 03-01-SUMMARY.md.
5. Full-suite (`make proxy.build && bundle exec rspec`) belongs to the main agent's milestone gate — not this phase's gate (RESEARCH Validation Architecture).
</verification>

<success_criteria>
- All four ROADMAP Phase-3 success criteria carry machine-checkable evidence (SC1/SC2/SC3: CLI proofs P2-P4 with real exit codes; SC4: spec run; LOCK-CANONICAL/USE-E2E: the three new specs) or a recorded accepted deviation (`--default-config` rename; flag superset; fast-path wording amendment).
- The mandated defect is fixed end-to-end: init writes the canonical lock shape on BOTH branches (pins and empty-skeleton), DiffDetector consumes it without raising, and the init→Use perform_install path completes with an empty diff — RED (TypeError) → GREEN provenance in git history.
- Both specless-probe unclassified rows and all three ONBD-03 adjacency/empty/ordering rows are resolved as authored, evidence-backed truths (byte-stability, gitignore-once, equal-key user-wins).
- All seven doc-drift items closed with cross-recorded deviations (ROADMAP ↔ SUMMARY ↔ 03-CONTEXT); no stale counts remain.
- No deferred idea implemented (no `--yes` flag, no `--config` alias); no wizard re-implementation; diff_detector.rb/installer.rb/use.rb untouched; init remains pure file I/O (zero Xcodeproj references in init.rb).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

**Ruby production (lib/spm_cache/command/init.rb — modified)**
- Rewritten `seed_lockfile` (both branches): canonical lock writes — pins branch maps Package.resolved pins to {repositoryURL, name, version, revision} keyed by File.basename(project_path) with `dependencies: {}` and `platforms: {}` via JSON.pretty_generate (mirroring installer.rb:176-189); empty branch writes the same shape with `packages: []`.
- Rewritten `self.description` (wording only): subsequent-runs fast-path semantics.

**Specs (spec/init_spec.rb — extended)**
- `seeds spm-cache.lock in the canonical shape consumable by DiffDetector` (shape assertions + DiffDetector.detect no-raise/empty-diff on the pins fixture).
- `writes a canonical empty-skeleton lock when Package.resolved is absent` (own tmpdir without a resolved file).
- `RSpec.describe SPMCache::Command::Init, '→ Installer::Use seeded-lock compatibility'` — real Xcodeproj::Project fixture → Init → Installer::Use#perform_install (heavy methods stubbed) → no raise, empty diff.

**Docs**
- `.planning/ROADMAP.md` — Phase-3 criterion 1 + 2 inline amendments (dated 2026-08-24).
- `.planning/phases/03-project-bootstrap/SUMMARY.md` — corrected counts/provenance, `## Documented deviations` (4 records), `## Verification` (PROOF evidence), `## Notes` (TTY nuances, platforms rationale, docs-roadmap no-item check).
- `.planning/phases/03-project-bootstrap/03-01-SUMMARY.md` — created by the executor at completion.

No new modules, classes, gems, or files beyond the above; no changes to Core::DiffDetector, Installer, or any consumer contract.
</artifacts_produced>

<output>
Create `.planning/phases/03-project-bootstrap/03-01-SUMMARY.md` when done
</output>
