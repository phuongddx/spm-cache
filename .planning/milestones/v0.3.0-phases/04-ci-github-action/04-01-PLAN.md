---
phase: 04-ci-github-action
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/action_spec.rb
  - action/action.yml
  - action/README.md
  - .planning/ROADMAP.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/phases/04-ci-github-action/SUMMARY.md
autonomous: true
requirements: [ONBD-04]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low   # estimate-calibration: sample_count 0, factor 1 — derived, not self-rated

must_haves:
  truths:
    # ROADMAP Phase-4 criterion 1 (ONBD-04) — input surface, per 04-CONTEXT Input-surface decision (accepted superset 2026-08-24)
    - "SC1: `bundle exec rspec spec/action_spec.rb` proves the input surface: action/action.yml declares EXACTLY the six inputs command, backend, backend-url, branch, config, creds (ROADMAP's four-name list plus the accepted branch/creds superset, 04-CONTEXT), each carrying a description; defaults command=pull, backend=git, branch=main, config=debug, creds empty; the README Inputs table matches names/required/defaults row-for-row. No input is added, renamed, or removed (input surface locked 04-CONTEXT)."
    # ROADMAP Phase-4 criterion 2 (ONBD-04) — thin shell-out, per 04-CONTEXT Execution-shape decision
    - "SC2: the shell-out contract is machine-proven by the same spec: runs.using == composite with 4 steps and shell on every run step; step 2 runs `gem install spm-cache --no-document` (executable name matches gemspec spec.executables); the init step's emitted long-flags are a SUBSET of init.rb's own self.options (post-F1-fix: default-config, remote, remote-url, branch, creds — the inherited base `--config` at command.rb:19 is excluded by construction); the remote step emits only `--config`, which pull.rb and push.rb each define; the sync branch composes `remote pull` then `remote push` inside the action (the gem ships no `remote sync` subcommand — lib/spm_cache/command/remote/ contains exactly pull.rb and push.rb); zero caching/storage/auth logic exists in action.yml beyond CLI invocations (execution shape locked 04-CONTEXT — the only line changed is the F1 flag spelling)."
    # ROADMAP Phase-4 criterion 3 (ONBD-04) — accepted external-dependency deviation, per 04-CONTEXT Criterion-3 decision
    - "SC3: the locally provable remainder of criterion 3 is proven (YAML.safe_load_file clean; composite schema rules green), and the unreachable remainder (smoke test in the Action's own repo's CI) is RECORDED, not silently dropped: a dated ROADMAP criterion-3 amendment + a SUMMARY `Documented deviations` block naming the owner-action chain (gem push → verify `gem install spm-cache` → publish phuongddx/spm-cache-action → tag v1 → action-repo smoke workflow), including F2 (gem NOT on RubyGems as of 2026-08-24 — rubygems.org API 404, RESEARCH-verified) — user-accepted deviation, 04-CONTEXT."
    # Mandated defect fix (RESEARCH F1 — orchestrator resolved FIX)
    - "F1-FIX: action.yml line 55 passes the init-seeding config value under the flag init actually defines (`--default-config`, init.rb:19, read at init.rb:31, consumed by resolve_default_config at init.rb:78-87) instead of the silently-ignored inherited base `--config` (command.rb:19) — so `config: release` users get `default_config: release` seeded, not debug. Proven TDD: RED run of spec/action_spec.rb against the pre-fix action.yml fails exactly the init-flag cross-reference example (failure message names the emitted-but-undefined flag `config`); GREEN after the one-line rename reports 12 examples, 0 failures (9 action-owned + 3 spec_helper smoke). Remote steps are UNTOUCHED (pull/push define `--config` themselves)."
    # Specless-probe UNCLASSIFIED row (ONBD-04) — authored truth backed by the new spec
    - "PROBE-ONBD-04 (authored truth): the composite-schema and input-wiring invariants are machine-enforced, not assumed — spec/action_spec.rb asserts (a) strict-Psych parse with no aliases/classes, (b) name/description present, runs.using == composite, exactly 4 steps, `shell` on every run step (composite schema rule: required when run is set), (c) ruby-version pinned as a QUOTED string \"3.2\" satisfying gemspec required_ruby_version >= 3.1.0 (Pitfall 4), (d) every declared input reaches run scripts only through step env assignments (union of env context references == the six inputs), (e) no GitHub inputs-context macro expansion inside any run script body (injection safety, RESEARCH Pattern 1)."
    # Doc-drift closure (RESEARCH F4 catalogue + F3/F6/F7 notes + F2/gemspec checklist)
    - "DOC-CLOSURE: all RESEARCH doc items closed — (1) INTEGRATIONS.md line 92 reworded so the gem wrap claim no longer implies a `remote sync` subcommand (sync is pull+pull composed by the action; line 93's input-values mention is correct and untouched); (2) ROADMAP criteria 1-3 carry dated 2026-08-24 inline amendments (superset inputs; action-composed sync + F1 fixed; criterion-3 external deviation incl. F2) bringing the file-wide amendment count to 8; (3) action/README.md gains a Caveats section disclosing the F3 silent-green mode (tolerant init + warn-and-skip storage exits 0 while caching nothing), the F6 required-not-enforced gap (omitted backend-url yields the same green-with-warnings path), and the F7 git-backend push credential note [ASSUMED]; (4) phase SUMMARY gains the Documented deviations block + a Release checklist (owner actions) that includes the gemspec homepage placeholder fix (`https://github.com/your-org/spm-cache`, gemspec line 12) — recorded as a checklist item, the gemspec itself is NOT edited (surgical boundary; release hygiene, not ONBD-04); (5) the SUMMARY 'Validated: well-formed YAML' claim is re-pointed at spec/action_spec.rb as its machine check."
  artifacts:
    - .planning/phases/04-ci-github-action/04-01-PLAN.md
    - spec/action_spec.rb
    - action/action.yml
    - action/README.md
    - .planning/ROADMAP.md
    - .planning/codebase/INTEGRATIONS.md
    - .planning/phases/04-ci-github-action/SUMMARY.md
    - .planning/phases/04-ci-github-action/04-01-SUMMARY.md
  key_links:
    - "action.yml init-step ARGS construction (line 55) → init.rb self.options slice through `.concat(super)` (init.rb:15-24, base `--config` excluded) → argv.option('default-config') (init.rb:31) → resolve_default_config (init.rb:78-87) → write_config default_config key — the F1 repair link, proven RED→GREEN by the spec's emitted-minus-defined assertion"
    - "action.yml remote-step `case \"$COMMAND\"` → `spm-cache remote pull|push --config=` ↔ pull.rb:12-13 / push.rb:12-13 self.options — the criterion-2 shell-out link; sync branch = in-action composition of the two"
    - "action/README.md Inputs table ↔ action/action.yml inputs block — the drift-detection link, spec-enforced (README edits that desync the table go red)"
    - ".planning/ROADMAP.md Phase-4 amendments ↔ phase SUMMARY `Documented deviations` + `Release checklist` ↔ 04-CONTEXT accepted decisions — the verifier cross-reference chain (phase-2/3 pattern)"
  prohibitions:
    - requirement_id: ONBD-04
      category: transparency
      status: unresolved
      verification: unverified
      statement: "the Action MUST NOT leave its silent-green failure mode undocumented — a workflow that restores/saves nothing still exits 0 (tolerant init + Storage::Base warn-and-skip), so users MUST be told via the README that misconfiguration currently exits green with warnings and which log lines to check; the tolerant control flow itself is accepted as shipped (04-CONTEXT boundary — no control-flow changes)."
      reason: "Kept from adversarial prohibition recall (RESEARCH F3/F6 candidate space); descriptor-less and flagged unverified — verify-phase confirms the README Caveats section exists and names the silent-green mode plus the two Core::UI.warn markers ('No remote cache configured. Skipping …' / 'Configure remote cache …', storage/base.rb:22-24)."
    - requirement_id: ONBD-04
      category: safety
      status: unresolved
      verification: unverified
      statement: "user-supplied workflow input values MUST NOT be macro-expanded into run script bodies — every input reaches shell programs only through step env assignments (RESEARCH Pattern 1); the wrapper must never interpolate input contexts directly into script text, and this invariant must be machine-checked so future action edits cannot silently regress it."
      reason: "Kept from adversarial prohibition recall (injection candidate; canon-adjacent but no eslint/gsd-secure-phase consumer exists in this Ruby repo, so it is minted here rather than breadcrumb-only); descriptor-less (RSpec, not node-test/lint-rule kinds) and flagged unverified — verify-phase confirms the spec's injection-safety example is green and that env indirection covers all six inputs."
    - requirement_id: ONBD-04
      category: safety
      status: unresolved
      verification: unverified
      statement: "the Action MUST NOT be published/tagged v1 while step 2 cannot succeed — `gem install spm-cache` 404s until the owner runs gem build + gem push (F2), so publishing phuongddx/spm-cache-action or tagging v1 before the gem exists on RubyGems ships an action that fails on every runner; the release checklist must fix the order (gem push → verify install → publish action repo → tag v1 → action-repo smoke CI)."
      reason: "Kept from adversarial prohibition recall (publishing-order, RESEARCH Pitfall 5); descriptor-less and flagged unverified — verify-phase confirms the SUMMARY Release checklist records the ordered owner actions and the dated ROADMAP criterion-3 amendment references it."
---

<objective>
Verification-scoped closure of the ALREADY-SHIPPED composite Action (commit 9e35030: `action/action.yml` 86 lines + `action/README.md` 49 lines) PLUS one mandated defect fix: the init shell-out passes the config value under a flag `init` never reads (RESEARCH F1 — `--config` is the inherited base-command flag at command.rb:19, silently accepted and ignored; `init` defines and reads `--default-config`, init.rb:19/31/78-87), so `config: release` users get `default_config: debug` seeded. The fix is a one-line flag rename proven TDD-style: RED = the Wave-0 `spec/action_spec.rb` (04-VALIDATION) cross-referencing action.yml against the gem's real CLI sources, failing on current action.yml; GREEN = the rename. The second task proves nothing further in code (RESEARCH F5 already inventoried every locally provable check as PASS — the spec encodes that inventory as regression net) and closes the doc set: criterion-3 external-deviation records (publication + F2 RubyGems absence → release checklist), ROADMAP dated amendments, INTEGRATIONS.md sync-wording drift, README hardening notes (F3/F6/F7). NO re-implementation, NO input-surface or execution-shape changes (locked 04-CONTEXT), NO control-flow edits to action.yml.

Purpose: ONBD-04 is marked Complete in REQUIREMENTS.md but was never GSD-verified; the one genuine defect research found (F1) invalidates criterion 2's "commands matching the gem's actual CLI" for the init step, and criterion 3 is unreachable from this repo without owner actions that must be recorded, not dropped. The phase must leave machine-checkable proofs for criteria 1-2, an explicit accepted-deviation record for criterion 3, and zero unexplained gaps for the verifier.

Output: spec/action_spec.rb (9 examples, RED→GREEN provenance for F1); action.yml one-line flag fix; README Caveats; ROADMAP amendments (3, dated); INTEGRATIONS.md one-line rewording; SUMMARY deviations + release checklist; 04-01-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ci-github-action/04-CONTEXT.md
@.planning/phases/04-ci-github-action/RESEARCH.md
@.planning/phases/04-ci-github-action/04-PATTERNS.md
@.planning/phases/04-ci-github-action/04-VALIDATION.md
@action/action.yml
@action/README.md
@lib/spm_cache/command/init.rb
@spec/spec_helper.rb
</context>

<open_question_resolutions>
Decisions routed from RESEARCH.md Open Questions 1-4, resolved at plan time under the acceptance hierarchy (04-CONTEXT user decisions 2026-08-24 > orchestrator mandate > doc amendment):

1. **F1 disposition (OQ1) → FIX, and it is the orchestrator-mandated defect.** The orchestrator resolved FIX. CONTEXT freezes the input *surface* and *execution *shape* — not the flag spelling; "shell-out commands matching the gem's actual CLI" is exactly the cross-check this phase exists to perform, and the fix makes the emitted flag set a subset of init.rb's own options. Type: TDD — Task 1. No other line of action.yml changes; the remote steps keep `--config` (pull.rb/push.rb define it).
2. **Gemspec homepage placeholder (OQ2) → release-checklist item, NOT an edit.** The placeholder string on gemspec line 12 is release hygiene outside ONBD-04; editing it here would breach the surgical boundary this phase is bound to. Recorded verbatim in the SUMMARY Release checklist (Task 2).
3. **README hardening notes (OQ3) → all three (F3 silent-green, F6 required-not-enforced, F7 git-push credentials), one Caveats section.** Claude's discretion covers doc phrasing (04-CONTEXT); 3-6 added lines, no behavior change. F7 is prefixed [ASSUMED] per the research assumption log.
4. **actionlint (OQ4) → not adopted.** Not installed locally; Psych-based spec assertions cover this phase's needs. No environment dependency added.

Deferred (2 items) — neither appears in any task: gem version-pinning input (rejected 2026-08-24; the unpinned `gem install` stays, recorded as an accepted deviation); requiring user-authored spm-cache.yml instead of init-seeding (rejected 2026-08-24; init stays inside the Action).
</open_question_resolutions>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: spec/action_spec.rb RED (F1 flag mismatch) → GREEN (one-line action.yml fix)</name>
  <files>spec/action_spec.rb, action/action.yml</files>
  <read_first>
    - action/action.yml (full file — the 6-input block at 8-31; step 2 `gem install spm-cache --no-document` at 41-43; the init step 45-65 whose ARGS construction at line 55 is the F1 defect site, env map 47-52, backend if/elif with ::error exits 56-64, tolerant `|| true` at 65; the remote step 67-86 whose case branch composes sync as pull-then-push at 78-81)
    - lib/spm_cache/command/init.rb:15-36 (self.options — the seven OWN options ending `.concat(super)`; initialize reads argv.option('default-config') at line 31 — never @config) and init.rb:78-87 (resolve_default_config falling to 'debug' when nil and non-interactive — the silent-misconfig consequence)
    - lib/spm_cache/command.rb:15-25 (base Command self.options defining `--config=CONFIG` at line 19 — the silently-accepted inherited flag that makes F1 invisible to CLaide; this is why the spec must slice ONLY init.rb's own options block)
    - lib/spm_cache/command/remote/pull.rb:1-25 and push.rb:1-25 (both define `--config=CONFIG` in their OWN options at lines 12-13 — why the remote step is correct and must stay untouched)
    - spec/doctor_spec.rb:1-30 (file-header + describe-string + let-block conventions to mirror)
    - spec/spec_helper.rb (full — requires spm_cache/main and defines 3 top-level smoke examples; EVERY spec run of a single file reports +3 examples, hence the 12-example expectation for 9 action-owned examples)
    - action/README.md:36-49 (Inputs table rows at 38-45 — shape `| `name` | desc | yes/no | `default` or — |` — and the Design section)
    - spm_cache.gemspec:12,24-27 (homepage placeholder — context only; executables ["spm-cache"]; required_ruby_version ">= 3.1.0")
    - .planning/phases/04-ci-github-action/RESEARCH.md — "Code Examples" (the drafted spec skeleton this task completes: its 4th example as drafted is VACUOUS — it asserts init.rb contains flag strings without checking what action.yml emits; this task's behavior block replaces it with the emitted-minus-defined cross-reference), "Verification Findings F1/F5" (defect evidence; the already-passing inventory the spec encodes), "Pattern 1"/"Pitfalls 1-4" (env indirection; CLAide silent acceptance; composite shell rule; quoted ruby-version)
    - .planning/phases/04-ci-github-action/04-PATTERNS.md — "spec/action_spec.rb" section (conventions) and "action/action.yml (config, one-line fix)" section (the exact BEFORE/AFTER pair for line 55)
    - .planning/phases/04-ci-github-action/04-VALIDATION.md (Wave 0 = this spec; per-task sampling command)
  </read_first>
  <behavior>
    - Example 1 'parses as strict YAML and satisfies composite schema rules': YAML.safe_load_file("action/action.yml", permitted_classes: [], aliases: false) returns without raising; parsed["name"] and parsed["description"] are non-empty strings; parsed.dig("runs","using") == "composite"; parsed.dig("runs","steps").size == 4; every step that has a "run" key also has a "shell" key (composite rule: shell required when run is set).
    - Example 2 'declares the accepted input surface with per-input metadata': parsed.fetch("inputs").keys.sort == %w[backend backend-url branch command config creds].sort; every input hash has a non-empty "description"; defaults: command "pull", backend "git", branch "main", config "debug", creds "".
    - Example 3 'never expands GitHub input contexts inside run script bodies' (injection safety, Pattern 1): for every step with "run", expect(run).not_to include("${{ inputs.") — the literal macro token.
    - Example 4 'routes every declared input through step env assignments': across all steps, collect env values matching the inputs-context reference; the set of referenced input names equals the six declared inputs exactly (each input is consumed via env, none orphaned).
    - Example 5 'init step emits only flags the gem's init command defines' (THE RED EXAMPLE): locate the step whose "run" includes "spm-cache init"; scan its run text for long-flag tokens (--name= pattern), yielding pre-fix {config, remote, remote-url, branch, creds}; slice lib/spm_cache/command/init.rb from "def self.options" through ".concat(super)" and collect its own option names {project, platform, default-config, remote, remote-url, branch, creds}; expect(emitted - defined).to be_empty. RED today: the difference is ["config"] — init.rb's own options do not define it (only the base command does, outside the slice). GREEN after the fix: difference is [].
    - Example 6 'remote step emits only flags remote pull/push define and composes sync in-action': the remote step's emitted long-flags {config} minus the union of pull.rb and push.rb own option names {config} is empty; the run contains 'remote pull --config=' and 'remote push --config=' invocations; the run contains NO 'remote sync' invocation string; Dir entries of lib/spm_cache/command/remote/ (*.rb) == ["pull.rb", "push.rb"] — proving sync is action-composed, not a gem subcommand.
    - Example 7 'pins a quoted ruby version satisfying the gemspec': the setup step's with["ruby-version"] IS a String (not Float — Pitfall 4) equal to "3.2"; parse Gem::Specification or gemspec text required_ruby_version ">= 3.1.0" and assert Gem::Version.new("3.2") satisfies it (simplest: assert the gemspec source includes '>= 3.1.0' and compare Gem::Version.new("3.2") >= Gem::Version.new("3.1.0")).
    - Example 8 'installs and invokes the executable the gemspec ships': some step run includes "gem install spm-cache --no-document"; every "spm-cache " invocation prefix in run bodies matches the gemspec spec.executables name; no step invokes any other gem binary.
    - Example 9 'README input table matches the action inputs': parse action/README.md rows between "## Inputs" and the next "##" heading; assert the row input-name set equals the six action inputs; per row, required yes/no maps to input "required" true/false; the default cell maps `value` → input default string, and the em-dash cell → input default nil or "".
    - Regression guard: the 3 spec_helper smoke examples keep passing (unchanged file); no existing spec file is modified.
  </behavior>
  <action>
    RED first — create spec/action_spec.rb (NEW file) following 04-PATTERNS conventions: frozen_string_literal magic comment, require "spec_helper", top-level `RSpec.describe "action/action.yml"` with `let(:action) { YAML.safe_load_file("action/action.yml", permitted_classes: [], aliases: false) }`; implement the nine examples from the behavior block; helper lets for init_step/remote_step (steps found by run-content), init_defined_flags (init.rb self.options slice scanned for its own option-name literals), remote_defined_flags (pull.rb + push.rb options slices), readme_rows (README table parse). Example 5 is the cross-reference: emitted flags from the init step's run script minus init.rb's OWN defined flags must be empty — the slice ends at concat(super) so the base-command option cannot leak in and mask the defect (Pitfall 1). Do NOT check whether init.rb merely contains flag substrings — assert on what action.yml EMITS (the research skeleton's 4th example was vacuous; this replaces it). Run `bundle exec rspec spec/action_spec.rb --format documentation`: expect 12 examples, EXACTLY 1 failure — Example 5, failure output naming the emitted-but-undefined flag "config". Capture the RED output verbatim for the executor summary; commit spec only: `test(04-01): add action_spec — RED: init step emits flag init does not define (F1)`.
    GREEN — edit action/action.yml line 55 ONLY: apply the exact BEFORE/AFTER replacement from 04-PATTERNS "action/action.yml (config, one-line fix)" — rename the config flag on the ARGS line to the spelling init.rb:19 defines (`--default-config=`, value still `${CONFIG}` from the unchanged `config` input, per the 04-CONTEXT Input-surface decision). Touch nothing else: the env maps, backend if/elif, tolerant `|| true`, remote step, and all other lines stay byte-identical (execution shape locked 04-CONTEXT). Run the spec again: 12 examples, 0 failures. Commit: `fix(04-01): pass --default-config to spm-cache init (F1) — --config is the inherited base flag, silently ignored`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/action_spec.rb --format documentation && test $(grep -c -- '--default-config=' action/action.yml) -eq 1 && test $(grep -c 'ARGS="--config=' action/action.yml) -eq 0 && test $(grep -c 'remote "$COMMAND" --config="$CONFIG"' action/action.yml) -eq 1 && ruby -ryaml -e 'YAML.safe_load_file("action/action.yml", permitted_classes: [], aliases: false); puts "safe_load ok"' && echo TASK1-GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - RED provenance: git log shows the `test(04-01)` commit BEFORE the `fix(04-01)` commit; the captured RED run shows `12 examples, 1 failure` with the failure in the init-flag example naming `config` (emitted-but-undefined), all other 8 action examples + 3 smoke green pre-fix (RESEARCH F5 inventory confirmed by execution).
    - GREEN: `bundle exec rspec spec/action_spec.rb` → `12 examples, 0 failures` (9 action-owned + 3 spec_helper smoke); documentation formatter lists the nine example strings from the behavior block.
    - `grep -c -- '--default-config=' action/action.yml` == 1 and `grep -c 'ARGS="--config=' action/action.yml` == 0 (the pre-fix ARGS spelling is gone; note `ARGS="--default-config=` does not match that pattern — the gate is precise).
    - The remote step is untouched: `grep -c 'remote "$COMMAND" --config="$CONFIG"' action/action.yml` == 1; `git diff` for the GREEN commit touches EXACTLY one line in action/action.yml (line 55) and nothing else; `git diff --stat` for the task's two commits together touches exactly spec/action_spec.rb (new) and action/action.yml (1 line).
    - `ruby -ryaml` safe_load smoke prints `safe_load ok` (YAML validity re-proven post-edit).
    - The spec slices init.rb's options from `def self.options` through `.concat(super)` — verify by reading the committed spec: the extraction must not scan command.rb or init.rb outside the options block (a spec that greps the whole file could match the base flag inside `--default-config` substrings or mask future regressions; the slice construction is load-bearing).
  </acceptance_criteria>
  <done>Every flag the Action's init step emits exists in init.rb's own options; config: release seeds default_config release; the nine schema/wiring/cross-reference examples are green alongside the 3 smoke examples; two atomic commits (RED then GREEN) on gsd/v0.3.0-milestone; action.yml changed by exactly one line.</done>
  <precondition>The F1 defect is present at task start: `grep -n 'ARGS="--config=' action/action.yml` returns line 55 — if it returns nothing, the fix already landed; stop and surface the delta before writing the spec. `bundle exec rspec spec/spec_helper.rb` is green (3 examples) as the baseline bundle check.</precondition>
</task>

<task type="auto">
  <name>Task 2: Doc-drift closure + criterion-3 deviation records (ROADMAP amendments, INTEGRATIONS rewording, README caveats, SUMMARY deviations + release checklist)</name>
  <files>action/README.md, .planning/codebase/INTEGRATIONS.md, .planning/ROADMAP.md, .planning/phases/04-ci-github-action/SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md:63-71 (Phase-4 section — criteria at lines 69-71, currently unamended) and the amendment precedents at 38-41 (Phase 2) / 54-55 (Phase 3) — the inline `— amended 2026-08-24: … (source)` suffix pattern; note the file currently contains 5 occurrences of `amended 2026-08-24`
    - .planning/codebase/INTEGRATIONS.md:88-93 (line 92 is the drift — the wrap sentence implying the gem runs a sync subcommand; line 93's parenthetical listing the command INPUT's pull/push/sync values is CORRECT and must not be touched)
    - action/README.md (full — insert point for the Caveats section is between the Inputs table and `## Design`; the Note blockquote at line 5 already covers the publish-to-own-repo mechanics)
    - .planning/phases/04-ci-github-action/SUMMARY.md (full — current Deliverables/Note/Commits; the Note's "Validated: well-formed YAML" claim gets its machine-check pointer)
    - .planning/phases/04-ci-github-action/RESEARCH.md — F2 (RubyGems 404 evidence, publication prerequisite), F3 (silent-green chain: tolerant init + Storage::Base warn-and-skip with the two warning strings at storage/base.rb:22-24), F4 (doc-drift catalogue — the authoritative item list this task closes), F6 (required-true not runner-enforced; omitted backend-url → empty remote-url → same green-with-warnings path), F7 ([ASSUMED] git-backend push credentials — checkout token is workflow-repo-scoped), Pitfall 5 (publishing order), "Open Questions" 2-3 (dispositions in this plan's open_question_resolutions)
    - .planning/phases/02-diagnostics-command/SUMMARY.md (the Documented-deviations section structure to mirror, per house pattern)
    - spm_cache.gemspec:12 (the homepage placeholder string to record verbatim in the checklist — NOT edited)
    - .planning/phases/04-ci-github-action/04-CONTEXT.md (the three decision sections the amendments cite)
  </read_first>
  <action>
    Four scoped edits, wording/docs only — zero code, zero YAML:
    (1) action/README.md: insert a `## Caveats` section (4-6 lines) between the Inputs table and `## Design`, covering: (a) the Action requires an `.xcodeproj` at the repository root — `spm-cache init` auto-detects in cwd and the tolerant init step stays green even when seeding fails (nested-project repos seed nothing); (b) misconfiguration currently exits GREEN with warnings — an unsupported/empty backend-url leaves no remote configured and pull/push warn-and-skip with exit 0, so check logs for the "No remote cache configured. Skipping …" warning lines instead of trusting the green checkmark; note that `required: true` on backend-url is not enforced by GitHub runners; (c) [ASSUMED] pushing to a git backend from a runner needs credentials for the CACHE repository — the workflow's checkout token is scoped to the workflow repository, so configure a PAT or deploy key. No other README line changes (the usage example and input table are verified accurate by the spec).
    (2) .planning/codebase/INTEGRATIONS.md line 92 ONLY: reword the wrap sentence by applying the exact replacement quoted verbatim in 04-PATTERNS ".planning/codebase/INTEGRATIONS.md" section (the Fix blockquote) — the gem-invocation list becomes `spm-cache init` + `spm-cache remote pull/push`, followed by that section's trailing clause clarifying `sync` (an action-side composition, not a gem subcommand). Line 93 stays byte-identical.
    (3) .planning/ROADMAP.md Phase-4 criteria (lines 69-71), three dated inline amendments in the Phase-2/3 pattern, each naming its source: criterion 1 — input surface shipped as the accepted superset adding `branch` and `creds` (mirrors init's remote flags; 04-CONTEXT); criterion 2 — `sync` is pull-then-push composed inside the action (the gem has no `remote sync` subcommand) and the init flag defect found in verification (`--config` vs the gem's `--default-config`, RESEARCH F1) was FIXED in 04-01 Task 1; criterion 3 — accepted external-dependency deviation: smoke-testing in the Action's own repo's CI requires publishing to `phuongddx/spm-cache-action` and the gem existing on RubyGems (unpublished as of 2026-08-24, RESEARCH F2); both recorded as release-checkl…
    (4) .planning/phases/04-ci-github-action/SUMMARY.md: append two sections — `## Documented deviations (user-accepted / accepted-as-shipped)`, dated 2026-08-24 with sources: (a) criterion-3 external deviation — own-repo CI smoke test unreachable from this repo; owner-action chain recorded (gem push → verify install → publish action repo → tag v1 → action-repo smoke workflow); includes F2 (gem not on RubyGems: rubygems.org API 404 probed 2026-08-24); (b) unpinned `gem install spm-cache` — pinning rejected 2026-08-24 (04-CONTEXT), semver discipline is the mitigation; (c) silent-green misconfiguration mode kept as shipped (tolerant init + warn-and-skip storage) — disclosed in README Caveats instead of changed (04-CONTEXT verification boundary). And `## Release checklist (owner actions, external)` numbered: 1. replace the gemspec homepage placeholder `https://github.com/your-org/spm-cache` (gemspec line 12) with the real repo URL; 2. `gem build` + `gem push`; 3. verify `gem install spm-cache` resolves on a clean machine (re-probe the RubyGems API 404 → 200); 4. publish action/ contents to phuongddx/spm-cache-action; 5. tag v1; 6. add the action-repo smoke workflow and run it (completes criterion 3). Also update the existing Note line: the "Validated: well-formed YAML" claim is now backed by spec/action_spec.rb (9 examples) rather than an ad-hoc parse, and record the F1 fix under Commits-style provenance (the test/fix commit pair from Task 1).
    Do NOT edit spm_cache.gemspec (the homepage fix is checklist item 1, not this phase); do NOT touch action/action.yml (Task 1 owns the only code-adjacent edit); do NOT amend ROADMAP criteria outside Phase 4.
  </action>
  <verify>
    <automated>bundle exec rspec spec/action_spec.rb && test $(grep -c "amended 2026-08-24" .planning/ROADMAP.md) -eq 8 && test $(grep -c "composed by the action" .planning/codebase/INTEGRATIONS.md) -eq 1 && test $(grep -c "remote pull/push/sync" .planning/codebase/INTEGRATIONS.md) -eq 0 && test $(grep -c "## Caveats" action/README.md) -eq 1 && test $(grep -c "Documented deviations" .planning/phases/04-ci-github-action/SUMMARY.md) -ge 1 && test $(grep -c "Release checklist" .planning/phases/04-ci-github-action/SUMMARY.md) -ge 1 && test $(grep -c "your-org" .planning/phases/04-ci-github-action/SUMMARY.md) -ge 1 && git diff --stat -- spm_cache.gemspec action/action.yml | wc -l | grep -qx 0 && echo TASK2-GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "amended 2026-08-24" .planning/ROADMAP.md` == 8 (5 pre-existing + exactly 3 new, one per Phase-4 criterion; no other phase's text altered).
    - `grep -c "composed by the action" .planning/codebase/INTEGRATIONS.md` == 1 and `grep -c "remote pull/push/sync" .planning/codebase/INTEGRATIONS.md` == 0 (line 92 drift closed; the line-93 input-values parenthetical survives — its `(pull/push/sync)` text does not match the grepped phrase `remote pull/push/sync`).
    - `grep -c "## Caveats" action/README.md` == 1; the section names the `.xcodeproj`-at-repo-root requirement, the green-with-warnings behavior, and the git-backend credential note; README delta is the inserted section only (≤ 8 added lines, zero modified/removed lines — `git diff` shows one insertion hunk).
    - SUMMARY.md contains `Documented deviations` (records a-c) and `Release checklist` (items 1-6, item 1 quoting the homepage placeholder — hence the `your-org` grep ≥ 1); the Note points the YAML-validity claim at spec/action_spec.rb.
    - `git diff --stat -- spm_cache.gemspec action/action.yml` (across this task's commit) is EMPTY — gemspec untouched, action.yml owned solely by Task 1.
    - `bundle exec rspec spec/action_spec.rb` still → 12 examples, 0 failures after the doc edits (README Inputs table untouched by the Caveats insertion — example 9 guards it).
  </acceptance_criteria>
  <done>All RESEARCH F4 catalogue items dispositioned (SUMMARY numeric claims already accurate — now spec-backed; omission closed via deviations block; INTEGRATIONS reworded; ROADMAP amended; README checked no-drift), F3/F6/F7 disclosed in README, F2 + publication chain recorded as the criterion-3 deviation and ordered release checklist, gemspec homepage recorded-not-edited; cross-reference chain ROADMAP ↔ SUMMARY ↔ 04-CONTEXT complete for the verifier.</done>
  <precondition>Task 1's GREEN commit is on the branch (the README-table spec example and the F1 fix are in place; the SUMMARY provenance line cites the test/fix pair).</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow `with:` → composite inputs | User-supplied input values (command, backend, backend-url, branch, config, creds) cross into runner shell programs — the injection surface |
| inputs → run scripts | Macro-expansion boundary: input contexts expanded before shell execution (mitigated by env indirection — Pattern 1, spec-enforced) |
| runner → rubygems.org | Unpinned `gem install spm-cache` pulls the latest published release (supply chain; currently blocked by F2 — nothing published) |
| action.yml ↔ gem CLI flag contract | The shell-out spelling boundary this phase repairs (F1): emitted flags must be a subset of the invoked command's own options |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering (script injection) | run-script bodies consuming user inputs (action.yml steps 3-4) | high | mitigate | Keep the shipped env-indirection pattern for all six inputs and machine-enforce it: spec example 3 fails on any inputs-context expansion inside a run body; spec example 4 fails if an input stops being routed through env. No new expansion sites — Task 1's edit changes a flag name only. |
| T-04-SC | Supply chain (package installs) | `gem install spm-cache --no-document` (action.yml:43) | medium | accept | Unpinned install is a LOCKED user decision (pinning rejected 2026-08-24, 04-Context) — semver discipline is the mitigation; recorded as SUMMARY deviation (b). No installs occur IN this phase (gate vacuous, RESEARCH audit table empty); F2 means nothing can execute pre-publish anyway. |
| T-04-02 | Repudiation (silent success) | tolerant init (`|| true`, action.yml:65) × Storage::Base warn-and-skip (storage/base.rb:22-24) | medium | mitigate | Behavior accepted as shipped (04-CONTEXT boundary — no control-flow changes); the integrity gap is closed by disclosure: README Caveats (b) documents green-with-warnings and the log markers; F6's non-enforced `required: true` noted in the same section; prohibition 1 carries the must-not-stay-undocumented invariant to verify-phase. |
| T-04-03 | Tampering (misconfiguration) | F1 flag mismatch — config value silently ignored by init | low | mitigate | FIXED this phase: flag renamed to the option init defines; spec example 5 is the permanent regression net (any future emitted-but-undefined flag goes red); `config: release` now seeds correctly. |
| T-04-04 | Information disclosure | creds input (S3 credentials PATH) flowing through env | low | accept | Value is a file path, never secret contents; env indirection keeps it out of script text; gem stores the path in spm-cache.yml (user-local). No change warranted. |

ASVS L1 (security_asvs_level 1, block_on high): no high-severity threat remains unmitigated at plan close — T-04-01 (the only high) is mitigated by the shipped pattern + new spec enforcement.
</threat_model>

<verification>
Phase gate (all locally runnable, per 04-VALIDATION.md sampling contract; no runner, no network, no Xcode toolchain needed):
1. `bundle exec rspec spec/action_spec.rb --format documentation` → `12 examples, 0 failures` (9 action-owned + 3 spec_helper smoke); the nine example strings match the Task 1 behavior block.
2. YAML validity re-proof: `ruby -ryaml -e 'YAML.safe_load_file("action/action.yml", permitted_classes: [], aliases: false); puts "safe_load ok"'` (RESEARCH Pattern 2 command, re-runnable by the verifier).
3. TDD provenance: `git log --oneline` shows `test(04-01)` before `fix(04-01)` before the docs commit; the RED evidence (1 failure naming the emitted-but-undefined flag `config`) is quoted in 04-01-SUMMARY.md.
4. Doc gates: `grep -c "amended 2026-08-24" .planning/ROADMAP.md` == 8; `grep -c "composed by the action" .planning/codebase/INTEGRATIONS.md` == 1 with the drift phrase gone; `grep -c "## Caveats" action/README.md` == 1; SUMMARY contains `Documented deviations` (a-c) + `Release checklist` (1-6 incl. the homepage placeholder).
5. Surgical boundary: phase-wide `git diff --stat` touches exactly spec/action_spec.rb (new), action/action.yml (1 line), action/README.md (one insertion hunk), .planning/ROADMAP.md, .planning/codebase/INTEGRATIONS.md, .planning/phases/04-ci-github-action/SUMMARY.md — nothing else; spm_cache.gemspec and all lib/ sources untouched.
6. Full suite (`make proxy.build && bundle exec rspec`) belongs to the main agent's milestone gate, not this phase's gate — sibling phase 5 executes concurrently on this branch (04-VALIDATION per-wave sampling is satisfied by the per-task action_spec runs; the spec is purely additive + a one-line YAML string rename with zero surface in other suites).
</verification>

<success_criteria>
- ROADMAP criteria 1-2 carry machine-checkable evidence (SC1/SC2 via the nine spec examples: input surface + metadata, composite schema, env-indirection wiring, init/remote flag cross-references, sync composition, ruby pin, executable match, README parity); criterion 3's locally provable remainder is proven and its unreachable remainder is an explicit user-accepted deviation record with an ordered release checklist (SC3) — never a silent drop.
- The mandated F1 defect is fixed end-to-end with RED→GREEN provenance: init's emitted flags ⊆ init.rb's own options; `config: release` seeds `default_config: release`; remote steps untouched.
- The specless-probe unclassified row (ONBD-04) is resolved as an authored truth (PROBE-ONBD-04) backed by the new spec's composite-schema + input-wiring assertions; all three prohibitions are authored, descriptor-less, and flagged unverified for verify-phase confirmation.
- Every RESEARCH doc item dispositioned exactly as enumerated: F4 INTEGRATIONS sync wording reworded; README hardening notes (F3 silent-green, F6 required-not-enforced, F7 git-push credentials) added; gemspec homepage placeholder recorded in the release checklist (not edited); ROADMAP criteria 1-3 amended dated 2026-08-24.
- No deferred idea implemented (no version-pinning input; no user-authored-yml requirement; no control-flow change to the tolerant init or the unpinned install); no re-implementation of any caching logic in the action; execution shape and input surface byte-stable except the single flag rename.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

**Specs (spec/action_spec.rb — NEW, Wave 0 per 04-VALIDATION)**
- Nine examples turning RESEARCH F5's passing inventory into a permanent regression net: strict-Psych parse + composite schema (using/shell/steps); exact input surface with per-input metadata; injection safety (no inputs-context macro inside run bodies); env-routing wiring for all six inputs; init emitted-flags ⊆ init.rb own options (the F1 net, RED first); remote emitted-flags ⊆ pull∪push options + sync-composed-in-action proof (no `remote sync` string; remote/ dir contains exactly pull.rb/push.rb); quoted ruby pin satisfying the gemspec; gemspec-executable match; README↔action.yml input-table parity.

**Action (action/action.yml — one line)**
- Line 55 flag rename only: the init config flag now matches the option init defines and reads (`--default-config=`), value still sourced from the unchanged `config` input.

**Docs**
- action/README.md — inserted `## Caveats` (root-level .xcodeproj requirement; green-with-warnings disclosure incl. the warn marker lines and the `required: true` non-enforcement note; [ASSUMED] git-backend push credential guidance).
- .planning/codebase/INTEGRATIONS.md — line 92 reworded: gem wrap is `remote pull/push`; sync is pull+push composed by the action.
- .planning/ROADMAP.md — Phase-4 criteria 1-3 dated inline amendments (superset inputs; action-composed sync + F1 fixed; criterion-3 external deviation incl. F2).
- .planning/phases/04-ci-github-action/SUMMARY.md — `Documented deviations` (a external-deviation/F2, b unpinned install, c silent-green kept + disclosed) + `Release checklist (owner actions, external)` (homepage placeholder → gem push → verify install → publish action repo → tag v1 → action-repo smoke CI) + spec-backed Note.
- .planning/phases/04-ci-github-action/04-01-SUMMARY.md — created by the executor at completion.

No new gems, modules, workflow files, or code beyond the above; spm_cache.gemspec and all lib/ sources untouched.
</artifacts_produced>

<output>
Create `.planning/phases/04-ci-github-action/04-01-SUMMARY.md` when done
</output>
