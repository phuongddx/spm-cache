---
phase: 05-auto-sync-watcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/spm_cache/core/watcher.rb
  - spec/watch_signals_spec.rb
  - spec/watch_loop_spec.rb
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - AGENTS.md
  - .planning/research/SUMMARY.md
  - README.md
  - .planning/codebase/CONCERNS.md
  - docs/superpowers/specs/2026-08-10-v0.3.0-watch-init-doctor-design.md
  - .planning/phases/05-auto-sync-watcher/SUMMARY.md
  - .planning/phases/05-auto-sync-watcher/05-01-SUMMARY.md
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTO-05]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SIGTERM delivered to a running watch loop exits 0 with '[watch] stopped.' (was 143 pre-fix) — subprocess spec proves it"
    - "SIGINT and SIGTERM each flush a pending change before exiting 0 — a marker-recording installer shows 2 installs, not 1 (drop was the pre-fix behavior)"
    - "A watcher whose regeneration rewrites project.pbxproj (unconditional project.save, installer.rb:468) never re-triggers itself — marker count stays at the initial install across >=6 poll windows"
    - "Three rapid saves within one poll window cause exactly one regeneration using the final file state (burst collapse; last event not dropped)"
    - "Deleting a watched file mid-watch logs one timestamped transient failure and the loop idles sleep-bounded (no busy-loop, no non-zero exit); unstartable-project initial failures exit 1"
    - "watch --once remains a hermetic single sync-and-exit (existing specs green, untouched by the fixes)"
    - "Zero new gem dependencies: d7c0fff touched only 3 source files and this plan touches no gemspec; lib/ contains no Fiddle or listen require"
    - "README.md, AGENTS.md, .planning/PROJECT.md, .planning/STATE.md contain no FSEvents claim; ROADMAP criterion 5 (and criteria 2/4) carry dated 2026-08-24 amendments"
  artifacts:
    - "lib/spm_cache/core/watcher.rb — Signal.trap('TERM') wiring + flush_pending_event + post-regenerate re-snapshots (<= ~20 changed lines)"
    - "spec/watch_signals_spec.rb — 4 subprocess examples (SIGINT 0, SIGTERM 0, flush, fatal 1)"
    - "spec/watch_loop_spec.rb — 3 subprocess examples (self-trigger, burst-collapse, mid-watch deletion)"
    - ".planning/phases/05-auto-sync-watcher/05-01-SUMMARY.md — criterion proof matrix + Documented deviations"
    - "Corrected docs: ROADMAP.md, REQUIREMENTS.md, PROJECT.md, STATE.md, AGENTS.md, README.md, research/SUMMARY.md, 05-SUMMARY, CONCERNS.md, design-spec deviation note"
  key_links:
    - "Signal.trap('TERM') { raise Interrupt } at top of Watcher#run → rescue Interrupt → flush_pending_event → exit 0"
    - "Command::Watch --once branch (watch.rb:41-43) → Watcher#run_once → installer_factory → Installer::Use#perform_install (watch.rb:37)"
    - "@last_signatures = current_signatures immediately after EVERY regenerate call site inside run and flush ↔ unconditional project.save at installer.rb:468"
    - "Subprocess specs load the real Watcher via -I <SPMCache::ROOT>/lib, synchronize on a marker file, and bound every Process.wait2 with Timeout + KILL fallback"
  requirement_probe_rows:
    - "AUTO-01 | verified | watch regenerates via Installer::Use on a non-empty diff with no manual re-run — watch_spec.rb:46/:60 + Task-2 burst subprocess example (real loop) + anchors watcher.rb:53-67 and watch.rb:37; self-trigger caveat closed by the Task-2 guard"
    - "AUTO-02 | verified-with-precision | saves within one poll window collapse into one regeneration using final file state (Task-2 burst example); spanning-window bursts regenerate once per window — dated precision clause added to criterion 2 (flagged assumption A2: real Xcode package-add bursts land within one 2s window)"
    - "AUTO-03 | verified | run_once single sync-and-exit, hermetic via installer_factory/out seams — watch_spec.rb:52/:98 + watch.rb:41-43"
    - "AUTO-04 | verified-after-fix | transient continue + timestamp (watch_spec.rb:79 + Task-2 deletion example); fatal initial failure exits 1 (Task-1 fatal example); SIGINT/SIGTERM flush + exit 0 delivered by Task 1 (SIGTERM was 143 as shipped); mid-watch deletion semantics recorded as logged-once idle, not fatal exit (dated amendment)"
    - "AUTO-05 | verified-amended | zero new gem deps (git show --stat d7c0fff: exactly 3 files, no gemspec; no gemspec change this plan) + hermetic --once; mechanism FSEvents/Fiddle superseded by stdlib mtime+size polling per dated amendment (05-CONTEXT user decision 2026-08-24)"
  prohibitions:
    - "P-05-1 | safety | resolved | test | The watcher MUST NOT regenerate in response to its own regeneration writes (unconditional project.save rewriting the watched project.pbxproj) — pinned by the SelfWritingInstaller subprocess example (marker count stays at 1)"
    - "P-05-2 | safety | resolved | test | Burst collapsing MUST NOT drop the last event of a burst — regeneration must use the final file state, pinned by the RecordingInstaller burst example (final recorded size == final file size)"
    - "P-05-3 | safety | resolved | test | The loop MUST NOT busy-loop (unbounded zero-delay regeneration) when watched files become unreadable or deleted mid-watch — pinned by the mid-watch deletion example (attempt count bounded at 2, process alive, clean TERM exit 0)"
---

<objective>
Verification-scoped closure of Phase 5 (Auto-Sync Watcher). The watcher shipped at d7c0fff; this plan does NOT re-implement it. It (1) FIXES the two mandated defects found in verification — AUTO-04 signal handling (SIGTERM exits 143 with no flush today; wire explicit trap + interrupt flush so SIGINT AND SIGTERM flush a pending event and exit 0) and the self-trigger hazard (regeneration's unconditional project.save rewrites the watched project.pbxproj while signatures are snapshotted before regenerate, so the loop can re-fire forever) — both via TDD RED→GREEN; (2) PROVES all 5 ROADMAP criteria against live code with hermetic subprocess specs; (3) records the user-accepted polling deviation as a dated criterion-5 amendment; and (4) closes the 18 enumerated doc-drift items.

Purpose: Phase 5 cannot verify green while criterion 4's signal clause is factually false (SIGTERM 143) and the loop can degenerate into a ~2s self-trigger cycle; the FSEvents/Fiddle doc claims contradict the accepted polling reality.

Output: patched lib/spm_cache/core/watcher.rb (surgical, <= ~20 lines), two new spec files (7 subprocess examples), 18 doc corrections, dated ROADMAP/REQUIREMENTS amendments, and 05-01-SUMMARY.md carrying the criterion proof matrix + Documented deviations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auto-sync-watcher/05-CONTEXT.md
@.planning/phases/05-auto-sync-watcher/RESEARCH.md
@.planning/phases/05-auto-sync-watcher/05-PATTERNS.md
@.planning/phases/05-auto-sync-watcher/05-VALIDATION.md
@lib/spm_cache/core/watcher.rb
@lib/spm_cache/command/watch.rb
@lib/spm_cache/installer/use.rb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AUTO-04 signals — SIGTERM trap + interrupt flush (TDD RED→GREEN)</name>
  <files>spec/watch_signals_spec.rb, lib/spm_cache/core/watcher.rb</files>
  <read_first>
  - lib/spm_cache/core/watcher.rb (full 115 lines — run loop 46-74, rescue Interrupt 68-69)
  - lib/spm_cache/command/watch.rb (full 56 lines)
  - spec/watch_spec.rb (full 137 lines — FakeInstaller 12-24, tmpdir boilerplate 30-44, loop-avoidance 71-76)
  - .planning/phases/05-auto-sync-watcher/RESEARCH.md — "Signal handling — the trap wiring question" section + Pitfall 5 (kill-signal testing strategy)
  - .planning/phases/05-auto-sync-watcher/05-PATTERNS.md — "Subprocess signal test pattern" section
  </read_first>
  <behavior>
  - RED 1 (SIGTERM): subprocess child runs the real Core::Watcher#run (FakeInstaller factory, debounce 5, fixture tmpdir .xcodeproj + Package.resolved); parent sends Process.kill('TERM', pid) after the initial-install marker appears; expects Process.wait2 exitstatus == 0 and child stdout includes "[watch] stopped." — currently exits 143 (verified empirically in RESEARCH), so this example FAILS before the fix.
  - RED 2 (flush): same child shape, debounce 5; parent waits for marker line 1 (initial regenerate done — the signature snapshot at watcher.rb:50 precedes it), rewrites Package.resolved with different-size content (size alone changes the [path, mtime.to_i, size] signature — no sleep needed), then immediately Process.kill('INT', pid) well inside the 5s window; expects exitstatus == 0 AND exactly 2 marker lines (pending change flushed) — currently 1 line (change silently dropped), so this example FAILS before the fix.
  - GREEN-at-birth guard (SIGINT): same child, Process.kill('INT', pid) → exitstatus == 0, stdout includes "[watch] stopped." (pins the already-working default-disposition path so the trap change cannot regress it).
  - GREEN-at-birth guard (fatal): child whose installer raises StandardError on perform_install → run's initial regenerate hits the outer rescue StandardError, re-raises → child exits non-zero; expects exitstatus == 1 and stdout includes "[watch] fatal:".
  </behavior>
  <action>
  Create spec/watch_signals_spec.rb with exactly the 4 subprocess examples above. Harness contract (RESEARCH Pitfall 5 recipe — no repo analog exists, this file sets the pattern): the spec writes a self-contained child script to the tmpdir, spawns it via Process.spawn(RbConfig.ruby, '-I', File.expand_path('lib', SPMCache::ROOT), script_path, project_dir, marker_path, debounce) with stdout redirected to a captured file; the child builds the fixture project mirroring spec/watch_spec.rb:30-44 (mkdir project.xcodeproj/project.xcworkspace/xcshareddata/swiftpm, write Package.resolved '{"pins":[],"version":1}', plus a project.pbxproj file), defines an inline FakeInstaller that appends one line per perform_install call to the marker file, constructs SPMCache::Core::Watcher with that factory and File.new(stdout_redirect) semantics kept via the default out: $stdout, and calls run. Parent-side synchronization is marker-file polling plus stdout-content polling, each bounded. EVERY child interaction is wrapped in Timeout.timeout(15) whose ensure block sends Process.kill('KILL', pid) then reaps — a watcher bug must fail the example, never hang the suite. NEVER invoke Watcher#run inside the RSpec process itself: Signal.trap would leak into the runner and interfere with the suite's own INT handling (RESEARCH anti-pattern). Run the file, confirm the 2 RED examples fail for the right reasons (exitstatus 143 / marker count 1), commit test-first per project convention.
  GREEN implementation in lib/spm_cache/core/watcher.rb, run method only: add Signal.trap('TERM') { raise Interrupt } as the first statement of run (per 05-PATTERNS "Signal-trap fix location"; a TERM then surfaces as Interrupt at the next safe point, aborting sleep, and joins the existing rescue Interrupt at watcher.rb:68). Extend the rescue Interrupt branch to call a new private method flush_pending_event before printing the "\n[watch] stopped." line. flush_pending_event body: return early when current_signatures == @last_signatures; otherwise set @last_signatures = current_signatures, emit an info line "\n[watch] SPM graph changed, flushing pending change...", and call regenerate inside a begin/rescue StandardError that logs "[#{Time.now}] [watch] flush failed: #{e.message}" — a failing flush must never turn Ctrl-C into a crash or non-zero exit. Do NOT touch run_once, the command layer, or the class doc comment (watcher.rb:10-13 already states exactly this target behavior — it becomes true now). Keep the diff surgical (target: <= ~12 lines). Re-run the spec file: 4 examples, 0 failures. Commit fix.
  </action>
  <verify>
    <automated>bundle exec rspec spec/watch_signals_spec.rb && bundle exec rspec spec/watch_spec.rb</automated>
  </verify>
  <acceptance_criteria>
  - SIGTERM example: Process.wait2 exitstatus == 0 and stdout contains "[watch] stopped." (pre-fix observed: 143).
  - SIGINT example: exitstatus == 0 and stdout contains "[watch] stopped.".
  - Flush example: marker file contains exactly 2 perform_install lines (initial + flushed pending change), exitstatus == 0.
  - Fatal example: exitstatus == 1 and stdout contains "[watch] fatal:".
  - grep -c "Signal.trap" lib/spm_cache/core/watcher.rb returns exactly 1 (code only — do not introduce the string in comments).
  - Existing suite unregressed: bundle exec rspec spec/watch_spec.rb reports 12 examples, 0 failures.
  - Every example completes under the 15s Timeout guard (total file runtime well under 60s).
  </acceptance_criteria>
  <done>spec/watch_signals_spec.rb: 4 examples, 0 failures — SIGINT exit 0, SIGTERM exit 0 (RED observed 143), flush 2 installs + exit 0 (RED observed 1 install), fatal exit 1; watcher.rb carries the TERM trap and flush_pending_event; existing watch_spec.rb unregressed (12 reported, 0 failures).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Self-trigger guard (evidence-gated) + loop-contract specs (TDD RED→GREEN)</name>
  <files>spec/watch_loop_spec.rb, lib/spm_cache/core/watcher.rb</files>
  <read_first>
  - lib/spm_cache/core/watcher.rb:45-104 (run loop, regenerate, current_signatures, file_signature)
  - lib/spm_cache/installer.rb:360-470 (integrate_proxy_into_project; unconditional project.save at 468)
  - lib/spm_cache/installer/use.rb:16-51 (perform_install always-run tail outside fast_path?, fast_path? contract)
  - .planning/phases/05-auto-sync-watcher/RESEARCH.md — Pitfall 1 (self-trigger chain), Pitfall 3 (mtime coarse signatures), Assumptions A1/A4, Open Question 2
  - .planning/phases/05-auto-sync-watcher/05-PATTERNS.md — "Post-regenerate re-snapshot fix" section
  - spec/watch_signals_spec.rb (Task 1 output — reuse its subprocess harness conventions)
  </read_first>
  <behavior>
  - A1 probe (recorded evidence, not a spec): settle empirically whether an unmodified Xcodeproj::Project reopen+save rewrites the pbxproj — RESEARCH left the self-trigger conclusion as inference precisely because this link (A1) was training knowledge.
  - RED 3 (self-trigger): child runs the real Watcher#run with a SelfWritingInstaller — perform_install appends a marker line AND appends one byte to the watched project.pbxproj, faithfully modeling the unconditional project.save at installer.rb:468 — debounce 0.3; parent waits for marker line 1 (initial regenerate + snapshot complete), sleeps ~2.5s (>=6 poll windows) with no external edits, then TERM (clean exit available since Task 1); expects marker count == 1 — pre-fix the count grows ~1 per window (snapshot precedes regenerate, self-write always looks like a change), so this example FAILS before the fix.
  - GREEN: post-regenerate re-snapshot — marker count stays at exactly 1 indefinitely.
  - Proof (green at birth, AUTO-02 / prohibition P-05-2): burst-collapse — child with a RecordingInstaller whose perform_install appends "install:<current byte size of Package.resolved>" to the marker, debounce 0.5; parent waits for the initial marker, performs 3 rapid writes of increasing sizes within one window (size varies, so the coarse mtime signature is irrelevant), waits ~2s (>=3 windows), TERM; expects exactly 2 marker lines and the last line's size == the final file size (one regeneration for the burst, using final state — the last event is never dropped).
  - Proof (green at birth, criterion-4 semantics / prohibition P-05-3): mid-watch deletion — child with a VerifyingInstaller whose perform_install increments the marker then raises StandardError 'project gone' unless the watched Package.resolved still exists, debounce 0.3; parent waits for the initial marker, deletes Package.resolved, sleeps ~1.5s (>=4 windows), TERM; expects marker count == 2 (initial + exactly one deletion-triggered attempt — bounded, no busy-loop), stdout contains "[watch] integration failed", exitstatus == 0 (logged-once-then-idle semantics, clean TERM exit).
  </behavior>
  <action>
  STEP 1 — run the A1 probe first and record the result (it decides the disposition wording, not whether the guard ships): in a tmpdir, create a project via Xcodeproj::Project.new(pbxproj_path) and save; File.stat it (raw mtime + size); reopen via Xcodeproj::Project.open(pbxproj_path), save again with NO modifications, stat again; repeat the reopen+save once more. If mtime or size changes on the unmodified save, A1 is CONFIRMED (every regeneration rewrites the watched pbxproj; combined with the already-verified chain — always-called tail use.rb:31-33, unconditional save installer.rb:468, snapshot-before-regenerate watcher.rb:50-51/:58, integer-second mtime in the signature vs the >=2s poll interval — the self-trigger loop is REAL on any integrated project). If nothing changes, A1 is REFUTED (live behavior benign; the guard downgrades to defensive hardening). If the probe cannot run at all (gem API error), record A1 UNVERIFIABLE and treat as the defensive case. Capture the probe's raw stat triples verbatim for 05-01-SUMMARY.md.
  STEP 2 — create spec/watch_loop_spec.rb with the 3 subprocess examples above, reusing the Task-1 harness contract (child script to tmpdir, RbConfig.ruby with -I lib, marker-file synchronization, Timeout.timeout(15) with KILL fallback in ensure; never call Watcher#run in-process). The child fixture mirrors Task 1's; SelfWritingInstaller and VerifyingInstaller write only the marker + (SelfWriting only) the watched pbxproj — never Package.resolved, which regeneration does not write. Run, confirm RED 3 fails with marker count >= 3, commit test-first.
  STEP 3 — GREEN in lib/spm_cache/core/watcher.rb: add @last_signatures = current_signatures immediately after EVERY regenerate call site that the loop owns — after the initial regenerate (watcher.rb:51), after the in-loop regenerate (watcher.rb:61), and after the flush regenerate inside flush_pending_event (Task 1) — so the watcher consumes its own write (per 05-PATTERNS "Post-regenerate re-snapshot fix"; keep the existing pre-regenerate @last_signatures = current lines untouched: pre-snapshot still decides WHETHER to regenerate, post-snapshot consumes the installer's write). Do NOT put the re-snapshot inside regenerate itself — run_once (watcher.rb:36-43) shares it and must keep its own contract. Do not touch installer.rb (the unconditional save is the integration contract; the watcher-side guard has the smaller blast radius). Re-run: 3 examples 0 failures. Commit fix.
  STEP 4 — record the disposition and trade-off: defect-vs-defensive wording per the probe outcome (STEP 1 rule), plus the guard's known trade-off — a change landing exactly DURING a regeneration is absorbed into the post-regenerate baseline and is not re-processed until the next change or the next watch/use run's initial sync (same healing property RESEARCH already documents for dropped interrupts). These sentences go into the SUMMARY deviation notes (Task 3 writes the file; keep the recorded text in the task notes meanwhile).
  </action>
  <verify>
    <automated>bundle exec rspec spec/watch_loop_spec.rb spec/watch_signals_spec.rb spec/watch_spec.rb</automated>
  </verify>
  <acceptance_criteria>
  - Self-trigger example: marker count == exactly 1 after >=6 poll windows with a pbxproj-writing installer (pre-fix: >= 3).
  - Burst example: exactly 2 marker lines after 3 intra-window writes; last recorded size == final Package.resolved size.
  - Deletion example: marker count == 2, stdout contains "[watch] integration failed", exitstatus == 0.
  - Post-regenerate re-snapshot present after all 3 regenerate sites inside run/flush; regenerate itself and run_once unchanged.
  - A1 probe result (stat triples + confirmed/refuted/unverifiable verdict) recorded for 05-01-SUMMARY.md.
  - Combined watcher.rb diff across Tasks 1+2 stays <= ~20 changed lines; spec/watch_spec.rb untouched.
  </acceptance_criteria>
  <done>spec/watch_loop_spec.rb: 3 examples, 0 failures — self-trigger marker count 1 (RED observed >=3), burst 2 installs using final file size, deletion 2 attempts with "[watch] integration failed" logged and clean exit 0; post-regenerate re-snapshot follows all 3 regenerate sites in run/flush; A1 probe verdict with stat triples captured for 05-01-SUMMARY.md; combined watcher.rb diff across Tasks 1+2 <= ~20 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Criterion proofs, dated amendments, 18-item doc-drift closure</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/STATE.md, AGENTS.md, .planning/research/SUMMARY.md, README.md, .planning/codebase/CONCERNS.md, docs/superpowers/specs/2026-08-10-v0.3.0-watch-init-doctor-design.md, .planning/phases/05-auto-sync-watcher/SUMMARY.md, .planning/phases/05-auto-sync-watcher/05-01-SUMMARY.md</files>
  <read_first>
  - .planning/phases/05-auto-sync-watcher/RESEARCH.md — "Doc Drift Catalogue (mandated corrections)" (all 18 rows) + Open Questions 1/3/4 + Pitfall 6
  - .planning/phases/05-auto-sync-watcher/05-PATTERNS.md — "Dated inline ROADMAP amendment" + "Documented deviations section" analogs
  - .planning/phases/02-diagnostics-command/SUMMARY.md (Documented deviations format, lines 30-50)
  - .planning/ROADMAP.md:78-88 (Phase 5 criteria)
  - The drifted lines themselves: .planning/PROJECT.md:60,68-69; .planning/STATE.md:51; AGENTS.md:16; .planning/research/SUMMARY.md:33; README.md:40; .planning/codebase/CONCERNS.md:104-108; docs/superpowers/specs/2026-08-10-v0.3.0-watch-init-doctor-design.md (watch section around :71); .planning/phases/05-auto-sync-watcher/SUMMARY.md:8-12
  </read_first>
  <action>
  PART A — PROOF BATTERY. Run and capture exact outputs for the 05-01-SUMMARY.md criterion matrix: (C1/AUTO-01) bundle exec rspec spec/watch_spec.rb — resolution + diff specs green; Task-2 burst example proves the real loop regenerates on change with no manual re-run; cite anchors watcher.rb:53-67 → watch.rb:37 factory → Installer::Use#perform_install. (C2/AUTO-02) Task-2 burst example (one regeneration, final state) + existing debounce value/flag specs watch_spec.rb:109/:118/:124. (C3/AUTO-03) existing hermetic specs watch_spec.rb:52/:98 + watch.rb:41-43 anchor. (C4/AUTO-04) Task-1 signal examples + Task-2 deletion example + existing continue-on-error contract spec watch_spec.rb:79. (C5/AUTO-05) git show --stat d7c0fff (exactly 3 files, 308 insertions, no gemspec) + grep -rn "Fiddle\|listen" lib/ showing no require of either + the hermetic --once spec.
  PART B — DATED AMENDMENTS (Phases 2-4 pattern: append " — amended 2026-08-24: ..." citing source; use Edit tool with scoped replacement, never whole-file Write, on these planning files).
  Item 1 — ROADMAP.md:88 (criterion 5, MANDATED): append amendment recording the mechanism supersession: mtime+size polling (Ruby stdlib) accepted 2026-08-24 per 05-CONTEXT, replacing the FSEvents/Fiddle wording; explicitly keep and confirm the two surviving clauses (no new gem dependency; --once unit-testable without the OS API) as verified true.
  Item 2 — ROADMAP.md:87 (criterion 4): append amendment stating the signal clause is DELIVERED by 05-01 Task 1 (Signal.trap('TERM') wiring + flush on Interrupt; subprocess specs prove exit 0 for both signals, SIGTERM having been 143 as shipped), and amending the fatal semantics to the shipped reading: fatal = pre-watch/unstartable project (non-zero exit); mid-watch deletion of watched files is logged once as transient and the loop idles (accepted-as-shipped, 05-CONTEXT; proven by the Task-2 deletion example). Also add the criterion-2 precision clause at ROADMAP.md:86 (Claude's discretion per 05-CONTEXT, anti-overclaim per RESEARCH Pitfall 6): debounce is a fixed poll interval — saves within one poll window collapse into one regeneration using final file state; bursts spanning windows regenerate once per window.
  Item 6 — REQUIREMENTS.md:27 (AUTO-05): amend wording to the accepted substance — stdlib mtime+size polling, no new gem dependency — with a dated note mirroring the ROADMAP amendment; keep the [x] status.
  PART C — DOC-DRIFT CLOSURE, all 18 catalogue rows, enumerated exactly:
  (1) ROADMAP.md:88 — done in Part B. (2) ROADMAP.md:87 — done in Part B. (3) PROJECT.md:60 — replace "watch uses native FSEvents to avoid `listen`" with "watch uses stdlib mtime polling to avoid `listen`" (keep the no-new-deps rule itself). (4) PROJECT.md:68 — correct the decision row's mechanism rationale to stdlib mtime+size polling / zero native binding, and flip Outcome "— Pending" to "✓ Shipped Phase 5 — stdlib polling (amended 2026-08-24)"; ALSO flip the adjacent row 69 Outcome "— Pending" to "✓ Shipped Phase 5" (same stale-status class, same table, verified-true behavior). (5) STATE.md:51 — replace "native FSEvents via Fiddle" with "stdlib mtime+size polling"; leave the rest of the line (no listen gem, watched files, continue-on-error loop) untouched — already accurate. (6) REQUIREMENTS.md:27 — done in Part B. (7) AGENTS.md:16 — apply the identical PROJECT.md:60 correction (GSD:project sync block; keep the two files in lockstep). (8) .planning/research/SUMMARY.md:33 — replace the FSEvents-binding sentence with the polling reality (no FSEvents binding exists; --once keeps the core path testable without any OS API) and drop the moot listen-fallback advice. (9) 05-SUMMARY.md:8 — "(58 lines)" → "(56 lines)". (10) 05-SUMMARY.md:9 — "(137 lines) — 12 specs covering..." → "(137 lines) — 9 specs (12 reported including 3 spec_helper.rb globals) covering...". (11) 05-SUMMARY.md:12 — "Phase 5 refactors that into a dedicated `Core::Watcher` class" → "Phase 5 adds a dedicated `Core::Watcher` alongside the retained `use --watch` loop (command/use.rb:50-80)". (12) design doc docs/superpowers/specs/2026-08-10-v0.3.0-watch-init-doctor-design.md — add ONE dated deviation note at the watch-mechanism section (~:71) pointing to the 05-CONTEXT polling acceptance; do NOT rewrite the historical body (planner discretion exercised: include the note). (13) README.md — replace the Watch Mode line (:40) with an entry pair matching the "- **Bold Title** — description." Key Features pattern: "**Auto-Sync Watch** — `spm-cache watch` auto-regenerates the cache proxy when `Package.resolved` or `project.pbxproj` changes (`--debounce=SECONDS`, `--once` for CI)." and keep/relabel the legacy line as "**Watch Mode (use)** — `spm-cache use --watch` monitors `Package.resolved` and re-integrates on change." (14) .planning/codebase/CONCERNS.md — no mechanism correction needed (rows already accurate); apply the optional :106 precision tweak: "collapsed correctly (single regeneration after the last change)" → "collapse into one regeneration at the end of the poll window". (15) .planning/codebase/CONVENTIONS.md:148-149 — CORRECT, no action; verify only. (16) .planning/codebase/INTEGRATIONS.md:143-144 — CORRECT, no action; verify only. (17) lib/spm_cache/command/use.rb:46 — CORRECT (explicitly says the FSEvents binding is NOT used), no action. (18) .planning/phases/05-auto-sync-watcher/05-CONTEXT.md — DO NOT EDIT (user decision record); its "12 specs" count is superseded numerically by item 10's SUMMARY correction. Record rows 15-17 as verified-no-change in 05-01-SUMMARY.md.
  PART D — VERIFICATION RECORD. Create .planning/phases/05-auto-sync-watcher/05-01-SUMMARY.md following 02-SUMMARY's Documented deviations format: (a) mechanism FSEvents→polling (user-accepted 2026-08-24, 05-CONTEXT); (b) signal fix delivered (was SIGINT-only exit 0 / SIGTERM 143 / no flush; now both flush + exit 0, 05-01 Task 1); (c) self-trigger disposition per the Task-2 probe verdict (defect confirmed OR benign/defensive, with the raw stat triples); (d) mid-watch deletion semantics (logged-once idle, amended wording); (e) debounce mechanics precision (poll interval, within-window collapse); (f) the re-snapshot trade-off (change during a regeneration absorbed into baseline; healed by next initial sync); (g) legacy `use --watch` duplication retained (command/use.rb:50-80 — catalogue only, migration out of scope); plus the criterion proof matrix from Part A, the 18-row closure table, and commit list. Then run the full-suite gate and record counts.
  </action>
  <verify>
    <automated>! grep -rn "FSEvents" README.md AGENTS.md .planning/PROJECT.md .planning/STATE.md && sed -n '84,89p' .planning/ROADMAP.md | grep -c "amended 2026-08-24" && grep -c "amended 2026-08-24" .planning/REQUIREMENTS.md && grep -c "spm-cache watch" README.md && test -f .planning/phases/05-auto-sync-watcher/05-01-SUMMARY.md && make proxy.build && bundle exec rspec</automated>
  </verify>
  <acceptance_criteria>
  - grep -rn "FSEvents" README.md AGENTS.md .planning/PROJECT.md .planning/STATE.md returns 0 matches (exit 1 → inverted gate passes).
  - sed -n '84,89p' .planning/ROADMAP.md | grep -c "amended 2026-08-24" returns 3 (criteria 2, 4, 5).
  - grep -c "amended 2026-08-24" .planning/REQUIREMENTS.md returns >= 1 (AUTO-05).
  - README.md contains "spm-cache watch" (new entry) and the legacy use --watch line relabeled.
  - .planning/phases/05-auto-sync-watcher/SUMMARY.md contains "(56 lines)", "9 specs (12 reported", and "alongside the retained".
  - .planning/phases/05-auto-sync-watcher/05-01-SUMMARY.md exists with: proof matrix covering all 5 criteria (exact commands + results), Documented deviations (a)-(g), the 18-row closure table (rows 15-17 marked verified-no-change, row 18 marked do-not-edit), and the Task-2 probe verdict with stat triples.
  - git show --stat d7c0fff output recorded in the proof matrix (3 files, 308 insertions, no gemspec).
  - Full suite green: make proxy.build && bundle exec rspec — 0 failures.
  </acceptance_criteria>
  <done>All 5 ROADMAP criteria proven with recorded evidence in 05-01-SUMMARY.md (criterion 5 under its dated polling amendment); ROADMAP criteria 2/4/5 and REQUIREMENTS AUTO-05 carry dated 2026-08-24 amendments; all 18 doc-drift rows closed exactly as enumerated; FSEvents zero-match gate passes on README/AGENTS/PROJECT/STATE; make proxy.build && bundle exec rspec full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user argv → CLaide flags | `--debounce` string coerced via .to_i (watch.rb:26) |
| local filesystem → watcher | watched files resolved via Dir.glob under the discovered .xcodeproj (same-trust local files) |
| OS signals → watcher process | SIGINT/SIGTERM delivered to the poll loop |
| spec child processes | test-only: RSpec parent spawns Ruby children running the real Watcher#run |

## STRIDE Threat Register (ASVS L1, security_block_on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | DoS | Watcher#run poll loop vs unconditional project.save (installer.rb:468) | high | mitigate | Self-trigger guard: post-regenerate @last_signatures re-snapshot at all 3 regenerate sites + SelfWritingInstaller regression spec (Task 2) — without it `watch` is unusable (~2s regeneration cycle, CPU + constant pbxproj rewrites) |
| T-05-02 | Tampering (data loss) | signal handling / pending event | medium | mitigate | flush_pending_event on Interrupt so SIGINT/SIGTERM process a pending change before exit 0 (Task 1); residual: a change landing during a regeneration is absorbed into the post-regenerate baseline — healed by the next watch/use initial sync (recorded, deviation f) |
| T-05-03 | DoS | Command::Watch --debounce parsing (watch.rb:26) | low | accept | .to_i coercion: garbage → 0 tight spin, negative → ArgumentError fatal exit; local dev tool, user-supplied argv, self-inflicted only (RESEARCH V5 row) — recorded as accepted sharp edge in 05-01-SUMMARY; a clamp is rejected as scope creep (05-CONTEXT: accepted as shipped) |
| T-05-04 | Tampering | resolve_watched_files Dir.glob (watcher.rb:87-93) | low | accept | Globs under the locally discovered .xcodeproj only; same-trust local files; watcher shells out to nothing — no new exposure (RESEARCH security domain) |
| T-05-SC | Tampering | package installs | low | accept | Zero package installs this phase (gemspec untouched at d7c0fff and by this plan) — no supply-chain surface; no legitimacy checkpoint required |
</threat_model>

<verification>
Phase-level checks (all locally runnable, no external services):
1. bundle exec rspec — full suite green including the 7 new subprocess examples (spec/watch_signals_spec.rb: 4; spec/watch_loop_spec.rb: 3); existing watch_spec.rb still 12 reported examples.
2. Signal matrix (from Task-1 examples): SIGINT → 0, SIGTERM → 0 (was 143), flush → 2 installs + exit 0, fatal → exit 1.
3. Self-trigger: marker count 1 across >=6 windows with a pbxproj-writing installer (was >=3 pre-fix); burst → 2 installs, final size recorded; deletion → 2 attempts, "[watch] integration failed" logged, clean exit 0.
4. Doc gates: zero FSEvents matches in README/AGENTS/PROJECT/STATE; "amended 2026-08-24" on ROADMAP criteria 2/4/5 and REQUIREMENTS AUTO-05; 05-01-SUMMARY.md proof matrix complete.
5. Static proofs recorded: git show --stat d7c0fff; no Fiddle/listen require in lib/.
Every subprocess example is Timeout(15)-bounded with KILL fallback — no possible hang.
</verification>

<success_criteria>
- All 5 ROADMAP criteria proven against live code, each with a recorded proof (criterion 5 under its dated polling amendment; criterion 4 with the signal clause delivered and mid-watch-deletion semantics amended; criterion 2 with the poll-window precision clause).
- MANDATED FIX 1 shipped RED→GREEN: SIGTERM child exited 143 in RED, exits 0 in GREEN; SIGINT unaffected; flush proven.
- MANDATED FIX 2 dispositioned on evidence: A1 probe verdict recorded; guard shipped either way (defect fix or defensive hardening); regression spec pins it.
- All 18 doc-drift rows closed exactly as enumerated (3 no-action rows recorded as verified; 05-CONTEXT.md untouched).
- No scope beyond: no installer.rb changes, no legacy use --watch migration, no debounce clamp, no new gems, watch_spec.rb untouched.
</success_criteria>

## Artifacts this phase produces

- lib/spm_cache/core/watcher.rb — patched: TERM trap, flush_pending_event, post-regenerate re-snapshots (surgical diff, <= ~20 lines).
- spec/watch_signals_spec.rb — new: 4 Timeout-bounded subprocess signal examples (also the repo's first subprocess-signal pattern).
- spec/watch_loop_spec.rb — new: 3 subprocess loop-contract examples (self-trigger, burst-collapse, mid-watch deletion).
- .planning/phases/05-auto-sync-watcher/05-01-SUMMARY.md — verification record: 5-criterion proof matrix, Documented deviations (a)-(g), 18-row doc-closure table, A1 probe verdict.
- Corrected/amended: .planning/ROADMAP.md (criteria 2/4/5), .planning/REQUIREMENTS.md (AUTO-05), .planning/PROJECT.md (rows 60/68/69), .planning/STATE.md (locked-decision line), AGENTS.md (sync block), .planning/research/SUMMARY.md, README.md (watch entry), .planning/codebase/CONCERNS.md (:106 precision), design-spec dated deviation note, 05-SUMMARY.md numeric/refactor-wording corrections.

<output>
Create `.planning/phases/05-auto-sync-watcher/05-01-SUMMARY.md` when done
</output>
