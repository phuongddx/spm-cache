---
phase: 12-run-log-capture-foundation
plan: 04
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - lib/spm_cache/spm/build_pipeline.rb
  - lib/spm_cache/spm/build.rb
  - lib/spm_cache/installer/build.rb
  - lib/spm_cache/command/pkg/build.rb
  - lib/spm_cache/installer/use.rb
  - spec/build_pipeline_spec.rb
  - spec/installer_build_spec.rb
  - spec/installer_use_fast_path_spec.rb
autonomous: true
requirements: [LOGS-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low   # estimate-calibration sample_count=0 → factor 1, confidence low

must_haves:
  truths:
    - "SC2/D-04: package_start and package_end events bracket every SPM::BuildPipeline.run invocation — the single choke point shared by Installer::Build's per-package loop and pkg build — with package_end carrying status ok on success and failed on raise — bundle exec rspec spec/build_pipeline_spec.rb"
    - "SC2/D-04: xcodebuild subprocess output reaches the active run log because the pipeline threads its run_log sink into Buildable#xcodebuild, which passes live_log_out:/live_log_err: StreamSinks to Core::Sh — verified with a real echo subprocess standing in for xcodebuild — bundle exec rspec spec/build_pipeline_spec.rb"
    - "D-04: phase markers (minimum detect, integrate, build, fidelity) are emitted at the existing branch boundaries — Installer::Use#perform_install (detect after detect_diff, integrate before the fast/full branch), Installer::Build#perform_install (build before the missed loop), SPM::BuildPipeline#run (fidelity around report_fidelity) — bundle exec rspec spec/installer_use_fast_path_spec.rb spec/installer_build_spec.rb"
    - "Nil-disables: every new kwarg/opt defaults to nil and callers without a run log behave byte-identically (pkg build and all existing specs pass untouched); Core::RunLog.current is nil in every existing spec, so nil-guarded event calls are no-ops — bundle exec rspec (full suite)"
    - "EDGE empty (zero-pins case, resolved-explicit): a run with an empty missed set emits no package_start/package_end lines while phase markers still appear — spec/installer_build_spec.rb"
    - "Never-fail guard: event emission wrapped so a logging failure degrades (warn) and can never fail or alter the build — the pipeline's own never-let-metadata-mask-the-operation convention (build_pipeline.rb report_fidelity rescue) — spec/build_pipeline_spec.rb"
  artifacts:
    - lib/spm_cache/spm/build_pipeline.rb (run gains run_log: nil kwarg; package_start/package_end events at run entry/exit; phase fidelity marker; run_log threaded into the Buildable opts)
    - lib/spm_cache/spm/build.rb (Buildable#xcodebuild constructs and forwards live_log_out/live_log_err StreamSinks from the threaded run_log)
    - lib/spm_cache/installer/build.rb (build_single_target passes run_log: Core::RunLog.current into BuildPipeline.run; phase build marker)
    - lib/spm_cache/command/pkg/build.rb (BuildPipeline.run call passes run_log: Core::RunLog.current)
    - lib/spm_cache/installer/use.rb (phase detect + integrate markers)
    - spec/build_pipeline_spec.rb (extended), spec/installer_build_spec.rb (extended), spec/installer_use_fast_path_spec.rb (extended)
  key_links:
    - "Installer::Build#build_single_target → SPM::BuildPipeline.run(run_log: Core::RunLog.current) → Buildable#xcodebuild → Core::Sh.run(live_log_out:, live_log_err:) → RunLog body lines — the SC2 end-to-end chain"
    - "Installer::Use#perform_install → Core::RunLog.current&.event('phase', name: ...) — nil-guarded so every existing caller/spec path is a no-op"
  prohibitions:
    - statement: "MUST NOT truncate, clip, or mangle a run log to bound disk — fidelity is absolute (D-05); only whole-file retention pruning (D-06) bounds growth"
      status: resolved
      verification: test
    - statement: "MUST NOT alter terminal bytes, stream interleaving, or exit codes when capture is enabled — the tee is invisible (SC3)"
      status: resolved
      verification: test
    - statement: "MUST NOT let a run-log write failure fail or change the wrapped command — logging degrades to disabled-with-warning, never raises into the run"
      status: resolved
      verification: test
    - statement: "MUST NOT record secrets or credentials in run logs — argv is captured verbatim only because today's flag surface carries none (verified command.rb:16-24); env vars and any future token-bearing flag stay out of capture"
      status: resolved
      verification: judgment
---

# Plan 12-04 — Structured event vocabulary + subprocess activation (D-04, SC2 completion)

<objective>
Emit D-04's structured event vocabulary from the real pipeline seams and activate the
Plan 12-02 Sh sinks from the build path. `SPM::BuildPipeline.run` gains a nil-disables
`run_log:` kwarg emitting `package_start`/`package_end` (the consolidated insertion
point shared by the installer loop and pkg build) plus a `fidelity` phase marker, and
threads the sink into `Buildable#xcodebuild` so xcodebuild output lands in the run log
via `live_log_out:`/`live_log_err:` StreamSinks. Installers emit the `detect`,
`integrate`, and `build` phase markers from their existing branch boundaries through
the nil-guarded `Core::RunLog.current`.

Purpose: SC2 completion — with this plan a build run's log reconstructs offline:
narrative (Plan 12-01 tee), subprocess output (Plan 12-02 sink + this activation),
and per-package/phase structure (this plan). D-04 is the vocabulary Phase 14's SSE
tailer and browser anchors build on — flagged COSTLY-reversible: fix it now, exactly
as specified, because changing it after Phase 14 means reworking tailer, frontend, and
regression specs together (12-CONTEXT.md D-04 reversibility note).

Output: five modified lib files, three extended spec files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-run-log-capture-foundation/12-CONTEXT.md
@.planning/phases/12-run-log-capture-foundation/12-RESEARCH.md
@.planning/phases/12-run-log-capture-foundation/12-PATTERNS.md
@.planning/phases/12-run-log-capture-foundation/12-01-SUMMARY.md
@.planning/phases/12-run-log-capture-foundation/12-02-SUMMARY.md
@lib/spm_cache/spm/build_pipeline.rb
@lib/spm_cache/spm/build.rb
@lib/spm_cache/installer/build.rb
@lib/spm_cache/installer/use.rb
@lib/spm_cache/command/pkg/build.rb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pipeline package events + fidelity marker + xcodebuild sink activation (RED → GREEN)</name>
  <read_first>
  - lib/spm_cache/spm/build_pipeline.rb lines 42-45 (nil-disables kwarg precedent: resolved_pins_file), 50-102 (run structure: begin/ensure, the "single consolidated insertion point" comment, report_fidelity rescue-to-warn), 220-237 (never-let-metadata-mask-the-operation guard)
  - lib/spm_cache/spm/build.rb lines 80-91 (the ONLY live_log pass-through site — Buildable#xcodebuild forwarding via opts)
  - lib/spm_cache/installer/build.rb lines 52-54 and 157-184 (build_single_target → BuildPipeline.run call site)
  - lib/spm_cache/command/pkg/build.rb lines 43-49 (the second BuildPipeline.run call site)
  - .planning/phases/12-run-log-capture-foundation/12-RESEARCH.md — "Run-file shape" (event field names), Code Examples (package_* at pipeline entry/exit), Pitfall 4 (per-stream sinks), Pitfall 5
  - .planning/phases/12-run-log-capture-foundation/12-PATTERNS.md — spm/build_pipeline.rb section, spm/build.rb section, Shared Patterns "Nil-option disables" and "output(line) duck contract"
  - lib/spm_cache/core/run_log.rb (RunLog.event signature and StreamSink from Plan 12-01)
  - lib/spm_cache/core/sh.rb (live_log_out/live_log_err opts from Plan 12-02)
  - spec/build_pipeline_spec.rb (existing pipeline spec harness — how run is invoked in specs today; match its stubbing/tmpdir conventions)
  </read_first>
  <files>lib/spm_cache/spm/build_pipeline.rb, lib/spm_cache/spm/build.rb, lib/spm_cache/installer/build.rb, lib/spm_cache/command/pkg/build.rb, spec/build_pipeline_spec.rb</files>
  <behavior>
  - RED first: pipeline examples asserting package_start/package_end lines in a real RunLog file fail (no events emitted today).
  - Package bracket: calling SPM::BuildPipeline.run with run_log: <real RunLog> (stubbed perform_build internals per existing spec conventions) writes {"event":"package_start","ts":...,"name":<target>} before the build and {"event":"package_end","ts":...,"name":...,"status":"ok"} on success.
  - Failure bracket: forcing the build path to raise still writes package_end with status "failed" (emit from the ensure, driven by the existing success flag) and the exception propagates unchanged.
  - Fidelity marker: the phase fidelity event appears immediately before report_fidelity runs.
  - Sink activation: with run_log set, the opts reaching Buildable#xcodebuild carry live_log_out/live_log_err StreamSinks over that run_log; verify with the existing pipeline spec harness by letting the stubbed xcodebuild run a real echo through the real Core::Sh into the file (echo precedent, core_spec.rb:6-31) OR by asserting the forwarded opts identity — either way the file must gain stream-tagged echo lines.
  - Nil-disables: calling run without run_log (every existing spec and pkg-build default path) emits nothing and forwards nothing — the existing suite is the regression net.
  - Zero-pins edge: run with a name but an empty build workload writes package_start + package_end ok with nothing between — no phantom lines.
  </behavior>
  <precondition>Plans 12-01 and 12-02 complete: RunLog/StreamSink exist; Core::Sh accepts live_log_out/live_log_err.</precondition>
  <action>
  Add `run_log: nil` kwarg to SPM::BuildPipeline.run (build_pipeline.rb:50 signature), documented nil-disables exactly like resolved_pins_file (build_pipeline.rb:42-45 comment style). Emit package_start at run entry right after the "Target name required" guard (name field = the target name), and package_end from the run ensure with status "ok"/"failed" derived from the existing success flag — both via a private helper that no-ops when run_log is nil and wraps emission in a rescue-to-warn Core::UI.warn guard mirroring the report_fidelity rescue (a logging failure must never fail or alter the build). Emit the phase fidelity event ("phase", name: "fidelity") immediately before the report_fidelity call inside the existing begin/rescue. Thread run_log through the perform_build opts so it reaches Buildable#xcodebuild's **opts. In lib/spm_cache/spm/build.rb Buildable#xcodebuild (lines 80-91): when opts[:run_log] is non-nil, construct Core::RunLog::StreamSink.new(opts[:run_log], "out") and ("err") and pass them as live_log_out:/live_log_err: to both Core::Sh.run calls (the initial build AND the low-deployment-target retry — both calls forward); keep the legacy live_log: forwarding untouched. Update both BuildPipeline.run call sites to pass `run_log: Core::RunLog.current`: installer/build.rb build_single_target and command/pkg/build.rb line 43. Cite D-04/SC2/LOGS-01 and the consolidated-insertion-point comment where the events land. Commit RED then GREEN.
  </action>
  <verify>
    <automated>bundle exec rspec spec/build_pipeline_spec.rb spec/build_pipeline_seeding_spec.rb spec/build_pipeline_provenance_spec.rb</automated>
    <fails_when>exit code is non-zero; or "0 examples, 0 failures"; or the pipeline's existing (pre-change) examples fail — nil-disables was violated</fails_when>
  </verify>
  <acceptance_criteria>
  - lib/spm_cache/spm/build_pipeline.rb run signature contains `run_log:` with nil default; the file contains event names "package_start" and "package_end" and a "fidelity" phase marker
  - lib/spm_cache/spm/build.rb xcodebuild forwards live_log_out/live_log_err on BOTH Sh.run calls when opts[:run_log] is set
  - lib/spm_cache/installer/build.rb and lib/spm_cache/command/pkg/build.rb BuildPipeline.run calls pass run_log: Core::RunLog.current
  - spec/build_pipeline_spec.rb exits 0 including bracket-success, bracket-failure, nil-disables, and zero-pins examples
  </acceptance_criteria>
  <done>package and fidelity events land from the single choke point for both build paths; xcodebuild output streams into the run log through the activated Sh sinks; all existing pipeline specs green; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: installer phase markers — detect / integrate / build (RED → GREEN)</name>
  <read_first>
  - lib/spm_cache/installer/use.rb lines 16-47 (perform_install: UI.section, verify_projects!, detect_diff, fast_path? branch, full branch inside with_build_lock)
  - lib/spm_cache/installer/build.rb lines 18-58 (perform_install: missed resolution, "Building N target(s)" info line, missed.each loop)
  - .planning/phases/12-run-log-capture-foundation/12-RESEARCH.md — Code Examples "Phase markers: minimum {detect, integrate, build, fidelity}; emit from Installer::Use#perform_install branches (use.rb:21-45) and Installer::Build#perform_install"
  - .planning/phases/12-run-log-capture-foundation/12-PATTERNS.md — installer/use.rb + installer/build.rb section (phase-marker events from existing branch boundaries, no restructuring; injected-dependency precedent)
  - lib/spm_cache/core/run_log.rb (RunLog.current nil-guarded event call shape)
  - spec/installer_use_fast_path_spec.rb and spec/installer_build_spec.rb (existing harnesses — where the marker assertions extend)
  </read_first>
  <files>lib/spm_cache/installer/use.rb, lib/spm_cache/installer/build.rb, spec/installer_use_fast_path_spec.rb, spec/installer_build_spec.rb</files>
  <behavior>
  - RED first: installer examples asserting phase lines fail (no markers emitted today).
  - Use flow: with a RunLog set as Core::RunLog.current around a (stubbed) perform_install, the file gains {"event":"phase","name":"detect"} after detect_diff and {"event":"phase","name":"integrate"} before the fast/full branch — exactly one integrate marker per run regardless of branch.
  - Build flow: Installer::Build#perform_install emits {"event":"phase","name":"build"} before the missed.each loop; with an empty missed set (zero-pins) the marker still appears and no package events follow.
  - Nil-safety: with Core::RunLog.current nil (every existing installer spec), the marker calls are no-ops — existing installer suites stay green untouched.
  </behavior>
  <precondition>Task 1 GREEN and committed.</precondition>
  <action>
  In Installer::Use#perform_install (use.rb:16-47) add two nil-guarded event calls via Core::RunLog.current&.event("phase", name: ...): name "detect" immediately after the detect_diff call, and name "integrate" once, immediately before the `if fast_path?` branch (both branches integrate — single site, no duplication). In Installer::Build#perform_install (build.rb:18-58) add one nil-guarded event call with name "build" immediately after the "Building N target(s)" info line, before the missed.each loop. Use the exact call shape Core::RunLog.current&.event("phase", name: "detect") — the &. guard makes every existing caller/spec path a no-op with zero restructuring of the perform_install branches (markers land at existing boundaries; no new abstractions). Cite D-04/LOGS-01 in comments at each site. Extend the two named spec files with the marker assertions using their existing harness (set Core::RunLog.current to a real RunLog in a tmpdir, run the (stubbed-as-they-already-are) install, read the file, clear current in ensure). Commit RED then GREEN.
  </action>
  <verify>
    <automated>bundle exec rspec spec/installer_use_fast_path_spec.rb spec/installer_build_spec.rb spec/installer_spec.rb</automated>
    <fails_when>exit code is non-zero; or "0 examples, 0 failures"; or any pre-existing installer example fails (nil-guard violated — a marker call raised on a nil current)</fails_when>
  </verify>
  <acceptance_criteria>
  - lib/spm_cache/installer/use.rb contains two `Core::RunLog.current&.event("phase"` call sites (detect, integrate); lib/spm_cache/installer/build.rb contains the "build" marker site
  - spec/installer_use_fast_path_spec.rb and spec/installer_build_spec.rb exit 0 including the marker and zero-pins examples
  - `bundle exec rspec` full suite exits 0
  </acceptance_criteria>
  <done>The minimum event set of D-04 (run_start/package_start/package_end/phase markers/run_end) is complete and observable end-to-end; committed.</done>
</task>

</tasks>

## Edge Coverage — LOGS-01 (specless probe)

| Edge | Probe question | Resolution | Owner |
|---|---|---|---|
| adjacency | concurrent runs / interleaved events | resolved-explicit: separate files per run (ms+pid names); each run's events land only in its own file via its own run_log handle; the pipeline's Mutex serializes appends | Plan 12-01 (concurrency) + this plan (per-run handle threading) |
| empty | zero-pins / empty missed set | resolved-explicit: empty missed set → phase markers present, zero package_start/package_end lines — spec/installer_build_spec.rb; zero-output run → two-line file (Plan 12-01) | this plan Task 2 (zero-pins example) |
| ordering | equal-element order stable | resolved-explicit: events append in emission order — package_start precedes its package_end, phase markers precede the loop's package events; file position is monotonic | this plan Task 1 (bracket assertions) + Plan 12-01 (append order) |

## Artifacts this phase produces

Full phase list (★ = created by THIS plan):
- ★ `SPM::BuildPipeline.run(run_log: nil)` kwarg (nil-disables)
- ★ Event names `package_start` ({event, ts, name}) and `package_end` ({event, ts, name, status: ok|failed}) at the consolidated pipeline insertion point
- ★ Phase markers `phase` with name detect / integrate (Installer::Use#perform_install), build (Installer::Build#perform_install), fidelity (SPM::BuildPipeline#run before report_fidelity)
- ★ `Buildable#xcodebuild` live_log_out/live_log_err StreamSink forwarding (activates Plan 12-02's sinks from the build path, including the low-deployment-target retry call)
- ★ BuildPipeline.run call sites passing `run_log: Core::RunLog.current` (installer/build.rb build_single_target; command/pkg/build.rb)
- ★ extended spec/build_pipeline_spec.rb, spec/installer_build_spec.rb, spec/installer_use_fast_path_spec.rb
- `Core::RunLog` + TeeIO + StreamSink + `Config#runs_dir` + `--no-run-log` + `--log-dir` override + run_start/run_end vocabulary + spec/main_run_log_spec.rb, spec/run_log_spec.rb (Plan 12-01)
- `Core::Sh` opts `live_log_out:`/`live_log_err:` + popen3 failure_detail tails + capture3 `sh` events + spec/sh_run_log_sink_spec.rb (Plan 12-02)
- `Config#runs_keep`/`runs_max_mb` + yml-template docs + `prune` + `.spm-cache/` gitignore entry (Plan 12-03)
- `RunLog.cycle_wrapper` + `Command::Watch` wrapped installer_factory + `trigger: "watch"` cycle files (Plan 12-05)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| subprocess stdout/stderr → run log | untrusted tool/package-controlled text crosses into a structured log (log-forging surface); package NAMES also enter event fields here |
| local user → runs dir / --log-dir | same-trust-domain local paths (no remote input) |

## STRIDE Threat Register (ASVS Level 1; workflow.security_block_on = high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-01 | Tampering (log-forging via package names / subprocess text) | package_start/package_end name fields; body lines | medium | mitigate | event lines are produced ONLY by RunLog#event with JSON.generate — a package named with JSON-breaking or event-lookalike text is escaped into the name VALUE of a real event or into body text; body lines never carry an `event` key. Comment at the emission site documents: Phase 14 renderers key on the `event` field and treat all text/name values as data |
| T-12-04 | DoS (disk-fill) | full-fidelity events + subprocess capture now active on every build | high | mitigate | bounded by Plan 12-03 retention (already landed before this wave): runs_keep=50 / runs_max_mb=500MB pruned at every run start |
| T-12-05 | Information disclosure (target names in events) | package/phase event fields | low | mitigate | names are the same identifiers the CLI already prints to the terminal; no env or credential data crosses into events |
| T-12-02 | Tampering (path traversal via --log-dir) | runs_dir | low | accept | unchanged from Plan 12-01 disposition |
| T-12-03 | Tampering (symlink swap) | run log append path | low | accept | unchanged; this plan adds no directory operations |

No package installs in this phase (stdlib only; Package Legitimacy Audit table empty) — no [ASSUMED]/[SUS] package checkpoint required.
</threat_model>

<verification>
- `bundle exec rspec spec/build_pipeline_spec.rb spec/build_pipeline_seeding_spec.rb spec/build_pipeline_provenance_spec.rb spec/installer_use_fast_path_spec.rb spec/installer_build_spec.rb spec/installer_spec.rb` — this plan's surfaces plus the pipeline regression triple, green.
- `bundle exec rspec` — full suite green (nil-disables is proven by every pre-existing example passing unmodified).
</verification>

<success_criteria>
- SC2 complete: a build run's log now reconstructs offline — narrative (tee), subprocess output (activated sinks), per-package brackets, and phase structure.
- D-04's minimum event vocabulary (run_start, package_start/package_end, phase markers, run_end) is live, interleaved in one file, one schema — frozen for Phase 14 (costly-reversible; no checkpoint required, but do not reshape fields without re-checking Phase 14 plans).
- Every new kwarg/opt nil-disables; zero existing-spec churn.
</success_criteria>

<output>
Create `.planning/phases/12-run-log-capture-foundation/12-04-SUMMARY.md` when done
</output>
