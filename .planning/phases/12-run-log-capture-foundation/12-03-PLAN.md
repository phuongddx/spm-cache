---
phase: 12-run-log-capture-foundation
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - lib/spm_cache/core/config.rb
  - lib/spm_cache/assets/templates/spm-cache.yml.template
  - lib/spm_cache/core/run_log.rb
  - lib/spm_cache/command/init.rb
  - spec/config_spec.rb
  - spec/run_log_spec.rb
  - spec/init_spec.rb
autonomous: true
requirements: [LOGS-01]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low   # estimate-calibration sample_count=0 → factor 1, confidence low

must_haves:
  truths:
    - "SC4/D-06: retention is a count + size hybrid — after each new run's header lands, prune oldest run logs until count <= runs_keep AND total bytes <= runs_max_mb (defaults 50 / 500), configurable in spm-cache.yml — bundle exec rspec spec/run_log_spec.rb (retention group)"
    - "EDGE ordering (resolved-explicit): pruning deletes strictly oldest-first by lexicographic filename (timestamp-prefixed names make lexicographic == chronological); the spec asserts exactly WHICH fabricated files survive — spec/run_log_spec.rb retention group"
    - "D-07: cleanup runs at run start, after the new header is written, never before — the just-opened run's own file is never a prune candidate — spec/run_log_spec.rb retention group"
    - "Pitfall 6 (CP14 applied at birth): a fabricated old run file whose run_start pid is alive (Process.pid) and which lacks a run_end line is never pruned even when over budget; a dead-pid file is — spec/run_log_spec.rb retention group"
    - "D-06 config: Config#runs_keep and Config#runs_max_mb read flat snake_case keys with defaults 50/500; non-integer yml values coerce back to defaults (Integer() with ArgumentError/TypeError rescue — yml is user-authored, not adversarial) — bundle exec rspec spec/config_spec.rb"
    - "D-02: Command::Init appends a .spm-cache/ entry to .gitignore via the same append-once, comment-labeled pattern as spm-cache/ — idempotent on re-init — bundle exec rspec spec/init_spec.rb"
    - "T-12-04 (disk-fill, severity high) is disposed: repeated runs against a tiny runs_keep/runs_max_mb leave exactly the retained set on disk — spec/run_log_spec.rb retention group"
  artifacts:
    - lib/spm_cache/core/config.rb (DEFAULT_CONFIG gains "runs_keep" => 50 and "runs_max_mb" => 500; readers runs_keep / runs_max_mb with Integer coercion)
    - lib/spm_cache/assets/templates/spm-cache.yml.template (commented documentation of the two keys)
    - lib/spm_cache/core/run_log.rb (RunLog#prune(keep:, max_bytes:) called from open after the header lands)
    - lib/spm_cache/command/init.rb (ensure_gitignore gains the .spm-cache/ entry)
    - spec/config_spec.rb (extended), spec/run_log_spec.rb (extended), spec/init_spec.rb (extended)
  key_links:
    - "RunLog.open → header rename lands → prune(keep: Config.instance.runs_keep, max_bytes: Config.instance.runs_max_mb * 1024 * 1024) — D-07 ordering is load-bearing: the current file must exist before pruning so it is excluded by identity"
    - "spm-cache.yml → Config (raw[...] || default readers) → prune budgets — the yml template documents the keys so `Command::Init#write_config` merges keep them discoverable for existing configs untouched"
  prohibitions:
    - statement: "MUST NOT truncate, clip, or mangle a run log to bound disk — fidelity is absolute (D-05); only whole-file retention pruning (D-06) bounds growth"
      status: resolved
      verification: test
    - statement: "MUST NOT alter terminal bytes, stream interleaving, or exit codes when capture is enabled — the tee is invisible (SC3)"
      status: resolved
      verification: test
    - statement: "MUST NOT let a run-log write failure fail or change the wrapped command — logging degrades to disabled-with-warning, never raises into the run"
      status: resolved
      verification: test
    - statement: "MUST NOT record secrets or credentials in run logs — argv is captured verbatim only because today's flag surface carries none (verified command.rb:16-24); env vars and any future token-bearing flag stay out of capture"
      status: resolved
      verification: judgment
---

# Plan 12-03 — Retention, config keys, and the .gitignore entry (SC4, D-02/D-06/D-07)

<objective>
Bound run-log disk growth and finish the config/gitignore surface: DEFAULT_CONFIG gains
`runs_keep` (50) and `runs_max_mb` (500) with Integer-coercing readers; `Core::RunLog`
gains a prune loop invoked at run start after the header lands (count + size hybrid,
oldest-first, never the current run, never a live-pid run); the yml template documents
the keys; `Command::Init` appends `.spm-cache/` to .gitignore.

Purpose: SC4 (repeated runs accumulate logs without unbounded growth) and the D-02
gitignore half. This disposes the phase's only high-severity threat (T-12-04 disk-fill).

Output: modified config.rb / run_log.rb / init.rb / yml template, three extended spec
files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-run-log-capture-foundation/12-CONTEXT.md
@.planning/phases/12-run-log-capture-foundation/12-RESEARCH.md
@.planning/phases/12-run-log-capture-foundation/12-PATTERNS.md
@.planning/phases/12-run-log-capture-foundation/12-01-SUMMARY.md
@lib/spm_cache/core/config.rb
@lib/spm_cache/core/run_log.rb
@lib/spm_cache/command/init.rb
@lib/spm_cache/assets/templates/spm-cache.yml.template
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runs_keep / runs_max_mb config keys + yml template docs (RED → GREEN)</name>
  <read_first>
  - lib/spm_cache/core/config.rb lines 15-22 (DEFAULT_CONFIG verbatim), 122-148 (the raw[...] || default reader convention to copy)
  - lib/spm_cache/assets/templates/spm-cache.yml.template (entire file — 13 lines; the commented-out-key convention at lines 2 and 7-13)
  - .planning/phases/12-run-log-capture-foundation/12-RESEARCH.md — Pattern 5 (Retention), Security Domain V5 row (Integer coercion with rescue-to-default)
  - .planning/phases/12-run-log-capture-foundation/12-PATTERNS.md — core/config.rb section (all three verbatim patterns) and the yml template row
  - spec/config_spec.rb (existing reader-spec conventions + Config.instance.reset! Singleton handling)
  </read_first>
  <files>lib/spm_cache/core/config.rb, lib/spm_cache/assets/templates/spm-cache.yml.template, spec/config_spec.rb</files>
  <behavior>
  - RED first: spec examples asserting Config.instance.runs_keep == 50 and runs_max_mb == 500 fail (methods undefined).
  - Defaults: with no yml keys, runs_keep is 50 and runs_max_mb is 500.
  - Override: a written spm-cache.yml with runs_keep: 3 and runs_max_mb: 12 yields 3 and 12 (remember Config is a Singleton — reset! in before/after per init_spec.rb:148,219 precedent).
  - Coercion: runs_keep: "many" (a non-integer string) falls back to 50; runs_max_mb: nil falls back to 500 — Integer() rescue ArgumentError/TypeError, yml is user-authored, not adversarial.
  </behavior>
  <precondition>Plan 12-01 complete (config.rb already carries runs_dir).</precondition>
  <action>
  Add "runs_keep" => 50 and "runs_max_mb" => 500 to DEFAULT_CONFIG (config.rb:15-22) as flat snake_case keys, D-06 citation comment. Add readers runs_keep and runs_max_mb in the raw[...] || default style (config.rb:122-148): read raw["runs_keep"] / raw["runs_max_mb"], Integer()-coerce the value, and rescue ArgumentError/TypeError back to the respective default. Document both keys in spm-cache.yml.template as commented-out lines matching the existing `# cache_only: []` style, with a one-line comment explaining the hybrid retention (keep newest N run logs; then prune oldest until total size is under the MB budget). Existing user ymls keep working untouched — Command::Init#write_config merges over DEFAULT_CONFIG. Commit RED then GREEN.
  </action>
  <verify>
    <automated>bundle exec rspec spec/config_spec.rb</automated>
    <fails_when>exit code is non-zero; or "0 examples, 0 failures"; or the defaults/override/coercion examples are pending</fails_when>
  </verify>
  <acceptance_criteria>
  - lib/spm_cache/core/config.rb DEFAULT_CONFIG contains the literal keys "runs_keep" => 50 and "runs_max_mb" => 500; readers `def runs_keep` and `def runs_max_mb` exist
  - lib/spm_cache/assets/templates/spm-cache.yml.template contains commented "# runs_keep: 50" and "# runs_max_mb: 500" lines
  - spec/config_spec.rb exits 0 covering default, override, and both coercion cases
  </acceptance_criteria>
  <done>Config keys land with defaults 50/500, Integer coercion, and template documentation; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunLog#prune — count+size hybrid at run start (RED → GREEN)</name>
  <read_first>
  - lib/spm_cache/core/run_log.rb (the file Plan 12-01 produced — open/header/finish structure the prune call slots into)
  - .planning/phases/12-run-log-capture-foundation/12-RESEARCH.md — Pattern 5 (Retention: ~20-line count+size loop), Pitfall 6 (retention racing a concurrent reader/pruner; live-pid rule), Pattern 6 (lexicographic ordering guarantee)
  - .planning/phases/12-run-log-capture-foundation/12-PATTERNS.md — "No Analog Found" row for the retention prune loop; Shared Pattern "Comments cite the planning-doc ID"
  - spec/run_log_spec.rb (Plan 12-01's spec — extend its retention group; tmpdir + default-deny guard already in place)
  </read_first>
  <files>lib/spm_cache/core/run_log.rb, spec/run_log_spec.rb</files>
  <behavior>
  - RED first: retention examples fail (prune undefined).
  - Count bound: fabricate runs_keep+2 files with ordered timestamp names in a tmpdir runs dir; open a new RunLog; exactly the newest runs_keep files plus the just-opened one remain.
  - Size bound: fabricate files whose total exceeds max_bytes; opening with a small budget prunes oldest-first until the total is under budget (assert exactly which names survive and which died first).
  - Ordering edge: the deletion order is strictly oldest-first lexicographic — the over-budget spec asserts the oldest name is gone and the newest fabricated name survives.
  - Current-run immunity: the just-opened run's own file always survives, even with runs_keep: 0 and max_bytes: 0 beyond its own size.
  - Live-pid immunity (Pitfall 6): a fabricated file whose run_start line carries pid == Process.pid and no run_end line survives pruning; the same-shaped file with a dead pid (use an out-of-range pid such as 2_000_000 — kill(0, pid) raises ESRCH) is pruned.
  - D-07 ordering: pruning happens after the header lands — observable because the current file exists during prune and is excluded by identity.
  - Prune failures degrade: a prune that cannot stat/read a candidate (already gone) is skipped, never raised into the run.
  </behavior>
  <precondition>Task 1 GREEN and committed (prune reads the config budgets).</precondition>
  <action>
  Add an instance method `prune(keep:, max_bytes:)` to Core::RunLog (~20 lines of stdlib per research Pattern 5): list runs_dir/*.jsonl, sort ascending (timestamp-prefixed names → lexicographic == chronological, oldest first); walk from the OLDEST end, deleting while (remaining count > keep) OR (remaining total bytes > max_bytes); skip the current run's own path by identity; before deleting any candidate, read its first line, parse the run_start pid, and skip the file when the pid is alive — probe with Process.kill(0, pid) rescuing Errno::ESRCH as dead and treating any other error (e.g. Errno::EPERM) as alive. File sizes via File.size; wrap the whole walk in a rescue-to-warn degradation (a prune failure never fails the run). Call prune from RunLog.open immediately AFTER the header rename lands and BEFORE returning self, with keep: Core::Config.instance.runs_keep and max_bytes: Core::Config.instance.runs_max_mb * 1024 * 1024 (D-07: cleanup at run start; rotation-time, no separate maintenance command). Cite D-06/D-07/SC4/Pitfall 6 in comments. Commit RED then GREEN.
  </action>
  <verify>
    <automated>bundle exec rspec spec/run_log_spec.rb</automated>
    <fails_when>exit code is non-zero; or "0 examples, 0 failures"; or the retention examples are pending</fails_when>
  </verify>
  <acceptance_criteria>
  - lib/spm_cache/core/run_log.rb contains `def prune` and open calls it after the header write with config-derived keep/max_bytes
  - spec/run_log_spec.rb retention group covers: count bound, size bound, oldest-first deletion order, current-file immunity, live-pid immunity + dead-pid pruning, degradation
  - `bundle exec rspec spec/run_log_spec.rb spec/main_run_log_spec.rb` exits 0 (prune did not break the tracer's e2e specs — they now also exercise prune implicitly)
  </acceptance_criteria>
  <done>Retention caps both count and total size at every run start, oldest-first, with current-run and live-pid immunity; committed.</done>
</task>

<task type="auto">
  <name>Task 3: .gitignore entry for .spm-cache/ (D-02)</name>
  <read_first>
  - lib/spm_cache/command/init.rb lines 200-211 (ensure_gitignore append-once pattern: read lines, early-return when present, append comment + entry)
  - .planning/phases/12-run-log-capture-foundation/12-CONTEXT.md — D-02 (".gitignore entry ships in this phase")
  - .planning/phases/12-run-log-capture-foundation/12-PATTERNS.md — command/init.rb section ("Extend independently — the existing shape is one entry per concern") and spec/init_spec.rb section
  - spec/init_spec.rb lines 55-70 (gitinclude assertion at line 60) and 140-170 (idempotency pair asserting scan count == 1)
  </read_first>
  <files>lib/spm_cache/command/init.rb, spec/init_spec.rb</files>
  <behavior>
  - After Command::Init runs on a fresh project, .gitignore contains both 'spm-cache/' and '.spm-cache/' entries, each under its own labeled comment.
  - Re-running init does not duplicate either entry (scan count for each literal == 1).
  - An existing .gitignore without the entry gains it appended after a blank line, matching the existing append shape.
  </behavior>
  <precondition>None beyond Plan 12-01 (init.rb is untouched by earlier plans).</precondition>
  <action>
  Extend Command::Init#ensure_gitignore (init.rb:200-211) to also append the entry '.spm-cache/' with its own comment line (e.g. '# spm-cache run logs'), keeping the existing 'spm-cache/' entry and its comment exactly as they are — one entry per concern, per the pattern note; do not generalize the method into an entry list unless it stays two independent append-once checks. Mirror the early-return idempotency check per entry. Add assertions to spec/init_spec.rb next to the existing ones (init_spec.rb:60 and the idempotency pair at 146-168): the '.spm-cache/' entry exists after init and gitignore.scan('.spm-cache/').length == 1 after re-init (careful: '.spm-cache/' contains 'spm-cache/' as a substring — the existing scan('spm-cache/').length eq(1) assertion must be updated or the new entry made substring-safe; prefer asserting exact line membership via include? on chomped lines plus a regex-anchored count for each entry). Cite D-02/LOGS-01 in the spec comment.
  </action>
  <verify>
    <automated>bundle exec rspec spec/init_spec.rb</automated>
    <fails_when>exit code is non-zero; or the idempotency example reports a scan count other than 1 for either entry; or "0 examples, 0 failures"</fails_when>
  </verify>
  <acceptance_criteria>
  - lib/spm_cache/command/init.rb ensure_gitignore appends '.spm-cache/' idempotently alongside the existing 'spm-cache/' entry
  - spec/init_spec.rb exits 0 with both entries asserted present-once after fresh init and after re-init
  </acceptance_criteria>
  <done>The runs dir is gitignored by init; committed.</done>
</task>

</tasks>

## Edge Coverage — LOGS-01 (specless probe)

| Edge | Probe question | Resolution | Owner |
|---|---|---|---|
| adjacency | concurrent runs / simultaneous prunes | resolved-explicit: separate files always; prune deletes only its own candidates oldest-first, never the current or live-pid runs; cross-process unlink races are benign on POSIX (open fds stay valid) | Plan 12-01 (concurrency) + this plan Task 2 (live-pid immunity) |
| empty | zero-output run / empty runs dir | resolved-explicit: prune on an empty or under-budget dir deletes nothing (covered by the count-bound example's setup); a zero-output run still gets retention treatment | this plan Task 2 |
| ordering | equal-key ordering stable | resolved-explicit: prune deletes strictly oldest-first lexicographic — the spec names exactly which files survive | this plan Task 2 (ordering edge truth) |

## Artifacts this phase produces

Full phase list (★ = created by THIS plan):
- ★ `Core::Config#runs_keep` / `Core::Config#runs_max_mb` + DEFAULT_CONFIG keys "runs_keep" (50) / "runs_max_mb" (500)
- ★ `Core::RunLog#prune(keep:, max_bytes:)` invoked from RunLog.open after the header lands (D-07)
- ★ yml-template documentation lines for runs_keep / runs_max_mb
- ★ `Command::Init` .gitignore entry `.spm-cache/`
- ★ extended spec/config_spec.rb, spec/run_log_spec.rb, spec/init_spec.rb
- `Core::RunLog` + TeeIO + StreamSink + `Config#runs_dir` + `--no-run-log` + `--log-dir` override + run_start/run_end vocabulary + spec/main_run_log_spec.rb (Plan 12-01)
- `Core::Sh` opts `live_log_out:`/`live_log_err:` + popen3 failure_detail tails + capture3 `sh` events + spec/sh_run_log_sink_spec.rb (Plan 12-02)
- `SPM::BuildPipeline.run(run_log:)` + `package_start`/`package_end` + `phase` markers + Buildable#xcodebuild sink forwarding (Plan 12-04)
- `RunLog.cycle_wrapper` + `Command::Watch` wrapped installer_factory + `trigger: "watch"` cycle files (Plan 12-05)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| subprocess stdout/stderr → run log | untrusted tool/package-controlled text crosses into a structured log (log-forging surface) — mitigated in Plans 12-01/02, unchanged here |
| local user → runs dir / spm-cache.yml / .gitignore | same-trust-domain local files (no remote input) |

## STRIDE Threat Register (ASVS Level 1; workflow.security_block_on = high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-04 | DoS (disk-fill via log growth) | RunLog append path / retention | high | mitigate | DISPOSED HERE: prune (count runs_keep=50 AND size runs_max_mb=500MB, oldest-first) runs at every run start after the header lands — the only bound D-05 full-fidelity capture needs; spec proves repeated runs leave exactly the retained set |
| T-12-03 | Tampering (symlink swap on runs dir) | RunLog#prune | low | accept | prune unlinks individual *.jsonl candidate names only — no rm_rf of the runs dir, no directory replacement; local project dir is same-trust (research Security Domain V4) |
| T-12-05 | Information disclosure (run logs in VCS) | runs dir placement | low | mitigate | this plan's .gitignore entry (.spm-cache/, D-02) keeps run logs out of git alongside the existing spm-cache/ sandbox entry |
| T-12-01 | Tampering (log-forging) | body lines | medium | mitigate | unchanged from Plans 12-01/02: JSON.generate escaping; events distinguishable by the `event` key; prune itself parses only its own run_start lines and ignores body content |
| T-12-02 | Tampering (path traversal via --log-dir) | runs_dir | low | accept | same-user local input; unchanged; retention budget values additionally Integer-coerced with rescue-to-default (yml is user-authored, not adversarial — research V5) |

No package installs in this phase (stdlib only; Package Legitimacy Audit table empty) — no [ASSUMED]/[SUS] package checkpoint required.
</threat_model>

<verification>
- `bundle exec rspec spec/config_spec.rb spec/run_log_spec.rb spec/init_spec.rb` — this plan's three surfaces, green.
- `bundle exec rspec` — full suite green.
</verification>

<success_criteria>
- SC4: repeated runs accumulate logs without unbounded growth — count+size hybrid proven with fabricated old runs.
- D-06 (hybrid retention + yml configurability), D-07 (rotation-time cleanup at run start), D-02 (.gitignore entry) all landed with specs.
- Ordering probe edge resolved-explicit (oldest-first, deterministic survivor set).
- T-12-04 (the phase's only high-severity threat) mitigated and spec-proven.
</success_criteria>

<output>
Create `.planning/phases/12-run-log-capture-foundation/12-03-SUMMARY.md` when done
</output>
