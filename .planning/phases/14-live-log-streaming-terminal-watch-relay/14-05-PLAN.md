---
phase: 14-live-log-streaming-terminal-watch-relay
plan: 05
type: execute
wave: 3
depends_on: ['14-03', '14-04']
files_modified:
  - lib/spm_cache/web/assets/log.js
  - lib/spm_cache/web/assets/index.html
  - lib/spm_cache/web/assets/styles.css
  - spec/web_frontend_spec.rb
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-VALIDATION.md
autonomous: false
requirements: [LOGS-02, LOGS-04, LOGS-05]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low   # estimate-calibration: 0 samples → factor 1, confidence low

must_haves:
  truths:
    - "D-07 (anchor vocabulary): the rail covers the FULL frozen Phase-12 D-04 vocabulary — phase chips 'detect' 'integrate' 'build' 'fidelity' (verbatim) + one chip per distinct package_start name, deduped by name (first position wins, A10) — chips render only as their anchor events arrive, no placeholders — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-08 (rail placement): the chip rail is sticky at the log panel's edge (200px fixed, own inner scroll, 'Phases' then 'Packages' group labels always rendered, chips stacked vertically) — visible while following the tail — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-09 (jump + filter): chip click jumps to the anchor's divider line AND sets the filter — a package filter matches its package_start..package_end segment inclusive; a phase filter matches from the marker to the line before the next phase marker or package_start; lines before the first anchor dim under any filter; filtering DIMS via the muted color, never hides (A5 — positions stay stable); the active chip renders in the accent badge style with aria-pressed=true; clicking the active chip clears the filter with the view staying put — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-09 (filter pill): 'filtered: {package}' / 'filtered: {phase}' — ONE button; activating it clears the filter (view position stays); ellipsizes at a 240px max-width with a tooltip — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-10 (piercing): on run failure while a filter is active, the banner renders REGARDLESS of filter state; activating 'Jump to first error' CLEARS the filter (pill removed, chips revert) and THEN jumps to the error's real position — failure visibility beats filter intent — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-04 (auto-switch): a switch event switches the stream UNCONDITIONALLY (even mid-read of an older run — including a run pinned via ?run=: 14-03 delivers switch broadcasts to pinned connections, and the handler answers by dropping the pin, closing the EventSource, and reconnecting unpinned): fresh replay from byte 0, follow on, filter cleared, banner re-derived for the new run, and a persistent switch notice 'switched to new run — previous: {run-id}' — ONE slot (latest wins, A7), never auto-dismissed, {run-id} an interactive control that loads the previous run in place (the 14-03 ?run= path); NO notice when there is no previously-displayed run (first run of a session / a switch into the empty state — the card simply populates) — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-12 (dropdown): a native select labeled 'Recent runs'; every OPEN re-fetches /api/runs (X-SPM-Token header, envelope check — statuses freshly derived, never cached); entries '{glyph} {command} · {relative}' with ' · viewing' on the displayed run, 10 newest first; empty dir → one disabled 'No runs yet' entry; fetch pending → a 'Loading…' entry; fetch failure → 'Couldn't load the run list: {message}. Reload the page to retry.'; selecting an entry loads that run in place from byte 0 (?run=) — live selection follows, finished selection replays with follow off — bundle exec rspec spec/web_frontend_spec.rb"
    - "LOGS-04/D-05 (in-stream attribution): the lock-wait line renders VERBATIM as a normal out line — no badge, no special styling; the attribution IS the line (A11); server notice lines render verbatim as '! {message}' in warn — the two known strings '{N} lines dropped' and 'run log pruned while viewing; switching to newest' pass through untouched — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-14 (recorded probe): the agent-browser streaming probe runs against a REAL server + REAL terminal run and is recorded verbatim: live render with identity card + anchor rail + follow-tail; mid-run replay in a second tab; kill/restart reconnect without lost or duplicated lines; failing run → sticky banner + ✗ card; (plus the remaining 14-VALIDATION manual rows: filter/banner piercing, watch-cycle auto-switch, lock-wait attribution) — recorded in 14-05-SUMMARY.md with the 14-VALIDATION manual table ticked — human-check verification, never auto-passed"
    - "UI-SPEC state:empty/anchor rail — group labels 'Phases'/'Packages' always render; zero chips until anchors arrive — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:empty/recent-runs dropdown — single disabled entry 'No runs yet' — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:loading/recent-runs dropdown — on-open fetch shows a single 'Loading…' entry until /api/runs lands — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:error/recent-runs dropdown — the pinned fetch-failure copy renders — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:error/failure banner — THE failure surface: pierces filters (D-10) — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:error/in-stream notices — '! {message}' warn lines; the pruned-run notice falls back to fresh replay — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:populated/anchor rail — 4 phase chips + one chip per distinct package_start name, deduped, first position — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:partial/anchor rail — chips appear only as their anchors arrive; no placeholder chips for unseen phases/packages — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:overflow/anchor rail — rail inner-scrolls at viewport height; 200px width fixed — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:overflow/in-stream notices — one switch-notice slot (latest wins); notice text wraps to two lines max — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:zero-one-many/anchor rail — one vertical stack, zero/one/many chips alike; chip labels are names — no counted copy — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:long-text/anchor rail — chip labels ellipsize at the 200px rail width with title tooltips — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:long-text/pills — the filter pill ellipsizes at 240px max-width with a tooltip — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:long-text/in-stream notices — two-line max wrap; run ids inside notices ellipsize with tooltips — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:long-text/recent-runs dropdown — the compact entry template keeps options platform-single-line — bundle exec rspec spec/web_frontend_spec.rb"
  artifacts:
    - "lib/spm_cache/web/assets/log.js (MOD: anchor rail + chip dedupe, jump+filter dimming, filter pill, banner piercing via the Task-2 clearFilter seam, auto-switch + switch-notice control, recent-runs dropdown, notice rendering)"
    - "lib/spm_cache/web/assets/index.html (MOD: the dropdown select + label slot materialized in the panel header)"
    - "lib/spm_cache/web/assets/styles.css (MOD: chip rail, dim/filter classes, filter pill, dropdown, switch-notice rules — existing tokens only)"
    - "spec/web_frontend_spec.rb (MOD: anchor/filter/switch/dropdown/notice describes — the plan's automated gates)"
    - ".planning/phases/14-live-log-streaming-terminal-watch-relay/14-VALIDATION.md (MOD: manual table rows ticked with dates/evidence once the D-14 probe is recorded)"
  key_links:
    - "Chip jumps and the jump-to-error ride the SAME position registry (line element ↔ anchor identity) — D-10's real-position jump and A5's dim-not-hide make positions stable and meaningful"
    - "The switch-notice's {run-id} control and every dropdown selection funnel through ONE loadRun(name) path that closes the current EventSource and reconnects with ?run= (14-03's validated param) — no second replay mechanism"
    - "The dropdown's /api/runs fetch is log.js's own token-gated fetch with the X-SPM-Token header (the app.js request-layer pattern, duplicated — the modules do not import each other); envelope {status,data} checked; failures render the pinned copy and keep the last good list"
    - "Filter state is pure CSS-class toggling over the ring (no DOM restructuring) — layout never shifts when filters toggle"
  prohibitions:
    - statement: "MUST NOT hide filtered lines — filtering DIMS in the muted color; hidden lines would break D-10's real-position jump and shift layout (A5)"
      status: resolved
      verification: test
    - statement: "MUST NOT render the banner conditionally on filter state — it pierces; and the jump exits the filter first (clear, then jump)"
      status: resolved
      verification: test
    - statement: "MUST NOT cache /api/runs results across opens — statuses are freshly derived per open (CP10 in the UI; UI-SPEC interaction row)"
      status: resolved
      verification: test
    - statement: "MUST NOT add special styling or a badge to the lock-wait line — it renders as a normal out line, verbatim (A11; D-05 chose inline attribution)"
      status: resolved
      verification: test
    - statement: "MUST NOT stack switch notices — exactly one slot, latest wins (A7); history lives in the dropdown"
      status: resolved
      verification: test
---

# Plan 14-05 — Frontend completion: anchors/filter/piercing, auto-switch, recent runs, notices + the D-14 recorded probe (phase weld)

<planning_context>
phase: 14 — Live Log Streaming + Terminal/Watch Relay
mode: standard
sources: 14-UI-SPEC.md (anchor/filter/banner-pierce/switch/dropdown/notice contracts + remaining state-coverage rows), 14-CONTEXT.md D-04/D-05/D-07..D-10/D-12/D-14, 14-RESEARCH.md § Validation Architecture manual table + Open Question 1 (?run=, confirmed), 14-VALIDATION.md (the 7 manual rows this plan executes and records), ROADMAP Phase 14 SC1-SC4, REQUIREMENTS LOGS-02/04/05
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase and the phase's final gate before /gsd-verify-work: its Task 3 recording is the evidence base for the D-14 sign-off row in 14-VALIDATION.md. Phase 15's build controls ride the same stream module and the reserved UI badge without edits to this plan's surfaces.
</downstream_consumer>

<objective>
Complete the log panel's interaction surface — the full-vocabulary anchor rail with jump+filter
and banner piercing, unconditional auto-switch with the previous-run notice, the recent-runs
dropdown over /api/runs, and verbatim notice/lock-wait rendering — then weld the phase shut:
run and record the D-14 agent-browser probe against a real server and real terminal runs, with
the full suite green.

Purpose: SC1's anchors (LOGS-02), SC3's same-view relay proven live (LOGS-04), SC4's identity
surfaces complete (LOGS-05), and the manual verification net that caught Phase 13's G-13-1
pointed at the streaming stack before the phase seals.

Output: completed log.js/index/styles, the extended frontend spec, the recorded probe in
14-05-SUMMARY.md + a ticked 14-VALIDATION manual table.
</objective>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|---|---|---|
| Anchor rail + chip model | asset (mod) | log.js: chips from package_start + phase events, dedupe-by-name (A10), position registry, jump + filter-set/clear |
| Filter/dim engine + filter pill | asset (mod) | CSS-class dimming (A5), package/phase segment matching, 240px pill, banner piercing via the clearFilter seam |
| Auto-switch + switch notice + loadRun | asset (mod) | unconditional switch handling, one-slot notice (A7), single ?run= reconnection path |
| Recent-runs dropdown | asset (mod) | native select, per-open fresh /api/runs fetch, entry template + viewing suffix, failure copy |
| Notice + lock-wait rendering | asset (mod) | '! {message}' warn lines; lock-wait line as a plain out line (A11) |
| D-14 probe recording | record | 14-05-SUMMARY.md section + 14-VALIDATION.md manual-table ticks — dates, commands, observed outcomes verbatim |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-CONTEXT.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-SPEC.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-RESEARCH.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-VALIDATION.md
@lib/spm_cache/web/assets/app.js
@lib/spm_cache/web/assets/log.js
@lib/spm_cache/web/assets/index.html
@lib/spm_cache/web/assets/styles.css
@spec/web_frontend_spec.rb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Anchor rail, jump + filter dimming, filter pill, banner piercing (D-07..D-10) (RED → GREEN)</name>
  <precondition>Plan 14-04 committed (the rail container, position registry seam, and clearFilter seam exist).</precondition>
  <reversibility rating="reversible">Interaction layer behind the shipped DOM contract and copy pins.</reversibility>
  <read_first>
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-SPEC.md — Interaction row 'Anchors (D-07–D-09)' (segment-matching rules verbatim), row 'Failure (D-03/D-10)' (pierce + exit-filter semantics), Copywriting rows 'Filter pill', 'Anchor rail group labels', 'Anchor chips', 'Filter piercing'
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-CONTEXT.md — D-07 (full frozen vocabulary — the four phase markers + package_start, no subset), D-08/D-09/D-10
  - lib/spm_cache/web/assets/log.js (14-04 output — the ring, registry, and banner seams this task builds on)
  - lib/spm_cache/web/assets/app.js lines 241-252 (renderLegend — the chip-rail DOM precedent)
  </read_first>
  <files>lib/spm_cache/web/assets/log.js, lib/spm_cache/web/assets/styles.css, spec/web_frontend_spec.rb</files>
  <behavior>
  - RED first: 10 new failing examples:
    1. Chip generation: a package_start entry yields a package chip named verbatim; the four phase markers yield 'detect'/'integrate'/'build'/'fidelity' chips; duplicate package names yield ONE chip (first position, A10); no chips for package_end/run_start/run_end/sh/unknown keys.
    2. Rail DOM: 'Phases' label precedes 'Packages'; chips stacked; labels ellipsize at rail width with title tooltips (CSS class pins).
    3. Jump: chip activation scrolls to the anchor's divider line element (registry lookup), disengages follow (the 14-04 seam).
    4. Filter set — package: lines within the package's package_start..package_end segment (inclusive) stay primary; all others (including lines before the first anchor) get the dim class — NO line is removed from the DOM (A5).
    5. Filter set — phase: from the phase marker to the line before the next phase marker OR package_start.
    6. Filter pill: 'filtered: {package}' / 'filtered: {phase}' — one button; activating clears the filter (dim classes removed, pill removed) with the scroll position unchanged; 240px max-width + ellipsis + tooltip.
    7. Active chip state: accent badge styling + aria-pressed=true; clicking the ACTIVE chip clears the filter (view stays put).
    8. Piercing: with a filter active, a failing run_end still renders the banner (the banner path is filter-independent — source-structure pin).
    9. Exit-filter jump: 'Jump to first error' under an active filter clears the filter FIRST (pill removed, dim removed, chips revert) and THEN jumps to the error line's real position.
    10. Eviction interplay: an anchor jump whose target line has been evicted from the ring lands on the oldest retained line (the head notice already names reload) — never a no-op.
  </behavior>
  <action>
  Extend spec/web_frontend_spec.rb with the anchor/filter describe (source-contract pins per the file's idiom). Implement in log.js: the chip model (event-driven, dedupe-by-name), the position registry (anchor identity → line element, eviction-aware), segment bookkeeping on package_start/package_end/phase entries as they stream in (both live and during replay — the same entry path), the filter engine as pure CSS-class toggling, the filter pill, and the piercing wiring through 14-04's clearFilter seam. styles.css: chip/dim/pill rules on existing tokens (accent fill on the active chip per the badge pattern; dim = muted text AND glyph). Commit RED first: `test(14-05): failing anchor/filter/pierce specs (10 examples)`, then GREEN: `feat(14-05): anchor rail + jump/filter dimming + banner piercing`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or any gate observes line REMOVAL under filter (A5 violation); or the banner render path is gated on filter state (pierce broken)</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_frontend_spec.rb green with the 10 new rows
  - The full frozen vocabulary renders as anchors — all four phase markers plus package chips (no subset)
  </acceptance_criteria>
  <done>Anchors jump and filter with stable positions, filters dim rather than hide, and failure visibility pierces every filter state. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-switch + switch notice, recent-runs dropdown, notice/lock-wait rendering (D-04/D-05/D-12) (RED → GREEN)</name>
  <precondition>Task 1 GREEN and committed; Plan 14-03 committed (?run= and /api/runs live).</precondition>
  <reversibility rating="reversible">Behavior layer on shipped contracts; the ?run= path and payload shapes are already fixed server-side.</reversibility>
  <read_first>
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-SPEC.md — Interaction rows 'Auto-switch (D-04)' (unconditional; replay/follow/filter/banner re-derivation), 'Recent-runs dropdown (D-12)', 'Structured events in-stream', Copywriting rows 'Switch notice', 'Recent-runs dropdown label', 'Dropdown entry template', 'Server notice lines (CP11)', 'Lock-wait line (D-05)', 'Log-panel fetch failure'
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-CONTEXT.md — D-04 (previous stays reachable), D-12 (no DB — a directory listing), D-05 display half
  - lib/spm_cache/web/assets/log.js (14-04 Task 1 shipped the switch/notice handlers as minimal renders — deepen them here)
  - lib/spm_cache/web/assets/app.js lines 70-96 (the request layer pattern the dropdown fetch duplicates: token header, envelope check, error throw)
  </read_first>
  <files>lib/spm_cache/web/assets/log.js, lib/spm_cache/web/assets/index.html, lib/spm_cache/web/assets/styles.css, spec/web_frontend_spec.rb</files>
  <behavior>
  - RED first: 12 new failing examples:
    1. Switch handling: a switch event triggers fresh replay, follow ON, filter cleared, banner slot re-derived for the new run — unconditional (no view-state guard). Cross-plan rule (pinned with 14-03): switch broadcasts arrive on ?run=-pinned connections too; the handler drops the pin, closes the EventSource, and reconnects via the plain stream URL (no ?run= — current-or-newest is the new run). On an unpinned connection either in-place reset (the tailer already serves the new run from byte 0) or reconnect is valid — both land in the identical D-04 rendered state.
    2. Switch notice: 'switched to new run — previous: {run-id}' renders in the notice slot; ONE slot — a second switch REPLACES the text; never auto-dismissed; {run-id} is an interactive control in the accent style.
    3. Notice suppression: no previously-displayed run → NO notice (first run of a session; a switch arriving into the empty state).
    4. loadRun: the {run-id} control and dropdown selections share ONE path — close the EventSource, reconnect with '?run=' + encodeURIComponent(name) — no page reload, no second mechanism.
    5. Dropdown fetch: on OPEN, a token-gated fetch of '/api/runs' with the X-SPM-Token header; envelope status checked; the ' · viewing' suffix marks the displayed run.
    6. Entry template: '{glyph} {command} · {relative}' with the glyph vocabulary per derived state (● running, ✓ success, ! interrupted, ✗ failed); 10 newest, newest first.
    7. Empty dir: one disabled 'No runs yet' entry.
    8. Fetch pending: a single 'Loading…' entry.
    9. Fetch failure: 'Couldn't load the run list: {message}. Reload the page to retry.' rendered in the panel; the last good list kept (app.js panel-error posture).
    10. Fresh derivation: no caching of the runs response across opens (source pin: the fetch fires per open event).
    11. Notice rendering: notice events render '! {message}' in warn, message verbatim; the two known server strings pass through byte-identical (pin both literals as pass-through, not client-authored).
    12. Lock-wait: the 'Waiting for build lock…' body line renders as a plain out line — no badge class, no special-case code path for it in log.js (absence pin).
  </behavior>
  <action>
  Materialize the dropdown in index.html (native select + 'Recent runs' label in the header slot 14-04 reserved). Implement in log.js per the bullets: the switch handler's full reset semantics, the one-slot notice with its interactive run-id control, loadRun(name) over ?run=, the dropdown controller (per-open fetch, template rendering, viewing suffix, failure copy), and verbatim notice rendering. styles.css: dropdown/notice/accent-control rules on existing tokens. Commit RED first: `test(14-05): failing switch/dropdown/notice specs (12 examples)`, then GREEN: `feat(14-05): auto-switch + recent-runs dropdown + notice rendering`. Then the pre-probe gate: full suite `bundle exec rspec` green.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb spec/web_integration_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the switch path is guarded by view state (not unconditional); or a second notice slot can accumulate (A7 violation); or the lock-wait line gains a badge/styling class (A11 violation)</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_frontend_spec.rb green with the 12 new rows (55 new examples across 14-04+14-05)
  - Full suite `bundle exec rspec` green BEFORE the probe runs
  </acceptance_criteria>
  <done>Terminal- and watch-started runs share the view with everything else, previous runs stay reachable in place, and every server notice renders verbatim. The panel is feature-complete per the UI contract. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: D-14 recorded agent-browser probe — real terminal run, live render, second-tab replay, kill/restart reconnect, failing-run banner (+ the remaining manual rows) + phase gate</name>
  <precondition>Tasks 1-2 GREEN and committed; full suite green; a scratch project directory is available for a real run (do not use a project with uncommitted user work).</precondition>
  <reversibility rating="reversible">Verification-only: no production edits expected; any gap found is fixed in the owning module with its spec first.</reversibility>
  <read_first>
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-VALIDATION.md — § Manual-Only Verifications (the 7 rows; this task executes and records them)
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-CONTEXT.md — D-14 (the probe is a recorded, repeatable step — the net that caught G-13-1)
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-05-SUMMARY.md (the G-13-1 precedent: why the real-browser net exists)
  - lib/spm_cache/web/marker.rb (server.json carries port/token for the probe's dashboard URL)
  </read_first>
  <files>.planning/phases/14-live-log-streaming-terminal-watch-relay/14-VALIDATION.md</files>
  <behavior>
  - Executed protocol (agent-driven where tooling allows; the browser-visible assertions run in the user's default browser via the existing auto-open path — D-14's own shape; the user confirms what only eyes can confirm):
    1. Real boot: `spm-cache web --no-open --port=0` in a scratch real project; read port/token from the marker; open the dashboard (auto-open or the printed URL).
    2. Real terminal run: start `spm-cache use` (or build) in a terminal in that project — assert LIVE render: lines appear as the run proceeds, identity card shows trigger terminal + command, anchor chips appear, follow-tail tracks.
    3. Mid-run replay: open a SECOND dashboard tab mid-run — assert it replays the run from the first line, then follows (LOGS-03).
    4. Reconnect without loss: kill -9 the server mid-run, restart `spm-cache web`, resume/reopen the tab — assert the stream resumes via Last-Event-ID with NO lost and NO duplicated lines (the integration row proved the mechanism; this proves it browser-true).
    5. Failing run: force a failing run (a failing `use` in an empty dir exercises the failure paths without xcodebuild) — assert the sticky banner with exit status, the jump-to-first-error anchor working, and the card dot flipping to ✗.
    6. Remaining manual rows (14-VALIDATION): filter/banner piercing on a package filter + foreign-package failure; watch-cycle relay + auto-switch — setup pinned per the cross-plan rule: run `spm-cache watch` until ≥ 2 cycle runs exist, select an OLDER finished cycle from the recent-runs dropdown (the ?run= pin — the stricter setup: it proves switch broadcasts reach pinned connections), then trigger a new cycle; assert the view switches to the new cycle's run (fresh replay, follow on) with the 'switched to new run — previous: {run-id}' notice naming the previously-pinned run; lock-wait attribution (hold the build lock from one terminal run, start a second — the blocked run shows the line inline).
    7. Record EVERY row in 14-05-SUMMARY.md: the exact commands, timestamps, observed outcomes (pass/fail + evidence), and tick the corresponding rows in 14-VALIDATION.md's manual table with the date.
  - Honesty rule: any row that cannot be executed (tooling, environment) is recorded as NOT RUN with the blocker — never marked pass, never silently skipped; a FAILING row is a gap: fix the owning module (spec first) and re-run the row before this task completes.
  </behavior>
  <action>
  Execute the protocol above. Work in a scratch tmp project (tmpdir + a real Package.swift/Xcode fixture is acceptable — the point is a REAL CLI run writing a REAL run log, not a fixture file). The agent-executable subset (boot, terminal run, raw-socket SSE checks alongside the browser view, kill -9/restart) is driven directly; the browser-visible assertions are confirmed with the user through the open dashboard. Record everything verbatim in the SUMMARY, tick 14-VALIDATION.md's manual rows, then run the phase gate: full suite `bundle exec rspec` green. Commit: `test(14): D-14 recorded agent-browser probe + manual table ticks` (include the 14-VALIDATION.md edit).
  </action>
  <verify>
    <automated>bundle exec rspec</automated>
    <fails_when>exit code non-zero; or the summary lacks a per-row recording; or any 14-VALIDATION manual row remains unticked without a NOT RUN + blocker note</fails_when>
    <human-check>The D-14 browser assertions as observed in the user's browser: (1) live render of a terminal-started run with identity card + anchor rail + follow-tail; (2) second-tab mid-run replay from the first line then follow; (3) after kill -9 + server restart, the tab reconnects and resumes with no lost/duplicated lines; (4) failing run shows the sticky banner, working jump anchor, and ✗ card; (5) filter active + failure in another package still shows the banner and the jump clears the filter; (6) while an older finished run is pinned via the recent-runs dropdown, a newly triggered watch cycle auto-switches the view with the 'switched to new run — previous: <run-id>' notice naming the pinned run; (7) lock-blocked run shows 'Waiting for build lock…' inline. Each is recorded pass/fail with evidence in 14-05-SUMMARY.md.</human-check>
  </verify>
  <acceptance_criteria>
  - All 7 manual rows recorded in 14-05-SUMMARY.md (pass with evidence, or NOT RUN + blocker — no silent gaps)
  - 14-VALIDATION.md manual table ticked with dates; the frontmatter wave_0/status lifecycle fields are left for validate-phase
  - Full suite `bundle exec rspec` green — the phase gate
  </acceptance_criteria>
  <done>The phase's streaming claims hold in a real browser against real CLI runs — the same net that caught Phase 13's ship-blocker is green — and the evidence is recorded where the phase audit will look. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run-log text + package names → anchor chips, dropdown entries, notices | attacker-adjacent data reaching new DOM surfaces — the el()/textContent discipline extends to every new element (T-13-13, carried from 14-04) |
| /api/runs responses → dropdown | server-authored but project-derived data (command names from argv); rendered as text, never interpreted |
| probe recording → phase sign-off | the recorded evidence is the audit's source — a fabricated or silently-skipped recording would launder an unverified claim |

## STRIDE Threat Register (ASVS Level 1; block on high)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-14-21 | Tampering (stored XSS via package names / run ids in chips, dropdown, notices — new surfaces of the T-14-16 class) | log.js new renderers | high | mitigate | every new element built through el()/textContent (the spec's XSS gates cover the whole file, not just 14-04's surfaces); chip labels are names as text, never markup |
| T-14-22 | Tampering (a filter that hides failure — the D-10 pierce regressed by a future refactor) | filter/banner interplay | medium | mitigate | spec rows: banner render path filter-independent + jump clears filter first; the D-14 manual row exercises it browser-true |
| T-14-23 | Information disclosure (stale cached /api/runs misattributing state — CP10 violated client-side) | dropdown controller | low | mitigate | per-open fetch, no response caching (spec pin); statuses always freshly derived server-side |
| T-14-24 | Tampering (probe recording integrity — claiming verification that did not happen) | the D-14 recording | medium | mitigate | per-row evidence with commands + timestamps; NOT RUN + blocker is the only sanctioned non-pass state; the human-check gate keeps browser assertions honest |

No package installs in this plan.
</threat_model>

<verification>
- `bundle exec rspec spec/web_frontend_spec.rb spec/web_integration_spec.rb` — green.
- `bundle exec rspec` — full suite green (the phase gate, run in Task 3).
- The recorded D-14 probe + ticked manual table in 14-VALIDATION.md — the manual half of the phase's sign-off (per <human-check> in Task 3).
</verification>

<success_criteria>
- SC1 complete (LOGS-02): single live stream with the full-vocabulary anchor rail, working jump/filter/pierce mechanics.
- SC3 complete (LOGS-04): terminal- and watch-initiated runs proven live in the same browser view as the (future) UI-triggered shape — recorded browser-true.
- SC4 complete (LOGS-05): identity surfaces finished (trigger/command/status + interrupted honesty + in-stream lock attribution), with D-14 evidence.
- The phase is ready for /gsd-verify-work with zero unticked validation rows.
</success_criteria>

<output>
Create `.planning/phases/14-live-log-streaming-terminal-watch-relay/14-05-SUMMARY.md` when done (including the D-14 probe recording section)
</output>
