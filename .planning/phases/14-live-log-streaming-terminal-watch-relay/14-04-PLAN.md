---
phase: 14-live-log-streaming-terminal-watch-relay
plan: 04
type: execute
wave: 2
depends_on: ['14-01']
files_modified:
  - lib/spm_cache/web/assets/log.js
  - lib/spm_cache/web/assets/index.html
  - lib/spm_cache/web/assets/styles.css
  - spec/web_frontend_spec.rb
autonomous: true
requirements: [LOGS-02, LOGS-03]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low   # estimate-calibration: 0 samples → factor 1, confidence low

must_haves:
  truths:
    - "LOGS-02 (stream core): a connected log panel renders the live stream — body 'out' lines verbatim in primary text, 'err' lines with the '✗ ' prefix in :fail, package_start/phase events as '── {name} ──' muted dividers, run_start/package_end/sh rendering NO line, unknown event keys ignored (T-12-01) — bundle exec rspec spec/web_frontend_spec.rb"
    - "LOGS-03/D-13 (cold load): three branches render — live run: '● running' card + replay from byte 0 + follow engages at replay completion; no live run but runs exist: newest run replayed, follow off, card carries the end state + 'completed {relative} ago'; empty runs dir: 'No runs yet' + 'Run spm-cache build to produce the first run log.' with only the connection pill live — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-01 (follow/pause): follow pins the viewport to the newest line on every append; ANY upward scroll disengages; the pill 'paused — {N} new lines · jump to live' (ONE button, whole label) appears once ≥ 1 line queued, {N} live-incrementing and uncapped; activating it re-engages, clears {N}, removes the pill — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-02 (ring): the DOM holds the newest 500 line elements of the displayed run; eviction updates the head notice '… {N} earlier lines — reload to replay from start' ({N} = total evicted, plain text, never clickable) — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-03 (failure): non-zero run_end → sticky banner 'Run failed — exit status {N}' + button 'Jump to first error', card flips to '✗ failed', err lines styled :fail — no dismiss control (replaced only by viewing another run) — bundle exec rspec spec/web_frontend_spec.rb"
    - "CP14 (interrupt): pid-dead-no-exit (hello/interrupt status) renders '!' + 'interrupted — exit unknown' — the interrupt banner 'Run interrupted — exit unknown.' + the same jump button — never 'running' — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-06 (identity card): three pinned rows above the stream — status glyph+word · trigger badge · command (mono 600) · 'Config {config}'; 'Started {MMM d, HH:MM}' + ' · completed {relative} ago' when finished; argv + run id ellipsized one line with title tooltips, ' · credentials redacted' appended when the header says so; unattributable Config renders '—' — bundle exec rspec spec/web_frontend_spec.rb"
    - "D-11 (badge): the trigger value renders verbatim with NO allowlist and NO special case — 'terminal' neutral, 'watch' plugin(indigo), unknown values verbatim neutral; the 'UI' badge is data-absent in Phase 14 (no code path emits it) and appears unchanged when Phase 15 emits it — bundle exec rspec spec/web_frontend_spec.rb"
    - "Connection pill (UI-SPEC): '● connecting…' before first open → '● connected' on open → '↻ reconnecting…' on transient drop; permanent failure (readyState CLOSED) is NOT a pill — main.content is replaced by the locked token-invalid page 'This page's access token is no longer valid. Restart spm-cache web and open the URL it prints.' — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:empty/stream viewport — zero runs renders 'No runs yet' + the spm-cache-build next step; card/banner/pills absent — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:empty/identity card — card absent entirely with no run to show; the empty copy owns the panel — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:loading/stream viewport + card + pill — muted 'Loading…' at Label size before the first stream byte; no card until the hello payload arrives; '● connecting…' until first open — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:error/stream viewport — CLOSED → token-invalid page; transient loss keeps last lines + '↻ reconnecting…' — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:error/identity card — pid-dead-no-exit renders the honest interrupt, never 'running' — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:populated/stream viewport — follow-locked mono tail; err lines ✗-prefixed in fail; dividers mark anchors — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:populated/identity card — pinned rows per D-06 above — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:partial/identity card — unattributable Config '—'; redacted argv suffix — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:partial/stream viewport — missing run_end renders running — never guessed-complete — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:overflow/stream viewport — 500-line ring + head notice; the viewport is the panel's one sanctioned inner-scroll surface — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:overflow/identity card — card rows never scroll the panel; over-long values ellipsize — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:overflow/failure banner — single-line bar between card and stream; the stream shrinks; nothing overlaps — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:zero-one-many/stream viewport — any line count, identical geometry — the ring absorbs volume — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:zero-one-many/identity card — exactly the one displayed run (N runs live in the dropdown, 14-05) — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:long-text/identity card — run id and argv ellipsize on one line with title tooltips of the full value — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:long-text/pills — mono {N} digits never shift layout — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC state:long-text/failure banner — the template interpolates the numeric status only; wraps on narrow viewports, never clips — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC layout — panel order: Run Log FIRST, then Cache State, Doctor, Dependency Graph (A1); card → xl(32px) break → stream group stacked sm; stream viewport 480px/#0D1117/pre-wrap Label-mono + anchor-rail slot 200px; overlay pill row inside the viewport's bottom edge — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC a11y — panel is a section with h2 'Run Log'; stream viewport role=log, aria-live=off, aria-label='Run output', tabindex=0; card status container + connection pill aria-live=polite; failure banner role=alert; :focus-visible accent ring on every control; native button/select only; no color-only encoding (every status pairs glyph + word) — bundle exec rspec spec/web_frontend_spec.rb"
    - "UI-SPEC interaction — EventSource('/api/events?token=' + token) from the sessionStorage token; NO polling in the log panel; app.js's state-table loop continues independently (a dead stream never takes the table down, and vice versa); jump targets degrade honestly under ring eviction (first err line → fallback final line → fallback oldest retained line, never a dead control) — bundle exec rspec spec/web_frontend_spec.rb"
    - "Relative times compute from SERVER timestamps only (hello 'now' / run ts), never the client clock — bundle exec rspec spec/web_frontend_spec.rb"
  artifacts:
    - "lib/spm_cache/web/assets/log.js (NEW: second ES module — EventSource lifecycle, hello/entry/notice rendering, identity card, follow/pause pill, 500-line ring, failure + interrupt banners, cold-load branches, connection pill, token-invalid reuse)"
    - "lib/spm_cache/web/assets/index.html (MOD: Run Log panel section FIRST + log.js module tag after app.js, referenced RELATIVE as assets/log.js)"
    - "lib/spm_cache/web/assets/styles.css (MOD: log-section rules on the existing token sheet — zero new tokens; accent extended to controls+liveness only per A3)"
    - "spec/web_frontend_spec.rb (MOD: log.js/index/styles source-contract describes — XSS/date/offline gates extended to the new module)"
  key_links:
    - "log.js consumes 14-01's wire contract verbatim: named events hello/entry/switch/notice via addEventListener; JSON.parse per data line; rendering keyed ONLY on the parsed 'event' field (T-12-01); switch/notice handlers exist from the start even though their UI surfaces complete in 14-05 (the module ships whole, never half-wired)"
    - "Token path: sessionStorage key 'spm-cache-web-token' (same key app.js wrote at bootstrap) → EventSource query param; the ONE place log.js cannot copy app.js's fetch layer — SSE has no envelope and no 401 branch; CLOSED → the shared token-invalid page replacement"
    - "index.html references assets/log.js through the SAME relative 'assets/<name>' form Phase 13's G-13-1 fix pinned — it resolves through the router's /assets/* arm"
    - "styles.css adds log classes only on existing tokens (the 13-UI-SPEC sheet is untouched); verdict glyphs never take accent (A3)"
  prohibitions:
    - statement: "MUST NOT use innerHTML, insertAdjacentHTML, document.write, or outerHTML anywhere in log.js — every dynamic string (log text, run ids, argv, package names) renders through el()/textContent; run text is untrusted subprocess output (UI-SPEC Prohibition 2, T-13-13)"
      status: resolved
      verification: test
    - statement: "MUST NOT reference any asset with a scheme-absolute URL or a non-relative path, and MUST NOT add a framework, build step, npm dependency, or CDN (UI-SPEC Prohibition 1; G-13-1)"
      status: resolved
      verification: test
    - statement: "MUST NOT call Date.now() (or any client clock) for stamps or relative times — server timestamps only (UI-SPEC Prohibition 5, T-13-16)"
      status: resolved
      verification: test
    - statement: "MUST NOT animate the stream beyond scrolling — no pulses, transitions, spinners, or smooth-scroll; follow and jumps set scroll position instantly (UI-SPEC Prohibition 4)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT use color-only status encoding — every status color pairs a glyph and, in the card, a word (UI-SPEC Prohibition 3)"
      status: resolved
      verification: test
    - statement: "MUST NOT touch app.js — the dashboard's and the stream's failure domains stay separate (a stream bug must never take down the state table); log.js is a sibling module, not a merge"
      status: resolved
      verification: test
---

# Plan 14-04 — Frontend stream core: log.js module, identity card, follow/ring, banners, cold load

<planning_context>
phase: 14 — Live Log Streaming + Terminal/Watch Relay
mode: standard
sources: 14-UI-SPEC.md (checker-approved design contract — Copywriting/Layout/Interaction/Accessibility contracts + the state-coverage rows lifted into must_haves), 14-CONTEXT.md D-01/D-02/D-03/D-06/D-11/D-13 + the LOC-budget note permitting a second module, 14-RESEARCH.md Pattern 6 (frontend mechanics), 14-PATTERNS.md (app.js analog — el()/textContent discipline, sessionStorage token, empty-state and panel-error postures; EventSource lifecycle = NO analog), ROADMAP Phase 14 SC1/SC2, REQUIREMENTS LOGS-02/LOGS-03
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase. It builds the log panel's structural core that 14-05 completes (anchor rail chips + filter/piercing, auto-switch notice, recent-runs dropdown, D-05 line rendering are 14-05's surfaces — this plan ships their containers and event wiring, not their behaviors). Phase 15's UI-triggered runs ride this module unchanged.
</downstream_consumer>

<objective>
Add the Run Log panel and its dedicated ES module: a live SSE-driven stream view with the
D-06 identity card, D-01 auto-follow with pause pill, D-02 bounded render ring, D-03 failure
and CP14 interrupt banners with jump-to-first-error, the three-branch D-13 cold load, the
connection pill with the token-invalid terminal state, and honest empty/loading/error shapes —
all in the Phase 13 discipline (vanilla ES module, zero innerHTML, fully offline, server-time
stamps only).

Purpose: SC1's display half (LOGS-02) and SC2's browser half (LOGS-03) — the surfaces a user
actually watches while a build runs.

Output: `assets/log.js` (new module), the index.html panel, the styles.css log section, and
the extended frontend source-contract spec.
</objective>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|---|---|---|
| `assets/log.js` | asset (new) | IIFE/'use strict' ES module, app.js's sibling; every pinned copy string from 14-UI-SPEC verbatim |
| `Run Log` panel section | asset (mod) | index.html: panel FIRST (A1), header (title + connection pill slot + dropdown slot), card/stream/rail/overlay skeleton, `assets/log.js` module tag |
| log-section CSS | asset (mod) | styles.css: viewport 480px, card, pills, banners, dividers, dim/filter classes (filter behaviors 14-05), accent-liveness rules (A3) — zero new tokens |
| `spec/web_frontend_spec.rb` extension | spec (mod) | log.js source-contract describes: copy pins, XSS/date gates, offline gates, DOM skeleton, ring/follow/banner logic markers |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-CONTEXT.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-SPEC.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-RESEARCH.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-PATTERNS.md
@lib/spm_cache/web/assets/app.js
@lib/spm_cache/web/assets/index.html
@lib/spm_cache/web/assets/styles.css
@spec/web_frontend_spec.rb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: log.js + panel skeleton — connect, hello/card, cold load, line rendering, empty/loading/error states (RED → GREEN)</name>
  <precondition>Plan 14-01 committed (the /api/events contract exists).</precondition>
  <reversibility rating="costly">The DOM ids/class names and the pinned copy strings are the panel's contract with 14-05 and the styles — renaming later touches three files and the spec gates.</reversibility>
  <read_first>
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-SPEC.md — IN FULL: Design System (module/glyph additions ● ↻), Copywriting Contract (every pinned string), Layout contract (panel order, card rows, section break, stream row geometry), Interaction contract (connect/cold-load/body-lines/no-poll rows), Accessibility contract
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-PATTERNS.md — 'log.js' section: el()/textContent discipline, sessionStorage token, empty-state (app.js:101-106) and panel-error (app.js:85-93) postures, legend-chip rail precedent, the EventSource delta (NO analog — build from UI-SPEC)
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-RESEARCH.md — Pattern 6 (frontend mechanics), Assumptions A6 (CLOSED → token-invalid page, not a pill state)
  - lib/spm_cache/web/assets/app.js (the idiom set: el(), byId(), boot/token/bootstrap flow, renderTokenInvalid, renderEmpty, poll-loop teardown guard, STATUS vocabularies)
  - lib/spm_cache/web/assets/index.html (panel skeleton shape, script registration order)
  - lib/spm_cache/web/assets/styles.css (token sheet, panel/badge/btn/legend classes to extend)
  - spec/web_frontend_spec.rb (the source-contract gate style this task extends — copy the describe/it idioms)
  </read_first>
  <files>lib/spm_cache/web/assets/log.js, lib/spm_cache/web/assets/index.html, lib/spm_cache/web/assets/styles.css, spec/web_frontend_spec.rb</files>
  <behavior>
  - RED first: new describes in spec/web_frontend_spec.rb fail (log.js absent) — expect 20 examples, 20 failures:
    1. Served/served-as: GET /assets/log.js → 200 application/javascript; index.html carries the module tag AFTER app.js with the RELATIVE src 'assets/log.js'; gem spec.files membership.
    2. Token path: reads sessionStorage 'spm-cache-web-token'; connects "new EventSource('/api/events?token=' + token)".
    3. Named-event wiring: addEventListener registrations for hello/entry/switch/notice exist (switch/notice handlers present even though their full UI lands in 14-05 — the module ships whole).
    4. Connection pill copy pins: '● connecting…', '● connected', '↻ reconnecting…' — and onerror with readyState CLOSED triggers the token-invalid page replacement (the locked 13 sentence verbatim, replacing main content).
    5. Cold load — live: hello status running → card '● running' (accent dot+word), replay renders, follow engages at completion.
    6. Cold load — last completed: card end-state glyph+word per run_end status ('✓ success'/'✗ failed'), time row 'Started {MMM d, HH:MM} · completed {relative} ago'.
    7. Cold load — empty: 'No runs yet' + 'Run spm-cache build to produce the first run log.' with the command in the accent .cmd span; card/banner/rail-chips absent; only the pill lives.
    8. Loading state: muted 'Loading…' before the first stream byte; no card until hello.
    9. Body lines: out → textContent verbatim (ANSI codes render as literal characters — no interpretation); err → '✗ ' prefix + :fail class.
    10. Dividers: package_start and phase render '── {name} ──' (muted mono); run_start/package_end/sh render NO line; unknown event keys ignored.
    11. Identity card rows: status glyph+word · trigger badge · command mono-600 · 'Config {config}' ('—' when unattributable); Started row (+ completed suffix when finished); argv + run-id rows ellipsized with title tooltips; ' · credentials redacted' appended when the header's redacted flag is set.
    12. Trigger badge verbatim: no allowlist/filter of the trigger value anywhere in log.js (assert absence of a trigger allowlist array; 'terminal'/'watch'/unknown all render).
    13. Card status vocabulary: running/success/failed/interrupted pairs (glyph+word) exactly per the UI-SPEC status-colors table; 'interrupted — exit unknown' for CP14.
    14. XSS gates: log.js contains no innerHTML/insertAdjacentHTML/document.write/outerHTML; every dynamic string flows through el()/textContent.
    15. Date gate: no Date.now() anywhere; relative times derive from the hello 'now' stamp + event ts (server timestamps only).
    16. Offline gates: no scheme-absolute URLs, no cdn. references.
    17. index.html skeleton: Run Log panel is the FIRST panel; h2 'Run Log'; viewport element with role=log, aria-live=off, aria-label='Run output', tabindex=0; anchor-rail container with 'Phases'/'Packages' group labels; overlay pill row; card status container aria-live=polite; connection pill aria-live=polite.
    18. styles.css: log-section rules use only existing tokens; viewport 480px fixed height + #0D1117 fill + pre-wrap mono Label lines; accent appears on controls/liveness classes only (never verdict glyphs — assert the accent-color scan count matches the sanctioned selectors).
    19. Independence: log.js contains no window.setTimeout poll loop (the state poll remains app.js's ONLY timer — the existing 'only one setTimeout' gate extends to log.js); app.js untouched (its describes stay green, byte-identical file).
    20. Glyph inventory: '●' and '↻' present; the verdict triple '✓'/'!'/'✗' reused (not redefined).
  </behavior>
  <action>
  Extend spec/web_frontend_spec.rb with a 'log.js + Run Log panel (Plan 14-04)' describe group following the file's source-contract idiom (File.read the assets; served() helper for the two served rows; source-string pins for copy and mechanics). Then build:

  index.html: insert the Run Log panel FIRST (A1 — SC1's live tail is the phase's purpose; D-13 guarantees it is never empty-or-embarrassing on cold load), copying the panel skeleton: header row (h2 'Run Log'; panel-actions with the connection-pill span and a dropdown slot container for 14-05); body with the identity-card container, the stream group (banner slot, switch-notice slot, stream row: viewport + anchor rail), and the overlay pill row; register the module AFTER app.js as src='assets/log.js' type='module' (relative — the G-13-1 rule; log.js needs no globals and no ordering vs cytoscape).

  log.js: app.js's sibling module — same IIFE/'use strict' shape, own el() (no imports between the modules; sessionStorage is the shared seam). Implement per the behavior bullets: token read → EventSource connect → pill state machine; hello handler builds the card (D-06 rows) and drives the cold-load branches; entry handler JSON.parses and switches ONLY on the parsed 'event' key (T-12-01) — body lines, err styling, dividers, no-line events; ring buffer capped at 500 with the head elision notice ('… {N} earlier lines — reload to replay from start', plain text); relative-time helper over server stamps (vocabulary: 'just now' < 60s, '{N} min ago', '{N} hr ago', '{MMM d, HH:MM}' ≥ 24h) using the hello 'now' as the base, re-derived on each hello; CLOSED → the token-invalid page replacement (same copy file as app.js renders — duplicate the string, do not import). Leave the switch/notice handlers as honest no-op-ish minimal renders (their full surfaces are 14-05): switch handler stores the payload, notice handler renders the '! {message}' warn line — the wiring ships whole, the behaviors deepen next plan. Follow/pause mechanics land in Task 2 — render positions only here.

  styles.css: append the log-section block on the existing sheet: viewport (480px, #0D1117, inner scroll, pre-wrap mono Label), identity card (sm padding, border, radius; mono muted rows; ellipsis + min-width:0), divider/err/notice line classes, card/status/badge classes reusing the badge pattern (text on 10%-alpha fill), pill + overlay row, banner bar (fail 10%-alpha fill), accent usage per A3 (follow pill button, jump button, running dot, connected pill, focus rings) — verdict glyphs stay :ok/:warn/:fail. Zero new tokens; zero animations.

  Commit RED first: `test(14-04): failing log.js panel source-contract specs (20 examples)`, then GREEN: `feat(14-04): log.js stream core — card, cold load, line rendering, connection states`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb spec/web_assets_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or any XSS/date/offline gate fails (a forbidden API or URL shape present in log.js); or index.html references log.js non-relatively; or any pre-existing app.js/styles describe turns red (app.js touched or tokens redefined)</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_frontend_spec.rb green: the 20 new examples plus every pre-existing describe (app.js byte-untouched)
  - assets/log.js served as application/javascript through the relative path; index.html panel first; gem files membership green
  - bundle exec rspec spec/web_integration_spec.rb still green (index changed — the asset-ref scan still resolves every ref)
  </acceptance_criteria>
  <done>The Run Log panel renders the live stream with an honest identity card, correct line taxonomy, all cold-load branches, and pinned empty/loading/error states — offline, XSS-safe, server-time-honest. Committed RED then GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Follow/pause pill, failure + interrupt banners, jump-to-first-error, a11y completion (RED → GREEN)</name>
  <precondition>Task 1 GREEN and committed.</precondition>
  <reversibility rating="reversible">Interaction mechanics behind the Task-1 DOM contract; behavior-tunable without structural churn.</reversibility>
  <read_first>
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-SPEC.md — Interaction rows 'Follow/pause (D-01)', 'Ring (D-02)', 'Failure (D-03/D-10 — the piercing half is 14-05; the jump + filter-exit seam lands here)', Copywriting rows 'Follow-tail pill', 'Failure banner' both variants, 'Jump target' (incl. ring-eviction degradation), Accessibility rows
  - lib/spm_cache/web/assets/log.js (Task 1 output — extend: the follow/ring/banner state machine)
  - lib/spm_cache/web/assets/app.js lines 309-313 (the teardown-safe loop guard — log.js's scroll/reconnect handlers follow the same bail-out posture when panels are replaced)
  </read_first>
  <files>lib/spm_cache/web/assets/log.js, lib/spm_cache/web/assets/styles.css, spec/web_frontend_spec.rb</files>
  <behavior>
  - RED first: 10 new failing examples:
    1. Follow mechanics: on append with follow engaged, scrollTop is set to scrollHeight instantly (no smooth behavior — 'behavior: smooth' absent from the file); ANY upward scroll disengages.
    2. Pause pill: appears only once ≥ 1 line queued while paused; '{N}' increments live and is uncapped; the label is ONE button carrying 'paused — {N} new lines · jump to live'; activating re-engages, clears the count, removes the pill.
    3. During initial replay, follow engages at completion only — the pause pill is a live-tail affordance, never shown mid-replay.
    4. Banner — failed: non-zero run_end → sticky banner 'Run failed — exit status {N}' + 'Jump to first error' button; role=alert; NO dismiss control anywhere (assert absence of a dismiss/close handler on the banner).
    5. Banner — interrupted: CP14 status → 'Run interrupted — exit unknown.' + the same jump button.
    6. Card flip: the status container (aria-live=polite) flips to '✗ failed' / '! interrupted — exit unknown' on the corresponding event.
    7. Jump target chain: first err line; fallback the run's final line; under ring eviction the oldest retained line — the head elision notice already names the reload affordance; never a silent no-op (the jump function always scrolls somewhere real).
    8. Banner replaced only by viewing a different run (a new run's replay re-derives the banner slot — the seam 14-05's auto-switch completes).
    9. Focus/a11y: :focus-visible accent ring rules cover pills, chips-slot controls, and the select slot; all interactive elements are native button/select; pill accessible names are their full visible labels.
    10. Teardown safety: the token-invalid page replacement stops the stream handlers (no handler touches a replaced DOM — the app.js bail-out guard pattern, present in log.js's render entry points).
  </behavior>
  <action>
  Extend spec/web_frontend_spec.rb with the 10 rows (source-contract pins + copy assertions; the repo has no JS runtime — gate mechanics by their source markers, matching the file's established style). Implement in log.js: a follow state machine (scroll listener with an upward-delta test, pending counter, pill element rebuilt on change); the banner module (both variants, jump chain over the ring, filter-exit hook point for 14-05 — a single clearFilter()-before-jump seam); instant scrolling only. Extend styles.css with the pill/banner/focus rules on existing tokens. Commit RED first: `test(14-04): failing follow/banner/a11y specs (10 examples)`, then GREEN: `feat(14-04): follow/pause pill + failure/interrupt banners + jump chain`. Then the wave gate: full suite `bundle exec rspec` green.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or a smooth-scroll/animation API appears in log.js (Prohibition 4); or the banner rows find a dismiss control; or any pre-existing describe turns red</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_frontend_spec.rb green with the 10 new rows (30 new total across the plan)
  - Full suite `bundle exec rspec` green (Wave 2 merge gate)
  </acceptance_criteria>
  <done>The tail follows, pauses honestly with a counted pill, and failures stick as banners with a truthful jump — the live view a user watches during a build is complete except for the anchor/filter/switch/dropdown surfaces 14-05 layers on. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run-log text (subprocess output, attacker-adjacent) → DOM | every rendered string crosses this boundary: log lines, package names, run ids, argv, notice messages — stored-XSS defense is the load-bearing control (T-13-13/T-12-01) |
| SSE payloads → behavior | the client trusts only parsed 'event' keys; text is never interpreted (no ANSI handling, no HTML) |
| sessionStorage token → EventSource URL | the token rides the query param (EventSource cannot send headers); the locked 13 posture — never rendered, URL-cleaned by app.js at bootstrap |

## STRIDE Threat Register (ASVS Level 1; block on high)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-14-16 | Tampering (stored XSS via log text — xcodebuild output can contain attacker-influenced strings from package manifests) | log.js rendering | high | mitigate | el()/textContent-only discipline (spec gate: the four markup-writing APIs absent); rendering keyed only on the parsed 'event' field (T-12-01); package names and run ids flow through the same textContent path |
| T-14-17 | Spoofing (client-clock stamps — a wrong local clock misrepresents 'completed N min ago' / follow honesty) | relative-time helper | low | mitigate | server 'now' from hello is the only delta base; Date.now() absent by spec gate (T-13-16 extension) |
| T-14-18 | Information disclosure (token in the stream URL) | EventSource construction | medium | mitigate | locked posture: sessionStorage-held token, app.js's URL cleaning already done before log.js runs, AccessLog disabled server-side (T-13-03), Cache-Control no-store on the stream |
| T-14-19 | DoS (unbounded DOM growth — a long xcodebuild run balloons the panel) | render ring | medium | mitigate | D-02 ring: newest 500 elements + counted elision notice; the full run stays on disk and reload replays it — spec-gated |
| T-14-20 | Tampering (a dead stream silently freezing the state table — cross-module coupling) | module separation | medium | mitigate | log.js is a separate module with its own failure domain; no shared timers/handlers with app.js; the state poll remains app.js's only timer (spec gate) |

No package installs in this plan (no npm — the asset is hand-rolled by locked decision).
</threat_model>

<verification>
- `bundle exec rspec spec/web_frontend_spec.rb spec/web_assets_spec.rb` — green.
- `bundle exec rspec spec/web_integration_spec.rb` — the served-page asset-ref scan still resolves every reference after the index.html change.
- `bundle exec rspec` — full suite green (Wave 2 merge gate).
- Browser-visible behavior (follow feel, pill states, banner rendering) is exercised by the D-14 recorded probe in 14-05 Task 3 — the repo has no JS runtime; source-contract gates are the automated half.
</verification>

<success_criteria>
- SC1 display half (LOGS-02): the single live stream with identity card, follow/pause, ring, err styling, and dividers renders from the real SSE contract.
- SC2 browser half (LOGS-03): cold load replays (all three branches), transient drops show the reconnecting pill and keep content, permanent failure lands on the token-invalid page.
- CP14 honesty reaches the UI: 'interrupted — exit unknown' is a first-class state.
- The UI-SPEC prohibitions are spec-enforced on the new module (XSS/date/offline/animation/no-color-only).
</success_criteria>

<output>
Create `.planning/phases/14-live-log-streaming-terminal-watch-relay/14-04-SUMMARY.md` when done
</output>
