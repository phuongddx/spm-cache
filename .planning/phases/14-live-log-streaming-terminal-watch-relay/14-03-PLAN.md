---
phase: 14-live-log-streaming-terminal-watch-relay
plan: 03
type: execute
wave: 2
depends_on: ['14-01']
files_modified:
  - lib/spm_cache/web/read_models/runs.rb
  - lib/spm_cache/web/router.rb
  - lib/spm_cache/web/events.rb
  - spec/web_runs_read_model_spec.rb
  - spec/web_events_route_spec.rb
  - spec/web_integration_spec.rb
autonomous: true
requirements: [LOGS-03, LOGS-05]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low   # estimate-calibration: 0 samples → factor 1, confidence low

must_haves:
  truths:
    - "LOGS-05/CP10 (derivation): the build-lock probe acquires-and-releases atomically (LOCK_EX|LOCK_NB, never holds); a thread-held lock → probe reports held; held + attributable live run (header pid alive AND no run_end) → running with that run's identity; held + unattributable → 'unknown holder'; free → idle — bundle exec rspec spec/web_runs_read_model_spec.rb"
    - "LOGS-05/CP14 (honesty): a run whose header pid is dead and whose file lacks a run_end line derives 'interrupted — exit unknown', never 'running' — pid liveness is authoritative for liveness; the missing exit line only means the status is unknown — bundle exec rspec spec/web_runs_read_model_spec.rb"
    - "D-12 (listing): GET /api/runs (token-gated, GET-only) answers the standard envelope with a newest-first runs listing — identity (run id, command, trigger, started_at, ended_at) + derived status per entry, read straight from the runs dir, zero storage, zero caching — bundle exec rspec spec/web_runs_read_model_spec.rb"
    - "D-12 (reachability): GET /api/events?run=<name> with a VALID run name streams that run pinned — replay from its byte 0, follow its growth, NO auto-switch away; an invalid/hostile name falls back to current-or-newest through the same regex+containment machinery as Last-Event-ID (never an error surface); a pruned/nonexistent-but-valid name → notice + fresh replay — bundle exec rspec spec/web_events_route_spec.rb"
    - "D-06/D-11 (identity data): the hello event's data carries the parsed run_start header verbatim (command, argv, redacted, pid, started_at, spm_cache_version, trigger, cycle) plus a server-derived status and lock field plus a server 'now' timestamp — trigger passes through with no allowlist (the UI badge is Phase-15 forward-compatible) — bundle exec rspec spec/web_events_route_spec.rb"
    - "LOGS-03 + WEB-03 (the one boot): the phase's single port-0 integration boot proves end-to-end SSE: 200 + text/event-stream + no-store, hello, replay of fixture lines, a LIVE appended line, exact Last-Event-ID resume across a reconnect, AND server.shutdown joining within bound WITH AN OPEN STREAM (the sentinel proof against webrick server.rb:210) — bundle exec rspec spec/web_integration_spec.rb"
    - "Statelessness: Web::ReadModels::Runs re-reads the runs dir + probes the lock on EVERY call (the State shape, never the Doctor cache) — CP10 forbids run-state memory in the server — bundle exec rspec spec/web_runs_read_model_spec.rb"
  artifacts:
    - "lib/spm_cache/web/read_models/runs.rb (NEW: SPMCache::Web::ReadModels::Runs — stateless .call(config:) → newest-first listing + {runs, lock, now} payload; shared derivation helpers consumed by Events' hello)"
    - "lib/spm_cache/web/router.rb (MOD: '/api/runs' api_read row; events_stream accepts a validated ?run= pin)"
    - "lib/spm_cache/web/events.rb (MOD: hello consumes the shared derivation incl. the lock field + server now; pinned-run streaming reuses the resume machinery)"
    - "spec/web_runs_read_model_spec.rb (NEW: CP10 matrix + D-12 listing + empty-dir shapes + 401 mount row — thread-held flock helper built here)"
    - "spec/web_events_route_spec.rb (MOD: ?run= rows — pinned replay, hostile-name fallback, pruned-name notice)"
    - "spec/web_integration_spec.rb (MOD: SSE describe on the existing single boot — replay, live, resume, shutdown-within-bound)"
  key_links:
    - "ReadModels::Runs.call is consumed by BOTH /api/runs (envelope) and Events' hello (identity + status + lock) — one derivation, two surfaces, zero drift (D-06 card data and D-12 dropdown entries agree by construction)"
    - "Events.resolve_run_name (landed 14-01) is the single validation path for ?run= — the same regex + File.expand_path containment under Config#runs_dir as parse_resume_id (T-13-04 posture; hostile names never reach File.open)"
    - "spec/web_integration_spec.rb keeps exactly ONE before(:all) boot (CP7) — the SSE describe rides the existing boot with a runs-dir fixture whose header pid is the spec process's own pid (genuinely alive → 'running'), and the shutdown-within-bound example is the FINAL example (order: :defined) so closing the server harms nothing after it"
    - "The dropdown's '10 newest entries' bound is the read model's LIST_LIMIT constant (discretion per 14-CONTEXT); statuses freshly derived per request — never cached (UI-SPEC: 'statuses freshly derived per CP10, never cached')"
  prohibitions:
    - statement: "MUST NOT cache run state in the server across requests — Doctor's {data, generated_at} instance pattern is explicitly the WRONG shape here (14-PATTERNS: 'Do NOT copy Doctor's statefulness into runs.rb'); CP10"
      status: resolved
      verification: test
    - statement: "MUST NOT hold or linger on the build flock — the probe acquires and releases atomically inside one File.open block; the server never holds the lock (milestone stance, CP4's flip side)"
      status: resolved
      verification: test
    - statement: "MUST NOT accept an unvalidated ?run= value anywhere near File.open — hostile names fall back to current-or-newest silently (no 4xx worth probing)"
      status: resolved
      verification: test
    - statement: "MUST NOT boot a second server anywhere in this plan — the integration rows extend the EXISTING single boot (CP7); unit rows use with_server per example group as established"
      status: resolved
      verification: test
---

# Plan 14-03 — Runs read model (CP10) + `/api/runs` + hello/lock + `?run=` replay + the one-boot SSE integration weld

<planning_context>
phase: 14 — Live Log Streaming + Terminal/Watch Relay
mode: standard
sources: 14-RESEARCH.md Pattern 4 (state derivation — flock probe, pid attribution, CP14 honesty) + Pattern 2 fresh-connect choice + Open Question 1 recommendation (?run= — confirmed by 14-UI-SPEC's dropdown interaction row) + § Validation Architecture (integration row incl. shutdown-within-bound), 14-CONTEXT.md D-06/D-11/D-12/D-13, 14-PATTERNS.md (read-model analog state.rb, thread-held flock helper = NO repo ancestor), 14-UI-SPEC.md (dropdown entry template, cold-load rows), ROADMAP Phase 14 SC2/SC4, REQUIREMENTS LOGS-03/LOGS-05
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase. Its outputs are contracts for 14-05: the /api/runs payload feeds the recent-runs dropdown, the hello lock field is server-internal (the client renders nothing for it — 14-UI-SPEC external-run row), and ?run= is the in-place replay path the switch-notice and dropdown selections ride.
</downstream_consumer>

<objective>
Derive run identity and status honestly from disk (flock probe + header pids + CP14 rules) in
one stateless read model serving two surfaces — GET /api/runs and the stream's hello event —
and complete the transport's phase-weld proofs: pinned older-run replay via ?run=, and the
one-boot integration rows including the shutdown-with-open-stream sentinel proof.

Purpose: SC4 (LOGS-05) — visible, honest run identity with external-run detection — plus the
SC2 evidence rows that only a real boot can prove.

Output: `web/read_models/runs.rb`, the `/api/runs` route, hello/lock + `?run=` in Events, and
the extended single-boot integration spec.
</objective>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|---|---|---|
| `SPMCache::Web::ReadModels::Runs` | lib (new) | `lib/spm_cache/web/read_models/runs.rb`; stateless `.call(config:)`, String-keyed payload (JSON.generate drops symbol keys — state.rb:44-53 lesson), LIST_LIMIT = 10 |
| Non-blocking lock probe + pid attribution + CP14 status helper | lib (new) | inside runs.rb; `Process.kill(0, pid)` reuse (run_log.rb:395-402), header parse via the run_start_pid shape (run_log.rb:415-419) |
| `GET /api/runs` route | lib (mod) | router.rb — fourth api_read row: token gate → GET check → envelope |
| hello lock field + server `now`; `?run=` pinned streaming | lib (mod) | events.rb — hello consumes the shared derivation; stream(run:) pins a validated run |
| `spec/web_runs_read_model_spec.rb` (+ its thread-held flock helper) | spec (new) | fixed Wave 0 file; the helper is the repo's first cross-thread lock fixture (built once here, 14-02's inline helper is site-specific) |
| SSE integration rows on the single boot | spec (mod) | `spec/web_integration_spec.rb` — replay/live/resume/shutdown-within-bound |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-CONTEXT.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-RESEARCH.md
@.planning/phases/14-live-log-streaming-terminal-watch-relay/14-PATTERNS.md
@lib/spm_cache/web/read_models/state.rb
@lib/spm_cache/web/router.rb
@lib/spm_cache/web/events.rb
@lib/spm_cache/core/run_log.rb
@spec/web_state_spec.rb
@spec/web_integration_spec.rb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReadModels::Runs — CP10 derivation + D-12 listing + GET /api/runs (RED → GREEN)</name>
  <precondition>Plan 14-01 committed (router wiring + events contract exist).</precondition>
  <reversibility rating="reversible">A stateless read model behind the standard envelope — internal shape adjustable without breaking callers.</reversibility>
  <read_first>
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-RESEARCH.md — Pattern 4 (probe sketch, attribution rules, CP14 honesty, race note), "Don't Hand-Roll" row for the listing (directory glob + header/exit parse; no DB — D-12 forbids storage)
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-PATTERNS.md — read_models/state.rb analog (stateless .call, String keys, merge-with-injectables) + explicit warning NOT to copy Doctor's statefulness; run_start_pid header-parse shape; thread-held flock helper note
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-SPEC.md — dropdown entry template + suffix ' · viewing' data needs, '10 newest entries' bound, empty-dir disabled entry
  - lib/spm_cache/web/read_models/state.rb (the shape to match exactly) and graph.rb (the missing-file guard variant for the empty runs dir)
  - lib/spm_cache/web/router.rb (api_read row pattern — /api/runs is a fourth row; read_models wiring)
  - lib/spm_cache/core/run_log.rb lines 128-148 (header keys), 280-291 (run_end status/ended_at shape), 395-419 (pid_alive? + run_start_pid), 31/117 (filename chronology)
  - spec/web_state_spec.rb (read-model unit spec + router-mount 401 row idioms) and spec/web_doctor_spec.rb lines 24-33 (Config-swap around-block)
  </read_first>
  <files>lib/spm_cache/web/read_models/runs.rb, lib/spm_cache/web/router.rb, spec/web_runs_read_model_spec.rb</files>
  <behavior>
  - RED first: spec/web_runs_read_model_spec.rb — expect 9 examples, 9 failures:
    1. Lock probe (held): a background thread holds LOCK_EX on Config#build_lock_path → Runs.call reports lock 'held'; the thread joins and a subsequent call reports 'free' (acquire-and-release proven — the server never holds).
    2. Attribution (running): held lock + a fixture run whose header pid is the SPEC process pid (alive) and whose file lacks run_end → the payload attributes running with that run's identity.
    3. Attribution (unknown holder): held lock + no attributable live run (fixtures with dead pids or completed runs only) → 'unknown holder' — never a guess (LOGS-05's external-run detection: a --no-run-log holder or pre-Phase-12 process).
    4. CP14: header pid dead (a reaped child's pid or an unlikely pid like 2_000_000_000), no run_end → status 'interrupted — exit unknown' — never 'running'.
    5. Status vocabulary: run_end status 0 → 'success'; non-zero → 'failed'; alive pid + no run_end → 'running'; ended_at surfaces per entry.
    6. D-12 listing: three fixture runs (mixed end states) → newest-first order (filename sort == chronological), each entry carrying run/command/trigger/started_at + derived status; at most LIST_LIMIT (10) entries.
    7. Empty/absent runs dir → {runs [], lock derived, now ISO8601} — the graph.rb guard shape, never a raise.
    8. String keys at every level (JSON.generate round-trip preserves all keys — the state.rb lesson).
    9. Router mount: GET /api/runs without a token → 401 envelope (with_server row, web_state_spec idiom); with token → 200 envelope {status 'ok', data…, generated_at}.
  </behavior>
  <action>
  Create spec/web_runs_read_model_spec.rb per the behavior bullets. Fixtures: hand-authored JSONL in tmpdir runs-dirs (header/body/run_end shapes per the Phase 12 vocabulary — run_log_spec fixture idiom); the thread-held flock helper lives in this file (Thread.new + LOCK_EX on the real build_lock_path under the tmpdir project, bounded join, release in ensure). Use the Config-swap around-block (web_doctor_spec:24-33) and clean up in after.

  Create lib/spm_cache/web/read_models/runs.rb: module-level .call(config:) — NO instance state (comment citing 14-PATTERNS' explicit no-Doctor-statefulness warning + CP10). Listing: glob the runs dir, sort newest-first (filename sort), take LIST_LIMIT, parse each header (run_start_pid shape: File.open(path, &:gets) + JSON.parse rescue-to-nil) and scan for the LAST run_end line only when needed for status (a cheap reverse read or full small-file read — files are bounded by retention; do not over-engineer). Status derivation per the research rules: alive pid + no run_end → running; dead pid + no run_end → interrupted — exit unknown; run_end 0 → success; non-zero → failed. Lock: the non-blocking acquire-and-release probe inside one File.open block (comment: the server NEVER holds the lock — CP10/CP4-flip-side; cite Pattern 4); ENOENT → free (never taken yet). Payload String-keyed throughout, plus a server 'now' ISO8601 stamp (the frontend's relative-time base — 14-UI-SPEC: deltas compute from server timestamps only). Wire '/api/runs' as a fourth api_read row in router.rb with runs: registered in the read_models hash (callable entry, State-like — injectable for specs like the others).
  Commit RED first: `test(14-03): failing runs read-model specs (9 examples)`, then GREEN: `feat(14-03): ReadModels::Runs CP10 derivation + /api/runs`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_runs_read_model_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the held-lock probe leaves the lock held (a subsequent free assertion fails — the server held the lock); or any fixture with a dead pid derives 'running' (CP14 violation)</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_runs_read_model_spec.rb green with the 9 described examples; runs.rb is a stateless callable (no instance variables carrying data across calls)
  - GET /api/runs answers the standard envelope; 401 row green
  - bundle exec rspec spec/web_state_spec.rb spec/web_server_spec.rb still green
  </acceptance_criteria>
  <done>Run identity and status derive honestly from flock + headers on every request, with CP14 honesty and unknown-holder attribution — served at /api/runs and ready for hello. Committed RED then GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hello consumes the shared derivation (lock + now) and ?run= pins older-run replay (RED → GREEN)</name>
  <precondition>Task 1 GREEN and committed.</precondition>
  <reversibility rating="reversible">hello payload grows additively (clients ignore unknown fields); ?run= is an additive query param with a safe fallback.</reversibility>
  <read_first>
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-RESEARCH.md — Pattern 3 hello sketch (identity + derived status for the D-06 card), Open Question 1 (?run= recommendation + rationale), Assumption A4 (dropdown reachability without reloads)
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-UI-SPEC.md — interaction rows 'Recent-runs dropdown' (in-place load from byte 0 via ?run=), 'External-run lock state' (the lock field is server-internal; the client renders NOTHING for it), relative-time vocabulary (server timestamps only)
  - lib/spm_cache/web/events.rb (14-01 output — hello construction + resolve_run_name + the stream loop; the pinned-run path reuses the resume machinery)
  - spec/web_events_route_spec.rb (14-01 output — extend with the ?run= rows; keep the file's raw-socket helper usage)
  </read_first>
  <files>lib/spm_cache/web/events.rb, lib/spm_cache/web/router.rb, spec/web_events_route_spec.rb</files>
  <behavior>
  - RED first: 5 new failing examples in spec/web_events_route_spec.rb:
    1. hello carries the full derivation: run + parsed header (command/argv/redacted/pid/started_at/trigger/cycle) + status per the Task-1 vocabulary + lock ('held' or 'free' per a fixture-held lock) + a server 'now' ISO8601 — one object, no drift with /api/runs' derivation (assert against a simultaneously fetched /api/runs payload for the same fixture: same run, same status).
    2. ?run=<valid completed run> while a NEWER run exists → the stream serves the NAMED run from byte 0 (ids carry the named run's filename), follows its growth (append a line to the named fixture → frame arrives), and does NOT auto-switch when an even-newer file appears (pinned semantics).
    3. ?run=<live run> → same pinning with follow engaged (the dropdown's live-selection contract).
    4. ?run= hostile values ('../../spm-cache.yml', '/etc/hosts', 'not a run file', empty) → identical behavior to no param: current-or-newest replay; traversal canary never opened.
    5. ?run=<valid shape, pruned/nonexistent> → notice 'run log pruned while viewing; switching to newest' + fresh replay of the newest run (graceful degrade, message verbatim).
  </behavior>
  <action>
  Extend events.rb: hello construction moves onto the Runs derivation helpers (import/reuse — do not duplicate the rules; if cleanest, expose a narrow Runs.derive_current(config:) used by both .call and hello). The events_stream route arm reads req.query['run'] and, when Events.resolve_run_name validates it, the per-client stream pins that file (replay from byte 0 + its own follow loop — the tailer's read machinery reused per client; ENOENT at open → pinned pruned-notice + fall back to the unpinned path). Invalid names never reach File.open and never change the response shape. Router changes stay minimal: pass the resolved (or nil) run through to the stream call. Comments cite D-12/D-13 and the T-13-04 posture.
  Commit RED first: `test(14-03): failing hello-derivation + ?run= specs (5 examples)`, then GREEN: `feat(14-03): hello lock/now from the shared derivation + pinned ?run= replay`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_events_route_spec.rb spec/web_runs_read_model_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or hello's status disagrees with /api/runs for the same fixture (derivation drift); or a pinned stream switches to a newer run (pin broken); or the traversal canary's content appears in any frame</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_events_route_spec.rb green with the 14-01 rows plus these 5 (≥ 11 examples)
  - hello and /api/runs share ONE derivation path (no duplicated status rules — single source in runs.rb or a helper it owns)
  - Invalid ?run= values are byte-equivalent to omitting the param
  </acceptance_criteria>
  <done>The card's data and the dropdown's data agree by construction, and any listed run is reachable in place without a page reload — D-12's 'previous stays reachable' made literal. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: One-boot SSE integration rows — replay, live, resume, shutdown-within-bound (RED → GREEN, phase weld)</name>
  <precondition>Tasks 1-2 GREEN and committed.</precondition>
  <reversibility rating="reversible">Additive rows on the existing boot; the only permitted production edit is a genuine transport gap fix (fix the source, never the test).</reversibility>
  <read_first>
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-RESEARCH.md — § Validation Architecture integration row (exactly what this task encodes), Pitfall 2 (the join-hang this row kills), § Code Examples boot-harness anchor (web_server_boot.rb:17-32)
  - .planning/phases/14-live-log-streaming-terminal-watch-relay/14-PATTERNS.md — 'spec/web_integration_spec.rb + spec/support/web_server_boot.rb (EXTEND)' section (the before(:all) boot quoted; the bounded join IS the sentinel proof)
  - spec/web_integration_spec.rb (the existing single boot — before(:all)/after(:all), order: :defined, the runtime-bound example)
  - spec/support/web_server_boot.rb (the raw-SSE helpers from 14-01)
  </read_first>
  <files>spec/web_integration_spec.rb</files>
  <behavior>
  - RED first: a new 'live log stream (Phase 14 weld)' describe fails until the rows exist (expected: mostly green immediately once written — this is a verification net; any failing row is a real bug to fix in events.rb/router.rb before GREEN):
    1. Headers: raw GET /api/events?token → 200 + text/event-stream + Cache-Control no-store + X-Frame-Options DENY + Connection close.
    2. Replay + hello: a runs-dir fixture (header pid = the spec process pid, no run_end → 'running'; two body lines) → hello frame (run + header + status running + lock) then both body lines as entry frames with byte-offset ids.
    3. LIVE: append one line to the fixture after connect → the frame arrives within a bounded read (short poll interval injectable through the router's events collaborator — the boot constructs the Router, so construct it with an Events double-wrapped-or-short-interval instance; keep the real code path).
    4. Resume without loss/duplication: capture the last id from row 3's stream, close, reconnect with that Last-Event-ID, append two more fixture lines before reconnecting → the resumed stream delivers exactly those two lines, never re-delivering earlier ones.
    5. Shutdown-within-bound (FINAL example, order: :defined): open a raw stream and leave it OPEN, call server.shutdown, and assert the server thread join completes within 10 s — WEBrick's accept-loop join (server.rb:210) returned because every body proc saw the sentinel (WEB-03 proof with a live stream).
    6. Runtime bound hygiene: the pre-existing 15 s bound example continues to measure the pre-SSE phase (the SSE describe is defined AFTER it); the SSE describe carries its own bounded reads (each read ≤ 2 s) and the whole file stays under ~25 s.
  </behavior>
  <action>
  Extend spec/web_integration_spec.rb: add the fixture writes to the existing before(:all) tmpdir project (runs dir under the configured project), construct the router with a real Events instance configured with a short poll interval (e.g. 0.05 s) so live rows are fast while riding the production code path, and add the describe with the six rows above — the shutdown row LAST. after(:all) is unchanged (shutdown is idempotent and join-safe; row 5 already performed it). If any row fails: fix the owning module (events.rb / router.rb / server.rb) — this task's files list names only the spec because that is the expected edit surface; record any production gap + fix in the SUMMARY (13-04 precedent). Then the wave gate: full suite `bundle exec rspec` green.
  Commit RED first: `test(14-03): SSE integration rows on the single boot (weld)`, then GREEN (with any owning-module fixes): `test(14-03): one-boot SSE proof — replay, live, resume, shutdown-within-bound`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_integration_spec.rb spec/web_server_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the shutdown-within-bound row exceeds its join bound (sentinel broken — WEB-03 regression, the highest-severity failure this file can report); or the resume row observes a duplicated or missing boundary line</fails_when>
  </verify>
  <acceptance_criteria>
  - spec/web_integration_spec.rb green with the six new rows; still exactly ONE boot in the file; total runtime under ~25 s
  - Full suite `bundle exec rspec` green (Wave 2 merge gate)
  - Any transport gap found was fixed in events.rb/router.rb/server.rb with its unit spec already covering the fixed behavior
  </acceptance_criteria>
  <done>The phase's transport claims are proven from one real boot: replay, liveness, exact resume, and a clean shutdown with an open stream — the evidence base for the LOGS-03/WEB-03 rows in 14-VALIDATION. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/runs and /api/events?run= | untrusted query values select run files — the ?run= name is attacker-controlled exactly like Last-Event-ID (T-13-04 posture) |
| build lock file → server read model | the probe reads contention state only; acquiring the lock even briefly must never serialize or block serving |
| run-log headers → derived identity | header pids and statuses are data authored by other processes; dead-pid and missing-exit cases must degrade honestly, never fabricate |

## STRIDE Threat Register (ASVS Level 1; block on high)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-14-11 | Information disclosure / Tampering (path traversal via ?run=) | events_stream + Events.resolve_run_name | high | mitigate | the SAME regex + expand_path containment as parse_resume_id; invalid → silently fall back to current-or-newest (no error surface worth probing); traversal canary fixture proves the target is never opened |
| T-14-12 | Information disclosure (stale/wrong run identity served from memory — CP10 drift) | ReadModels::Runs / Events hello | medium | mitigate | stateless-per-call derivation (spec row: repeated calls re-probe), shared single derivation path for hello + /api/runs (drift impossible by construction), restart-transparency note in research Pitfall 7 |
| T-14-13 | Tampering (misattribution: dead run shown as running — dishonest liveness) | status derivation | medium | mitigate | CP14 rows: pid liveness authoritative (Process.kill(0) reuse), dead + no exit → 'interrupted — exit unknown'; unknown-holder case never fabricates an identity |
| T-14-14 | DoS (lock probe blocking the request thread — server holding/serialized on the flock) | the LOCK_NB probe | medium | mitigate | acquire-and-release inside one File.open with LOCK_NB (never blocks, never holds — spec row 1 proves both directions); ENOENT → free |
| T-14-15 | Information disclosure (pid/argv detail in /api/runs to a tokenless caller) | /api/runs route | low | mitigate | standard token gate + Host/Origin (401/403 rows); same trust level as every /api route — no new exposure class |

No package installs in this plan.
</threat_model>

<verification>
- `bundle exec rspec spec/web_runs_read_model_spec.rb spec/web_events_route_spec.rb` — green.
- `bundle exec rspec spec/web_integration_spec.rb spec/web_server_spec.rb` — green (the weld).
- `bundle exec rspec` — full suite green (Wave 2 merge gate).
</verification>

<success_criteria>
- SC4 (LOGS-05) backend complete: identity (trigger/command/status) derived honestly, external runs detected and attributed from the lock, CP14 interruptions labeled truthfully.
- D-12/D-13 data contracts fixed: newest-first listing, empty-dir shapes, in-place ?run= reachability, hello carrying everything the D-06 card renders.
- The integration weld pins LOGS-03's reconnect claims and WEB-03's shutdown contract against the real server.
</success_criteria>

<output>
Create `.planning/phases/14-live-log-streaming-terminal-watch-relay/14-03-SUMMARY.md` when done
</output>
