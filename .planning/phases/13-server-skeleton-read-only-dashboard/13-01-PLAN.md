---
phase: 13-server-skeleton-read-only-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spm_cache.gemspec
  - lib/spm_cache/web/server.rb
  - lib/spm_cache/web/router.rb
  - lib/spm_cache/web/middleware.rb
  - lib/spm_cache/web/assets.rb
  - lib/spm_cache/web/marker.rb
  - lib/spm_cache/web/port_prober.rb
  - lib/spm_cache/command/web.rb
  - lib/spm_cache/core/config.rb
  - .gitignore
  - spec/web_middleware_spec.rb
  - spec/web_server_spec.rb
  - spec/web_lifecycle_spec.rb
  - spec/web_assets_spec.rb
  - spec/web_signals_spec.rb
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-04, DASH-03]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low   # estimate-calibration: no samples for this task profile → factor 1, confidence low

must_haves:
  truths:
    - "WEB-01: Web::Server binds explicitly to 127.0.0.1 (never 0.0.0.0, never 'localhost' — that resolves ::1 first) on an ephemeral port in specs, and Web::PortProber skips 5000 and 7000 unconditionally while probing upward from 7915 — bundle exec rspec spec/web_server_spec.rb spec/web_lifecycle_spec.rb"
    - "WEB-02: a live marker (pid alive via Process.kill(0, pid)) makes a second Command::Web invocation print the running URL and return without starting a server; a dead-pid or malformed marker is cleared and a fresh server starts — bundle exec rspec spec/web_lifecycle_spec.rb"
    - "WEB-03: SIGTERM and SIGINT to a real spawned `spm-cache web --no-open` process stop the server, remove the marker file, and exit 0 — bundle exec rspec spec/web_signals_spec.rb"
    - "WEB-04: every route served through the single catch-all router enforces Host allowlist and Origin-if-present allowlist (403 on violation) and every /api/* route requires a valid token (401 otherwise); GET / without ?token redirects 302 to /?token=<t> — bundle exec rspec spec/web_middleware_spec.rb spec/web_server_spec.rb"
    - "WEB-04 (token hygiene): the WEBrick access log is disabled and the marker file is written 0600 via Tempfile+rename — bundle exec rspec spec/web_lifecycle_spec.rb spec/web_server_spec.rb"
    - "DASH-03 (server side, tracer slice): GET /api/graph with a valid token returns the {status, data, generated_at} envelope with graph nodes from <project>/spm-cache/packages/proxy/graph.json — bundle exec rspec spec/web_server_spec.rb"
    - "Path traversal: /assets/<anything outside the assets root> (including encoded .., %2e%2e, ..%2F forms) never resolves outside lib/spm_cache/web/assets (specs use a tmpdir assets root) — bundle exec rspec spec/web_assets_spec.rb"
  artifacts:
    - "lib/spm_cache/web/server.rb (NEW: SPMCache::Web::Server — WEBrick adapter; 127.0.0.1 bind; #port; quiet logging)"
    - "lib/spm_cache/web/router.rb (NEW: SPMCache::Web::Router — single catch-all WEBrick servlet dispatching /, /assets/*, /api/*; envelope helper)"
    - "lib/spm_cache/web/middleware.rb (NEW: SPMCache::Web::Middleware — pure request predicates allowed_host?/allowed_origin?/valid_token?)"
    - "lib/spm_cache/web/assets.rb (NEW: SPMCache::Web::Assets — traversal-safe static file resolution + content types)"
    - "lib/spm_cache/web/marker.rb (NEW: SPMCache::Web::Marker — read/write/clear of .spm-cache/web/server.json, 0600, atomic, symlink-rejecting, pid-liveness)"
    - "lib/spm_cache/web/port_prober.rb (NEW: SPMCache::Web::PortProber — skip-list {5000, 7000}, bounded upward probe)"
    - "lib/spm_cache/command/web.rb (NEW: SPMCache::Command::Web — CLAide verb; --port=PORT, --no-open; reuse/probe/serve/signal lifecycle)"
    - "spm_cache.gemspec (MOD: add_runtime_dependency webrick >= 1.8, < 2 — research verdict)"
    - "lib/spm_cache/core/config.rb (MOD: Config#web_dir — <project>/.spm-cache/web, sibling of runs_dir)"
    - ".gitignore (MOD: dev-repo .spm-cache/ entry — marker files carry the launch token)"
    - "spec/web_middleware_spec.rb, spec/web_server_spec.rb, spec/web_lifecycle_spec.rb, spec/web_assets_spec.rb, spec/web_signals_spec.rb (NEW)"
  key_links:
    - "Command::Web#run → Marker.read → (live? print URL + optional browser open + return) | (dead/absent? PortProber.pick → Web::Server.new(port:, token: SecureRandom.hex(32), …) → Marker.write(pid, port, token) → Signal.trap TERM/INT → server.shutdown → StartCallback browser-open (unless --no-open) → server.start blocking → ensure Marker.clear)"
    - "Web::Server mounts exactly ONE servlet (Web::Router) at '/' — middleware uniformity is structural: no route can exist outside the gate. Web::Server#port must return the actually-bound port and must work with port: 0 (resolve via TCPServer bind-read-close before handing WEBrick a concrete port)"
    - "Web::Router consults Web::Middleware for every request, then dispatches: GET / (bootstrap redirect or index via Web::Assets), GET /assets/* (Web::Assets), /api/graph (read model), anything else 404"
    - "Main.run already skips the run-log tee for the web verb (main.rb pre_scan main_log_skipped?) — load-bearing: the token would otherwise land in argv-captured run logs. Do not change this."
  prohibitions:
    - statement: "MUST NOT bind any interface other than 127.0.0.1 (CP9: 'localhost' resolves ::1 first; 0.0.0.0 would expose the dashboard off-loopback)"
      status: resolved
      verification: test
    - statement: "MUST NOT log the token anywhere (WEBrick access log records full request URLs including ?token= — disable AccessLog; never puts/inspect the marker content)"
      status: resolved
      verification: test
    - statement: "MUST NOT serve a path resolved outside the assets root — asset names are validated basenames; traversal attempts 404"
      status: resolved
      verification: test
    - statement: "MUST NOT install a second mutex or write any project state — the server only reads files and writes its own marker (milestone stance: stateless file reader)"
      status: resolved
      verification: judgment
---

# Plan 13-01 — Tracer: WEBrick server skeleton + security middleware + `spm-cache web` lifecycle

<planning_context>
phase: 13 — Server Skeleton + Read-Only Dashboard
mode: standard
sources: 13-CONTEXT.md (locked decisions: "Launch & Port Behavior", "Token & Security Middleware (WEB-04)", "API & Read-Model Shape"), 13-UI-SPEC.md (approved; token-bootstrap interaction contract), ROADMAP Phase 13 SC1–SC3, REQUIREMENTS WEB-01..04 + DASH-03, research/SUMMARY.md (HIGH confidence: webrick is the ONLY sanctioned new runtime dependency; CP7/CP8/CP9/CP13 land in this phase)
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase. The route/auth matrix, the JSON envelope {status, data, generated_at}, and the module names (Web::Server / Web::Router / Web::Middleware / Web::Assets / Web::Marker / Web::PortProber) are CONTRACTS: Plan 13-02 mounts read models into Router, Plan 13-03 builds frontend assets served by Assets, Plan 13-04 proves the cross-route matrix. Do not rename or reshape them.
</downstream_consumer>

<objective>
Build the thinnest production-quality end-to-end slice of the web layer: `spm-cache web`
boots a WEBrick server bound to 127.0.0.1, every request passes through one hardened
middleware gate (Host allowlist + Origin-if-present allowlist + per-launch token on API
routes), `GET /` bootstraps the token via redirect, `/api/graph` returns real graph.json
data in the locked envelope, and the full CLI lifecycle works (port probing past AirPlay's
5000/7000, marker-file idempotent relaunch, SIGTERM/SIGINT → cleanup → exit 0, browser
auto-open with `--no-open` suppression). Adds the milestone's single sanctioned runtime
dependency (`webrick >= 1.8 < 2`) to the gemspec.

Purpose: SC1/SC2/SC3 (WEB-01/02/03) plus the WEB-04 middleware BEFORE any other web
surface exists — Plans 13-02/03/04 only ever add routes/assets behind this gate, and
Phase 15's mutating endpoints inherit it (CP13).

Output: `lib/spm_cache/web/{server,router,middleware,assets,marker,port_prober}.rb`,
`lib/spm_cache/command/web.rb`, gemspec webrick declaration, `Config#web_dir`, five new
spec files, all green in the full suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md
@.planning/phases/13-server-skeleton-read-only-dashboard/13-UI-SPEC.md
@.planning/research/SUMMARY.md
@lib/spm_cache/main.rb
@lib/spm_cache/core/watcher.rb
@lib/spm_cache/core/config.rb
@lib/spm_cache/cache/cachemap.rb
@lib/spm_cache/command/watch.rb
@spm_cache.gemspec
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Web::Server + Web::Middleware + Web::Router — hardened /api/graph slice end-to-end (RED → GREEN)</name>
  <precondition>`bundle exec rspec` is green on the current tree before starting; network available for `bundle install` of webrick after the gemspec edit.</precondition>
  <reversibility rating="costly">The gemspec runtime dependency is public install surface for every user — constraint ">= 1.8", "< 2" per research verdict; changing it later ripples into released formulas.</reversibility>
  <read_first>
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md — sections "Launch & Port Behavior" and "Token & Security Middleware (WEB-04)" (LOCKED: default 7915, probe upward, skip AirPlay 5000/7000; token bootstrap redirect; Origin-present-and-mismatch reject; middleware gates ALL routes; SecureRandom.hex(32) per launch)
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-UI-SPEC.md — "Token bootstrap (locked)" interaction bullet
  - .planning/research/SUMMARY.md — "Recommended Stack" (webrick >= 1.8, < 2 load-bearing, machine-probed), Critical Pitfalls CP8/CP9/CP13, and the Phase-2 deliverables paragraph
  - lib/spm_cache/main.rb lines 19-28 (web verb already excluded from run-log capture — the token must never reach a run log; load-bearing, do not regress)
  - lib/spm_cache/core/config.rb lines 104-118 (build_lock_path/runs_dir outside-sandbox comments — mirror this comment style for web_dir) and lines 161-175 (Integer-coercion reader convention for later plans)
  - lib/spm_cache/cache/cachemap.rb lines 66-84 (depgraph_for_viz raw-entries→elements transform; self.load tolerant of absent file)
  - lib/spm_cache/installer.rb lines 695-701 (gen_cachemap_viz — graph.json lives at Config#proxy_dir/graph.json)
  - lib/spm_cache/spm/build_pipeline.rb lines 220-237 (Tempfile+rename atomic write — the marker-write pattern in Task 2)
  - lib/spm_cache/core/run_log.rb lines 391-399 (pid_alive? Process.kill(0, pid) probe — Errno::ESRCH = dead, other errors = alive)
  - lib/spm_cache/command/watch.rb lines 16-58 (CLAide flag declarations + factory seam; Command::Web copies this shape)
  - lib/spm_cache/core/watcher.rb lines 48-86 (signal contract: trap → Interrupt → IGNORE-mask during shutdown — Task 2 mirrors it)
  - spec/doctor_spec.rb lines 55-80 (hermetic stubbing of Core::Sh — the browser-open stub pattern)
  - spm_cache.gemspec lines 15-34 (files glob already covers lib/**/* — vendored assets ship without a glob change; runtime dependency block)
  </read_first>
  <files>spm_cache.gemspec, lib/spm_cache/web/server.rb, lib/spm_cache/web/middleware.rb, lib/spm_cache/web/router.rb, spec/web_middleware_spec.rb, spec/web_server_spec.rb</files>
  <behavior>
  - RED first: spec/web_middleware_spec.rb fails with NameError (SPMCache::Web::Middleware undefined) before any production code.
  - Middleware predicates (pure, no boot): allowed_host? true for "127.0.0.1:PORT" and "localhost:PORT" (host part case-insensitive, port exact), false for "evil.com:PORT", "127.0.0.1:OTHER", missing Host; allowed_origin? true for nil Origin (same-nav/curl), "http://127.0.0.1:PORT", "http://localhost:PORT", false for "http://evil.com", "https://127.0.0.1:PORT", any null-origin string; valid_token? true only for the exact expected token via a fixed-time compare (digest both sides before comparing), false for wrong/absent token — and it must not short-circuit on length (wrong-length token still runs the compare against a digest).
  - Live server (spec boots Web::Server on port: 0 in a thread, real Net::HTTP against 127.0.0.1):
    - GET / with no token param and valid Host → 302 with Location "/?token=<the launch token>" (exact token from server.token).
    - GET /?token=anything with valid Host/Origin → serves index.html IF the file exists under the assets root, else 404 (tracer gap: the real index.html lands in Plan 13-03; assert the 404 now, the 200 there).
    - GET /api/graph with valid Host, no Origin, valid X-SPM-Token header → 200, JSON envelope exactly {"status"=>"ok", "data"=>{…}, "generated_at"=>ISO8601}; against a tmpdir project with a fixture graph.json ([{module:"A", status:"hit", hasMacro:false}, {module:"B", status:"missed", hasMacro:true}]) the data contains present: true, graph_generated_at: <graph.json mtime ISO8601>, and nodes: the Cachemap#depgraph_for_viz elements [{data:{id:"A", module:"A", status:"hit", hasMacro:false}}, …].
    - GET /api/graph with absent graph.json → 200, present: false, nodes: [], graph_generated_at: null.
    - Reject paths: /api/graph with Origin "http://evil.com" → 403; with Host "evil.com:PORT" → 403; with absent Origin and no token → 401; with absent Origin and wrong token → 401. Same three reject assertions for GET / and GET /assets/whatever (Host/Origin enforced on non-API routes too).
    - POST /api/graph with Origin "http://evil.com" → 403 (method-agnostic middleware — Phase 15's POSTs inherit the gate; there is no POST handler, so a passing-Host/Origin POST simply 404s after the gate).
    - Every response carries X-Frame-Options: DENY and Cache-Control: no-store.
  - Malformed graph.json (invalid JSON) → 500 envelope {"status"=>"error", "data"=>{"message"=><parse error message>}, "generated_at"=>…} — the shape Plan 13-03's error copy consumes.
  - Web::Server#port returns the actually-bound port when constructed with port: 0.
  - WEBrick access log disabled: after a boot + several requests, no access-log lines are emitted to $stderr (StringIO swap assertion); a request URL containing a token never appears in any captured output.
  </behavior>
  <action>
  Gemspec: add `spec.add_runtime_dependency "webrick", ">= 1.8", "< 2"` (research verdict verbatim — machine-probed: require fails on all target rubies without it, CP8), then run `bundle install`. No files-glob change: Dir["{lib,…}/**/*"] already ships lib/spm_cache/web/** (Plan 13-04 pins this with a packaging spec).

  Create lib/spm_cache/web/middleware.rb (# frozen_string_literal, module SPMCache > Web): class Middleware — no WEBrick dependency, pure class methods taking plain values (host:, origin:, token:, port:, expected_token:) so specs never boot a server. Three predicates per the behavior bullets. Fixed-time compare: SHA256-digest both supplied and expected token, then XOR-fold the digests — never `==` on the raw token (local timing side-channel, CP13). Allowed sets are derived ONLY from the bound port: hosts {"127.0.0.1:port", "localhost:port"}, origins {"http://127.0.0.1:port", "http://localhost:port"} — never a wildcard.

  Create lib/spm_cache/web/server.rb (module SPMCache > Web): class Server. Constructor keyword args: port:, token:, router: (a Web::Router instance), logger: optional injectable (defaults to a silenced WEBrick log at ERROR level). Lazy-require 'webrick' inside the constructor/boot path (NOT at file top — Main.load_all requires every lib file on every CLI invocation; a machine without the gem must still run `spm-cache use`; rescue LoadError → raise Core::GeneralError "the webrick gem is required for spm-cache web — install with: gem install webrick"). Port resolution: for port 0, bind TCPServer.new('127.0.0.1', 0), read addr[1], close — then hand WEBrick the CONCRETE port (single source of truth; expose it as #port). WEBrick config: BindAddress '127.0.0.1' (never a hostname — CP9: localhost resolves ::1 first), Port <concrete>, AccessLog: [] (a default access log records ?token= URLs — token-leak vector), Logger: WEBrick::Log.new($stderr, WEBrick::Log::ERROR) or quieter, DoNotReverseLookup: true. Mount exactly ONE servlet — the injected router — via mount_proc('/')-equivalent or a mounted AbstractServlet at '/' so no route can bypass middleware. Support :StartCallback (used by Task 2 for browser-open after the socket accepts — research CP12 health-before-open). #start blocks (WEBrick run loop); #shutdown is thread-safe callable from a signal trap; #stop alias. Verify StartCallback/AccessLog option names against the installed webrick gem source (`gem which webrick` → read httpserver.rb/generic server config keys) — do not trust memory.

  Create lib/spm_cache/web/router.rb: class Router — a WEBrick servlet (AbstractServlet or a proc mounted at '/'). Constructor takes token:, port:, assets: (a Web::Assets instance — Task 3; nil-tolerant until then), read_models: hash (graph: callable — tracer wires an inline graph reader built on Cache::Cachemap.load(File.join(Config.instance.proxy_dir, 'graph.json')); Plan 13-02 replaces the inline readers with read-model objects behind the same callable interface). Every request flows: extract host/origin/token(header or ?token param) → Middleware.allowed_host?/allowed_origin? else 403 → dispatch:
    - GET / : no ?token → 302 Location "/?token=<token>" (the ONLY token-delivery path, per locked decision); with ?token → serve assets/index.html via the assets resolver if present, else 404.
    - GET /assets/<name> : resolve via Web::Assets (Task 3; until then 404) — Host/Origin already checked, no token required (static files ship in the gem; link/script tags cannot send custom headers).
    - GET /api/graph : token required (valid_token? else 401) → envelope.
    - anything else → 404.
  Envelope helper: ok_envelope(data) → {"status"=>"ok", "data"=>data, "generated_at"=>Time.now.utc.iso8601} (require 'time'); error_envelope(message) → 500 with {"status"=>"error", "data"=>{"message"=>message}, …}; every response adds X-Frame-Options: DENY and Cache-Control: no-store (token-in-URL hygiene). Graph reader behavior: File.exist?(proxy_dir/graph.json) false → {present: false, nodes: [], graph_generated_at: nil}; present → Cachemap.load(...).depgraph_for_viz for nodes, File.mtime.utc.iso8601 for graph_generated_at; JSON::ParserError → raise into error_envelope. JSON.generate for bodies.

  Route/auth matrix (pinned for the whole phase): Host allowlist + Origin-if-present allowlist on ALL routes (403); token (X-SPM-Token header or ?token param) required on ALL /api/* routes (401); GET / without token redirects; GET /?token= and /assets/* serve without token check. This is the interpretation of the locked "middleware gates ALL routes uniformly": the Host/Origin gate is uniform on every byte served; the token gate covers every data route and the bootstrap redirect delivers it. Assets exempt from token because they cannot send headers and carry zero project data — record this rationale as a comment citing 13-CONTEXT "Token & Security Middleware".

  Specs: spec/web_middleware_spec.rb (pure predicate unit matrix incl. the fixed-time-compare length-mismatch case) and spec/web_server_spec.rb (ephemeral-port boot + Net::HTTP behavior matrix + tmpdir project fixture via Core::Config.instance configure/reset!). Config Singleton hygiene: configure project_dir to a tmpdir per example group and reset! in after-hook (config_spec precedent). No Core::Sh calls anywhere in these specs — no Sh guard needed beyond convention.
  Commit RED first (`test(13-01): failing specs for web middleware + server slice`), then GREEN (`feat(13-01): WEBrick server skeleton + security middleware + /api/graph tracer`).
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_middleware_spec.rb spec/web_server_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or output contains "NameError" (Web module not loaded); or any 200 observed for a bad-Origin/bad-Host/missing-token request in the spec transcript</fails_when>
  </verify>
  <acceptance_criteria>
  - spm_cache.gemspec contains add_runtime_dependency "webrick", ">= 1.8", "< 2"; `bundle exec ruby -e "require 'webrick'"` succeeds
  - lib/spm_cache/web/{server,middleware,router}.rb exist with # frozen_string_literal: true and contain Server/#port/shutdown, Middleware.allowed_host?/allowed_origin?/valid_token?, Router with the /, /assets/, /api/graph dispatch
  - spec/web_middleware_spec.rb + spec/web_server_spec.rb green with ≥ 20 examples combined covering: both allowlists, token 401s, bootstrap 302 with exact token, envelope shape, present:true/false graph data, traversal-free 404s, X-Frame-Options/no-store, port:0 exposure
  - `grep -n "AccessLog" lib/spm_cache/web/server.rb` shows the disabled access log with the token-leak rationale comment
  </acceptance_criteria>
  <done>The hardened /api/graph slice works end-to-end through a real ephemeral-port WEBrick boot: bootstrap redirect, token-gated envelope reads, 401/403 reject matrix, no token in any log. Committed RED then GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Web::Marker + Web::PortProber + Config#web_dir + Command::Web lifecycle (reuse, signals, browser open)</name>
  <precondition>Task 1 GREEN and committed.</precondition>
  <reversibility rating="reversible">Marker path/format and CLI flags are local runtime surface; adjustable without migration (marker is per-launch state).</reversibility>
  <read_first>
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md — "Launch & Port Behavior" (LOCKED: default 7915 probe-up skipping 5000/7000; foreground Ctrl-C; auto-open with --no-open mirroring the --no-merge-slices flag shape; marker .spm-cache/web/server.json with pid/port/token at 0600)
  - lib/spm_cache/core/run_log.rb lines 391-399 (pid_alive? — copy the 4-line probe with a comment citing this precedent)
  - lib/spm_cache/core/config.rb lines 104-118 (outside-sandbox placement comments — web_dir mirrors runs_dir) 
  - lib/spm_cache/spm/build_pipeline.rb lines 220-237 (Tempfile+rename — marker write; rename replaces a directory entry, never writes through a symlink)
  - lib/spm_cache/core/watcher.rb lines 73-86 (signal shutdown masking: IGNORE further traps during cleanup, exit-0 contract)
  - lib/spm_cache/command/watch.rb lines 16-58 (flag declaration + argv.flag?/argv.option parse before super)
  - spec/doctor_spec.rb lines 61-80 (Core::Sh stubbing — browser `open` is stubbed in lifecycle specs; default-deny elsewhere)
  - .gitignore (repo root — add the dev-repo .spm-cache/ entry; Command::Init already writes it for user projects since Phase 12 D-02)
  </read_first>
  <files>lib/spm_cache/web/marker.rb, lib/spm_cache/web/port_prober.rb, lib/spm_cache/core/config.rb, lib/spm_cache/command/web.rb, .gitignore, spec/web_lifecycle_spec.rb</files>
  <behavior>
  - RED first: spec/web_lifecycle_spec.rb fails (SPMCache::Web::Marker / PortProber / Command::Web undefined).
  - Marker: write(path-override-able for tests) creates parent dirs, writes JSON {pid, port, token, started_at ISO8601} with mode 0600 (File.chmod after atomic Tempfile+rename in the marker dir); read returns nil for absent file, malformed JSON, and a symlink at the marker path (File.lstat check — defense against a pre-planted symlink swap); live? is true only when the pid parses and Process.kill(0, pid) succeeds (Errno::ESRCH → false, any other error → true); clear unlinks if present.
  - PortProber.pick(host: '127.0.0.1', start_port: 7915, attempts: 25) returns the first port where TCPServer binds, NEVER returning 5000 or 7000 even when start_port ≤ 5000 (feed it start_port: 4999 → returns 4999-adjacent ports then 5001… skipping both); raises Core::GeneralError after `attempts` consecutive failures with a message naming the tried range.
  - Command::Web reuse path: with a live marker (stub a fake live pid — e.g. Process.pid of the spec process itself), invoking run with --no-open prints the running instance message containing "http://127.0.0.1:#{port}" to stdout and RETURNS WITHOUT starting a server (Web::Server never constructed — spy on it) and WITHOUT rewriting the marker.
  - Command::Web stale path: marker with a dead pid (spawn+reap a real child to get a genuinely dead pid, or use an unlikely pid like 2_000_000_000) → stale marker cleared, fresh server started (inject a server double whose start returns immediately), fresh marker written with the double's port and the generated token (SecureRandom.hex(32) — 64 hex chars), marker cleared in ensure.
  - Browser open: without --no-open, Core::Sh receives `open http://127.0.0.1:#{port}` exactly once AFTER the server signals ready (StartCallback) — assert ordering via the stubbed server's callback invocation; with --no-open, Core::Sh receives nothing; an `open` failure (stub raises GeneralError) degrades to a warn, server keeps running.
  - --port=PORT parses (argv.option); garbage like --port=abc falls back to the default 7915 (Integer-coercion rescue, yml-is-user-authored posture).
  - Signal wiring: traps for TERM and INT are installed calling server.shutdown, further traps masked IGNORE during cleanup (watcher.rb:73-86 shape); after shutdown returns, ensure clears the marker and run returns normally (exit 0).
  </behavior>
  <action>
  Create lib/spm_cache/web/marker.rb: class Marker with class methods .read(path:), .write(path:, pid:, port:, token:), .clear(path:), .live?(hash-or-path) per the behavior bullets. Path default derives from Core::Config.instance.web_dir + 'server.json' but every method takes path: for tmpdir tests. Atomic write: Tempfile in the marker dir → write JSON → chmod 0600 → File.rename (rename replaces the symlink itself rather than writing through it — symlink-swap defense, build_pipeline.rb:220 precedent); comment cites the threat. Read: File.lstat.symlink? → nil; JSON::ParserError → nil. pid liveness copied from run_log.rb:391-399 with citation comment.

  Create lib/spm_cache/web/port_prober.rb: class PortProber with SKIP_PORTS = [5000, 7000].freeze (AirPlay ControlCenter, machine-probed — comment cites research CP9) and .pick(host:, start_port:, attempts: 25): candidate = start_port, loop attempts times — skip candidate if in SKIP_PORTS; try TCPServer.new(host, candidate) → close → return candidate; rescue EADDRINUSE → candidate += 1. Exhaustion → Core::GeneralError "no available port for spm-cache web in [start, start+attempts) (skipping AirPlay 5000/7000)".

  Add Config#web_dir to lib/spm_cache/core/config.rb: File.join(project_dir, '.spm-cache', 'web') with a comment mirroring runs_dir's outside-sandbox rationale (sibling of runs_dir; 13-CONTEXT "Launch & Port Behavior"; recreate_dirs never touches it).

  Create lib/spm_cache/command/web.rb: class Web < Command (CLAide auto-registers the 'web' verb via load_all — self.command derived from the class name; do NOT edit command.rb). self.summary/self.description; self.options: [['--port=PORT', 'Port to serve on (default: 7915; probes upward on collision)'], ['--no-open', 'Do not open the dashboard in the browser']].concat(super); initialize parses before super (argv.option('port') Integer-coerced with rescue → 7915; argv.flag?('open', true)). run: config = Core::Config.instance with tolerant load (doctor.rb:28-32 shape); marker = Marker.read; if marker && Marker.live?(marker) → Core::UI.info "spm-cache web is already running at http://127.0.0.1:#{marker['port']}" + open_browser(url) unless @open suppressed → return. Else Marker.clear if marker. port = PortProber.pick(start_port: @port). token = SecureRandom.hex(32) (require 'securerandom'). Build the server via a factory method (def build_server(port:, token:) — overridable seam so specs inject a double; watch.rb installer_factory precedent). Marker.write(pid: Process.pid, port:, token:). Install Signal.trap('TERM')/'INT' → server.shutdown, IGNORE-masked during cleanup (watcher.rb:73-86 pattern, cite WEB-03). Server StartCallback → open_browser unless suppressed. server.start (blocks). ensure: Marker.clear. open_browser(url): Core::Sh.run("open #{url}") rescued to Core::UI.warn (macOS-only project; failure never kills the server). THE TOKEN IS NEVER PRINTED — the printed/opened URL is bare "http://127.0.0.1:#{port}" (the bootstrap redirect delivers the token server-side).

  Append '.spm-cache/' entry with '# spm-cache web marker + run logs' comment to the DEV repo .gitignore (append-once, init.rb:200-211 shape) — dev dogfooding must never commit a token-bearing marker; user projects already covered by Command::Init (Phase 12 D-02).

  Create spec/web_lifecycle_spec.rb covering every behavior bullet hermetically: tmpdir marker paths, fake live/dead pids, injected server double (spy: start/shutdown/callback), Core::Sh stub for `open` (default-deny otherwise), StringIO stdout assertions. No real WEBrick boot in this file (Task 1 + Task 3 own sockets).
  Commit RED first (`test(13-01): failing lifecycle specs for marker/prober/Command::Web`), then GREEN (`feat(13-01): web lifecycle — marker relaunch, port probe, signal contract, browser open`).
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_lifecycle_spec.rb spec/web_server_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or a live-marker example ever constructs Web::Server (reuse broken); or marker permissions != 0600 in the transcript</fails_when>
  </verify>
  <acceptance_criteria>
  - lib/spm_cache/web/marker.rb + port_prober.rb + command/web.rb exist; Config#web_dir defined; .gitignore carries .spm-cache/
  - spec/web_lifecycle_spec.rb green with ≥ 12 examples: 0600 atomic marker write, symlink-read-nil, malformed-read-nil, live/dead pid semantics, skip-list probing (4999 start), reuse-without-second-server, stale-clear-and-restart, browser-open ordering + --no-open + failure degradation, Integer-coercion fallback, signal trap masking
  - Command::Web prints a bare URL (no token substring anywhere in captured stdout — assert absence of the token value)
  </acceptance_criteria>
  <done>Idempotent relaunch, AirPlay-skipping port probe, watch-style signal contract, and health-before-open browser launch all proven hermetically; full suite green; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Web::Assets traversal-safe static routing + real-subprocess signal contract spec</name>
  <precondition>Tasks 1–2 GREEN and committed.</precondition>
  <reversibility rating="reversible">Asset serving is internal plumbing behind the Router contract; swappable without breaking 13-02/13-03.</reversibility>
  <read_first>
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md — "Frontend Architecture (offline constraint)" (assets served from lib/spm_cache/web/assets/; Plan 13-03 fills the real files — this task builds the serving mechanism + fixture-based proof)
  - lib/spm_cache/web/router.rb (Task 1 output — wire the assets dispatch arm)
  - lib/spm_cache/web/server.rb (Task 1 output — Router constructor takes assets:)
  - spec/watch_signals_spec.rb lines 1-100 (self-contained CHILD_SCRIPT child-process pattern + Timeout + RbConfig.ruby invocation — web_signals copies this scaffold verbatim in spirit)
  - lib/spm_cache/core/watcher.rb lines 73-86 (shutdown contract the child must satisfy)
  </read_first>
  <files>lib/spm_cache/web/assets.rb, lib/spm_cache/web/router.rb, spec/web_assets_spec.rb, spec/web_signals_spec.rb</files>
  <behavior>
  - RED first: both spec files fail (Web::Assets undefined / signal examples fail with no web verb).
  - Web::Assets(root:): resolve("app.js") → absolute path root/app.js; content_type("app.js") → "application/javascript"; .html → "text/html"; .css → "text/css"; anything else → "application/octet-stream". Rejected (returns nil): any name containing "/", "\\", "..", a NUL, or starting with ".". Fixture tmpdir root with real tiny files proves serve-through-server: GET /assets/app.js (valid Host, no token) → 200 with exact bytes + content type; GET /assets/%2e%2e/marker.json, /assets/..%2Fserver.json, /assets/../server.rb, /assets/.hidden → 404 and the resolved path never escapes root (assert File.expand_path(result).start_with?(root) for the accept cases).
  - Signal contract (real subprocess): child = RbConfig.ruby -I lib running Main.run(['web', '--no-open', '--port=0']) inside a tmpdir project dir (Dir.chdir in child); parent polls for the marker file (≤ 15s), asserts marker JSON has live pid + a port, sends SIGTERM, expects process exit status 0 within 10s and the marker file REMOVED. A second example sends SIGINT with the same assertions. Third: kill -9 (SIGKILL) leaves the marker (honest stale state) and a subsequent Marker.read → live? false (dead pid) — the stale path from Task 2 heals it.
  </behavior>
  <action>
  Create lib/spm_cache/web/assets.rb: class Assets — new(root:); #resolve(name) validates (basename-only: reject separators, "..", leading dot, NUL) then File.expand_path within root with a defensive start_with?(root) check; nil on anything suspicious or nonexistent (File.exist?). #content_type(ext) map. #serve(name) → {body: File.binread, content_type:} or nil. Default root: File.expand_path('../web/assets', __dir__) — but specs inject a tmpdir root. Comment cites the path-traversal threat (T-13-04).

  Wire Router (mod lib/spm_cache/web/router.rb): GET /assets/<name> → assets.resolve → 200 bytes + content-type + Cache-Control (asset caching headers discretionary; index stays no-store) or 404. Host/Origin checks already apply (Task 1) — add one spec line proving /assets is Host-gated (bad Host → 403 before any file IO).

  Create spec/web_assets_spec.rb (fixture tmpdir root; both direct unit calls and 2-3 serve-through-server examples reusing the Task-1 boot helper) and spec/web_signals_spec.rb (CHILD_SCRIPT scaffold mirroring watch_signals_spec.rb: child requires spm_cache/main, chdir'd into ARGV tmpdir, runs SPMCache::Main.run(['web', '--no-open', '--port=0']); parent uses Timeout + Process.wait; wrap child stderr to a file for failure diagnosis; mark the group as the phase's real-process e2e — keep total runtime under ~30s).
  Commit RED first (`test(13-01): failing assets + signal-contract specs`), then GREEN (`feat(13-01): traversal-safe asset routing + SIGTERM/SIGINT exit-0 contract`).
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_assets_spec.rb spec/web_signals_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or any traversal request returns 200; or a signal example exits non-zero or leaves the marker behind</fails_when>
  </verify>
  <acceptance_criteria>
  - lib/spm_cache/web/assets.rb exists with the validated-basename contract; Router serves /assets/* with correct content types
  - spec/web_assets_spec.rb green (≥ 10 examples: accept cases + separator/encoded/dot/hidden rejections + Host-gated asset route)
  - spec/web_signals_spec.rb green (3 examples: SIGTERM exit 0 + marker removed; SIGINT exit 0 + marker removed; SIGKILL leaves stale marker that reads dead)
  - Full suite `bundle exec rspec` green
  </acceptance_criteria>
  <done>Static asset serving is traversal-proof and Host-gated; the WEB-03 signal contract is proven against a real spawned CLI process for TERM, INT, and the SIGKILL stale-marker edge; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/any web page → 127.0.0.1:PORT | untrusted: any page on the internet can issue requests to localhost (drive-by GET/POST, DNS-rebinding re-hosts) — CP13 |
| local multi-user → marker file / process | semi-trusted: other local users cannot read 0600 but can pre-plant files/symlinks in project-adjacent dirs they control |
| request path/host/token → router dispatch | untrusted header-derived values select files/behavior before any auth'd code runs |

## STRIDE Threat Register (ASVS Level 1; localhost is NOT a trust boundary — research CP13)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-13-01 | Spoofing (drive-by CSRF GET/POST) | Web::Middleware / Web::Router | high | mitigate | Origin-present-and-mismatch → 403; token (X-SPM-Token or ?token) required on all /api/* → 401 for absent-Origin requests (curl/no-CORS forms carry no Origin); single catch-all servlet makes the gate structural (Task 1) |
| T-13-02 | Spoofing (DNS rebinding) | Web::Middleware allowed_host? | high | mitigate | Host allowlist {127.0.0.1:port, localhost:port} exact-match on EVERY route incl. assets — a rebound evil.com:port host never matches (Task 1; spec row in Task 1 + matrix in 13-04) |
| T-13-03 | Information disclosure (token leakage) | WEBrick access log / marker / stdout | high | mitigate | AccessLog: [] (default log records ?token= URLs); marker 0600 atomic; token never printed (bare URL only); dev .gitignore entry; Cache-Control: no-store; web verb excluded from run logs (main.rb, load-bearing) |
| T-13-04 | Tampering (path traversal via asset routes) | Web::Assets | high | mitigate | basename-only validation (no separators, .., leading dot), expand_path containment check, 404 on any miss; encoded-form specs prove end-to-end (Task 3) |
| T-13-05 | Tampering (marker-file symlink swap) | Web::Marker | medium | mitigate | write via Tempfile+File.rename in marker dir (rename replaces the symlink entry, never writes through it); read rejects symlinks via lstat; 0600 after write (Task 2) |
| T-13-06 | Side-channel (timing on token compare) | Web::Middleware valid_token? | low | mitigate | digest-then-XOR-fold fixed-time compare; never == on raw token (Task 1) |
| T-13-07 | Tampering (clickjacking / iframe embedding) | Web::Router | low | mitigate | X-Frame-Options: DENY on every response (Task 1) |
| T-13-08 | DoS (port-probe exhaustion / request flood) | Web::PortProber / WEBrick threads | low | accept | bounded 25-attempt probe with clear error; localhost dev tool — WEBrick per-request threads acceptable at dev-tool request rates |

Package legitimacy: `webrick` is the milestone's single sanctioned new runtime dependency, machine-probe-verified by research/SUMMARY.md (HIGH confidence, ruby/org-maintained, zero transitive deps, pinned ">= 1.8", "< 2") — no [ASSUMED]/[SUS] package checkpoint required. No other package install tasks exist in this plan.
</threat_model>

<verification>
- `bundle exec rspec spec/web_middleware_spec.rb spec/web_server_spec.rb spec/web_lifecycle_spec.rb spec/web_assets_spec.rb spec/web_signals_spec.rb` — the plan's five files, all green.
- `bundle exec rspec` — full suite green (pre-existing examples are the regression net: Main.run web-exclusion, config, watch signal contract).
- Structural greps: `grep -n 'webrick' spm_cache.gemspec` shows the runtime dependency; `grep -n 'def web_dir' lib/spm_cache/core/config.rb` shows the reader; `grep -n 'AccessLog' lib/spm_cache/web/server.rb` shows the disabled access log.
- `bundle exec ruby -e "require 'webrick'; puts WEBrick::VERSION"` — the CP8 smoke (require succeeds outside the gem's own files).
</verification>

<success_criteria>
- SC1 (WEB-01): 127.0.0.1 binding + AirPlay-skipping upward probe + browser auto-open with --no-open — Task 1/2 specs + manual browser-open item in 13-VALIDATION.
- SC2 (WEB-02): marker pid-liveness reuse (no second server, no error) — Task 2 specs; SIGKILL stale path heals.
- SC3 (WEB-03): SIGTERM/SIGINT → cleanup (marker removed) → exit 0 — Task 3 real-subprocess specs (WEB-04's offline/reject half completes in 13-03/13-04).
- WEB-04 (middleware half): uniform Host/Origin gate + token on all API routes + bootstrap redirect — Tasks 1–3; cross-route exhaustive matrix lands in 13-04 once all routes exist.
- DASH-03 (server half): /api/graph envelope from graph.json with present/generated_at semantics — Task 1.
- All four 13-CONTEXT locked decision areas implemented exactly (Launch & Port Behavior; Token & Security Middleware; Frontend Architecture serving mechanism; API & Read-Model Shape envelope) — layout details per Claude's-discretion section.
</success_criteria>

<output>
Create `.planning/phases/13-server-skeleton-read-only-dashboard/13-01-SUMMARY.md` when done
</output>
