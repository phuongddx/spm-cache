---
phase: 13-server-skeleton-read-only-dashboard
plan: 03
type: execute
wave: 2
depends_on: ['13-01']
files_modified:
  - lib/spm_cache/web/assets/cytoscape.min.js
  - lib/spm_cache/web/assets/index.html
  - lib/spm_cache/web/assets/styles.css
  - lib/spm_cache/web/assets/app.js
  - spec/web_frontend_spec.rb
  - spec/web_server_spec.rb
autonomous: true
requirements: [WEB-04, DASH-01, DASH-02, DASH-03]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low   # estimate-calibration: no samples for this task profile → factor 1, confidence low

must_haves:
  truths:
    - "WEB-04 (offline): GET /?token=<t> serves index.html referencing ONLY same-origin relative assets; the three FIRST-PARTY assets (index.html, app.js, styles.css) contain zero scheme-absolute URLs and zero cdn. references (automated grep gate); the vendored cytoscape.min.js is pinned structurally instead — first-line version comment with NO scheme-prefixed URL, > 300 KB, committed and served from /assets/ (its bundled MIT license comments legitimately name external hosts and are never fetched at runtime) — bundle exec rspec spec/web_frontend_spec.rb"
    - "WEB-04 (vendored): lib/spm_cache/web/assets/cytoscape.min.js is committed, served from /assets/cytoscape.min.js, and its first line is a comment recording the vendored version — bundle exec rspec spec/web_frontend_spec.rb"
    - "DASH-01: app.js renders the state table from /api/state with columns Package / Config / Size / State / Fidelity, status badges per the locked color vocabulary, mono 12px size column, and the exact copy strings 'No cached packages yet' + 'Run spm-cache build to populate the cache, then Refresh.' on empty — spec/web_frontend_spec.rb (served-source pins) + manual browser walkthrough in 13-VALIDATION"
    - "DASH-02: app.js renders doctor rows as marker + name + message with a second '↳ {fix_hint}' line for non-ok checks with hints, the summary line '{N} ok · {N} warnings · {N} failures', 'Doctor has not run yet' + 'Select Run Doctor to check your environment.' empty state, 'Cached — generated at {HH:MM:SS}' stamp, and a Run Doctor button that switches to 'Running…' while the synchronous ?run=1 request is in flight — spec pins + manual"
    - "DASH-03: app.js renders graph nodes via the vendored cytoscape (elements = data.nodes as-served), node colors by status, macro nodes as diamonds with the table using a '◆' name prefix, legend fixed top-right, and the locked empty copy 'No dependency graph yet' + 'Run spm-cache use to generate graph.json, then Refresh.' — spec pins + manual"
    - "Token bootstrap per locked decision: on load, token moves from location.search into sessionStorage and history.replaceState cleans the URL to '/' BEFORE first render; every fetch sends X-SPM-Token; the token value never appears in any DOM node; 401/403 replaces all panels with the full-page copy 'This page's access token is no longer valid. Restart spm-cache web and open the URL it prints.' — spec pins + manual"
    - "State panel auto-polls /api/state at data.poll_seconds (default 5s); the header stamp 'Updated {HH:MM:SS} · auto-refresh {N}s' always shows the effective interval; a failed poll keeps the last rows and never stops the loop — spec pins + manual"
  artifacts:
    - "lib/spm_cache/web/assets/index.html (NEW: header bar + 1280px content column + three panels in locked order Cache State / Doctor / Dependency Graph)"
    - "lib/spm_cache/web/assets/styles.css (NEW: hand-rolled dark-first CSS — the full 13-UI-SPEC spacing scale, typography roles, color tokens, badge rules)"
    - "lib/spm_cache/web/assets/app.js (NEW: vanilla ES module, ~300–400 LOC — token bootstrap, fetch layer, three panel renderers, polling)"
    - "lib/spm_cache/web/assets/cytoscape.min.js (NEW: vendored from the official cytoscape dist release, version recorded in a first-line comment, committed — never fetched at runtime)"
    - "spec/web_frontend_spec.rb (NEW: served-content pins, offline gate, copy-string verbatim pins, version comment, MIME types)"
  key_links:
    - "index.html → (relative) styles.css + cytoscape.min.js + app.js (type=module) — all served by Web::Assets from 13-01 Task 3; GET /?token= starts returning 200 the moment index.html exists (the 13-01 tracer's documented fill-in point)"
    - "app.js → GET /api/state, /api/doctor, /api/doctor?run=1, /api/graph with X-SPM-Token — consuming EXACTLY the payload shapes pinned in Plan 13-02 (same wave: 13-02 builds the server side against the same pinned contracts)"
    - "app.js graph render → cytoscape({ elements: data.nodes }) directly — the server already serves Cachemap#depgraph_for_viz element objects, so the client performs no transform (DASH-03 raw-entries-to-elements transform happens server-side, once)"
  prohibitions:
    - statement: "MUST NOT reference any absolute URL, CDN, or external font/icon host in any FIRST-PARTY asset (index.html, app.js, styles.css) — the dashboard loads fully offline (13-CONTEXT 'Frontend Architecture'; SC3). The vendored cytoscape.min.js is byte-level exempt: its bundled MIT license attribution comments name external hosts (checker-live-measured: the official dist carries scheme URLs only in license text) and stripping them would delete required attribution — it is instead pinned structurally (version comment, size, local serving)"
      status: resolved
      verification: test
    - statement: "MUST NOT render dynamic strings via innerHTML/string concatenation — package names, check messages, and fix hints come from project files a hostile repo controls; all dynamic text goes through textContent/createTextNode (stored-XSS defense)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT deviate from the 13-UI-SPEC copy strings, color values, spacing multiples, or typography roles — the contract is checker-verified; verbatim means verbatim (including the '·' middots and the '—' em dash in 'Cached — generated at')"
      status: resolved
      verification: test
    - statement: "MUST NOT introduce a build step, framework, or npm dependency — vanilla ES modules, hand-rolled CSS, ~300–400 LOC JS budget (locked)"
      status: resolved
      verification: test
---

# Plan 13-03 — Offline frontend: index.html + styles.css + app.js + vendored cytoscape, implementing 13-UI-SPEC verbatim

<planning_context>
phase: 13 — Server Skeleton + Read-Only Dashboard
mode: standard
sources: 13-UI-SPEC.md (APPROVED — every dimension Copywriting/Visuals/Color/Typography/Spacing signed off; binding), 13-CONTEXT.md (locked "Frontend Architecture (offline constraint)"), ROADMAP SC3/SC4/SC5, REQUIREMENTS WEB-04 + DASH-01/02/03, research/SUMMARY.md (offline-assets stance; "template is a husk — replace, don't fix")
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase. The /api/* payload shapes consumed here are the ones pinned in Plan 13-02 (running in the same wave — do not reconcile by changing either side's keys; the planner owns the contract). The 13-UI-SPEC copy strings and token values in this plan are VERBATIM; a gsd-ui-checker audit verifies them post-hoc. Visual verification is a manual item in 13-VALIDATION (real browser); automated specs pin served bytes.
</downstream_consumer>

<objective>
Build the entire user-facing surface as static assets served by the 13-01 server: a single-page
dashboard (header bar, three stacked panels — Cache State, Doctor, Dependency Graph) with the
locked token bootstrap, per-panel Refresh, 5s state auto-poll, on-demand Run Doctor, and a
vendored-cytoscape nodes-only graph. Zero build step, zero CDN, dark-first hand-rolled CSS,
system font stack, and the doctor's ✓/!/✗ + ↳ vocabulary reused verbatim.

Purpose: the visible half of SC3 (fully-offline load), SC4 (the state table UI), SC5 (doctor
panel + graph panel UI). This is the plan a user actually sees.

Output: four files under lib/spm_cache/web/assets/ + spec/web_frontend_spec.rb, all green in
the full suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-server-skeleton-read-only-dashboard/13-UI-SPEC.md
@.planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md
@.planning/phases/13-server-skeleton-read-only-dashboard/13-02-PLAN.md
@lib/spm_cache/web/router.rb
@lib/spm_cache/command/doctor.rb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendored cytoscape + index.html + styles.css — the full static skeleton</name>
  <precondition>Plan 13-01 committed (assets route serves /assets/*; GET /?token= currently 404s awaiting index.html — this task fills that documented gap). Network available to fetch the cytoscape dist once at execution time.</precondition>
  <reversibility rating="reversible">Static files swappable without server changes; the vendored version is recorded and upgradable by re-fetching.</reversibility>
  <read_first>
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-UI-SPEC.md — ENTIRE FILE, twice: Design System table, Spacing Scale, Typography, Color (+ status colors), Copywriting Contract, Layout contract, Interaction contract, State coverage table (all binding)
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md — "Frontend Architecture (offline constraint)" (assets list, zero build/CDN, dark-first, status vocabulary reuse)
  - lib/spm_cache/command/doctor.rb lines 56-64 (format_line: '✓'/'! '/'✗' markers and the "    ↳ " fix-hint prefix — the exact glyphs the UI reuses)
  - lib/spm_cache/assets/templates/cachemap.js.template (status-color precedent — Material-500 family the palette locks; the template itself is a husk, replace don't fix)
  - lib/spm_cache/web/assets.rb + router.rb (13-01 output — content types, /assets/* and /?token= serving)
  - spec/web_assets_spec.rb + spec/web_server_spec.rb (13-01 output — boot helper to reuse; the GET /?token= 404 example that flips to 200 here)
  </read_first>
  <files>lib/spm_cache/web/assets/cytoscape.min.js, lib/spm_cache/web/assets/index.html, lib/spm_cache/web/assets/styles.css, spec/web_frontend_spec.rb, spec/web_server_spec.rb</files>
  <behavior>
  - RED first: spec/web_frontend_spec.rb fails (assets absent; GET /?token= still 404).
  - Vendoring: lib/spm_cache/web/assets/cytoscape.min.js exists, > 300 KB, parses as JS text (no HTML error page), and line 1 matches /cytoscape.*\d+\.\d+\.\d+/ with NO scheme-prefixed URL on that line (the version-recording comment, e.g. "/*! cytoscape 3.30.4 | MIT license | vendored by spm-cache Phase 13 | github.com/cytoscape/cytoscape.js */" — version recorded, sourced from the official dist release).
  - GET /?token=<t> now returns 200 text/html (the 13-01 gap closes); the served HTML contains, verbatim: page title "spm-cache", the three panel titles "Cache State", "Doctor", "Dependency Graph", button labels "Refresh" (three) and "Run Doctor" (one), the loading line "Loading…", and the exact empty-state strings listed in the Copywriting table (they may be authored by app.js OR baked in index.html — wherever they live, the SERVED bytes of the asset set contain them verbatim).
  - index.html references exactly three relative assets: styles.css, cytoscape.min.js, app.js (type="module"); NO absolute URL, NO protocol-relative URL, NO leading-slash path (relative only, so the spec fixtures and any future mount work identically).
  - styles.css declares the spacing tokens as multiples of 4 (4/8/16/24/32/48/64 px usage values), the four typography roles (20px/600/1.2 display, 16px/600/1.3 heading, 14px/400/1.5 body, 12px/400/1.4 label), both font stacks (system + ui-monospace), the color tokens exactly (#0D1117, #161B22, #30363D, #2196F3, #F44336, #E6EDF3, #8B949E, #4CAF50, #FF9800, #9E9E9E, #607D8B, #3F51B5, #9C27B0), badge styling (colored text on a 10%-alpha fill of the same color, 4px/8px padding, 4px radius), the 48px header bar, the 1280px max-width centered column, 8px panel radius, and the 480px graph canvas height.
  - Offline gate (automated, scoped per checker finding): a spec greps ONLY the three FIRST-PARTY assets (index.html, app.js, styles.css) for /https?:\/\//i and /cdn\./i — ZERO matches allowed. cytoscape.min.js is NEVER byte-gated (the official dist carries 4 scheme-URL matches inside bundled MIT license comments — attribution text, unfetchable); its pins are structural: first line matches /cytoscape.*\d+\.\d+\.\d+/ AND contains no scheme-prefixed URL, and the file is > 300 KB. The runtime guarantee is unchanged: nothing outside 127.0.0.1 is ever requested — every asset is served locally by Web::Assets.
  - Structural pins: header bar element with the title and a port-label placeholder (app.js fills "127.0.0.1:{port}"); three panels in DOM order Cache State, Doctor, Dependency Graph; each panel header has title + right-side stamp label + button; graph panel body has a canvas-mount div and a legend element.
  </behavior>
  <action>
  Vendor cytoscape: fetch ONCE at execution time from the official dist (e.g. `curl -L https://unpkg.com/cytoscape@3/dist/cytoscape.min.js` or the matching github release asset — pick the latest stable 3.x the registry offers), then PREPEND a first-line comment recording the exact vendored version and MIT license WITHOUT any scheme-prefixed URL, e.g. `/*! cytoscape v3.x.y — MIT — vendored from the official cytoscape dist by spm-cache Phase 13; see github.com/cytoscape/cytoscape.js */`. Do NOT strip or rewrite the file's bundled MIT license comments — attribution is required; the offline gate is scoped to first-party assets precisely because those license comments legitimately contain scheme URLs (checker-live-measured). COMMIT the file — runtime fetching is forbidden by construction (it is a static asset served by Web::Assets).

  Write index.html per the 13-UI-SPEC Layout contract: 48px header bar (#161B22, 1px #30363D bottom border): h1 "spm-cache" (Display role) left, port label span right (Label, mono, muted — filled by app.js from window.location). Below: single content column (max-width 1280px, centered, lg side padding = 24px), top margin 64px (3xl). Three <section class="panel"> stacked with 16px (md) gaps, order: Cache State, Doctor, Dependency Graph. Each panel: #161B22 background, 1px #30363D border, 8px radius, 16px (md) padding; header row = h2 title (Heading) left + right group (stamp <span> Label-size muted + CTA button). CTAs: Refresh on Cache State and Dependency Graph panels; Run Doctor on the Doctor panel (locked). Panel bodies start with a muted "Loading…" line (Label size) — app.js replaces content per state. Graph panel body: a fixed 480px-high div for the cytoscape canvas plus a legend (12px round swatches) positioned top-right INSIDE the canvas area. DOCTYPE, lang, meta charset + viewport; title "spm-cache". No inline styles beyond none — all styling in styles.css; no inline event handlers.

  Write styles.css implementing the tokens EXACTLY (define CSS custom properties --space-xs … --space-3xl, --c-bg, --c-panel, --c-border, --c-accent, --c-fail, --c-text, --c-muted, --c-ok, --c-warn, --c-neutral, --c-excluded, --c-plugin, --c-macro, plus font stacks): buttons = solid #2196F3 fill, white text, keyboard focus ring in accent; badges = status-colored text on rgba(status-color, 0.1) fill, 4px/8px padding, 4px radius; table = full-width, sm (8px) cell padding, page-level scroll only (no inner scroll, no pagination), 1px #30363D row borders; size column + timestamps + port label in the mono stack at 12px; long-text cells ellipsis (one line, native title tooltip via the title attribute set by app.js); fix hints wrap at a 24px indent. Accent appears ONLY on: the four buttons, focus rings, and inline command references in empty-state copy (a styled <span class="cmd">, accent color, NOT an anchor — empty states navigate nowhere). Status glyphs get their status colors; neutral #9E9E9E for ignored/not-graph-pinned; excluded #607D8B; plugin #3F51B5; macro #9C27B0. Weights: only 400 and 600. No italics anywhere.

  Create spec/web_frontend_spec.rb: boot-helper reuse (spec/web_server_spec.rb pattern) for the served-HTML assertions + direct File.read assertions on the asset files for the offline gate, copy strings, token values, and version comment. All assertions against FILE/SERVED bytes (no JS execution — behavior pins land in Tasks 2–3 as source-level contracts and in 13-VALIDATION as manual browser walkthroughs).
  Commit RED first (`test(13-03): failing frontend asset specs`), then GREEN (`feat(13-03): index.html + styles.css + vendored cytoscape per 13-UI-SPEC`).
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb spec/web_assets_spec.rb spec/web_server_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or the offline-gate example finds any scheme-absolute URL in a first-party asset; or the cytoscape version-comment/size pins fail; or GET /?token= still 404s</fails_when>
  </verify>
  <acceptance_criteria>
  - All four asset files exist and are committed; cytoscape first line records the version (no scheme-prefixed URL); file > 300 KB
  - GET /?token= serves 200 text/html containing the exact panel titles, button labels, and copy strings
  - Offline gate green: zero http(s):// and zero cdn. matches across the three first-party assets; cytoscape structural pins green (version comment, no scheme URL on line 1, > 300 KB)
  - styles.css carries every locked color token verbatim and every spacing value is a multiple of 4
  - spec/web_frontend_spec.rb green (≥ 15 examples); the 13-01 GET /?token= 404 expectation updated to 200 (edit spec/web_server_spec.rb's gap example — it documented this fill-in point)
  </acceptance_criteria>
  <done>The dashboard's static skeleton ships completely offline: real HTML, real CSS, vendored versioned cytoscape — every 13-UI-SPEC visual token in place. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: app.js core — token bootstrap, authed fetch layer, state table + auto-poll, all error/empty/loading states</name>
  <precondition>Task 1 GREEN and committed.</precondition>
  <reversibility rating="reversible">Client-only logic behind the pinned API contracts; freely editable as long as served-copy pins keep passing.</reversibility>
  <read_first>
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-UI-SPEC.md — Interaction contract (token bootstrap LOCKED; refresh disable behavior; auto-poll rules; Run Doctor Running…) + State coverage table + Copywriting Contract (error states, stale markers, 401/403 full page)
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-02-PLAN.md — must_haves.truths: the EXACT /api/state payload contract (packages[{name,config,size_bytes,state,fidelity,has_macro}], summary{…}, poll_seconds) and envelope shape
  - lib/spm_cache/web/assets/index.html (Task 1 output — element hooks to drive)
  - lib/spm_cache/web/router.rb (13-01/13-02 output — 401/403 semantics, error envelope shape {status:'error', data:{message}})
  </read_first>
  <files>lib/spm_cache/web/assets/app.js, spec/web_frontend_spec.rb</files>
  <behavior>
  - RED first: new spec examples fail (app.js absent the contracted strings/logic pins).
  - Token bootstrap ORDER (source-pinned and manually verified): app.js reads location.search token FIRST, stores sessionStorage, calls history.replaceState(null, '', '/') BEFORE any DOM render call, then renders from sessionStorage on every subsequent load. Spec pins (served-bytes): the file contains the sessionStorage set + the replaceState call ordering (replaceState textually before the first render invocation), and NO code path writes the token into any element's innerHTML/textContent (grep pin: token variable never passed to a DOM-text helper).
  - Fetch layer: single request(path) helper adding X-SPM-Token from sessionStorage; non-2xx or {status:'error'} → throws {message} consumed by panel error copy; 401/403 → renders the full-page token-invalid state REPLACING all panels (exact string: "This page's access token is no longer valid. Restart spm-cache web and open the URL it prints.").
  - State table render (spec pins the mapping table in app.js source + the DOM-building helpers): header row Package / Config / Size / State / Fidelity; one row per data.packages; name cell gets "◆ " prefix when has_macro; Size cell human-formatted (1 decimal, KB/MB/GB) in mono; State cell badge class per status (hit→ok, missed→warn, ignored→neutral, excluded→excluded, plugin→plugin; state null → muted "—" plain text, no badge); Fidelity cell colored per mapping (graph-pinned/host-pinned→ok, resolution-incompatible→warn, not-graph-pinned→neutral). All dynamic text via textContent/createTextNode — no innerHTML with interpolated data (source pin: absence of template-literal innerHTML assignment).
  - Polling: setInterval-style loop at (data.poll_seconds || 5) * 1000 re-fetching /api/state ONLY; on success re-render + update the stamp "Updated {HH:MM:SS} · auto-refresh {N}s" (N = effective poll_seconds, zero-padded HH:MM:SS local time); on failure KEEP last rows, prepend the error line "Couldn't load Cache State: {message}. Check that spm-cache web is still running, then Refresh.", and keep looping.
  - Empty/loading: packages.length === 0 → panel body renders heading "No cached packages yet" + body "Run spm-cache build to populate the cache, then Refresh." (with the two command references styled as .cmd accent spans); initial + in-flight renders show the muted "Loading…" line with the Refresh button disabled (label unchanged — no spinners).
  - Refresh buttons wired: Cache State panel Refresh → immediate /api/state fetch with disable-while-in-flight; failure keeps last-good data + error copy in the panel body area.
  - Port label: header right span filled with "127.0.0.1:" + location.port (mono Label styling from Task 1 CSS).
  </behavior>
  <action>
  Write lib/spm_cache/web/assets/app.js as ONE vanilla ES module (~150–200 LOC in this task; total ≤ 400 across Tasks 2+3 — the locked budget): sections in order — (1) token bootstrap (order-critical: parse → sessionStorage → replaceState → render), (2) tiny DOM helpers el(tag, {class, text, title}) building nodes with textContent only, (3) request(path) with X-SPM-Token + 401/403 full-page state, (4) state-table renderer + status→class map (define as a top-level const STATUS_CLASS = {hit:'ok', missed:'warn', …} — the spec pins this map), (5) humanBytes formatter, (6) the poll loop honoring data.poll_seconds, (7) initial render (parallel fetch of state; doctor/graph panels render their empty/loading shells — Task 3 completes them). Error copy helper renders exactly "Couldn't load {panel}: {message}. Check that spm-cache web is still running, then Refresh." with {panel} ∈ {"Cache State","Doctor","Dependency Graph"}. Keep every user-visible string VERBATIM from 13-UI-SPEC — middots "·" in stamps, the em dash in the doctor stamp, the exact capitalization.

  Extend spec/web_frontend_spec.rb with source-contract pins for every behavior bullet (served-bytes/file-content assertions — this repo has no JS runtime, so JS behavior verification is: source pins here + the manual browser walkthrough in 13-VALIDATION; where a pin can only approximate ordering (replaceState before render), pin it textually and note the manual cross-check).
  Commit RED first (`test(13-03): failing app.js core source-contract specs`), then GREEN (`feat(13-03): app.js — token bootstrap, authed fetch, state table + auto-poll`).
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or any copy-string pin fails (string not found verbatim in served app.js)</fails_when>
  </verify>
  <acceptance_criteria>
  - app.js exists (< 250 LOC at this point) with the seven sections; every behavior bullet has a passing source pin
  - Exact copy strings present verbatim: full-page token-invalid, Cache State error copy, empty-state heading+body, Updated/auto-refresh stamp format
  - No innerHTML-with-interpolation pattern; status→class map pinned; poll loop reads data.poll_seconds with 5 fallback
  - spec/web_frontend_spec.rb green; full suite green
  </acceptance_criteria>
  <done>The dashboard authenticates, renders the state table with the full status vocabulary, polls honestly, and degrades gracefully — all against the pinned API contract. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: app.js panels — on-demand Doctor (Running…, cached stamp, fix hints) + cytoscape graph (diamonds, legend, empty affordance)</name>
  <precondition>Task 2 GREEN and committed.</precondition>
  <reversibility rating="reversible">Client-only; same contract constraints as Task 2.</reversibility>
  <read_first>
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-UI-SPEC.md — Copywriting rows (Run Doctor, doctor stale marker "Cached — generated at {HH:MM:SS}", graph stale marker "Updated {MMM d, HH:MM} · generated by spm-cache use", doctor summary line), Color status table (macro diamond + ◆ prefix), Interaction contract (Run Doctor disables with label Running…; graph nodes not clickable), State coverage (doctor empty/graph empty/partial/zero-one-many rows)
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-02-PLAN.md — /api/doctor payload (has_run, checks[{name,status,message,fix_hint}], summary; ?run=1 semantics; generated_at = cache timestamp) and /api/graph payload (present, nodes = cytoscape elements, graph_generated_at)
  - lib/spm_cache/command/doctor.rb lines 47-64 (the exact terminal rendering vocabulary mirrored: marker, name, message, ↳ fix hint, Summary counts)
  - lib/spm_cache/web/assets/app.js (Task 2 output — helpers/request/error-copy to reuse)
  - lib/spm_cache/web/assets/index.html (Task 1 output — legend + canvas mount hooks)
  </read_first>
  <files>lib/spm_cache/web/assets/app.js, lib/spm_cache/web/assets/index.html, spec/web_frontend_spec.rb</files>
  <behavior>
  - RED first: new spec examples fail (doctor/graph render logic absent).
  - Doctor panel load: GET /api/doctor (no run param) → has_run false → body renders heading "Doctor has not run yet" + "Select Run Doctor to check your environment."; has_run true → rows + summary + stamp "Cached — generated at {HH:MM:SS}" (HH:MM:SS parsed from the envelope's generated_at — the cache's OWN stamp, not client now()).
  - Run Doctor click: button disables + label becomes exactly "Running…" (label swaps back on completion); fires GET /api/doctor?run=1; on success renders rows: marker cell "✓"/"!"/"✗" colored ok/warn/fail + name + message (one ellipsized line, title tooltip), and for every non-ok check with a non-empty fix_hint a second line "↳ {fix_hint}" wrapped at a 24px indent; then the summary line "{N} ok · {N} warnings · {N} failures" (values from data.summary); then updates the stamp with the NEW generated_at. Never auto-polled (no interval touches /api/doctor).
  - Graph panel load: GET /api/graph → present false → body renders heading "No dependency graph yet" + body "Run spm-cache use to generate graph.json, then Refresh." (cmd-styled reference); present true → cytoscape render with elements = data.nodes AS-SERVED (no client transform), node background-color per status map, macro nodes (data.hasMacro) shape 'diamond', labels = module name, deterministic layout 'grid', canvas 480px; stamp "Updated {MMM d, HH:MM} · generated by spm-cache use" with the date parsed from graph_generated_at (NOT client now()); legend fixed top-right inside the canvas with 12px swatches for hit/missed/ignored/excluded/plugin/macro.
  - Zero-one-many: nodes.length === 0 with present true → canvas area shows the muted empty line (any count renders; legend always present) — source pin for the guard.
  - Graph Refresh (panel header) re-fetches /api/graph with disable-while-in-flight; failure → "Couldn't load Dependency Graph: {message}. …" copy.
  - Non-interactive nodes: no tap/click handlers registered on cytoscape nodes (source pin — Phase 16 scope).
  - Total app.js ≤ 400 LOC (spec reads the file and asserts line count within budget).
  </behavior>
  <action>
  Extend lib/spm_cache/web/assets/app.js (Task 3 of 3; stay inside the 300–400 LOC locked budget — the file is deliberately lean, helpers are reused, no framework): (1) doctor renderer reusing Task 2 helpers — marker map {'ok':'✓','warn':'!','fail':'✗'}, status→class reuse, fix-hint second line with the ↳ prefix glyph exactly as doctor.rb prints it, summary from data.summary (not recounted client-side — server is the source of truth), stamp from envelope generated_at; (2) Run Doctor handler with the Running… disable + ?run=1; (3) graph renderer calling window.cytoscape (loaded by index.html BEFORE app.js — verify script order) with the pinned style/layout; (4) graph empty-state + stamp (date formatting from ISO graph_generated_at to "MMM d, HH:MM" via a small formatter, no Date library); (5) legend population (static swatch list, status→color map reuse). Fix hint/message/name ellipsis + title attributes via the el() helper. Adjust index.html only if a hook is missing (legend items, canvas div id).

  Extend spec/web_frontend_spec.rb with source pins for every bullet above (copy strings verbatim, marker map, hasMacro→diamond, grid layout constant, absence of node click handlers, LOC budget, stamp-from-generated_at not Date.now()).
  Commit RED first (`test(13-03): failing doctor/graph panel specs`), then GREEN (`feat(13-03): doctor panel + cytoscape graph panel per 13-UI-SPEC`).
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_frontend_spec.rb spec/web_server_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or any verbatim copy pin fails (incl. the ↳ prefix and · separators); or the LOC-budget example fails</fails_when>
  </verify>
  <acceptance_criteria>
  - app.js complete (≤ 400 LOC): both panels implemented, every behavior bullet pinned
  - Verbatim strings present: "Doctor has not run yet", "Select Run Doctor to check your environment.", "Running…", "Cached — generated at", "{N} ok · {N} warnings · {N} failures" pattern, "No dependency graph yet", "Run spm-cache use to generate graph.json, then Refresh.", "Updated … · generated by spm-cache use" pattern, ↳ fix-hint line
  - Cytoscape configured with elements as-served, status colors, diamond macros, grid layout, no node click handlers
  - spec/web_frontend_spec.rb green (≥ 35 examples total across Tasks 1–3); full suite `bundle exec rspec` green
  </acceptance_criteria>
  <done>All three panels render their full state space per the approved UI-SPEC, fully offline, within the locked vanilla-JS budget. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project-authored strings (module names, check messages, fix hints) → DOM | a hostile repository can plant HTML/script text in graph.json and provenance-derived fields — the browser must treat every dynamic string as inert text |
| vendored cytoscape file → page execution | third-party code executes in the dashboard origin; its integrity is a supply-chain surface |

## STRIDE Threat Register (ASVS Level 1)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-13-13 | Tampering (stored XSS via module names / messages / fix hints) | app.js DOM helpers | high | mitigate | ALL dynamic text through el()/textContent/createTextNode — never innerHTML with interpolated data (prohibition pinned; source-contract spec asserts absence of the pattern); browser walkthrough double-checks a fixture with markup-looking names |
| T-13-14 | Tampering (supply-chain: substituted/CDN cytoscape) | assets/cytoscape.min.js | medium | mitigate | file COMMITTED (served locally, never fetched at runtime), first-line version comment + > 300 KB structural pins, first-party offline gate proves zero external references; Plan 13-04's packaging spec proves it ships inside the gem |
| T-13-15 | Information disclosure (token in DOM/Referer) | app.js token bootstrap | medium | mitigate | token lives in sessionStorage only, never rendered (UI-SPEC locked), URL cleaned via replaceState before first render so subsequent Referers/refreshes carry no token; server sets no-store (13-01) |
| T-13-16 | Spoofing (stale panel presented as current) | app.js stamps | low | mitigate | every stamp derives from SERVER timestamps (envelope generated_at / graph_generated_at), never client now() — pinned by source contracts; failed polls keep last-good data visibly alongside the error copy, never silently |

No package installs (vendored file is a committed asset, not a dependency; zero npm surface — locked).
</threat_model>

<verification>
- `bundle exec rspec spec/web_frontend_spec.rb` — the full served-source contract, green.
- `bundle exec rspec` — full suite green.
- Offline proof, structural: zero scheme-absolute URLs across the three first-party assets (spec-enforced), cytoscape pinned structurally — repeated here as the phase's SC3 anchor.
- Manual browser walkthrough (13-VALIDATION manual table): real load, token URL cleanup, three panels, polling, Run Doctor, graph render, error states — the automation in this repo pins bytes; the walkthrough verifies behavior.
</verification>

<success_criteria>
- SC3 (offline half): all assets vendored + local (first-party URL gate green, cytoscape structurally pinned and license attribution preserved), zero CDN fetches — with 13-01's reject paths completing the criterion.
- SC4 (DASH-01 UI): state table with size/state/fidelity columns, status vocabulary, empty state — exact copy pinned.
- SC5 (DASH-02/03 UI): on-demand doctor with markers + fix hints + cached stamp + summary; graph nodes via vendored cytoscape with macro diamonds, legend, and the spm-cache-use empty affordance.
- Every 13-UI-SPEC dimension (copy, visuals, color, typography, spacing) implemented verbatim — ready for the gsd-ui-checker post-phase audit.
- 13-CONTEXT "Frontend Architecture" locked decisions: zero build step, zero CDN, vanilla ES modules within the 300–400 LOC budget, hand-rolled dark-first CSS, doctor vocabulary reuse.
</success_criteria>

<output>
Create `.planning/phases/13-server-skeleton-read-only-dashboard/13-03-SUMMARY.md` when done
</output>
