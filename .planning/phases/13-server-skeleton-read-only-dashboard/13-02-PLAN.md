---
phase: 13-server-skeleton-read-only-dashboard
plan: 02
type: execute
wave: 2
depends_on: ['13-01']
files_modified:
  - lib/spm_cache/cache/inventory.rb
  - lib/spm_cache/command/cache/list.rb
  - lib/spm_cache/core/config.rb
  - lib/spm_cache/assets/templates/spm-cache.yml.template
  - lib/spm_cache/web/read_models/state.rb
  - lib/spm_cache/web/read_models/graph.rb
  - lib/spm_cache/web/read_models/doctor.rb
  - lib/spm_cache/core/diagnostics.rb
  - lib/spm_cache/command/doctor.rb
  - lib/spm_cache/web/router.rb
  - spec/command_cache_list_spec.rb
  - spec/config_spec.rb
  - spec/web_state_spec.rb
  - spec/web_graph_spec.rb
  - spec/web_doctor_spec.rb
  - spec/doctor_spec.rb
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low   # estimate-calibration: no samples for this task profile → factor 1, confidence low

must_haves:
  truths:
    - "DASH-01: GET /api/state returns one row per cached xcframework across debug+release cache dirs with size_bytes (recursive lstat sum), the package's graph status (hit/missed/ignored/excluded/plugin or null when not in the current graph), fidelity from the .provenance.json sidecar, and has_macro — re-derived from the same files the CLI reads, per request — bundle exec rspec spec/web_state_spec.rb"
    - "DASH-01: `spm-cache cache list` output is byte-identical after the Cache::Inventory extraction (the existing spec passes unmodified) — bundle exec rspec spec/command_cache_list_spec.rb"
    - "DASH-02: GET /api/doctor (no run param) returns the cached result with its original generated_at, or has_run:false when never run; GET /api/doctor?run=1 runs Core::Diagnostics.run_all synchronously from the registry (data-driven — a stubbed extra check appears in the payload with no read-model change), refreshes the cache, and returns the new generated_at — bundle exec rspec spec/web_doctor_spec.rb"
    - "DASH-02: the doctor payload shape is EXACTLY the CLI --json shape {checks:[{name,status,message,fix_hint}], summary:{ok,warnings,failures}} — one payload method shared by Command::Doctor#print_json and the read model — bundle exec rspec spec/web_doctor_spec.rb spec/doctor_spec.rb"
    - "DASH-03: GET /api/graph returns present:true/false (File.exist? on proxy_dir/graph.json), nodes via Cache::Cachemap#depgraph_for_viz, graph_generated_at from the file's mtime — extracted into Web::ReadModels::Graph with the same contract the 13-01 tracer shipped — bundle exec rspec spec/web_graph_spec.rb"
    - "Envelope discipline: every read model answers through the {status, data, generated_at} envelope; a malformed graph.json yields the 500 error envelope with the parse message — bundle exec rspec spec/web_graph_spec.rb"
    - "Poll config: Config#web_poll_seconds Integer-coerces with rescue-to-default 5 (runs_keep precedent); /api/state data carries poll_seconds — bundle exec rspec spec/config_spec.rb spec/web_state_spec.rb"
  artifacts:
    - "lib/spm_cache/cache/inventory.rb (NEW: Cache::Inventory — shared cache-dir scan producing name/config/size_bytes/fidelity; one source of truth for `cache list` and the web state model)"
    - "lib/spm_cache/web/read_models/state.rb (NEW: Web::ReadModels::State — inventory scan joined with graph.json statuses + summary + poll_seconds)"
    - "lib/spm_cache/web/read_models/graph.rb (NEW: Web::ReadModels::Graph — extraction of the tracer's inline graph reader)"
    - "lib/spm_cache/web/read_models/doctor.rb (NEW: Web::ReadModels::Doctor — registry run + payload + in-memory {data, generated_at} cache with Mutex)"
    - "lib/spm_cache/core/diagnostics.rb (MOD: Diagnostics.payload(results) class method — the shared JSON shape)"
    - "lib/spm_cache/command/doctor.rb (MOD: print_json delegates to Diagnostics.payload)"
    - "lib/spm_cache/command/cache/list.rb (MOD: delegates the scan to Cache::Inventory; printed output unchanged)"
    - "lib/spm_cache/core/config.rb (MOD: DEFAULT_CONFIG['web_poll_seconds']=5 + Integer-coercing reader)"
    - "lib/spm_cache/assets/templates/spm-cache.yml.template (MOD: commented web_poll_seconds documentation line)"
    - "lib/spm_cache/web/router.rb (MOD: /api/state, /api/doctor mounts; graph arm re-points to ReadModels::Graph)"
    - "spec/web_state_spec.rb, spec/web_graph_spec.rb, spec/web_doctor_spec.rb (NEW); spec/command_cache_list_spec.rb, spec/config_spec.rb, spec/doctor_spec.rb (extended)"
  key_links:
    - "Router(/api/state) → Web::ReadModels::State.call → Cache::Inventory.scan(config:) + Cache::Cachemap.load(proxy_dir/graph.json) join → envelope — every value is re-read from disk per request; the server holds NO derived state (milestone stance: never a second source of truth)"
    - "Router(/api/doctor) → Web::ReadModels::Doctor#call(run:) → Diagnostics.run_all(config: tolerant-load) → Diagnostics.payload — the registry is the single source of check truth (DASH-02 data-driven); cache swap under a Mutex (WEBrick serves one thread per request)"
    - "Command::Doctor#print_json AND Web::ReadModels::Doctor both call Diagnostics.payload — the CLI and the dashboard can never disagree on the doctor JSON shape"
    - "Command::Cache::List AND Web::ReadModels::State both call Cache::Inventory.scan — same one-source-of-truth rule for the cache table"
  prohibitions:
    - statement: "MUST NOT cache read-model results across requests (state/graph) — freshness is the point; only the doctor result is cached, per the locked DASH-02 decision, and always with its generated_at"
      status: resolved
      verification: test
    - statement: "MUST NOT hard-code check names, counts, or orderings in the doctor read model — everything derives from Core::Diagnostics.registry"
      status: resolved
      verification: test
    - statement: "MUST NOT let `spm-cache cache list` printed output change by one byte in the Inventory refactor"
      status: resolved
      verification: test
    - statement: "MUST NOT mutate the envelope shape {status, data, generated_at} — Plan 13-03's frontend is written against it (same-launch client/server, no versioning per 13-CONTEXT)"
      status: resolved
      verification: test
---

# Plan 13-02 — Read models: shared Inventory + /api/state + /api/graph extraction + /api/doctor

<planning_context>
phase: 13 — Server Skeleton + Read-Only Dashboard
mode: standard
sources: 13-CONTEXT.md (locked "API & Read-Model Shape": exactly three endpoints GET /api/state, /api/doctor, /api/graph; envelope {status, data, generated_at}; state derivation reuses the CLI's own read path — server never maintains its own store; doctor synchronous in-request from the registry, cached in memory with generated_at), ROADMAP SC4/SC5, REQUIREMENTS DASH-01/02/03, research/SUMMARY.md ("Reuse, don't rebuild" — cache-list scan + Diagnostics payload extraction)
</planning_context>

<downstream_consumer>
This plan is an executable prompt for /gsd-execute-phase. The JSON payload shapes pinned in each task are CONTRACTS consumed in parallel by Plan 13-03's frontend (same wave — it codes against these shapes, not against this plan's Ruby). Plan 13-04's integration spec asserts them end-to-end. Do not rename keys.
</downstream_consumer>

<objective>
Complete the server's read surface: extract the cache-dir scan into a shared `Cache::Inventory`
(one source of truth for `spm-cache cache list` and the dashboard), build the three read models
on the CLI's own read paths, and mount /api/state + /api/doctor while formalizing /api/graph
from the 13-01 tracer into `Web::ReadModels::Graph`. Doctor gains the shared
`Diagnostics.payload` extraction so the CLI's --json output and the dashboard can never
diverge.

Purpose: SC4 (cache-state table data: size, cached/source state, fidelity — re-derived from
the same files the CLI reads) and SC5's data half (registry-driven doctor with cached
timestamp; graph nodes with the generating-command affordance signal).

Output: three read-model files, Cache::Inventory, Diagnostics.payload, three new spec files,
three extended spec files, all green in the full suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md
@.planning/research/SUMMARY.md
@lib/spm_cache/web/router.rb
@lib/spm_cache/core/diagnostics.rb
@lib/spm_cache/command/doctor.rb
@lib/spm_cache/command/cache/list.rb
@lib/spm_cache/cache/cachemap.rb
@lib/spm_cache/core/config.rb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cache::Inventory extraction + Config#web_poll_seconds — shared scan, one source of truth</name>
  <precondition>Plan 13-01 committed (router exists; this task touches no web files).</precondition>
  <reversibility rating="reversible">Internal extraction behind existing printed output; the config key is additive with a default.</reversibility>
  <read_first>
  - lib/spm_cache/command/cache/list.rb (entire file, 41 lines — the scan being extracted: debug+release dirs, *.xcframework glob, fidelity_status_for with the not-graph-pinned fallbacks; printed output must survive byte-identical)
  - spec/command_cache_list_spec.rb (existing pins on printed output — these ARE the refactor regression net; read fully before touching list.rb)
  - lib/spm_cache/core/config.rb lines 15-28 (DEFAULT_CONFIG with runs_keep/runs_max_mb comment block), 161-175 (Integer-coercion reader convention to copy for web_poll_seconds), 74-76 (cache_dir(config) shape)
  - lib/spm_cache/assets/templates/spm-cache.yml.template lines 6-14 (commented-key documentation convention)
  - spec/config_spec.rb (Singleton reset! + reader spec conventions to extend)
  - .planning/research/SUMMARY.md — "Reuse, don't rebuild" bullet (cache list glob+sidecar scan; sizes a trivial addition)
  </read_first>
  <files>lib/spm_cache/cache/inventory.rb, lib/spm_cache/command/cache/list.rb, lib/spm_cache/core/config.rb, lib/spm_cache/assets/templates/spm-cache.yml.template, spec/command_cache_list_spec.rb, spec/config_spec.rb</files>
  <behavior>
  - RED first: spec additions fail (Cache::Inventory undefined; Config#web_poll_seconds undefined).
  - Inventory.scan(config:) with a tmpdir cache root (stub CACHE_DIR constant or inject base — see action): fixture creates <root>/debug/Alamofire.xcframework/ (with a nested file of known size), <root>/debug/Ziph.xcframework/ + Ziph sidecar {"fidelity_status"=>"resolution-incompatible"}, <root>/release/Alamofire.xcframework/ → returns exactly three structs/hashes: {name:"Alamofire", config:"debug", size_bytes:<sum incl. root lstat>, fidelity:"not-graph-pinned"}, {name:"Ziph", config:"debug", size_bytes:…, fidelity:"resolution-incompatible"}, {name:"Alamofire", config:"release", …}; sorted deterministically (config then name).
  - size_bytes is a recursive lstat sum (symlink entries counted as link size, never followed — cache dirs may contain symlinked slices).
  - Sidecar tolerance copied from list.rb: absent sidecar → "not-graph-pinned"; malformed JSON / non-Hash → "not-graph-pinned"; missing fidelity_status key → "not-graph-pinned".
  - `spm-cache cache list` prints EXACTLY what it printed before (existing spec green unmodified; add one example asserting the delegation via a fixture if coverage gaps exist).
  - Config#web_poll_seconds: default 5; "web_poll_seconds: 12" in yml → 12; "web_poll_seconds: often" → 5 (rescue-to-default); nil/absent → 5.
  </behavior>
  <action>
  Create lib/spm_cache/cache/inventory.rb (# frozen_string_literal, module SPMCache > Cache): class Inventory. Class method .scan(config: Core::Config.instance, cache_root: nil) — cache_root is the injectable seam for tmpdir specs (defaults to Core::Config::CACHE_DIR; production callers never pass it). For %w[debug release]: Dir.glob(File.join(cache_root, cfg, '*.xcframework')).sort → per artifact: name = File.basename(path, '.xcframework'), size_bytes = dir_size(path) (private: File.lstat(path).size + Dir.glob(File.join(path, '**', '*')).sum { |f| File.lstat(f).size }), fidelity = fidelity_status_for("#{path}.provenance.json") (private method moved verbatim from list.rb with its rescue JSON::ParserError, SystemCallError → not-graph-pinned). Return array of Struct.new(:name, :config, :size_bytes, :fidelity, keyword_init: true) (diagnostics.rb Struct convention), sorted by [config, name]. Comment: single scan shared by `cache list` and the web state model — never a second read path (13-CONTEXT "API & Read-Model Shape").

  Refactor lib/spm_cache/command/cache/list.rb: run keeps its printed shape ("\
#{cfg.capitalize}:" headers + "  #{name} (#{fidelity})" rows, skipped empty dirs) but sources rows from Cache::Inventory.scan — printing "#{entry.name} (#{entry.fidelity})" grouped per config. The existing command_cache_list_spec MUST pass without edits if possible; only extend if the delegation needs a new pin. Do not print sizes here — that surface belongs to the web model (surgical: output unchanged).

  Add to lib/spm_cache/core/config.rb: DEFAULT_CONFIG['web_poll_seconds'] = 5 (with a comment: dashboard state-table auto-poll interval in seconds, 13-UI-SPEC "server-configurable") and the Integer-coercing reader web_poll_seconds copying runs_keep's rescue shape verbatim (config.rb:165-169).

  Add one commented documentation line to spm-cache.yml.template near the runs_* block: "# web_poll_seconds: 5   # dashboard state-table auto-refresh interval".

  Extend spec/config_spec.rb (3-4 web_poll_seconds examples per the behavior bullets, reset! hygiene) and spec/command_cache_list_spec.rb (Inventory fixtures via the cache_root seam: dirs, sidecar tolerance cases, sort order — plus the unchanged-output pin).
  Commit RED first (`test(13-02): failing inventory + poll-config specs`), then GREEN (`feat(13-02): Cache::Inventory shared scan + web_poll_seconds config`).
  </action>
  <verify>
    <automated>bundle exec rspec spec/command_cache_list_spec.rb spec/config_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or any pre-existing cache-list example fails (output regression)</fails_when>
  </verify>
  <acceptance_criteria>
  - lib/spm_cache/cache/inventory.rb exists with .scan(cache_root: seam) returning keyword Structs {name, config, size_bytes, fidelity}
  - `spm-cache cache list` output byte-identical (existing spec green; diff-level pin present)
  - Config#web_poll_seconds: default 5, Integer coercion with rescue-to-default; yml template documents it
  - spec/command_cache_list_spec.rb + spec/config_spec.rb green with the new examples
  </acceptance_criteria>
  <done>The cache-dir scan exists exactly once, shared by CLI and (next task) the web state model; poll interval is a real config knob. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Web::ReadModels::State + Graph — mount /api/state, formalize /api/graph</name>
  <precondition>Task 1 GREEN and committed (Inventory exists).</precondition>
  <reversibility rating="costly">The /api/state and /api/graph data shapes are consumed in the same wave by Plan 13-03's app.js — same-launch client/server with no versioning (13-CONTEXT), so key renames after 13-03 lands break the shipped frontend.</reversibility>
  <read_first>
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md — "API & Read-Model Shape" (LOCKED envelope; state reuses the CLI read path; graph empty-state affordance)
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-UI-SPEC.md — State coverage rows + stale-marker rows (the shapes below exist to feed those exact UI strings)
  - lib/spm_cache/web/router.rb (13-01 output — read fully; re-point the graph arm, add the state arm)
  - lib/spm_cache/web/server.rb (13-01 output — Router constructor's read_models hash)
  - lib/spm_cache/cache/cachemap.rb lines 14-48, 66-84 (hit/missed/ignored/excluded/plugin selectors, stats, depgraph_for_viz)
  - lib/spm_cache/cache/inventory.rb (Task 1 output)
  - lib/spm_cache/installer.rb lines 695-701 (graph.json location: proxy_dir/graph.json)
  - lib/spm_cache/core/config.rb lines 70-84 (sandbox_dir/proxy_dir)
  - spec/web_server_spec.rb (13-01 output — boot helper + envelope assertions to reuse)
  </read_first>
  <files>lib/spm_cache/web/read_models/state.rb, lib/spm_cache/web/read_models/graph.rb, lib/spm_cache/web/router.rb, spec/web_state_spec.rb, spec/web_graph_spec.rb</files>
  <behavior>
  - RED first: both spec files fail (SPMCache::Web::ReadModels undefined).
  - State read model (tmpdir project: fake cache_root via Inventory seam + fixture graph.json): returns data EXACTLY {"packages"=>[{"name"=>"Alamofire","config"=>"debug","size_bytes"=>N,"state"=>"hit","fidelity"=>"graph-pinned","has_macro"=>false}, …], "summary"=>{"total"=>2,"hit"=>1,"missed"=>1,"ignored"=>0,"excluded"=>0,"plugin"=>0}, "poll_seconds"=>5}. Join rule: state/has_macro come from the graph.json entry whose "module" equals the artifact name; a cached artifact absent from graph.json gets state nil and has_macro false (frontend renders the "—" cell). summary = Cachemap#stats when graph.json exists (zeros when absent). poll_seconds = Config#web_poll_seconds.
  - Empty cache dirs → packages: [] (frontend empty state "No cached packages yet" keys off this), summary zeros.
  - Freshness: mutate graph.json between two calls → second call reflects the new statuses (no memoization anywhere).
  - Graph read model: identical contract to the 13-01 tracer (present/File.exist?, nodes=depgraph_for_viz, graph_generated_at=mtime ISO8601, malformed → raises into the router's 500 error envelope) — extraction, zero behavior change; spec/web_graph_spec.rb re-asserts the tracer's cases against the extracted class PLUS: empty-entries graph.json (present true, nodes []) and graph_generated_at tracks an explicit File.utime fixture time.
  - Router mounting: /api/state and /api/graph both token-gated (401 no-token row each); both wrapped in the ok envelope; Config Singleton reset! per example group.
  </behavior>
  <action>
  Create lib/spm_cache/web/read_models/state.rb (module SPMCache > Web > ReadModels): class State with .call(config: Core::Config.instance, cache_root: nil) → the data hash above. Implementation: packages = Cache::Inventory.scan(config:, cache_root:).map joined with cachemap = Cache::Cachemap.load(File.join(config.proxy_dir, 'graph.json')) — build module→entry index from cachemap.graph_data (entry["module"] ⇒ entry) once per call; state = entry&.[]("status"), has_macro = entry&.[]("hasMacro") || false. summary = cachemap.stats. poll_seconds = config.web_poll_seconds. JSON-ready plain Hash with STRING keys (JSON.generate needs String keys — symbol keys would silently drop; assert string keys in specs).

  Create lib/spm_cache/web/read_models/graph.rb: class Graph with .call(config: Core::Config.instance) → {present:, nodes:, graph_generated_at:} — body lifted from the router's inline reader (13-01 Task 1), behavior identical: File.exist? on File.join(config.proxy_dir, 'graph.json'); nodes via Cachemap.load(...).depgraph_for_viz; File.mtime(...).utc.iso8601; JSON::ParserError propagates to the router's error envelope (do NOT swallow — the UI's "Couldn't load Dependency Graph: {message}" copy depends on the message).

  Modify lib/spm_cache/web/router.rb: read_models hash now {state: Web::ReadModels::State, graph: Web::ReadModels::Graph, doctor: <Task 3>}; route GET /api/state → token gate → ok_envelope(State.call) (constructor accepts injectable config for specs). /api/graph re-points to Graph.call. Keep the envelope + error helpers untouched.

  Create spec/web_state_spec.rb (read-model unit specs with tmpdir fixtures via the cache_root/config seams — no server boot needed) and spec/web_graph_spec.rb (unit + 2 serve-through-router envelope examples reusing the 13-01 boot helper: 200 ok envelope with token, 401 without).
  Commit RED first (`test(13-02): failing state/graph read-model specs`), then GREEN (`feat(13-02): /api/state + /api/graph read models on shared Inventory`).
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_state_spec.rb spec/web_graph_spec.rb spec/web_server_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or web_server_spec regresses (router rewiring broke the tracer contract)</fails_when>
  </verify>
  <acceptance_criteria>
  - lib/spm_cache/web/read_models/{state,graph}.rb exist; router serves /api/state and /api/graph token-gated in the locked envelope
  - spec/web_state_spec.rb green (≥ 10 examples: join hit/missed/nil-state, summary zeros, empty packages, poll_seconds, string keys, freshness re-read, size sums)
  - spec/web_graph_spec.rb green (≥ 8 examples: present true/false, nodes shape, mtime stamp, malformed→error envelope, empty entries, 401 gate)
  - No read-model memoization: a freshness example proves per-request re-reads
  </acceptance_criteria>
  <done>The server can answer every data question the dashboard asks, derived from the same files the CLI reads, with the frontend-facing JSON contracts frozen. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Diagnostics.payload + Web::ReadModels::Doctor — /api/doctor with in-memory cache + timestamp</name>
  <precondition>Task 2 GREEN and committed.</precondition>
  <reversibility rating="costly">/api/doctor's data shape and the ?run=1 semantics are consumed by Plan 13-03's Run Doctor button (same wave).</reversibility>
  <read_first>
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-CONTEXT.md — "API & Read-Model Shape" (LOCKED: doctor runs synchronously in-request from the check registry; result cached in memory with generated_at)
  - .planning/phases/13-server-skeleton-read-only-dashboard/13-UI-SPEC.md — Copywriting row "Run Doctor (on-demand only — never auto-polled)", stale marker "Cached — generated at {HH:MM:SS}", doctor summary line, interaction bullet "Run Doctor … disables with label Running… until the synchronous in-request run completes"
  - lib/spm_cache/core/diagnostics.rb lines 19-59 (Check/Result structs, registry, run_all with per-check rescue — the registry IS the data source; no check may be enumerated by name in the read model)
  - lib/spm_cache/command/doctor.rb lines 25-79 (tolerant config.load, run_all, print_json payload being extracted, Summary line vocabulary)
  - spec/doctor_spec.rb lines 37-52 (registry manipulation save/restore pattern — the stubbed-extra-check spec copies it) and lines 55-80 (hermetic Core::Sh stubbing for check execution)
  - lib/spm_cache/web/router.rb (Task 2 output — add the doctor arm)
  </read_first>
  <files>lib/spm_cache/core/diagnostics.rb, lib/spm_cache/command/doctor.rb, lib/spm_cache/web/read_models/doctor.rb, lib/spm_cache/web/router.rb, spec/web_doctor_spec.rb, spec/doctor_spec.rb</files>
  <behavior>
  - RED first: spec/web_doctor_spec.rb fails (Diagnostics.payload / ReadModels::Doctor undefined).
  - Diagnostics.payload(results) returns {checks: [{name:, status: r.status.to_s, message:, fix_hint:}], summary: {ok:, warnings:, failures:}} — EXACTLY the shape doctor.rb print_json builds today (string statuses); a payload spec pins it against a hand-built Result array.
  - Command::Doctor --json output is unchanged (existing spec green; it now delegates: JSON.pretty_generate(Diagnostics.payload(results))).
  - ReadModels::Doctor#call(run: false): never-run + run:false → data {"has_run"=>false, "checks"=>[], "summary"=>{"ok"=>0,"warnings"=>0,"failures"=>0}}, generated_at nil; after a ?run=1 → full payload with has_run true and generated_at set; a subsequent run:false returns the CACHED payload with the ORIGINAL generated_at (timestamp of the run, not of the read — the UI's "Cached — generated at" is honest).
  - Data-driven proof (DASH-02): temporarily register a stub check 'stub_web_check' (doctor_spec.rb:37-52 save/restore pattern) returning [:warn, "synthetic"]; ?run=1 payload includes it with its fix_hint and the summary counts it — the read model was not modified.
  - Hermetic check execution: default-deny Core::Sh guard (doctor_spec.rb:61-67 pattern) with targeted capture_output stubs — two examples max, asserting ok and fail verdicts flow through; no real toolchain probes in specs.
  - Concurrency hygiene: the {data, generated_at} swap happens under a Mutex (WEBrick serves each request in its own thread); two interleaved calls never expose a torn cache (one swapped-pair assertion is enough — set cache from thread A, read from thread B).
  - Router: GET /api/doctor (any ?run= value truthy — pin "1") token-gated → envelope. NOTE the envelope's generated_at equals the doctor cache's generated_at (nil before first run) — the router passes the read model's own timestamp through rather than stamping now() — pin with an example (a run:false after run:1 returns the OLD stamp even after sleeping past Time resolution if feasible; at minimum assert equality of the two fields).
  - Tolerant config: a project with no spm-cache.yml still runs checks (config.load rescued, doctor.rb:28-32 posture) — one tmpdir example.
  </behavior>
  <action>
  Add to lib/spm_cache/core/diagnostics.rb (class << self): def payload(results) building the hash above — move the mapping OUT of doctor.rb print_json verbatim (statuses to_s, summary counts); comment: shared by the CLI --json output and the web doctor read model (one shape, two consumers — 13-CONTEXT "reuses the CLI's own read path").

  Refactor lib/spm_cache/command/doctor.rb print_json to puts JSON.pretty_generate(Core::Diagnostics.payload(results)) — zero output change.

  Create lib/spm_cache/web/read_models/doctor.rb: class Doctor — instance (one per server; constructed by the router/server wiring with injectable config: and diagnostics: Core::Diagnostics for stubbing). #call(run: false): under @mutex — if run true OR @cache nil-and-run-false…: careful with the exact semantics: run:true → always execute run_all (synchronous, in-request — this is the Run Doctor button; can take seconds while checks shell out) → payload → @cache = {data: payload.merge("has_run" => true), generated_at: Time.now.utc.iso8601} → return cache. run:false → return @cache || {data: {has_run: false, checks: [], summary: zeros}, generated_at: nil}. Config load tolerant (rescue StandardError → nil is fine — checks receive the config instance regardless; doctor.rb precedent). run_all(config: config). NEVER enumerate check names.

  Modify lib/spm_cache/web/router.rb: read_models gains doctor: (a Doctor INSTANCE, not a callable class — it holds cache state); GET /api/doctor → token gate → envelope with the read model's data/generated_at (the doctor's generated_at overrides the router's stamp — see behavior bullet).

  Create spec/web_doctor_spec.rb covering every behavior bullet (registry stub save/restore, default-deny Sh with targeted stubs, Mutex swap, envelope passthrough) and extend spec/doctor_spec.rb with 2-3 payload-shape pins (including string statuses and the summary counts against a hand-built Result set).
  Commit RED first (`test(13-02): failing doctor read-model + payload specs`), then GREEN (`feat(13-02): /api/doctor — registry-driven, cached with timestamp, shared payload`).
  </action>
  <verify>
    <automated>bundle exec rspec spec/web_doctor_spec.rb spec/doctor_spec.rb spec/web_graph_spec.rb</automated>
    <fails_when>exit code non-zero; or "0 examples, 0 failures"; or any pre-existing doctor example fails (--json regression); or the cached-stamp example shows a fresh timestamp (cache not honored)</fails_when>
  </verify>
  <acceptance_criteria>
  - Diagnostics.payload exists and doctor.rb delegates; CLI --json byte-identical (existing spec green)
  - lib/spm_cache/web/read_models/doctor.rb exists; /api/doctor mounted token-gated; has_run/run semantics + cached generated_at proven; stubbed extra check appears with no read-model change (DASH-02 data-driven)
  - spec/web_doctor_spec.rb green (≥ 10 examples); spec/doctor_spec.rb extended and green
  - Full suite `bundle exec rspec` green
  </acceptance_criteria>
  <done>The doctor panel's data contract is live: on-demand synchronous registry runs, honest cached-at timestamps, one payload shape shared with the CLI. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project/cache files → read models → HTTP | data authored by build tooling (graph.json, sidecars, yml) crosses into responses consumed by the browser — a hostile repo could plant hostile strings in module names/messages (rendered by 13-03; XSS mitigation lives there) |
| WEBrick request threads → shared doctor cache | concurrent reads/writes of the one mutable server-side cache |

## STRIDE Threat Register (ASVS Level 1)

| threat_id | category | component | severity | disposition | mitigation_plan |
|---|---|---|---|---|---|
| T-13-09 | Information disclosure (project metadata via /api/*) | Web::ReadModels::* + Router | high | mitigate | inherited from 13-01: every /api/* route token-gated (T-13-01/T-13-03) — this plan adds no un-gated route; integration matrix in 13-04 re-proves per-endpoint |
| T-13-10 | Tampering (torn concurrent doctor cache) | Web::ReadModels::Doctor | low | mitigate | Mutex around the {data, generated_at} swap; data+timestamp swap atomically so a stale stamp can never label fresh checks |
| T-13-11 | Information disclosure (stale data presented as fresh) | State/Graph read models | medium | mitigate | per-request re-reads, zero memoization (freshness spec); doctor cache always carries its own generated_at which the envelope passes through verbatim — the UI stamps "Cached — generated at" from it (13-UI-SPEC honesty contract) |
| T-13-12 | DoS (doctor checks shell out synchronously per request) | Web::ReadModels::Doctor | low | mitigate | run:true is the ONLY path that executes checks; cache serves repeats; a runlock is unnecessary at dev-tool concurrency (single user, Run Doctor button disables itself while in flight per 13-UI-SPEC) |

No package installs in this plan (stdlib + existing gems only).
</threat_model>

<verification>
- `bundle exec rspec spec/web_state_spec.rb spec/web_graph_spec.rb spec/web_doctor_spec.rb` — the three read-model surfaces, green.
- `bundle exec rspec spec/command_cache_list_spec.rb spec/doctor_spec.rb spec/config_spec.rb` — the two CLI refactors + config, byte-identical outputs pinned.
- `bundle exec rspec` — full suite green.
- Structural greps: `grep -n 'def payload' lib/spm_cache/core/diagnostics.rb`; `grep -rn 'Inventory.scan' lib/` shows exactly two production call sites (cache list + state read model — the one-source-of-truth rule).
</verification>

<success_criteria>
- SC4 (DASH-01): /api/state serves per-package size, cached/source state, and fidelity re-derived from the same files the CLI reads — spec/web_state_spec.rb + the shared Inventory (one scan, two consumers).
- SC5 (DASH-02 half): registry-driven on-demand doctor with statuses + fix hints and cached-with-timestamp — spec/web_doctor_spec.rb, data-driven proof via stubbed check.
- SC5 (DASH-03 half): /api/graph formalized with present-flag affordance signal and mtime-based generated stamp — spec/web_graph_spec.rb.
- Locked "API & Read-Model Shape" decisions fully implemented: three endpoints exactly, locked envelope, CLI-path reuse, doctor in-request + cached.
- UI-SPEC feed contracts (empty-state triggers, stale-marker timestamps, summary counts) all derivable from the pinned payload shapes.
</success_criteria>

<output>
Create `.planning/phases/13-server-skeleton-read-only-dashboard/13-02-SUMMARY.md` when done
</output>
