---
context: phase
phase: 08-drift-read-back-fidelity-status-provenance
task: 0
total_tasks: 5
status: in_progress
last_updated: 2026-08-29T08:01:03.504Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **Wave order matters — 08-02 depends on 08-01.** Do not execute 08-02-PLAN.md before 08-01-PLAN.md has a SUMMARY.md; 08-02 reads the provenance sidecar schema 08-01 defines.
- [ ] CONSTRAINT: **`workflow.use_worktrees=false` for this project.** Executors run sequentially on the main working tree, not in isolated worktrees. Do not pass `isolation="worktree"` to the executor Agent() call.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fixer agent crashed mid-run, left incomplete fix + wrong test assertion | Phase 7's code-review `--fix --auto` fixer crashed on a transient API connection error after fixing only 1 of 2 criticals. Its own partial commit (CR-01) left the OTHER branch of the same fix unlocked AND added a test asserting the wrong invariant ("fast path doesn't need the lock"). Only caught by a deliberate re-review pass, not by trusting the fixer's own completion claim. | blocking | After ANY agent claims a fix/task is complete, independently verify via `git diff`/re-running the specific test the agent's own commit touched — never trust a "done" claim at face value, especially after any tool/connection error during the run. |
| `gsd-tools.cjs phase.complete` computed wrong `next_phase` | Marking Phase 7 complete via `phase.complete 7` returned `next_phase: "6"` (already-complete phase) instead of `8`, and wrote it into STATE.md. Caught by cross-checking `roadmap.analyze`'s phase list before trusting the tool's own output. Filed as feedback this session. | advisory | Before trusting any `phase.complete`/`state.*` tool output for phase-position fields, cross-check against `roadmap.analyze`'s live `disk_status` per phase. |

_Both discovered through actual failure this session, not predictions._

<current_state>
Milestone v0.4.0 (Build Fidelity & Release Automation), branch `gsd/v0.4.0-build-fidelity-release-automation`.

Phase 7 (Host-Faithful Checkout Seeding): **complete** — canonically verified (5/5 must-haves), code-reviewed
(2 Critical + 5 Warning fixed, 1 Warning + 3 Info deliberately deferred with documented rationale in
`07-REVIEW-FIX.md`), 342 examples/0 failures, transitioned.

Phase 8 (Drift Read-Back, Fidelity Status & Provenance): **fully planned, zero code executed.**
- `08-M2-MEASUREMENT.md`: 0/17 packages resolution-incompatible on the reference project — not a rescope trigger.
- `08-CONTEXT.md`: smart-discuss, 4 grey areas / 16 decisions, all accepted as recommended.
- `08-RESEARCH.md`: found the ROADMAP's "reuses `.shims.json`/`GraphEntry.Status` verbatim" claim half-wrong
  (`GraphEntry.Status` is Swift-side, never consumed by `cache list`; DIAG-02 is genuinely new Ruby code).
  Also found `cache list` today is a bare `Dir.entries` walker with a pre-existing bug: sidecar files already
  leak into its output as spurious entries.
- `08-PATTERNS.md`: 5 files classified, analogs found for 4/5.
- `08-01-PLAN.md` (Wave 1, no deps) + `08-02-PLAN.md` (Wave 2, depends on 08-01): plan-checker
  `## VERIFICATION PASSED`, requirements coverage 4/4 (FID-03, FID-04, CACHE-01, DIAG-02).

Session paused here — right before spawning the Wave 1 executor — because context usage hit 65% and
this is the cleanest possible boundary: fully planned, checker-verified, nothing built yet.
</current_state>

<completed_work>

Completed this session, most recent first:
- Phase 8 planning: `08-01-PLAN.md`/`08-02-PLAN.md` created, plan-checker passed, requirements/decision
  coverage gates passed, STATE.md/ROADMAP.md updated — commits `5791011`, `92bbb5b`, `2f49bd1`, `88c1acd`
- Phase 8 pattern mapping (`08-PATTERNS.md`) — folded into `2f49bd1`
- Phase 8 research (`08-RESEARCH.md`) — commit `7d7b402`
- Phase 8 smart-discuss (`08-CONTEXT.md`, 4 areas / 16 decisions) — commit `6b56057`
- Phase 8 M2 measurement (`08-M2-MEASUREMENT.md`, 0/17 resolution-incompatible) — commit `5ed3fb3`
- Phase 7 → Phase 8 transition (STATE.md/PROJECT.md, corrected the `phase.complete` next_phase bug) —
  commit `1c4e1e4`
- Phase 7 code review + fix (CR-01/CR-01b/CR-02 + 5 warnings; WR-05 deliberately kept) — commit `a2eb4bc`
  and earlier `c3bb440`, `a915188`, `c5d1aaa`, `3a972bb`
- Phase 7 canonical goal-backward verification (5/5 must-haves passed) — `07-VERIFICATION.md`
</completed_work>

<remaining_work>

- **08-01-PLAN.md** (Wave 1): drift read-back (FID-03), resolution-incompatible classification (FID-04),
  provenance sidecar write/cleanup (CACHE-01), build-output half of fidelity status (DIAG-02). 3 tasks
  against `lib/spm_cache/spm/build_pipeline.rb` + `lib/spm_cache/installer/build.rb` + new
  `spec/build_pipeline_provenance_spec.rb`.
- **08-02-PLAN.md** (Wave 2, depends on 08-01): `cache list` fidelity-status column, second half of
  DIAG-02, plus a fix for the pre-existing sidecar-leaks-into-cache-list bug. 2 tasks against
  `lib/spm_cache/command/cache/list.rb` + new `spec/command_cache_list_spec.rb`.
- Code review Phase 8 (execute:post `code-review` capability is active for this project — expect it to run).
- Canonical verification of Phase 8 (`gsd-verifier`).
- Transition Phase 8 → Phase 9.
- Phases 9 (Cache Identity & Invalidation), 10 (Fidelity Regression Coverage), 11 (Homebrew Release
  Automation) — each needs discuss → plan → execute. Phase 11 will pause for the GitHub App operator step.

</remaining_work>

<decisions_made>

- M2 measured retroactively via static manifest analysis (`swift package dump-package` against Xcode's
  own already-resolved checkouts in `DerivedData/StressMonitor-*/SourcePackages/checkouts/`) rather than
  a live resolve/build sweep — genuinely report-only, and the actual `resolution-incompatible` detection
  machinery is Phase 8's own deliverable, so a live sweep would need the feature to measure the feature.
- M4 found moot by direct code inspection, not run: `BuildPipeline#seed_host_graph` already classifies
  `vendored_xcodeproj?` checkouts as not-graph-pinned and skips seeding unconditionally before any build
  — nothing is ever claimed pinned on that path, so Phase 8 has nothing to detect drift against there.
- Single consolidated drift/provenance insertion point in `BuildPipeline.run` (right after `perform_build`
  succeeds), not duplicated across the three artifact-producing paths — also what gives Class E
  (`copy_prebuilt_binary_target`) a correct sidecar for free.
- Fidelity status line printed from inside `BuildPipeline.run` rather than changing its return contract —
  avoids an unrelated forced edit to `command/pkg/build.rb`'s bare-path-string handling.
- All 8 edge-probe rows from the spec-less probe fallback (no `08-SPEC.md` exists) were authored as
  explicit flagged `must_haves.assumptions` in the plans, never silently dropped. 6 kept prohibitions
  authored descriptor-less into `08-01`'s `must_haves.prohibitions`.

</decisions_made>

<blockers>
- Phase 11's GitHub App creation: genuinely blocking for that phase specifically, not the rest of the
  milestone. Autonomous will stop there and ask, same as recorded before.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — full narrative history including the M2/M4 gate closures and the phase.complete
   bug correction
2. `.planning/phases/08-drift-read-back-fidelity-status-provenance/08-01-PLAN.md` — start here
3. `.planning/phases/08-drift-read-back-fidelity-status-provenance/08-RESEARCH.md` — the "half wrong"
   ROADMAP-reuse finding and exact insertion-point line numbers, if execution needs them

## Infrastructure State
- Working tree: clean except pre-existing untracked dirs/files that predate this session
  (`.gsd/`, `.planning/graphs/`, `.planning/intel/`, `CLAUDE.md`, competitive-analysis HTMLs,
  `docs/journals/...`, `graphify-out/`) and an orphaned `.planning/phases/07-host-faithful-checkout-seeding/07-UAT.md`
  (a manual UAT session started before canonical verification superseded it — harmless, never finalized,
  safe to ignore or delete).
- Reference project `/Users/ddphuong/Projects/next-labs/stress-ai/ios-stress-app/StressMonitor` is on
  `main`, git-clean except its own pre-existing untracked config dirs and a stash unrelated to this work
  (verified and the known `.xcscheme` 1.7→1.8 drift from the Phase 7 benchmark was reverted this session).
- Full suite: `make proxy.build && bundle exec rspec` → 342 examples, 0 failures as of this pause.

<context>
This session ran `/gsd-autonomous` bare (no flags) starting from Phase 7 (unverified) through Phase 8
planning. Two checkpoints were surfaced to the user along the way (Phase 7 replanning risk, Phase 8's M2
gate) and both were resolved with real evidence rather than skipped. Paused at 65% context, at the
cleanest boundary this milestone offers: a fully planned, checker-verified phase with zero code written.
</context>

<next_action>
Start with: `/gsd-execute-phase 8` (or `/gsd-autonomous --from 8` to resume the full autonomous chain
with a fresh context budget) — re-enters at Wave 1 (`08-01-PLAN.md`), spawning a sequential `gsd-executor`
(no worktree isolation for this project).
</next_action>
