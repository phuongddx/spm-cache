---
milestone: v0.5.0
audited: 2026-09-02T01:30:00Z
status: passed
scores:
  requirements: 19/19
  phases: 5/5
  integration: 5/5
  flows: 5/5
gaps: {}
tech_debt:
  - phase: 16-package-toggles-panel-completion
    items:
      - "F-1 (WARNING, wiring-conditional): Apply-now converges saved→applied only when the spawned use takes its full regeneration path — an ignore-only change fast-paths (no diff → no gen-proxy → graph.json unchanged → applied_cached never moves). UI degrades honestly (bar persists; poll never lies). Backlog candidate: force a graph regen when the ignore set diverges from the graph."
      - "Observation: the state table lists the machine-global cache inventory annotated by the project graph (~45 real entries with state '—' on a dev machine). 13-era design."
      - "Observation: the pending divergence signal requires a graph entry — cache-only-ungraphed rows can be toggled but never count toward the unsaved-changes bar (documented in 16-03)."
  - phase: 14-live-log-streaming-terminal-watch-relay
    items:
      - "Observation (backlog): Watcher#resolve_watched_files globs **/project.pbxproj and picks the first match — projects with a nested duplicate .xcodeproj watch the wrong file (touches to the real pbxproj don't trigger cycles)."
      - "IN-02 (info, documented in 14-REVIEW): pid_alive? duplicated verbatim between read_models/runs.rb and core/run_log.rb — optional extraction declined in the fix pass."
  - phase: 15-ui-build-controls
    items:
      - "Observation: rollback removes the sandbox but leaves a dangling local proxy reference in the xcodeproj (pre-existing v0.2-era semantics; BLD-04 added the lock, not de-integration). Xcode resolution behavior with the dangling ref is a backlog question."
nyquist:
  compliant_phases: [12, 13, 14, 15, 16]
  partial_phases: []
  not_validated_phases: []
  missing_phases: []
  overall: compliant
---

# v0.5.0 "Web Interface" — Milestone Audit

## Requirements (19/19 satisfied — 3-source cross-reference)

All REQUIREMENTS.md rows are `[x]` (19/19, 0 unchecked), every phase VERIFICATION.md passed (12: 28/28, 13: 23/23, 14: 8/8, 15: 6/6, 16: 3/3), and every SUMMARY frontmatter lists its requirements-completed. Per the status matrix: **satisfied** throughout; zero orphaned IDs (every traceability row maps to a phase verification).

| Group | IDs | Phases | Status |
|---|---|---|---|
| Run-log capture | LOGS-01 | 12 | satisfied (verified 28/28; real-TTY parity executed + recorded) |
| Dashboard | DASH-01..03, WEB-01..04 | 13 | satisfied (verified 23/23; live-browser UAT closed G-13-1; SECURITY SECURED) |
| Live streaming | LOGS-02..05 | 14 | satisfied (verified 8/8; D-14 probe 7/7; two probe-caught defects fixed spec-first) |
| Build controls | BLD-01..04 | 15 | satisfied (verified 6/6; D-15 probe 7/7; confirm-bar order defect fixed spec-first; review CR-01/CR-02/W-01 fixed with specs) |
| Package toggles | TOGL-01..03 | 16 | satisfied (verified 3/3; D-16 probe 8/8; review CR-01/WR-01/WR-02 fixed with specs) |

## Phases (5/5)

All complete, verified, and validation-reconciled (nyquist 5/5 compliant — 12/14/15/16 promoted 2026-09-02 with scoped evidence re-runs: 241+116+289+361 examples green).

## Integration (5/5)

- 10/10 /api routes token-gated; Host→Origin→token order on every route; constant-time digest compare; bootstrap 302 + vendored /assets the only pinned ungated surfaces.
- Zero orphaned routes; zero orphaned exports (every 12-16 seam consumed: cycle_wrapper, set_ignored/set_ignored_all, binary_backed_names, Events drains, Runs derivations).
- Statelessness verified: read models re-read disk per request; Jobs remembers only {pid, scope} with disk-derived liveness; the build flock remains the only mutex.

## E2E Flows (5/5 — one with a recorded conditional leg)

1. **run → log → stream → render** — PASS, wired end-to-end (LOGS-01..05).
2. **click Build → spawn → stream → banner** — PASS, wired end-to-end incl. WEB-03 with an in-flight build (BLD-01..03).
3. **toggle → write → apply → converge** — PASS with WARNING: every leg wired; the final convergence leg is conditional on a full regen (F-1 above, UI honest throughout; milestone-recorded in 16-06 row 3).
4. **rollback confirm → CLI under the build lock** — PASS, wired end-to-end; terminal and UI callers share the flock (BLD-04/CP4).
5. **cross-cutting middleware + exit-0 chain** — PASS (trap → shutdown_events → sentinel fan-out → http.shutdown → masked ensure → exit 0; spec-pinned under open-SSE, in-flight-build, and in-flight-apply shapes).

## Suite

1095 examples, 0 failures at audit HEAD (grew 787 → 1095 across the milestone; every wave gated green; three recorded agent-browser probe nets executed 22 manual rows total, catching four real defects that 350+ green examples missed).

**Status: passed** — all requirements met, no critical gaps; tech-debt list is observations + one honest conditional-leg warning, all recorded in-phase with backlog candidates named.
